# 09 — A session reads the signal

**Status:** DONE — closed 2026-09-04 by card #23; the review is docs/reviews/2026-09-04-a-session-reads-the-signal.md
**Written:** 2026-09-04, from the owner's reading of the first triage sitting: 47 shipped Hello Revenue cards carried a signal only he could read, and "many of these plans use me as the gateway whilst you are a better gateway. I believe in giving you the autonomy you need to execute whenever we're aligned on intent. That goes for this as well." — said to the coordinating session; in this plan "you" means any Claude session with the project's tools, and "me" means the owner. Nowhere below does "you" address the executing session. And on one of them: "'builds stay under an hour' is just checking logs for wall-clock time? I don't think under an hour is true, nor am I sure whether it's the right measure because built campaigns have different amounts of assets."
**Effort gate:** high — the mechanics are a fourth reader in the signal grammar and a short reading session launched by the runtime; the judgment is in how each of the 47 signals is re-translated, which the lane writes with its reasoning per card and the owner can overturn.
**Sequencing:** after 06 (it shares the loop and the signal files with 06's live-write item). Before 08.

## Intent

The measure step of every loop is read by the machine or by a session, never by the owner's memory. A signal a session can investigate with the tools it has — the read-only production database, the ledger, deploy logs, the probes, git, the project's own commands — is read by a session at its due time, on its cadence, and the session's finding moves the card. The owner is asked only when the question is one of his own taste or experience, or when a session read the evidence and could not decide, in which case he gets the evidence with the question. A signal that turns out to be the wrong measure is replaced, not pretended.

### 1. A fourth reader: `session`

Done means: the WATCH grammar gains `session <what to check, where> [expect …]` beside url, file, command and owner; at the due time, on the cadence, the loop starts a reading session through the runtime in the project's main checkout — read-only for the repository, with the project's read-only data access as its own rules provide it (Hello Revenue: the read-only database role, the probes, the Railway log rules in its CLAUDE.md), never a lane — with the card, the DELIVERED row and the signal as its brief; the session ends its turn with one of three findings on the card, through `needle reading`: **delivered** with the evidence, **not delivered** with the evidence, or **cannot tell** with what it read and what would decide it; the board moves the card to Done, to Decision moment with the finding, or asks the owner with the session's evidence in the question. The reading session is listed on the card while it runs and never counts as hands on the tree.

### 2. A wrong measure is replaced, not pretended

Done means: a reading session that finds the signal unmeasurable or the wrong measure (a threshold nobody set from data, a measure that ignores size — "under an hour" for builds of different asset counts) does not guess; it writes a **replacement WATCH row** with the measure it can read (for #102: build wall-clock per asset from the ledger, against the 16 Aug baseline, per build), says so in its reading, and the loop reads the new signal from its next cadence; the owner sees the replacement on the card's history and can overturn it. The rings rule applies: a finding about the signal itself is inside the card; a finding about the product is filed as a suggestion with its kind.

### 3. The 47 are re-translated

Done means: every `owner` signal on Hello Revenue's Executed cards is re-read by the lane against one rule — could a session with the project's read-only tools read this? — and rewritten as `session` where yes, kept as `owner` only where the question is the owner's taste or his own experience (a reply "noticeably sooner" to him; a page "judged by eye" is a session's with a screenshot), with the reasoning per card in the close-out's table; the originals stay in each card's history; the first reading round runs on the lane's own cadence so the close-out reports how many of the 47 a session could settle on day one.

### 4. The owner's batch shrinks to what is his

Done means: the attention rail's batched owner list holds only `owner` signals and `cannot tell` findings, each carrying the session's evidence when there is any; the count on the rail is the count of decisions that are actually his.

## Terrain
- `board/signals.py` (grammar, readers), `api/loops.py` (the signal cadence and the moves it makes), `api/doors.py` and `runtime/` (starting a session that is not a lane: the Discuss door is the nearest shape — a conversation that never has hands; this one is windowless and writes back through a verb), `api/cli.py` (a `reading` verb), `board/assemble.py` (the rail's batch).
- Hello Revenue's data access rules: its `CLAUDE.md` § "Reading production data" (the read-only role, never the admin string; the Railway CLI's `head` trap), the probe workflow, the ledger tables named in `docs/wiki/` — the reading session's brief points at these, never restates them.
- The 47 signals as they are: `needle rows hellorevenue` once 08 lands, or the card detail API today.

## Acceptance criteria
1. A card with a `session` signal due now gets a reading session started by the loop, and the card shows it; the session's `delivered` finding moves the card to Done with the evidence on the history row; `not delivered` lands in Decision moment with the finding; `cannot tell` puts the question to the owner with the evidence.
2. A reading session that replaces a measure writes the new WATCH row, and the loop reads it on the next cadence; the owner sees both on the history.
3. Of the 47, the close-out says how many became `session`, how many stayed `owner` and why, and how many the first reading round settled.
4. The owner's batch on the rail counts only what is his.

## Rulings
Recorded as the build makes them, each with the alternative rejected.

1. **A reading is started through a runtime verb of its own, never `start`.** `Runtime.read_by_session` → `launch.read`: the same launch walk as a lane's (`_walk`, shared), with no `--worktree`, in the project's own checkout, named `reading-card-<n>-<slug>` so the registry, the scope and the one list tell it from the lane it is not. Rejected: reusing `runtime.start` — the Start ratchet holds that only the owner's click reaches it (plan 07, item 3), and a reading is not hands on a tree; rejected: the Discuss shape (`runtime.discuss`) — it opens a window and lives by the owner's presence, a reading is windowless and reports through a verb.
2. **A reading in flight is its own record (`reading_sessions`).** Open while it runs, ended by the verb or by the loop finding its process gone; the board lists it on the card and counts it on the attention line; the loop starts no second one for the card. Rejected: a `Discussion` row — its kind is the window it was opened through and its rail line is a conversation with the owner; a `LaneRecord` — a lane has a worktree and hands, and Executing is derived from it.
3. **A reading carries its actor.** `readings.actor` (migration 0007; the owner's earlier rows backfilled from their words). Rejected: telling a session's cannot-tell from a machine's unreadable by the words — the rail would have asked the owner about a reading session that died or a `curl` that failed, and item 4's count would have lied.
4. **The three findings land differently from a machine's reading.** Delivered goes to Done on `signal-delivered`; not delivered goes to Decision moment now, on `signal-failed`, whatever the due date; cannot tell stays in Executed, asks the owner from the batch with the session's words, and lands in Decision moment once past due, as an unreadable would. Rejected: a session's not delivered waiting for the due date as a URL's does — a URL not yet up is "not yet", a session's not delivered is the evidence in hand saying no, and that is the owner's decision moment.
5. **Two readings alive at once, at high effort, each bounded.** `READINGS_AT_ONCE = 2` across every project, the rest at the next tick of the signal loop; a reading is stopped when its finding lands and its turn is over, when its turn ends without a finding (the registry's `done`), or after thirty minutes; a limit mid-reading moves it with the runtime's one `move`, the record following the forked id. Rejected: unbounded readings — the first round would have put 51 sessions on the subscriptions at once and held the loops' lock through 51 launches; xhigh, the Discuss door's effort — reading evidence is bounded investigation, not open thinking, and the strongest model still does it by the one rule.
6. **The kind word after the separator names the reader.** Hello Revenue's #177 row asked "did a one-click ruling launch its session and close the card? — owner by …" and would have parsed as a session's signal with the question as its target. Rejected: the first kind word anywhere, which was the grammar until now.
7. **The translated rows read every two days.** 51 readings a day at high would be a subscription's whole day until the due date; every two days is four rounds before 2026-09-11, and a reading session that finds the evidence cannot exist yet lengthens the cadence itself through item 2's replacement row. The owner overturns any row with one `needle row hellorevenue N WATCH "…"`.
8. **One row stays the owner's (#244).** "Were the lane windows and the toast bearable" is his experience of his own desktop; the run-out half of that row is #180's measure, which a session reads there.
9. **The reading brief points at the project's data rules and never restates them.** The brief names where they live (the project's CLAUDE.md: the read-only role, the log rules, the probes) and forbids any credential those rules reserve; the one write a reading may make is a defect suggestion into the corpus, as the Idea door writes, because the rings rule sends a product finding there. Rejected: a brief that quoted Hello Revenue's rules — it would drift from them, and a project's rules stay in that project's repository.
10. **`needle_command` lives in `board/brief.py`.** The loop builds the reading brief and may not import the doors (plan 07's ratchet), so the one way to spell the command moved down a layer and the doors import it from there.

## Close-out
- **The 47 were 52 by the lane's day:** five more cards closed with owner signals between the plan and the build. 51 rewritten as `session`, 1 kept as `owner` (#244, ruling 8); #134 was already a `command` and is untouched; #143 carries no WATCH row at all (its close said the shared plan was split) and is the board's doubt to show, outside this change. The originals stay in each card's history: `needle row` rewrites a WATCH row and records what it read.
- **The translations, by card number** (a project's titles stay in its repository; the rows themselves are on the cards): 101 the crowned plays and the chat replies are rows · 102 the owner doubted "under an hour" (builds carry different asset counts), the ledger gives wall-clock per asset per build against the 16 Aug baseline, the measure the plan named · 103 the rehearsal verdict is a column · 117 "noticeably sooner" is his ear, but the close gave the numbers behind it and the ledger holds every call · 119 the ledger row · 124 the label text is stored and served · 125 the notice text is stored with the failure · 131 a grant, its moves and the note's send are rows; cannot-tell until a grant · 132 his hand turns the switch, the registry's read says whether the notice cleared · 133 the cost meter reads the ledger · 135 "went well" is judgment, but each seat's outcomes are ledger rows · 136 delivery, attribution and the watchdog are rows the sync keeps · 137 the self-test writes its archive and send rows · 170, 177, 189, 249 the first board is retired and what it did is in its commits and its journal · 171 audits and dispositions are files in git · 173 proposals, confirmations and executions are rows; cannot-tell until a Google campaign cycles · 175 the guards run from the checkout · 178 the close's refusal was the first board's; on Needle the record is the REVIEW row and the file · 180 the rescue ledger is the record of exactly this · 181 board-file conflicts ended with the first board; CI runs are readable · 182 worktrees and lane sentences are both readable · 185 failures, alerts and sends are rows · 186, 192, 229, 259 Needle's own histories and windows are rows · 188 the sync stores Google's conversions · 193 stage stamps and send rows · 194 grant rows, and the board half is history · 195 stances and pointers are in the archived plans · 198 attempt counts and the picked primary are stored · 201 proposal fates are rows · 208 the brief, the centre and the concepts are stored text, read one against the other · 209 the fields are on the direction · 210 ledger rows · 213 every half is a stored value · 217 the proposal card's fates are rows · 222 the closing read and its repairs are ledger rows · 225 the census test is in git, the chat answers are stored · 228 his asking is his act; the answer and the stored reasons are rows a session compares · 230 the send is machine-readable and already past due; the founders' replies land in his inbox and are his to add as a RULING · 231 judged by eye is a session's with a screenshot; the rows are stored · 235 job outcomes are ledger rows · 237 the asset's versions, timings and rail lines are stored · 238, 246 the review record and the plan wrote the query; a session runs it read-only · 239 the ledger holds cached-token counts per call · 241 deploys, watchdog rows and outcomes are readable.
- **The first reading round, on the served board (item 3):** the rows landed at 16:58Z; the loop started #101 and #102 within a minute, on the slot the rule chose, each in its own scope. #101 came back **cannot tell** at 17:01Z in 2 min 25 s — no Recommend click by a one-channel account since the ship, with the query it ran, the one Meta-only slate that says nothing either way, and what would decide it (one click from a Meta-only account, which the owner can force in minutes) — and sits in his batch with those words. #102, the plan's own example, came back **delivered** at 17:02Z in 3 min 28 s — the three closing stages start within 12 s of each other on all three real builds since the ship where the 16 Aug build ran them one after another, whole-build wall 70.7 min against 46.3 and 45.0 — and moved to Done; and it replaced its own measure: per-asset wall-clock ignores size the other way (a parallel fan-out's stage wall is set by its slowest unit, so a 4-page build looks slower per page than a 10-page one), so the card's row now reads the 45-minute floor and per-stage parallelism. The finished session was stopped and #103 started on the next tick. The round continues two at a time; the card's own WATCH row counts the readings as they land.
- **Proof of search before the new primitives.** Reused: `Start`'s launch walk (now `_walk`, shared with `read`), `runtime.move` for a limit mid-reading, `record_reading` and the `readings` table for a session's finding, `add_row`'s rewrite for the replacement WATCH row, `where_after` for delivered and cannot-tell, `OwnerAsk` and the rail's batch for the cannot-tell, `AskRow`, `Quiet` and `HistRow` on the page, `render` inside the reading brief. New, after the search: `SignalKind.SESSION`, `Finding`, `Reading.actor`, `ReadingSession` and its table, `ReadingStart`, `Runtime.read_by_session`, `where_after_finding`, `Doors.reading` and the `reading` verb, `reading_brief`, `Attention.signals_reading`, `CardSummary.reading`, `OwnerAsk.kind` and `.evidence`.
- **The served board needs the main checkout's `frontend/dist` rebuilt and `needle-serve.service` restarted after the fold** (migration 0007; a new signal kind the running server would refuse to parse; the page changes). Done by this lane before the rows were written, and recorded on the card.
- **Filed, not fixed (outside the change):** `docs/slice-suggestions/2026-09-04-a-close-without-a-review-record-is-accepted.md` — `needle close` takes `--review` as optional while the rule says nothing ships without one; found translating #178.

## Estimate
Execution clock: one lane-day, half of it the 47 translations and the first reading round. Gate clock: none; the owner overturns after the fact.
