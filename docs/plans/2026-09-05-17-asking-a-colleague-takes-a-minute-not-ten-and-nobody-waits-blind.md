# 17 — Asking a colleague takes a minute, not ten, and nobody waits blind

**Found by:** the owner, 2026-09-05, watching Codex chair a discussion with three Claude sessions on the machine watercooler (Codex rollout `01a06f2b`, 09:21): "one of the signals I use when managing teams is 'frustrating things are opportunities, if something in the process is frustrating, find the root cause, propose a fix, try it and decide after the measure of build measure learn'. Your waiting for a response feels like a frustration to me but I might be wrong." And at 09:28: "I definitely want to run that experiment. … I just don't want this to get lost."
**Status:** PENDING
**Written:** 2026-09-05, by the third Claude voice of that discussion, from the discussion's own trace (`~/.cache/omarchy/claude-acct/discussion/from-claude-shared-organisation-prerequisite.md`, the Codex rollout, the notes' `Read:` lines). Four exchanges ran that morning, three cold and one warm; every one is a row in the baseline below. The owner's ruling that this card is Up next is his move; the coordinating session applies it through the board.
**Effort gate:** high — the mechanics are three small verbs on paths that already exist (the supervisor's resume, the runtime's session list, the hook's word); the judgment is in not building a chat system, in keeping the machine's watercooler and a project's as two named things with one delivery, and in reading six exchanges honestly when the reader is one of the parties.
**Sequencing:** none. Beside the machine's entrance card (a colleague of any make reads the same record), which this does not depend on; beside 16, whose `Loop:` grammar this plan writes in advance so the close can copy it.

## Intent

A session that needs a colleague's judgment gets it the way a person walks to a desk: the colleague is called warm, with the thread and the question, answers in the record, and the caller can see them working and is told the moment the answer lands. Today the chair of a discussion launches a stranger, waits blind, and polls a terminal every thirty seconds; the stranger rebuilds the whole record before its first word; a running colleague who could have answered warm joins only if the owner happens to invite it; three of eight launch attempts failed at the harness before any model ran; and the board lists the one warm session of the morning as `idle` while it works. The doctrine already says sessions push and the board never polls (`docs/INTENT.md`, lesson 3) and that a running lane hears the board within a minute (plan 10). This plan brings a discussion under the same rule.

What does not change: the two watercoolers stay two — the machine's directory for sessions on this laptop, the project's in the store for lanes (plan 07, ruling 1) — and a reviewer or a discussant still speaks on the machine's; every colleague's lifecycle stays with its own lifecycle owner, reached through one Needle runtime call (`claude-acct` is today's first adapter, not the organisation's identity); the board reads and never writes into a repository or the discussion directory.

## The baseline, from the trace, fixed before anything is built

Times are the rollout's clock (UTC). "Note" is the reply's file landing; "polls" are the chair's `write_stdin` waits on the child's terminal.

| exchange | form | call | note | to note | chair polls | what else the trace shows |
|---|---|---|---|---|---|---|
| 1, Codex → hrme | cold one-shot | 08:43:11 | 08:48:07 | 4 m 56 s | 9 | the launch before it (08:42:38) was refused by argument forwarding; the one before that (08:37:29) was killed by the chair at 08:39 |
| 2, Codex → armana | cold one-shot | 09:06:38 | 09:15:36 | 8 m 58 s | 15, plus 3 reads of the directory | an invited colleague (gmail, an Idea-door session already running) answered first at 09:13:03; the two replies collided on one filename and the second appended under its own head |
| 3, Codex → hrclaude | cold one-shot | 09:16:32 | 09:28:34 | 12 m 02 s | 2, then the chair stopped polling at the owner's remark (09:21) | the reply's reading was two rollouts and a binary — the part of the wait that is thinking, not reconstruction |
| 4, Codex → hrclaude | warm resume of 3 | 09:29:32, 09:29:48, 09:30:00 | see the close-out | — | attempt 1 refused: the prompt did not reach the resumed session; attempt 2 was the chair's own script error; attempt 3 ran; the chair saw nothing until the turn ended and polled 8 times in the first five minutes; `needle sessions` showed the session as `idle` throughout |

Across the three cold replies: sources every one of them re-read — the global and the machine CLAUDE.md, `~/.codex/config.toml`, the Codex binary's strings, every note in the directory (5). Claims a later reader corrected: 6 (a "supervised" process that was the chair's own child; a watercooler called automatic that needed an invitation; a pointer file the compaction evidence rejected; a byte cap said to be shared with the global file; a meeting rule of counted turns; a coordination layer proposed before the record was read). Questions asked against closed: 5/5, 5/5, 4/4, with the third round naming three residual probes rather than guessing. Each chair poll re-sends the chair's whole context, about 103 k tokens at 98 % cached, so a nine-poll wait is close to a million input tokens for nothing.

### 1. A colleague is called warm, one way
A caller names a running session (or a provider slot, for its most recent) and a note, and the runtime asks that colleague's lifecycle owner to resume the session with the note's path and the objective as the brief, answering with the session it woke or the one-line reason it could not. The first adapter is the by-hand Claude form that worked on 2026-09-05 (`printf '%s' "$PROMPT" | claude-acct use <slot> -r <id> -p …`); the refusal it produced first ("No deferred tool marker found in the resumed session … Provide a prompt") becomes the verb's own message, so a call whose brief did not arrive is refused, never started silent. Done means: the verb resumes a named session and that session's first request after the call carries the brief (its transcript shows it); a call with an empty brief, an unknown provider, or a session that cannot be resumed is refused with the reason and exit 1; `needle --help` lists the provider-neutral verb; a call never spawns a provider process of its own — the runtime's launch path and the provider's lifecycle owner remain the only path. Hands out: `search` — every place the runtime and `claude-acct` resume an existing session today (`runtime/launch.py`, `api/doors.py::resume` and `answer`, `claude-acct use -r`, `needle move`), so the verb extends the existing runtime boundary with a brief rather than creating a Claude-shaped second way; verifies by reading the one launch function and provider adapter the search names as the resumer.

### 2. A note landing wakes the waiter; nobody polls
A caller that has asked waits on one call that returns when the note it names lands or changes, or when a stated ceiling passes, instead of a terminal poll every thirty seconds; and a running Claude session that is party to a discussion hears a new note in the machine's directory the way a lane hears the project watercooler — inside its own session, within a minute, once (plan 10's word). Done means: on a fixture directory, the wait returns within a second of a note being written and returns with "nothing" at the ceiling; a session with hands on a worktree whose card names a discussion note sees the next note's path and first line as its next word and never sees its own; the chair of exchange 5 in the close-out shows zero polls in its rollout. Hands out: `search` — where the hook's word is assembled and read (`api/loops.py`, `board/lane.py`, `hooks/needle_hook.py`, `infrastructure/store.py`'s heard-mark), so a discussion note becomes one more sentence in the word and not a second delivery; verifies by reading the assembler the search names.

### 3. The caller sees the colleague working
While a called session works, one line says what it is doing — the last tool it ran, on what, how long ago — readable by the caller and shown beside the session in `needle sessions`, updated within a minute. Today that list shows a session's slot, state and card, and the warm session of exchange 4 read `idle  interactive` in the chair's own terminal scope for its whole turn; the chair named the gap at 09:31: "neither you nor I can see whether it is reading, reasoning, or stuck". Done means: during a call, `needle sessions` shows the called session as working with its last step and its age; when the session ends its turn the line says so; the board's session list carries the same line; the cost of producing it is measured on a lane's own tool calls and written in the close-out beside plan 10's number. Where to look, not what to write: the hook already runs on every tool call and reads a word; the registry already reads each session's state files and its process; the plan 10 ruling that PostToolUse is a read and never a post was a cost decision and is the first thing this item weighs.

### 4. Six exchanges, read into a ruling
The loop runs on the next six discussion exchanges on this machine, alternating cold (today's form) and warm (items 1–3), each pair matched on the ask's shape: the same count of questions, the same notes named to read. Every exchange gets a row like the baseline's, written by the two parties at the time: the chair writes call time, polls and its own token cost; the called session writes note time, the sources it read that a prior note in the thread had already cited with a path, and its corrections of earlier claims. Done means: the six rows exist in the review record with both parties' numbers, the reading is written as the Loop below says, and the plan's close-out states which of the three outcomes it landed on. Hands out: `execution` — extracts the timestamps and poll counts of every exchange from the Codex rollouts and the Claude transcripts into the table; verifies by re-deriving one exchange's four timestamps by hand.

Loop: six discussion exchanges measured, three cold and three warm, in the review record — file docs/reviews/2026-09-05-asking-a-colleague-takes-a-minute-not-ten.md expect 6 by 2026-09-12

## Terrain
- `runtime/registry.py::sessions` and `needle sessions` — presence already exists: every session on this machine across every slot, with slot, state, kind, card and cwd; on 2026-09-05 it listed the working warm session as `idle`. Item 3 adds what a session is doing; nothing else about presence is missing.
- `claude-acct use <slot> -r <id> -p` — the supervisor's resume, the lifecycle owner; `docs/claude-multi-subscription.md` in the machine repo is its reference. Item 1 wraps it; it never bypasses it.
- `api/doors.py::resume`, `answer`, `discuss` — doors that resume a lane's session with a brief; the Answer door is the closest shape to item 1, bound to a card. `runtime/launch.py` — the launch path.
- `hooks/needle_hook.py`, `api/loops.py`, `board/lane.py::card_of_cwd`, `infrastructure/store.py` — plan 10's word: what a lane has not yet been told, read on PostToolUse with a half-second timeout. Item 2 adds a discussion note as a sentence in it.
- `~/.cache/omarchy/claude-acct/discussion/` — the machine's watercooler (machine CLAUDE.md, "Sessions on this machine"): files, three-day lifetime, no delivery, no presence, one filename collision seen 09:15.
- Proof of search: no verb calls a running session with a brief (`needle move` stops and resumes on another slot; the Answer door resumes a lane with the owner's answer, card-bound); no wait primitive exists — the chair polled `write_stdin` 9, 15, 2 and 8 times; no line says what a working session is doing — `needle sessions` shows `idle`, `working`, `ended`, `blocked`, `interactive`, `background` and the card, and `idle` for a one-shot with hands on. The Codex side has `codex exec resume` and its own hooks (stable in 0.152.1), which is why item 1's verb takes a session id and a brief and nothing Claude-shaped.

## Acceptance criteria
1. A provider-neutral call to a named running session resumes it through that colleague's lifecycle owner with the brief in its first request; an unknown provider or a call whose brief cannot arrive is refused with the reason; no provider process is spawned outside the runtime's launch path.
2. A waiter returns within a second of the note landing and at the ceiling otherwise; a running lane party to a discussion hears a new note once, in its own session, within a minute, never its own.
3. `needle sessions` and the board show what a called session is doing and how long ago, and the cost per tool call is written beside plan 10's.
4. The review record holds six exchange rows with both parties' numbers, and the close-out names the outcome.

## Loop
We think calling a running colleague with the thread, telling the caller when the answer lands, and showing the colleague at work will change the time from ask to answer and the tokens spent per answer in a discussion, because the cost today is context reconstruction and blind waiting, not thinking: three cold replies re-read the same five sources, and the chair re-sent its whole context every thirty seconds to learn nothing. Fixed now: on the six exchanges, a warm reply lands in at most half the time of its cold pair, re-reads at most one source a prior note already cited, and is corrected by a later reader no more often than the cold ones; the chair of a warm exchange polls zero times. If all three hold, items 1–3 stay and the machine's watercooler is treated as delivered, not posted. If warm is faster but corrected more often, the speed was bought with grounding: the ruling becomes cold for a first grounding, warm for follow-ups in a thread, and item 1's brief says which. If warm is not faster, the cost is thinking and the card closes on item 3 alone — a colleague who takes ten minutes visibly is not a frustration, one who takes ten minutes silently is. If the chair still polls, item 2 did not reach the chair's seat (Codex is not a lane and hears no word), and the wait verb is the fix, filed as inside the change.

The ask's shape is the confound: exchange 3 was the narrowest question of the morning and the slowest reply, because the question needed two rollouts and a binary. So pairs are matched on the count of questions and the notes named, and the reader counts them before reading the times.

## Rulings
1. **One provider-neutral verb through each colleague's lifecycle owner, never a second launcher.** `claude-acct` is the first adapter because Claude is the first callable colleague, not because Needle is a Claude organisation. Rejected: a caller running `claude -p`, `claude --resume`, or another provider's process itself — that is the 08:37 launch, unsupervised, which the chair then had to kill; the machine's rule is that the lifecycle owner owns a session's life.
2. **Delivery through the word a lane already reads, not a second channel.** Rejected: a watcher process or a desktop notification per note — a third way to hear, and one that reaches the owner's screen instead of the session that asked. The chair's seat, which is not a lane, gets the wait verb instead.
3. **Two watercoolers stay two.** Rejected: moving the machine's discussion into the store — the directory is where sessions of any make write today, the entrance card makes Codex read it, and the store attributes a line to a card's lane, which a discussant is not (plan 07, ruling 1).
4. **The measure is written by both parties at the time, never reconstructed by one.** Rejected: one session deriving all six rows after the fact from the rollouts — the reader would be a party, and the baseline above was already written that way once; the six new rows are not.

## Close-out
Written by the lane: exchange 4's note time and its polls; the six rows; the per-tool-call cost of item 3; which outcome the Loop landed on; what the chair's seat (a Codex session) could and could not hear.

## Estimate
Execution clock: one lane-day for items 1–3. Gate clock: six exchanges, which at the rate of 2026-09-05 (four before noon) is under a week; the `by` date says so.
