# Review — asking a colleague takes a minute, not ten, and nobody waits blind (plan 17)

**Plan:** docs/plans/done/2026-09-05-17-asking-a-colleague-takes-a-minute-not-ten-and-nobody-waits-blind.md
**Reviewer:** the build session (Claude Fable 5.1 at high, the card's gate), on its own work; walled on hrme mid-review and continued on armana, which the record's own subject makes worth noting. No second session was in the loop; the six exchanges the plan gates on are the reading this record cannot replace, and their table is at the end.
**Diff range:** f2b37c3..HEAD on the lane `card-51-17-asking-a-colleague-takes-a-mi` — one feature commit (the call and wait verbs, the doing line, the note in the word, the store rows and migration 0010, the loop's tending, the tests), the review's fixes in the same commit, and this record with the archived plan and one filed defect.
**Findings:** 5 — 3 fixed before this record, 1 no change with the reason, 1 filed as a defect outside the change.

## The passes

The review ran as a loop (`CLAUDE.md`): each pass one lens, the fixes landed, the next pass re-read the fixed work.

1. **The feature against the plan's "done means".** Item 1: `needle call` resolves a short id, a session id, a slot (its most recent background session) or a transcript's id; it resumes through `Runtime.call` → `launch.call` → `launch.move` with `spent=False` and the brief as the prompt — the floor's launch log shows `--resume <old id>` with the brief as the last argument, the old process stopped first, one live copy after; a colleague no registry holds is resumed by id from the directory its transcript's records name, through the same `argv_for`/`_launch`/`verify`/`_settle`; a session in a terminal of its own, a session mid-turn and an empty brief are refused with the reason and exit 1, and the launch log shows no process for any of them; `needle --help` lists `call` and `wait`. The wall clause ran on the floor (the plan's Deviated line says why not on `fake429.py`): a called colleague that blocks without the detector's file is left and a waiter told `blocked: … is blocked: <detail>`; once the file names the next slot the loop moves it once, the call follows the forked id (`resumed_from` in the registry), the one list holds one live colleague, `moved` on the record reads "moved to fable on beta as <short>: <reason>; the call follows it"; a second wall inside the hour ends the call with "hit a limit again within the hour … this one is the owner's". Item 2: `needle wait` returned in under two seconds with the answer written at 0.6 s, `nothing` at the ceiling with the colleague's step, `ended: … finished its turn without its note` in under eight seconds against a twenty-second ceiling; a lane whose plan names `discussion/from-codex-topic.md` heard the note once, heard nothing of its own append (the hook named the file it wrote), heard the next change by the other party, and heard nothing of a note its card does not name; a lane that called heard both the note it called with and the answer. Item 3: nine live rows on this machine at 13:17Z each carried a step and an age, the two terminal rows included; the card's lane section shows the same line; cost measured (0.4 ms a live row per list read, zero per tool call). Item 4: the table below, with exchange 4 completed and exchange 3 re-derived. Finding 1 came from this pass: the walled-colleague test parked on the first wall.
2. **The seams.** Concurrency: the word's note sentences run under the word's own lock with the mark, the notes are the loop's last read (never a directory read on the hook's path), and the discussion directory is watched like the registries so a note reaches the next tool call; the wait and the loop make one judgment (`runtime/calls.py::judge`), so a call cannot be ended by one and landed by the other; a fork is read from the registry's `resumeSessionId`, so whoever moved the colleague — the lane loop, the readings' tending, the owner's Answer door, the call loop — the record follows with no second mover. Failure and restart: the call is in the store, so a restart tends it on the next beat; a board that is down leaves `needle wait` reading the registry itself; a colleague that left every registry ends the call with that said. The truth of what the board shows: a call's `moved` and `words` are the runtime's sentences, never the verb's guess; the doing line is read from the transcript's own tail. Findings 2 and 3 came from this pass, and finding 5 from what the lane did to the machine while building.
3. **The boundaries.** The board never runs: `board/word.py::notes_word` is pure; the directory is read by `runtime/discussion.py` behind the façade. Layers: `api/loops.py` composes the runtime and the board and nothing below imports it; `runtime/registry.py` reads transcripts through `runtime/transcripts.py`. Typed edges: `Doing`, `Call`, `CallVerdict`, `CallOutcome`, `Note` are Pydantic, mirrored by `needle types` (`call.ts`, `session.ts`, `watercooler.ts`), `tsc` clean, no `any`; the four hand-built sessions in tests and the fixture tool carry the two new fields explicitly. One way: a call is a move, not a launcher of its own; the park rule is one function in the lane loop and the call loop; the wait's judgment is the loop's. One design system: the lane section's new line is a `Quiet`. Nothing half done: no deferral marker; the store's shape ratchet holds migration 0010. The synthetic-fixture ratchet: no real card title in this record. Finding 4 came from this pass.
4. **The fixed work again.** Re-read the loop's tending, the wait and the park rule as committed: `on_a_wall` is the one discriminator in both loops; the calls scan on the word's path is bounded by a day; the bump condition reads as it did before the change. Every test in `tests/runtime/test_calls.py`, `tests/api/test_calls.py`, `tests/infrastructure/test_calls_store.py`, the word tests and the hook script tests green; the whole backend suite with the ratchets green; `tsc` clean; 66 page scenarios. Nothing new. Clean.

## Dispositions

1. **A called colleague parked on its first wall.** The rescue ledger holds every move, including a resume by request — `launch.move` records "resumed with the owner's answer" or "moved by request" — and the park rule read any rescue within the hour as a wall's. A call is such a resume, so the colleague's first real wall read as the second within the hour and the call ended "parked". The same row would park a lane walled within an hour of the owner's Answer. Inside the change for calls, adjacent for lanes, one rule for both. FIXED: `launch.on_a_wall` decides what the park counts, in `_rescue` and `_move_called`; the reasons are named constants; the walled-colleague test holds the move and the park in one run.
2. **The word's party check scanned every call ever made, on every tool call of every lane.** The calls table only grows and the word is the hottest path on the machine (plan 10). FIXED: `Store.calls(since=)` and a one-day horizon; a thread older than that is the card's to name.
3. **The card-page bump condition had grown a clause that read as two rules.** `word.sentences and (mark is not None or party_to)` where `word.sentences` alone says it. FIXED: the condition as plan 10 wrote it.
4. **A colleague resumed from its transcript runs at the CLI's default effort, not the effort it was born with.** A session no registry holds has no `respawnFlags` to read; `argv_for` gets `effort=None`. NO CHANGE: the effort is the lifecycle owner's default for a resume with no record, and the plan's ruling 5 keeps the verb from inventing one; the call's brief says what the answer is for, which is where the effort belongs. Recorded here.
5. **The lane's own `needle` commands migrated the shared store to 0010 before the fold, twice, and blocked a Hello Revenue lane's close.** `Store()` migrates on open, and `uv --project ~/Work/needle run needle …` run from inside a worktree imports the worktree's code, so even the main checkout's command form ran the lane's migration. Outside the change. FILED: `docs/slice-suggestions/2026-09-05-a-lane-that-carries-a-migration-cannot-break-the-shared-store-before-it-folds.md` (`Fix: now`). The store was downgraded by hand both times with nothing lost, and the lane ran against a private copy until the fold.

## What was checked

- **The whole suite on the floor** after every fix: backend tests with the ratchets, `tsc --noEmit`, `vitest run`; the frontend fixture regenerated and current.
- **The doing line, live** on this machine's real registries, read-only: nine live rows with a step and an age; `needle sessions` in 0.54 s wall clock for 202 rows.
- **The cost, measured** on the real registries, five runs each, read-only: the one list in 14 ms with the tails and 10 ms without; 3 ms for nine live rows, 0.4 ms each; the hook untouched, so zero per tool call. Plan 10's word: 0.2–2.4 ms a tool call. The registry's `detail` line costs nothing new; it was always in the state file.
- **Exchange 4 from the rollout** (the `execution` role extracted; the numbers are in the plan's close-out and the table below), and **exchange 3's four timestamps re-derived by hand** as the plan asked: launch 09:16:32.6, the reply's file write 09:28:34.2 (12 m 02 s, as the baseline said), the chair's first sight 09:29:05.7 — and 16 polls where the baseline said 2, which corrects the baseline and is ruling 4's case in one row.
- **The served board after the fold** (origin/develop 585eb8d, dist rebuilt, `needle-serve.service` restarted, the store at 0010): `needle sessions` from the main checkout printed a step and an age under each of nine live rows, the two terminal rows included; `GET /api/projects/needle/cards/51` carried the lane's `doing` (`Bash cd /home/dennis && npm …`, 11:45:41Z) and its `detail`.
- **The first real call, exchange 5 (warm):** `needle call 6f059ca0-… <note>` at 11:47:05Z resumed the hrclaude colleague of exchanges 3 and 4 from its 2.6 MB transcript — no registry held it — on armana, named `call-6f059ca0`; the real CLI kept the session id (`sessionId == resumeSessionId`), where a background session's resume forks it (2026-09-04), and the record handles both. Sixteen seconds after the call the one list read `working … Bash cat …/from-needle-51-call-protocol.md, 16 s ago; "Reading the caller's note and check whether a reply file already exists"` — the caller saw the colleague working. The chair's wait: `needle wait 1 --ceiling 900`, started 11:47:26Z, zero polls by construction; it returned `landed: … at 11:47:58Z` — 53 s after the call, 32 s of which was the daemon starting the resumed session — and the loop ended the call with the same words on its next beat; the colleague's row reads `done` with its last step and age. The reply corrected nothing, re-read nothing a prior note had cited, and answered both questions.
- **Not done, stated:** the wall on the machine's `fake429.py` (the plan's Deviated line under item 1); a real warm exchange chaired through `needle call` and `needle wait` — none has run yet, so the Loop's first reading is still to come; the served board's own reading after the fold, which the close-out below carries once made.

## The six exchanges

Written by the two parties at the time, never reconstructed by one (ruling 4): the chair writes the call time, its polls and its token cost; the called session writes the note time, the sources it re-read that a prior note in the thread had already cited with a path, and its corrections of earlier claims. Pairs are matched on the ask's shape: the same count of questions, the same notes named to read; the reader counts them before reading the times. Times are UTC. The baseline's four rows are completed here; the WATCH row reads this file for the sixth measured row.

| exchange | form | chair | called | call | note | to note | chair polls | chair tokens | questions asked / closed | sources re-read already cited | corrected later | measured |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | cold one-shot | Codex | hrme | 08:43:11 | 08:48:07 | 4 m 56 s | 9 | ~103 k a poll at 98 % cached | 5 / 5 | 5 | 6 across rows 1–3 | baseline, one party |
| 2 | cold one-shot | Codex | armana | 09:06:38 | 09:15:36 | 8 m 58 s | 15 + 3 directory reads | as above | 5 / 5 | 5 | — | baseline, one party |
| 3 | cold one-shot | Codex | hrclaude | 09:16:32 | 09:28:34 | 12 m 02 s | 16 (13 `write_stdin`, 1 `wait`, 2 status) | as above | 4 / 4 | 5 | — | re-derived by hand |
| 4 | warm resume of 3, by hand | Codex | hrclaude | 09:30:00 (two refused attempts before it) | 09:40:48 | 10 m 48 s | 27 (16 `write_stdin`, 5 `wait`, 6 status), 10 in the first five minutes | as above | 3 / 3 | 3 of 4 reads | — | from the rollout and the transcript |
| 5 | warm, `needle call` + `needle wait` | the #51 lane (Claude) | hrclaude's 6f059ca0, resumed on armana | 11:47:05 (the note written 11:46:59) | 11:47:58 | **53 s** (the brief was its first tool call at 11:47:19) | **0** — one `needle wait`, returned on the file | one call and one wait, ~2 k tokens of the chair's context | 2 / 2 | 0 (it read the call note and nothing else) | 0 | both parties at the time |
| 6 | cold, the same two questions, Codex's to chair | | | | | | | | | | | |
| 7 | warm, `needle call` + `needle wait`, the first call to a colleague of the other make | the #57 lane (Claude) | Sol's rollout 01a07211 (`codex exec resume`, in `/home/dennis/Work/needle`) | 16:07:49 (the note written 16:07:12) | 16:08:58 (Sol's own `Finished:` 16:08:48) | **1 m 09 s** (the resume registered a process at 16:07:55; the first of five `exec` tool calls at 16:07:56, the last message at 16:08:57) | **0** — one `needle wait`, returned on the file the CLI wrote with `-o` | one call and one wait, ~1 k tokens of the chair's context; the worker used 39,633 | 2 / 2 | 2 (`runtime/codex.py`, `runtime/launch.py` — the note itself cited both; nothing older) | 2 claims in the note corrected (a source allowlist, not a default to worker; the tool-call set), both taken into the lane before the review closed | both parties at the time |
| 8 | cold, the same note and the same two questions, a fresh `codex exec` one-shot by the same chair | the #57 lane (Claude) | a fresh Codex session in `/home/dennis/Work/needle` | 16:10:05 | 16:11:44 (Codex's own `Finished:` 16:11:34) | **1 m 39 s** | 0 — the chair blocked on the one-shot's exit; no terminal was read | one blocking launch, ~1 k tokens of the chair's context; the worker used 49,741 | 2 / 2 | 2 (the same two files the note cited) | the same 2 corrections as row 7, in the same words, plus one more sentence on the `name` fallback | both parties at the time |
| 9 | warm | | | | | | | | | | | |
| 10 | cold | | | | | | | | | | | |

Rows 7 and 8 are the #57 lane's matched pair (2026-09-05): the warm call to a Codex worker through the verbs, then the same note cold to a fresh Codex one-shot by the same chair — the warm reply landed in 70 % of the cold one's time on 80 % of its tokens, re-read the same two sources (both named in the note, none older) and made the same two corrections; neither was corrected by a later reader. Row 6 stays as the record designed it (row 5's cold pair, Codex chairing) and is not this pair's. Row 5 ran first and warm, so the alternation is warm-cold from here; its cold pair is row 6, the same two questions chaired by Codex with a one-shot. The sixth measured row of 5–10 ends its last cell with the word `six-measured`, which is what the WATCH row expects. Card #57 watches the same table for its own loop: the next warm exchange with a colleague of another make, chaired by someone other than #57's lane, ends its last cell with the word `codex-warm-again`. Two marks, two loops, one table.

The called party's own line for row 5, verbatim from its reply (`from-6f059ca0-re-51-call-protocol.md`): "Read: the call note only. Nothing re-derived. … sources re-read that a prior note in the thread had cited with a path: 0 | claims of earlier notes corrected: 0". Its answer to the second question: keep the step on the transcript tail — a Codex colleague's rollout records every tool call with a timestamp for the same list-time reader, a subagent's dispatch reads honestly as "dispatched, waiting" with the sidechain beside it, a session thinking between tool calls is one the hook cannot see at all, and the tail's one blind spot (a session started from inside another stops saving its transcript — the machine doc's `CLAUDE_CODE_CHILD_SESSION` trap) is exactly the launch shape the plan refused; the runtime's launch path strips the marker. One warm reply, 53 s, corrected nothing and re-read nothing; one row is not the reading, and the plan's gate is six.

## The reading, when the six rows exist

Fixed in the plan before the build: a warm reply lands in at most half the time of its cold pair, re-reads at most one source a prior note already cited, and is corrected by a later reader no more often than the cold ones; the chair of a warm exchange polls zero times. All three hold: items 1–3 stay and the machine's watercooler counts as delivered, not posted. Warm faster but corrected more often: cold for a first grounding, warm for follow-ups, and the brief says which. Warm not faster: the cost is thinking, and the card closes on item 3 alone. The chair still polls: the wait verb did not reach the chair's seat, and that is inside the change.
