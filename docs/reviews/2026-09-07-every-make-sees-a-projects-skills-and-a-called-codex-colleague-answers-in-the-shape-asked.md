# Review — every make sees a project's skills, and a called Codex colleague answers in the shape asked

**Plan:** docs/plans/done/2026-09-07-every-make-sees-a-projects-skills-and-a-called-codex-colleague-answers-in-the-shape-asked.md
**Reviewer:** the build session (Claude Fable 5.1) through the three lenses in turn, and a colleague of another make — Codex 0.153.4, `gpt-6-astra`, through `codex review` read-only (item 3); every delegated line read at the file before it was used
**Diff range:** 62c6964..2ab8cc0 (the Codex pass's second run; its first ran on the same code before the rebase, from 66a0b06)
**Findings:** 7 from the Claude passes — 6 fixed in this lane, 1 no change with the reason; 0 from the Codex pass, twice

## The passes

The review runs as a loop (`CLAUDE.md`): each pass one lens, the fixes landed, the next pass re-reads the fixed work.

1. **The feature against the plan's "done means".** Item 1: `tests/api/test_cli.py::test_hook_install_lays_the_codex_skills_link_once_and_leaves_a_projects_own_alone` holds laid-and-said, silent without skills, a second run a no-op, a project's own directory and a link elsewhere left alone and named; Hello Revenue's tree carries `.agents/skills -> ../.claude/skills` as commit 17967ef6d on its trunk, pushed, naming this card; the read-only `codex exec` from its root listed the seven skills by name and quoted `hr-plan-write`'s description, checked against line 3 of that SKILL.md. Running the done-means live turned up finding 1: the installer, run from this lane by `uv --project <worktree>`, registered the lane's own hook path in three projects' settings before the line was read — the path dies with the lane, silently. Restored by hand and diffed against the trunk; the installer now refuses a lane. And a deviation, finding 6: the installer was not run in full on Hello Revenue. Item 2: the shape is `domain/call.py::Answer`; the schema lands beside the answer; the flag is on the argv; the reader is one function; the tests cover the shape, prose and off-shape JSON; live, a `needle call codex` on a private store landed the shape and the wait reported the `answer` field as its words (the plan's Met line quotes it), and the worker's claim — the first heading of the skill file — is line 6 of that file. Item 3: the Codex pass below. Findings 1 and 6 came from this pass.
2. **The seams.** Concurrency: the schema is written before every call at a path named for the answer; two calls on one answer path write identical bytes, and the write is a single `write_text`. Failure and restart: a schema that cannot be written is a dead launch with the reason; a Codex that refuses the schema exits without an answer and is reported "ended without an answer" with the log's last words, as before; an answer that is not the shape is read by its first line and says so, so nothing is lost; `_answered` still stats the file and a fast reply still lands. The truth of what the board shows: the call's verdict and the loop's tending share the reader; but `runtime/discussion.py::note_of` read a note's head with a first-line reader of its own, so a Codex reply landing as `from-<id>-re-<topic>.md` in the discussion directory would have been heard on the watercooler as the raw JSON — finding 2, fixed by routing the note's head through the one reader, and its test now holds a shaped reply heard by its `answer`. The fake `codex` claimed Codex refuses a schema that is not JSON in words nobody had checked — finding 3, verified live (`Output schema file … is not valid JSON`, exit 1, no answer file) and the fake's words aligned. Findings 2 and 3 came from this pass.
3. **The boundaries.** Layers: nothing under `board/` changed; the reader lives in `runtime/`, and `runtime/discussion.py` imports it from `runtime/calls.py`. Typed edges: `Answer` and `HowKnown` regenerated into `frontend/src/types/call.ts` by `needle types`; `tsc` clean. One way: the skills folder is one constant (`api.doors.SKILLS`) read by the Start door and the installer; two first-line readers became one. Nothing half-done: no deferral marker. Every commit names #73. The module head's verb line said the installer registers the hook and arms git's hooks and not that it lays the link — finding 4, [record]. `domain/call.py` said `additionalProperties: false` is "what Codex's structured output requires", a claim from recall with no source — finding 5, [verification], now a fact about the reader and what the live call showed. `ruff` on the touched files: five lines of this lane's tests over the project's hundred columns — finding 7; three pre-existing long lines in `api/board_cli.py` are outside the change and untouched. Findings 4, 5 and 7 came from this pass.
4. **A colleague of another make (item 3).** `codex review -c sandbox_mode="read-only" --base origin/develop`, from the lane, twice: once on the code before the rebase (session 01a07bb0, five commands, diffed from 66a0b06) and once on the final tree after pass 3's fixes (session 01a07bb6, five commands, diffed from 62c6964, read `docs/INTENT.md`, `CLAUDE.md`, the plan, `runtime/calls.py`, `runtime/discussion.py`, `runtime/launch.py` and `api/runtime_cli.py`, and tried to run the four test files). Its final message, verbatim, first run: "No actionable regressions were found in the diff or affected call paths. Test execution was blocked by the read-only sandbox, so this verdict is based on code inspection." Second run: "I found no actionable regressions in the diff or affected callers. The targeted tests could not start because the read-only sandbox prevented pytest from creating temporary files, so runtime verification remains incomplete." Findings: 0; surviving verification: 0. Beside it, the Claude passes: 7 findings, 6 fixed. The plan's second loop reads from these two counts. Two things the command taught: `codex review` takes no prompt beside `--base`, and its sandbox is set through `-c sandbox_mode`, not `-s`.
5. **The fixed work again, clean.** Re-read the installer and its refusal, the note reader on the one reader, the fake's refusal, the two docstrings; ran the backend suite with the ratchets, `tsc` and the frontend tests on the tree this record is committed to. Nothing new.

## Dispositions

1. [seam] `needle hook install` run from a lane registers the lane's worktree path as the hook's script, which dies with the lane; three projects' settings carried it for a minute on 2026-09-07 — FIXED in 8832e68: the installer refuses to run from a worktree, with the reason; `tests/api/test_cli.py::test_hook_install_refuses_to_run_from_a_lane`. The three files were restored by hand and the main checkout's diffed identical to the trunk's before the commit.
2. [seam] `runtime/discussion.py::note_of` read a note's first line itself, so a Codex reply in the shape would be heard on the watercooler as a JSON blob — FIXED in 69c6026: the head comes from `runtime/calls.py::read_answer`; the notes test holds a shaped reply heard by its `answer`.
3. [verification] The fake `codex` said Codex refuses a schema that is not JSON, in words nobody had checked — FIXED in 69c6026: verified live (exit 1, "Output schema file … is not valid JSON", no answer written) and the fake's stderr is Codex's own sentence.
4. [record] The module head of `api/board_cli.py` listed the installer's verb without the link — FIXED in 69c6026.
5. [verification] `domain/call.py::Answer` claimed what Codex's structured output requires, from recall — FIXED in 2ab8cc0: the docstring says what the closed schema does for the reader and that a worker held to it answered in it first time.
6. [feature] Item 1's letter runs the installer on every registered project; on Hello Revenue only the link was laid, through the installer's own function — NO CHANGE: card #60's review recorded that registering Needle's UserPromptSubmit hook there fires two anchors on one word until that project's card retires its own `backbrief.sh`, and that seam is that project's; the plan's Met line says so. The other three registered projects have no `.claude/skills`, so the installer had nothing to lay there.
7. [boundary] Five lines of this lane's tests exceeded the project's hundred columns (`pyproject.toml`, ruff E501) — FIXED in 69c6026.

## What was checked

- **The installer, by test and live.** The link test's four cases and the refusal test; the installer on `needle`, `omarchy-machine` and `hr3` (nothing laid, no skills folder) and the link function on Hello Revenue, by script, the output read verbatim; each project's settings read before and after; the link's target read with `ls -la`.
- **Codex's reading of the link.** `codex exec -s read-only` from Hello Revenue's root, stdin closed (open, it waits on stdin — the first run hung on that for five minutes), listed all seven skills and quoted the plan-writing skill's description, diffed against the SKILL.md.
- **The call, by test and live.** The three reader cases in `tests/runtime/test_calls.py`; the schema beside the answer in `tests/runtime/test_codex.py`; both makes' briefs in `tests/api/test_calls.py`; live, `needle call codex` on a private copy of the store resumed worker 01a07bae in Hello Revenue's root and `needle wait 4` returned the `answer` field as its words, the answer file read, the schema file read, the log's last message read, the claim checked at the file. The private store was deleted afterwards.
- **Codex's own refusal of a bad schema**, live, for finding 3.
- **The suites.** Backend with ratchets, `tsc --noEmit`, `vitest run` (68), all green on the tree this record lands on.

## What the build learned the plan got wrong

- The plan's item 1 sent the lane to run the installer itself; the installer registers this checkout's script by absolute path, so from a lane it registers a path that dies with the lane. The plan named the silent failure as an uncommitted link and missed the other one in the same command.
- The plan named `codex review --base <branch>` with a prompt; the command takes one or the other, and its sandbox is a config key.

## Not done, stated

- The installer was not run in full on Hello Revenue (disposition 6); that project's card retires its own backbrief hook first.
- The Codex pass ran with `reasoning effort: none` — Codex's default for `review` on this machine; no configuration was changed for it, and the plan's second loop reads the count as it is.
- The plan's first loop (a Codex rollout in Hello Revenue reading a `SKILL.md` under `.agents/skills/` within fourteen days) is open; the card's WATCH row names it.
