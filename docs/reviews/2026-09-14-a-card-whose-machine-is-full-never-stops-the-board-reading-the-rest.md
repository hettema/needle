# Review — a card whose machine is full never stops the board reading the rest (#148)

**Plan:** docs/plans/done/2026-09-14-a-card-whose-machine-is-full-never-stops-the-board-reading-the-rest.md
**Reviewer:** session 5ca8b465 (signed session_013MTPM9RWcE8px5o31v1uFx), a cold Claude session of this lane's own make with no share of its context, resumed from the ended reading session e978a9a4 through `needle call` (call 135) on the note at `/home/dennis/.claude-accounts/armana/jobs/448e8b2c/tmp/review-note.md`; its answer is at `/home/dennis/.claude-accounts/armana/jobs/448e8b2c/tmp/from-e978a9a4-re-review-note.md`. It read the whole diff and the plan, traced seven named questions through `api/dial.py`, `api/loops.py`, `domain/machine.py`, `runtime/service.py` and the store, ran the new module, `tests/api/test_dial.py` (95), `tests/api/test_defects_column.py` (7), `tests/api/test_the_triage_seat.py` (19) and every ratchet in the lane, and re-ran the plan's item 3 reverts in a scratch copy of the worktree.
**Diff range:** c3abdcd..7e9e5e1 reviewed; extended in 38abadf for finding 2 (final code revision), documents in e9a4c1a and this record after it.
**Findings:** 4 — 0 demonstrated defects inside the change; 2 adjacent hypotheses (one fixed in the lane, one filed); 2 preferences on the lane's own documents (both taken). None rejected.
**Verification:** the whole suite one module per process on 7e9e5e1 through an execution hand-out (106 modules green; `tests/api/test_the_triage_seat.py` killed by the 600 s cap and green alone, 18 passed in 1114 s); the ten modules the extension touches re-run one per process on 38abadf, all green; each item's test fails with its item reverted. Evidence below.
**Completion:** the plan's three items hold with `**Met:**` lines on the plan; the one adjacent finding that serves this slice's intent is fixed inside the change with its own test, the one that does not is filed as its own defect; nothing remains unverified except the Loop's first-day reading, which the WATCH row carries.

## Dispositions

1. [feature] the seven questions the note asked — the rooms' source and staleness, `choose_machine` on one- and two-machine boards, the spell's writers, the history reads per beat, the tests' discrimination — NO CHANGE: each traced by the reviewer to the line and answered as the change intends. Demonstrated: `tick_now` calls `headroom_now()` first on every beat (api/dial.py:255) and `Live.machine.machines` and `Live.headroom` come from that one read, so `_openable` never reads rooms staler than the ones `_full()` judged; on a one-machine board `choose_machine` returns the machine whatever its room and `_full()` alone refuses; on a two-machine board only a ground project's cards are ever left out; the only DIAL/MACHINE writers that touch an unread card are the started note, the launch refusal, the death note and #138's fuse, none per beat; the left-out set is one ground's handful of cards, each `history` read one indexed query.

2. [seam] `api/dial.py::_triage` — *a hypothesis, traced not run, adjacent:* a launch refused for a cause the rooms do not carry (a ground whose machine did not answer: `choose_machine` returns the ground when its room is None and `_started_elsewhere` is then refused; or no subscription with allowance) still spent the beat's one act every minute, the old hold with a different cause. Promise: the carried suggestion's own Fix line names the class as "any candidate the runtime refuses to open this beat, for room or any other reason the refusal names", so this serves the slice's intent. FIXED in 38abadf: `_triage` answers the launch's refusal (None when the act was spent), `_take_next` passes that project over for the rest of the beat and opens the next project's oldest, and the refusal is written once per spell through the same reader as a left-out note (`_say_once` over `board/dial.py::REFUSED`). Verified by `tests/api/test_a_card_whose_machine_is_full_never_stops_the_board_reading_the_rest.py::test_a_launch_refused_for_another_cause_holds_the_beat_no_more_than_room_does`: every start in the ground project refused for "no subscription has allowance", five beats each open the fixture project's next card on the rented machine, the ground's card carries one refusal and no left-out note. A first attempt to play the cause with the rented host down showed the floor keeps a machine's last room (card #123) and places every project there, so the refusal is played on the launch itself.

3. [boundary] `api/dial.py::_plan` — *a hypothesis, traced not run, adjacent and outside the plan's items:* a verified defect of a ground project whose machine is full goes to `_plan`, the planning start is refused, the fix lane is ended before it began and `_ran` keeps the card out of every later beat — the card becomes the owner's for a cause that clears by itself. Filed as `docs/slice-suggestions/2026-09-14-a-defect-that-fixes-itself-waits-for-room-on-its-machine-instead-of-landing-on-your-desk.md` (`**Kind:** defect`, `**Fix:** now`).

4. [record] the plan's Loop line counted only the launch's refusal, so a regression of item 2 (a left-out note per beat) would have read 0; and the first test's message said "on the rented machine" while the floor's launch log is shared between hosts and did not say which machine launched. TAKEN in 38abadf and e9a4c1a: the Loop's query counts both prefixes and the thesis says so; the first test asserts the launch's `config_dir` under the rented floor's root.

## Verification evidence

**On the reviewed revision 7e9e5e1:** the whole suite, one module per process, sequential, through an execution hand-out (`timeout 600 uv run pytest -q <module> -p no:cacheprovider`; summary at `/tmp/needle-148-suite/summary.txt`, logs under `/tmp/needle-148-suite/out/`): 106 modules rc=0 with no F or E mark; `tests/api/test_the_triage_seat.py` killed at 601 s by the cap after 9 dots, re-run alone unbounded `18 passed in 1113.88s`. The two trunk reds other lanes noted did not appear: `test_defects_column.py` 7 passed, `test_a_card_that_finishes_or_needs_you_rings.py` 7 passed. The reviewer's own runs in the lane agree (above). Item 3's reverts, run by this lane and again by the reviewer in a scratch copy: with `_openable` bypassed the first test fails `assert 0 == (0 + 1)` naming proj #105 as the card that should have opened and the second fails `assert (None is not None)`; with the spell check bypassed the second fails `assert 20 == 1`; the tree restored (`git status` clean) after each.

**On the final code revision 38abadf:** the ten modules the extension touches, one per process (`/home/dennis/.claude-accounts/armana/jobs/448e8b2c/tmp/rerun.sh`, summary at `…/tmp/rerun/summary.txt`, every log with no F mark): the new module (3 tests, 344 s), `tests/board/test_dial.py`, `tests/ratchets` (all), `tests/api/test_defects_column.py`, `test_parked_cards.py`, `test_title.py`, `test_dial.py` (524 s), `test_the_triage_seat.py` (1147 s), `test_reading.py`, `test_doors.py` — all rc=0. `ruff check` and `ruff format --check` clean on every file this lane touched. No frontend file changed, so `tsc` and `vitest` were not run. No migration.

**Limits:** the reviewer read 7e9e5e1; the extension in 38abadf (finding 2) was not read cold, by the execution decision that it is fifteen lines the reviewer itself proposed, held by its own test and the module re-run above. The served board on the laptop runs this code only after its restart, which the close does; the Loop's first reading is the next day's.
