# Review — work waiting for your feedback stays ready for your answer (card #155)

**Plan:** docs/plans/done/2026-09-16-work-waiting-for-your-feedback-stays-ready-for-your-answer.md
**Reviewer:** one fresh Codex thread, the other make, with no share of this lane's context — call 241, `needle call codex --fresh docs/reviews/notes/2026-09-16-155-cold-read.md`, thread 01a0ab51, effort high, read-only. The note is `docs/reviews/notes/2026-09-16-155-cold-read.md` and the answer is kept verbatim beside it at `docs/reviews/notes/from-codex-re-2026-09-16-155-cold-read.md`. Before the build, a challenge round of the same make (call 240, `docs/reviews/notes/2026-09-16-155-challenge.md` and its answer) landed four material corrections in the plan; those are the plan's, not this review's.
**Diff range:** ccb7b74..3e8f0b6 — the revision the reader was given, frozen while it read (its answer names HEAD as 3e8f0b6 throughout). The repairs are in e29587d, the final revision this record verifies.
**Findings:** 4 — all four demonstrated defects with reproductions the reader ran against 3e8f0b6, all four repaired in the lane. No rejections, nothing filed outside.
**Verification:** below, under its own head.
**Completion:** every verified in-scope finding is resolved, both items of the plan carry their stance, and the final revision is green module by module on the modules this change touches and on the ratchets, with the whole suite run once module by module before integration. One limit is named at the end and it is not this change's.

## Dispositions

1. [feature] `board/lane.py:728` (`answered_after`) — counted every ANSWERED audit row as the owner's answer, but `api/doors.py:565` writes that kind when his answer *fails* to resume the lane. Reproduced: a current ASK, a failed answer, a quiet ending — the reader returned None and the exit sent the card to Up next; without the failed row it chose Decision moment. Violates the checkpoint promise. — FIXED in e29587d: the failed attempt is written as the machine's report (`Actor.MACHINE`, same kind, `api/doors.py`), and `answered_after` counts only his own rows. Pinned in `tests/board/test_lane.py::test_an_ended_lane_with_a_current_ask_waits_for_the_owner_instead_of_starting_again` (the machine's report leaves the question standing) and `tests/board/test_parked.py::test_a_ruling_nobody_ruled_on_is_a_commitment_and_a_ruled_one_is_not` (the same for the parked card's commitments).
2. [feature] `board/assemble.py:419` (`_lane_died`) — the face said "asked you" while the head counted the same card under "lane died": the claim never read the sentence. Reproduced by the reader through `summarize` and `claim_counts`. Violates "not presented as lost work". — FIXED in e29587d: a lane whose sentence opens as his move is not a death, so the head counts it as a card in Decision moment and nothing else. Pinned in `tests/api/test_doors.py::test_a_lane_that_ends_at_a_checkpoint_it_handed_over_waits_for_the_owner`, which now reads the head's counts beside the face.
3. [feature] `board/lane.py:564` — the sentence told him to resume unconditionally, while the doors refuse Resume when the lane's copy of the code is gone (`board/lane.py:1416`) or when no session is known (`:1442`). Both reproduced. Violates the plan's "a clear path to continue". — FIXED in e29587d: both his-move sentences (the one that read the last words, which predates this change, and the new one) compute the way back from what is on disk and known — Resume with the session and its copy; Start opens a fresh copy when its own is gone; with a copy and no session, Start is closed while it stays, in the Start door's own words. Pinned in `tests/board/test_lane.py::test_an_ended_lane_with_a_standing_decision_reads_as_his_move_not_a_death`, which asserts each sentence against the doors' own answer.
4. [seam] `board/parked.py:168–180` (`commitments_of`) — the parked card's commitments still read the first ASK and let any RULED silence every RULING, so after this change's exit the lane's reader named the new request while the parked reader named the old one, or none; the stale-move safeguard at `api/doors.py:1391` then saw no commitment for an outstanding RULING. Reproduced. Adjacent seam this change reaches, and the fix serves this slice's intent. — FIXED in e29587d: `commitments_of` reads `newest_row` and the one `ruled_on` in `board/lane.py`, which `owner_decision_outstanding` reads too, so there is one reading of "the current request" for the lane's ending and the parked card alike. Pinned in the parked test named under finding 1.

Two things the review did not raise, recorded because the record must say what changed: (a) before the reader's revision, my own re-read found that an imported card carrying RULING and RULED rows with no writing on the history would have read as outstanding under the reader's first form; it was repaired and pinned in 3e8f0b6 before the call. (b) The rings module's `test_a_stop_that_becomes_an_exit_inside_the_grace_is_one_ring_the_exits` expected a lane that ended on "Merge it?" to go to Up next; it now goes to Decision moment, because `disposition` already read those last words as his (plan 68, ruling 7) and the exit now asks the same reader. The test's thesis — the exit is the one ring and the question never rings a second time — is unchanged and still pinned; its expected column and ring text follow the exit.

## Verification evidence

What the reader ran, on 3e8f0b6 and read-only: the complete diff `ccb7b74..3e8f0b6` and the lifecycle, storage, display, door and parked-reading dependencies; `tests/board/test_lane.py` and `tests/board/test_parked.py` (56 passed, `--noconftest -s -p no:cacheprovider`); the four reproductions above against the revision; `git diff --check`. Fixture-backed api tests were blocked by its sandbox and not run there.

What this lane ran, one module per process from the worktree, before the review at 3e8f0b6: `tests/board/test_lane.py` (41 passed), `tests/board/test_parked.py` (15), `tests/api/test_doors.py` (31), `tests/api/test_work_the_laptop_interrupted_comes_back.py`, `tests/api/test_parked_cards.py`, `tests/ratchets` — all green; `tests/api/test_a_card_that_finishes_or_needs_you_rings.py` had the one failure named in (b) above, which is the behaviour change, not a regression.

After the repairs, at e29587d, one module per process: `tests/board/test_lane.py` (41 passed), `tests/board/test_parked.py` (15), `tests/board/test_assemble.py` (9), `tests/api/test_doors.py` (32, after the head assertion learned to key on the claim's value rather than its words), `tests/api/test_a_card_that_finishes_or_needs_you_rings.py` (6), `tests/api/test_parked_cards.py` (2), `tests/api/test_work_the_laptop_interrupted_comes_back.py` (21), `tests/ratchets` (93). `ruff check` clean on every file this change touches (the two E501 lines it reports are the trunk's own, in `api/board_cli.py` and the rings module's untouched lines).

The whole suite, module by module, at e29587d, handed to an execution colleague and read back from its log (`suite.log`, 88 runs: every `tests/api`, `tests/board`, `tests/infrastructure` and `tests/runtime` module one per process, plus `tests/ratchets` as one run): 87 runs green, one run with one failure — the trunk's own `test_defects_column` red named under Limit. No timeout, no crash. About 90 minutes on the rented machine.

Nothing under `frontend/` changes: the `Lane` model the page mirrors gains no field (`LaneFacts` is the loop's own), so `tsc` and `vitest` were not run in this lane; the ratchet that holds the generated types against the models is in the run above.

The served board after the fold: SERVED_RESULTS

The Loop's first reading: LOOP_RESULTS

Limit: `tests/api/test_defects_column.py::test_a_defects_reading_without_a_grade_is_refused_and_a_graded_one_shows_its_word` is the trunk's own red, filed 2026-09-14, and is not this change's.
