# Review — auto-fix is switched on per project, not for every project at once

**Plan:** docs/plans/done/2026-09-12-auto-fix-is-switched-on-per-project-not-for-every-project-at-once.md
**Reviewer:** a Claude session of the lane's own make (Opus, the `gmail` slot, session 8d7fb4a3 resumed from 9b5e8d75), Needle call 107, reading the lane's worktree cold on this machine, asked through `/home/dennis/.cache/needle/notes/from-needle-80-auto-fix-per-project-review.md`; its answer is beside the note (`from-9b5e8d75-re-80-auto-fix-per-project-review.md`) and quoted in the dispositions. The board's team for the card was the hand alone, so nobody challenged the plan before build.
**Diff range:** 8899429..ef82c0b (the revision frozen while read: the feature commit with the plan's stances; rebased over origin/develop before the fold as 22a7caf, content unchanged). Repairs landed after it in aef29f3 (9907a07 after the rebase); the implementing lane verified them, and the reader is not represented as having read them.
**Findings:** 7 — five repaired, one disproved with evidence, one recorded as a preference and left.
**Verification:** `tests/api/test_dial.py` and `tests/board/test_dial.py` pass on aef29f3 with a case per repaired finding (the class-closer extended for finding 1; new cases for 2, 3, 4 and 6); `tsc` clean and `vitest` 83 passed on the frontend at ef82c0b, unchanged since; ruff clean on every changed file but the pre-existing long line in `api/board_cli.py`'s usage head; the full backend suite on aef29f3 is under "Verification evidence"; migration 0022 rehearsed up and down on a copy of the served store.
**Completion:** Complete: every verified finding inside the change is repaired and its repair holds in a test; finding 5 is a property of any downgrade and is disproved as a defect with the evidence below; finding 7 is a preference the plan chose deliberately and is recorded. What remains unverified is named in the plan's item 3 deviation: the served page in a browser against a live board, which the fold's restart and the card's WATCH row show.

## Dispositions

1. [feature] `api/dial.py::_follow` → `_start` (ef82c0b lines 557, 702) — a plan written while a board was on was still started by the beat after the owner turned that board off, and the Loop's counter, reading the planning moment only, printed 0 while it happened; violated the intent ("if I auto fix HR it only fixes HR defects") and the plan's `Class:` line, which says a card *started* on an off board is what item 3's verb counts. Demonstrated by the reader's probe (turn on, plan, land the plan, turn off, tick → one launch with `--worktree`, the card in Executing, count 0) — FIXED in aef29f3: `_start` reads the board's switch before the Start door and, off, holds the plan with "Start waits: this board's switch is off" on the record once (the same wait path a closed door takes); `board/dial.py::held_lanes` takes `switched_on` and counts such a plan as held, so it takes no slot and the head says "1 held"; `infrastructure/live.py::switched_on` is the one reader both use; `Dial.fixes` reads `switch_was_on` at the planning moment and, once started, at the start moment too. `tests/api/test_dial.py::test_the_beat_plans_a_defect_only_on_a_board_whose_switch_is_on` now lands B's plan, turns B off, ticks (no launch, Planned, held 1, the record's line), turns B on, ticks (started, Executing) and counts 0; `tests/board/test_dial.py::test_held_lanes…` holds the off-board planned card as held. The plan's item 3 stance names both moments.
2. [seam] `api/board_cli.py:713` — `needle dial` on a board with no project raised `StopIteration` (`next(iter(live.projects))`), where before the change the verb needed no project. Demonstrated by the reader against an empty temporary store — FIXED in aef29f3: the machine's number has its own store read, `Store.fix_lanes_at_most` (`dial`, `dials` and both verbs read it there), and `needle dial` with no project prints "no project is on the board; N fix lanes at most". `test_needle_dial_reads_the_number_on_a_board_with_no_project`.
3. [record] `infrastructure/store.py::turn_dial` — turning off a board that had no row inserted the row before comparing, so a turn that changed nothing wrote a row while the docstring said it writes nothing. Demonstrated by the reader on a rehearsed store — FIXED in aef29f3: a turn to off on a board with no row returns before writing (off is how a board is born). The switch test turns `two` off twice and reads the `dial` table unchanged and the audit unchanged.
4. [seam] `board/dial.py::switch_was_on` — read the audit in id order and broke at the first row after the moment, so a row whose `at` stepped back behind its predecessor (a clock step; a second writer) could end the scan early. Hypothesis, not reachable through today's one writer — FIXED in aef29f3 regardless, since the fix is cheaper than the argument: the rows are sorted by `(at, id)` and none ends the scan. `tests/board/test_dial.py::test_a_boards_switch_at_a_moment_is_read_from_the_audit_of_turns` covers pre-#80 rows, per-board rows, a number-only row and the moment before any turn.
5. [verification] migration 0022's `downgrade()` deletes the per-board audit rows, so a downgrade-then-upgrade cycle would erase every per-board turn and `switch_was_on` would read False for a lane planned in that window — NO CHANGE: a downgrade returns the store to the shape 0021's code reads, and that shape cannot hold a per-board turn — kept with its board dropped, 0021's code would read one board's turn as a turn of every board, which is the one lie worse than the gap. The pre-#80 rows (no board) survive, which the reader confirmed on its rehearsal and this lane on its own: the served store's copy downgraded to row `(1, 0, 4, 2026-09-05T18:34, 2026-09-05T01:20)` with three audit rows, byte-for-byte the pre-card shape. The one real downgrade this lane ran (see "Verification evidence") happened before any per-board turn existed, so nothing was lost; a downgrade after a fold is not a path the board takes.
6. [seam] `api/board_cli.py::fixes` (a preference by the reader) — `--count` without `--started-off` was accepted and ignored — FIXED in aef29f3: refused with "--count counts the lanes --started-off selects; name both", exit 1, covered in the class-closer test.
7. [feature] `frontend/src/components/ui/index.tsx::DialControl` (a preference by the reader) — the number input commits `onTurn(dial.on, lanes)`, so a page holding a stale switch re-asserts it when only the number was meant to change — NO CHANGE: plan 11's shape, kept deliberately by this plan (the note to the reader named it), and the blast radius is now one board rather than all; the route already accepts `on: null`, so the change is one line in the control when it is wanted. Recorded here, not filed: it names no intent it breaks.

Noted by the reader, not a finding: `board/assemble.py::NO_DIAL` carries `Dial(project="")` — inert, since the control posts the page's own slug and the store answers every real page.

## Verification evidence

- Independent reading: Needle call 107 at ef82c0b. The reader ran `git diff 8899429..ef82c0b`, read the files it names, rehearsed the migration up and down on a temporary copy shaped like the served store, ran `main(['dial'])` against an empty temporary store, ran `uv run pytest tests/api/test_dial.py -k switch` in the worktree (2 passed), and drove a probe through the lane's own test helpers to demonstrate finding 1 — none of it against the shared store.
- Repairs: `uv run pytest -q tests/board/test_dial.py tests/api/test_dial.py -k "switch or no_project or held_plan or off_until or reads_and_turns or audit_of_turns"` — 8 passed on aef29f3; ruff clean on every changed file.
- Frontend: `cd frontend && npx tsc --noEmit` clean and `npx vitest run` 83 passed at ef82c0b; no frontend file changed after.
- Migration on real data: `0022` rehearsed on a copy of `~/.local/share/needle/needle.db` (0021, one dial row `on=0, lanes=4`, three audit rows, `rail_at_on` for hellorevenue and needle): after the upgrade `dials()` reads every board off with the number 4, hellorevenue and needle carrying the 2026-09-05 first-on on their own rows, the three audit rows reading as turns of every board. The downgrade rehearsed on a second copy restores the pre-card row and both tables' SQL exactly.
- The shared store: at 15:50Z this lane ran a `needle` verb from inside its worktree, which upgraded the served store to 0022 (the #56 finding the plan's terrain warns of) and broke the main checkout's verbs and the other machine's reading for about ten minutes; the lane downgraded it with 0022's downgrade after rehearsing on a copy, confirmed `needle machines` from the main checkout, kept the 0022-shaped backup in its job directory, and said so on the watercooler. Every later lane verb ran against a private copy under `NEEDLE_DATA_DIR`, and board verbs through `uv --project /home/dennis/Work/needle`.
- Not run: the served page in a browser against a live board with two projects (item 3's deviation says why); the full suite is recorded below.
- Full suite: on aef29f3, every test module run as its own pytest process six at a time (`/home/dennis/.claude-accounts/hrclaude/jobs/b10ab3de/tmp/suite_parallel.sh`): 99 modules, 770 passed, 1 failed — `tests/ratchets/test_every_finding_says_its_class.py`, on this record's own dispositions 6 and 7, which opened with `[preference]` (not a class); both were re-classed as `[seam]` and `[feature]` with the reader's word kept beside them, and that ratchet with `tests/api/test_close_record.py` re-run green (13 passed). No code changed after aef29f3; the archive commit moves the plan and edits this record and the carried suggestion's citation.
