# Review — the board asks another machine one question a pass

**Plan:** docs/plans/done/2026-09-10-the-board-asks-another-machine-one-question-a-pass-and-answers-a-click-in-a-second.md
**Reviewer:** Codex, a fresh read-only thread (01a090bb, effort high) through `needle call codex --fresh`, call 105, asked for one review of the lane's commits against the plan and for breaks a person using the board would meet; its answer is `~/.cache/needle/card-123/from-codex-fresh-135015326509-re-review-note.md` on the rented machine.
**Diff range:** 5fe0d8a..4da267c (the revision reviewed); rebased onto #131's fold with every one of the lane's nine patches identical (`git range-diff`), repaired in b6ee5ef, 028aa8c and 91ba29a, and rebased onto #58's fold, where the only changes were keeping both new database tables and renumbering this card's migration to 0021; final revision 91ba29a
**Findings:** 7
**Verification:** each repair has a test that exercises the reviewed break; the replaced-signal test fails with its check undone; the full backend suite on 91ba29a — 767 passed, no failures or errors; the page's type check and its 82 tests pass
**Completion:** all seven findings were inside the change, all were verified against the code before repair, and all are fixed; verifying them caught one regression a repair caused (Stop's card held in Executing for a pass), corrected in the same commit and checked; the full suites after it caught two more, both corrected and checked (a comeback parked again under a held clock, and a database connection left open by a question thread at a test's teardown). The plan's item 4 live numbers remain unverified inside the lane: they are the plan's loop and the card's WATCH row after the fold.

## Dispositions

1. [feature] runtime/service.py `_put_acted`/`_laid_by_acts` — a late answer to a question older than a launch and a stop replayed both acts and the live row won, so a stopped session read as hands on — FIXED in b6ee5ef; only the newest act per session stands, in the kept list and in the rows laid over an answer; `tests/runtime/test_one_question_a_pass.py::test_the_newest_act_on_a_session_stands_over_an_older_answer`.
2. [feature] api/loops.py `land_signals_now` — a WATCH row replaced while the old command ran landed the old command's result, so a card could move on a signal it no longer names — FIXED in b6ee5ef; a read result lands only while the card is due by that same signal; `tests/api/test_reading.py::test_a_signal_replaced_while_it_was_read_lands_nothing`, which fails with the check undone.
3. [seam] runtime/service.py `ask_machines` — a machine added to a running board was read over the wire under the lock until its first answer, so clicks waited — FIXED in b6ee5ef; a machine other than the board's own is unread from the moment its first question goes out, and every read of it answers empty; `…::test_a_machine_is_unread_until_its_first_answer_and_nothing_reads_it_over_the_wire` (rooms, sessions, groups, boots and limits of a slow new machine, no wire read).
4. [seam] runtime/service.py `scope_pids` — the sweep read a group on another machine over the wire under the lock, whether or not that machine answered, so clicks could wait the read's deadline per group — FIXED in b6ee5ef; another machine's group is read from its answer, and nothing is established when it did not answer this pass; `…::test_another_machines_group_is_read_from_its_answer`.
5. [seam] runtime/service.py `limits` — a machine whose needle predates the question read a parked lane's limits over the wire under the lock — FIXED in b6ee5ef; the older needle's answer carries every subscription's limits, read before the lock, and a park reads the answer whatever the needle's age; `…::test_an_older_needle_on_the_desktop_answers_its_limits_and_windows`.
6. [seam] runtime/service.py `_accept` — a failed answer moved the question time forward while keeping the old window list, so a window opened after that list was recorded closed — FIXED in b6ee5ef; a failed answer keeps the question time of the observation it keeps; `…::test_a_failed_answer_keeps_the_question_time_of_what_it_keeps`.
7. [seam] runtime/service.py `_observe_old_way` — a desktop running an older needle never reported its windows, so a closed window stayed open and Watch tried to focus it — FIXED in b6ee5ef; the older needle's answer on the desktop carries its windows, read before the lock; `…::test_an_older_needle_on_the_desktop_answers_its_limits_and_windows`.

## Verification evidence

- The seven findings' tests, and the tests the repairs touch — the Watch-and-Stop door test, the owner's own Stop and the unnamed-cause ring, the killed-lane door test, the stop-on-either-machine test, the silent-machine and slow-machine tests, the click-while-stalled test, the group sweep and every reading test — pass on the repaired revision.
- Mutation check: with `land_signals_now`'s same-signal check changed back to the card alone, the replaced-signal test fails ("the result of a signal the card no longer names is not landed"); restored, it passes.
- The one regression verification caught: the hold that keeps a card in place while its ending is unnamed kept the Stop door's card in Executing for a pass. A door now names the endings its own act made before its apply; the hold stays only for answers applied late. The Watch-and-Stop door test passes.
- The full suite on the first repaired revision (734 tests) failed three and errored one; each was run alone and bisected. The second-wall comeback test failed on every lane commit after the rebase over #124 and passed on the trunk and on the lane before that rebase: probed, a pass #124 asks for on the comeback's hook took the same held-clock time as the comeback's act, so the act that ended the old session was dropped and the lane was parked again on its spent wall. Acts and questions are now ordered by one count, never by the clock, and a replayed row drops a handoff its act consumed (028aa8c); the test passes. The ring test for a crashed pass passed on the trunk and failed two of three on the lane: the Start's pass could still be pending when the test's crash was lifted. It now settles after its start and passed three of three. The two database-connection warnings did not recur when their tests were run alone.
- The killed-lane door test starts a lane and stages a kill without letting the Start's pass run; a probe of the same shape in the comeback tests showed that pass sighting the lane alive after the test stamped its journal line, so the board rightly named no cause. It now settles after its start and passed three runs of three.
- Two full suites on 51a4ea6 ended with an unclosed SQLite connection finalized inside an unrelated test (a different test each time); the trunk's own suite showed none. Two guesses were probed and disproved (a question still out when a store closes; a pass still running at shutdown). A plugin recording where each unclosed connection was opened named one path both times: a thread asking this machine its question looked the machine up in the store while a test tore down. The asker's caller now hands the machine in (91ba29a); with the recorder on, the two tests that leaked and the card's tests record none.
- Full backend suite on 91ba29a, rebased over #58's fold: 767 passed, no failures or errors.
- Frontend: `needle types` current, `npx tsc --noEmit` clean, `npx vitest run` 82 passed.

## What the build found before the review

The build and the full suite surfaced these before call 105; each was probed and repaired in the commit named, and none is a review finding:

- A pass that applied every machine at once let a stalled machine hold the board's own lanes for its deadline; then a pass that applied more than once broke what counts once a pass (a card moved before its ending was named, a group's note on the pass that asked, a handoff's expiry said twice). One apply per pass, with endings asked before it.
- A Start's new worktree and a Watch's new window were undone by an answer older than the act; a pass reused a question asked before the change that triggered it; the dial's beat and the signal loop decided on answers from before memory fell or a reading ended; a door asked for its pass beside #124's coalesced ask, so a door and posts caused a pass each.
- A lock probe on the fixture (thirty lanes on two floors): the apply held the lock 0.49 s and started thirty `git rev-parse` processes for fold proofs; after the proofs moved out, none.
- A guess that a cause was asked about the wrong groups was probed, shown wrong, and reverted.
