# Review — a fix says who else it reaches and what it assumes, and a colleague checks both before it ships (card #110)

**Plan:** docs/plans/done/2026-09-10-a-fix-says-who-else-it-reaches-and-what-it-assumes-and-a-colleague-checks-both-before-it-ships.md
**Reviewer:** the build session (Claude Fable 5.1, card #110's lane) for its own passes; a fresh Codex thread at high effort in a read-only sandbox, reached through `needle call codex --fresh` and `needle wait` — the call this card adds — for every round's cold read, each a row of the board's call table named on its verdict line
**Diff range:** 8033487 (the trunk at the lane's birth, #109's close) .. the close commit that carries this record — the build 07b0372, then one commit per round's repairs, named below
**Findings:** 2 so far — 2 the author's on the first pass, both fixed in 6de3898; the cold reads' verdicts are the lines under each pass
**Stop signal:** open — the loop is running; the line is rewritten at the fold

## What was checked

- The reader, on fixtures and on the three real corpora: `tests/board/test_review_rules.py` (the forms, the fate token, the addresses, the halves, the marks, the verdict lines read apart, the counts) and `tests/board/test_progress.py`; the corpus table over Needle's 35, Hello Revenue's 92 and the machine's 23 records before and after the repair (`/home/dennis/.claude-accounts/hrclaude/jobs/bff81378/tmp/before.tsv`, `after.tsv`, the numbers below), verified by an execution role that opened the campaign record and two records the table said carry no halves.
- The close door, on the floor: `tests/api/test_close_record.py` — a bare fix line refused by line and both halves closing; a record dated on or before the fold closing as before with the old head; an absolute path, a `..` path, an undated name and another card's plan refused; a docs-only lane's record validated; a round without a verdict, a verdict without its row, a reader of the lane's own kind, an answer that never landed and a broken claim unanswered, each refused by name; a lane the board's record places on the second machine read there over the wire; the opening brief's sentence.
- Needle's own records through the same rules: `tests/ratchets/test_every_fix_says_who_else_it_reaches.py`, and the class ratchet now reading dispositions through the one reader.
- The card: the progress line's two counts (`tests/board/test_review_rules.py`), the review block's chased line and verdict rows (`frontend/tests/board.test.tsx`), the fixture regenerated from a record carrying a verdict and a mark; `npx tsc --noEmit` and `vitest run` green.
- The call: `tests/api/test_fresh_call.py` (a fresh thread recorded as a row and landed by `wait`; the other make named; a low effort passed through; the picked line on a bare-name call with the session's effort and sandbox; a tool error reported by `wait`) and `tests/runtime/test_a_call_reaches_a_colleague_fit_to_answer.py` (a row's effort and sandbox from the rollout's `turn_context`; the log's last tool error; the judge's words); then the real thing, twice — the throwaway round below (call 68) and this record's own cold reads (call 69 on).
- The doctrine: the diff of `docs/HOW-WE-WORK.md` is the two sentences the owner ruled at the Plan door and nothing else (read back with `git diff` before the commit); `tests/ratchets/test_every_rule_says_what_holds_it.py` and `tests/ratchets/test_a_doctrine_edit_lands_on_a_card.py` green; the commit names #110.
- The suites: ratchets, board, runtime and infrastructure green on the lane but for the title ratchet, red on the trunk itself since 2026-09-08 over the docs-only-fold suggestion (outside this change, filed by #87's lane); the api suite's run is named at the fold.

## The passes

1. **The author's read of the build, the three lenses in one pass (on 07b0372).** Against the "done means": every item's cases are tests that ran, and the two live reads the plan asked for — a fresh reader breaking a false premise and an omitted reader, and a fresh call answering in the Answer shape — are on the record below verbatim with their rows. The seams: the close checked a record's plan stem before its date, so a Hello Revenue record written today under that project's old template — headed `**Card:**`, no plan line — would have been refused at a close the date rule exempts (1.1); a lane whose worktree is not on this disk is read from the project's checkout, which the fold has levelled, the same shape `lane_files` already has, and stands as a boundary; the fault list the door prints caps at four and counts the rest. The boundaries: `board/review_rules.py` imports only `domain`; the door reaches `runtime.calls` as `api` may; the other make is named through `domain.slot.Make` and not a literal; one reader, and the class ratchet now reads through it. The claims that stand on nothing: the Codex row's effort and sandbox were read from the first 64 KB of a rollout on the strength of one sample, and forty real rollouts measured this morning put the first `turn_context` at 24 KB, 275 KB and 3.1 MB (1.2). Two findings, both inside the change, both fixed in 6de3898.
Read cold by Codex (01a08aa8, a fresh thread at high effort, read-only) on 6de3898, call 69: broke 1.2 — "Line 2, assumes: append-only does not guarantee a complete final line. runtime/codex.py:179–187 advances the cached offset before JSON decoding succeeds. I drove _context_of with an unfinished turn_context, then appended its remaining bytes: both reads returned (None, None); clearing the cache recovered ('high', 'read-only'). Appending another turn instead made it report that later turn as the first. Resume from the last complete record boundary. The same premise also fails on the named test floor: tests/floor.py:425 rewrites the rollout at the same path. Driving write_rollout twice with changed settings left _context_of returning the original cached settings; clearing the cache exposed the replacement settings. Line 1 held: the old Card-only record passed the door despite a different card plan; the parser accepts a missing stem, progress skips unmatched records, and the ratchet exempts the old date." (`how_known: checked`; sources: the note, `git diff 07b0372..6de3898`, `runtime/codex.py:168–203` and the callers of `rollouts()`, `tests/floor.py:326–425`, `tests/fakes/bin/codex:228–249`, `api/doors.py:1694–1767`, `board/parse.py:713–730`, `board/progress.py:44–48`, `board/review_rules.py:29–57`, the ratchet, and read-only Python probes driving `_context_of`, `Floor.write_rollout` and the door with in-memory doubles.) Answered by 1.3, fixed in b956be5.
Read cold by Codex (01a08aac, a fresh thread at high effort, read-only) on b956be5, call 70: broke 1.3 — "both halves. Premise: runtime/codex.py:198–202 resumes an unsuccessful scan unless the file shrank. I drove the actual Floor.write_rollout with in-memory filesystem doubles: write without context, read, then rewrite the same path with effort=high and sandbox=read-only. Both reads returned (None, None); clearing the cache returned ('high', 'read-only'). The new context begins before the cached offset. A rewrite after reaching the scan cap likewise remains unknown despite putting context at the head. Premise: identical bytes at the same position do not establish that the record remains the FIRST turn_context. I replaced an earlier non-context line with an equal-length low-effort context, preserving the cached high-effort record and its offset exactly. runtime/codex.py:194–196 returned high; clearing the cache returned low. The cached record is unchanged, but its precedence changed. Reach: the line omits tests/fakes/bin/codex::lane, which writes rollout metadata and turn_context at lines 227–248; resume also appends records to these rollouts at lines 105–110. The driven partial-line completion, changed cached record, shrink, and independent-path cases held. No files were written." (`how_known: checked`; sources: the note, `git show b956be5`, `runtime/codex.py:184–230` and the callers of `rollouts()`, `tests/floor.py:326–425`, `tests/fakes/bin/codex:105–110, 227–248`, the test, read-only Python probes with in-memory doubles, `rg` over the callers.) Two rounds on one shape — a cache over a file that may be rewritten — is the signal about the representation: answered by 1.4, which removes the cache, fixed in 91c87da.
Read cold by Codex (01a08ab0, a fresh thread at high effort, read-only) on 91c87da, call 71: broke 1.4 — "assumes. The first complete turn_context is not always the first turn's, on actual Codex rollouts: 1. Worker 01a07bb6: line 3 starts its first turn; line 17 completes it without a turn_context. Line 19 starts a later turn; the first context, line 23, names that later turn. Driving runtime/codex.py::_context_of returned (None, 'workspace-write') from that later turn. Lines 174–198 never check turn boundaries. 2. Fork 01a08690: line 5 contains an inherited context; the fork's own turn starts at line 14 with its context at line 17, effort 'medium'. _context_of returns the inherited effort None. The forked rollout is another concrete sibling that defeats the premise. The caller/writer search found no omitted production path. Partial-line completion, rewrites, shrinking, malformed preceding lines and missing fields behaved consistently in read-only probes. The measured cap premise held today: 154 of 156 rollouts contained contexts, all before 4 MB; farthest 3,109,064 bytes. Scanning 59 recent rollouts took 14 ms. These checks do not rescue the first-turn attribution." (`how_known: checked`.) A different question from the two before — not how the answer is kept but which record is the answer: answered by 1.5, fixed in af35337.
Read cold by Codex (01a08ab4, a fresh thread at high effort, read-only) on af35337, call 72: broke 1.5 — "both halves. Premise: 1. runtime/codex.py:198 raises uncaught IndexError when the trailing unfinished line exceeds 64 KiB. The first block contains no newline; line 190 empties `lines`, then line 198 accesses `lines[0]`. Reproduced with a complete context followed by an unfinished 70 KB response record. This propagates through `_rollout_of` and `rollouts()`, breaking the list rather than ignoring the unfinished line. 2. runtime/codex.py:184–200 misses a complete context followed by a valid 9 MiB tool-result record, returning `(None, None)`. Growing blocks actually scan up to 8,323,072 bytes, exceeding the stated 4 MiB cap while still failing the latest-context promise. 3. tests/fakes/bin/codex::resume, lines 106–110, starts another turn without writing any context. Reproducing that sequence returns the previous turn's settings. The named writer therefore contradicts 'the last complete context is the latest turn's whatever wrote it.' Reach: 4. tests/runtime/test_a_call_reaches_a_colleague_fit_to_answer.py:5 still says the row carries its first turn's settings. domain/session.py:71 likewise describes `Session.effort` as the effort the session started with. Both sentences need reconciliation with latest-turn semantics. The revised absence descriptions at runtime/codex.py:88,92 and domain/session.py:95 also omit the case where an earlier context carries fields but the latest context lacks them; that returns `(None, None)`. Small files, short unfinished lines, boundary-aligned and split contexts, two contexts, inherited-then-own contexts, and malformed trailing records behaved as described. Scanning the last day's 59 real rollouts (129,527,064 bytes) took 53.4 ms; none had its last context beyond 4 MiB. That sample does not defend the general bound." (`how_known: checked`.) The first finding is live — an unfinished line longer than a block took the whole Codex list down — and the third pass running on this one function; answered by 1.6, fixed in d922a16, and the next cold read is asked the representation question §13 names.
Read cold by Codex (01a08abb, a fresh thread at high effort, read-only) on d922a16, call 73: broke 1.6 — "the fake's claimed carry-forward is false. At tests/fakes/bin/codex:111–120, a latest context with effort=null becomes 'medium'; following an earlier high-effort context, it becomes 'high'. I executed that actual block in memory and reproduced both. Null is a real input: worker 01a07bb6-bd09's context at line 23 records effort=null. The sandbox assignment has the same fallback problem when the latest field is absent. This defeats the claim that the fake carries the last turn's settings and can conceal missing-setting behaviour in callers' tests. The scanner repair held in my checks: 230 partial-tail, block-boundary and cap probes passed, including measured reads never exceeding 4 MiB. Its results matched a full forward reference read of 61 recent real rollouts. The caller search found no omitted production reader. These checks establish the bounded last-record behaviour, not next-turn settings. representation: I choose resolving settings through Codex at the call boundary, with the result bound to the invocation that will answer. A future turn's configuration is a property of that invocation; historical observations should be labelled historical. This is my recommendation, not a verified implementation. Reject live argv plus ended-rollout fallback: argv contains overrides, not necessarily the resolved configuration, and the next resume is a separate invocation. Reject session_meta alone: the two named real rollouts' heads contain neither effort nor sandbox settings. Reject persisted settings alone as a next-turn guarantee: state_5.sqlite's threads table has reasoning_effort and sandbox_policy — useful stored evidence, but I did not establish that exec resume adopts those fields unchanged. Reject the scan as the next-turn authority: even perfect parsing establishes only the last complete recorded context. Do not replace the scanner with another predictor and call the prediction checked." (`how_known: checked`.) The scanner stands; the finding is coverage (the fake) and the representation answer is adopted as words — the picked line labels what it says as the latest turn's, and predicts nothing: answered by 1.7, fixed in 3e8e4c4. A round of coverage and record findings, repaired and read for completeness, ends the loop (§13); the completeness read of this round is the next verdict, and the pass the floor owes after round four's live finding is pass 2 below.
Read cold by Codex (01a08ac1-3ac1, a fresh thread at high effort, read-only) on 3e8e4c4, call 74: broke 1.7 — "reaches and assumes, in the documentation. domain/session.py:99–100 still says the historical sandbox lets the caller see 'whether it can run the probe the question needs.' That claims present capability from historical settings, contrary to line 7's premise. runtime/codex.py:174–175 likewise justifies the scan as answering what the session 'runs at now.' This sentence was left outside the reconciliation. These are documentation findings, not a demonstrated runtime regression. The repaired behavior held: I executed the actual fake resume function with in-memory doubles across 10 null, absent, and present-setting cases, including earlier different settings. All preserved the latest context's values. picked_line passed four combinations of known/None effort and sandbox and explicitly labelled them historical. The search found the three updated expectations and no additional executable reader of picked_line's words requiring changes." (`how_known: checked`.) The record's words off, which the writer fixes and re-reads alone (§13): answered by 1.8, corrected in e026f8f, and the round closes.

2. **The whole change read cold, by the reader who was not the author (Codex 01a08ac1-6e75, a fresh thread at high effort, read-only, on e026f8f, call 75).** The three lenses over `git diff 8033487..HEAD`, the plan's stances and this record. Twelve findings, all inside the change, six live: an earlier verdict satisfied later, unread repairs in the same pass (2.1); a bare `[repair of …]` mark answered a broken claim (2.2); a verdict saying neither complete nor broke passed (2.3); a lane whose worktree exists only on the rented machine read as gone through a local `is_dir`, and the second-machine test hid it on a shared disk (2.4); the counters counted repairs, not findings, so two full-pass findings on one repair were one escape and a later catch erased both (2.5); every lane, a lane of Codex's included, was told to reach the other make through `call codex --fresh`, which the door then rightly refuses (2.6). Latent in behaviour: an unknown lane make read as Claude's (2.7); a fresh answer path unique only to the second (2.8); any earlier logged tool error replaced an ending the runtime had established (2.9); FIXED lines under a heading other than `## Dispositions` read as a zero-finding record (2.10). Coverage: the fake's null-setting resume had no test (2.11). Record: item 3's stance quoted a count the record no longer read (2.12). Its verification: twenty pure test functions, the fix-line ratchet, the doctrine checks, `tsc` and `git diff --check` run; the behavioural findings driven through in-memory probes of the production functions; `uv run pytest` could not start in the read-only sandbox. All twelve fixed or corrected in 29735dc, read cold below.
Read cold by Codex (01a08ad1, a fresh thread at high effort, read-only) on 29735dc, call 76: broke 2.1, 2.4, 2.7, 2.11 — "2.1 — Premise broke. board/review_rules.py:110 collects answers globally, without requiring them to follow the break. At line 132, an earlier pass's CORRECTED disposition satisfies a later verdict breaking the same address again. Reproduced: pass 1 corrects 1.1; pass 2 ends with 'broke 1.1' and supplies no new answer; record_faults returns []. The earlier-answer sibling remains open. 2.4 — Reach broke at the deleted-remote-tree sibling. _lane_root treats every remote path as standing (api/doors.py:1701). _refuse_a_code_lane_without_its_review consequently asks that path with tip=None, accepts its empty file list, and returns without checking the project checkout (api/doors.py:1673). Reproduced with folded code available in the project checkout: no review was required. The document-reading caller has a fallback; this caller does not. 2.7 — Attribution premise broke. The fallback matches only the lane basename against the global session ledger (api/doors.py:1769). Lane names omit the project (board/brief.py:45), while card numbers are allocated per project (infrastructure/store.py:628). Reproduced two projects with card-1-same-title: the later Claude row supplied the make for the earlier Codex lane, allowing its own-make Codex reader. Also, board/review_rules.py:168 still documents unknown authorship as Claude's, contradicting the repaired behavior. 2.11 — Premise broke, record only. 'Every call test' does not drive the fake's resume: test_a_fresh_call_starts_a_new_thread_records_it_and_wait_lands_the_answer explicitly checks a fresh launch (tests/api/test_fresh_call.py:37), dispatched through lane() at tests/fakes/bin/codex:310. The new null regression covers warm Codex resume. I drove the actual resume function with in-memory inputs; it preserves both null fields correctly. No further break established for the other seven lines. All 17 existing review-rule tests requiring no fixtures passed; the additional probes were in memory." (`how_known: checked`.) Answered by 2.13 to 2.17, fixed in fea48ac; the author's own re-read of the record through the reader found one more (2.13), fixed in the same commit.
Read cold by Codex (01a08ad8, a fresh thread at high effort, read-only) on fea48ac, call 77: broke 2.14, 2.15, 2.16 — "2.14 — Assumes broke. board/review_rules.py:111–124 associates answers by pass, not by verdict. Reproduced two verdicts under pass 1 breaking 1.1, with one CORRECTED disposition explicitly made before the second verdict: record_faults returned []. The earlier correction still satisfies the repeated break. Also reproduced a FIXED answer under pass 2, read complete under pass 2, being refused as unread by the check at lines 139–149 for pass 1. 2.15 — Assumes broke at the no-tip case. api/doors.py:1675–1685 skips the checkout entirely for a gone local lane with tip=None; the equivalent remote case accepts the empty remote result. Both probes passed without a review despite code available in the checkout. Missing observation does not establish 'folded nothing': Start initializes tip=None (api/doors.py:343), and the Git fold itself does not update the lane record (runtime/git.py:421). Standing lanes and gone lanes with recorded tips correctly refused in the probes. 2.16 — Both halves broke. The same cross-project attribution survives through the snapshot: api/loops.py:863 supplies all sessions, and board/lane.py:177 accepts a matching name regardless of project. Reproduced an ended Codex author and a live Claude session in another project's same-named lane. lane_for selected the foreign Claude session; api/doors.py:1776 accepted its make and allowed Codex readers of the author's own make. Supplying the actual author correctly refused. The reach omitted the snapshot's session selector. 2.13 held in the mark-placement probes and reader search. All 18 fixture-free review-rule tests passed. API probes used in-memory doubles; the fixture-based API suite was not run. No files were written." (`how_known: checked`.) Answered by 2.18 to 2.20, fixed in 4851aa3.
Read cold by Codex (01a08adf, a fresh thread at high effort, read-only) on 4851aa3, call 78: broke 2.18, 2.19, 2.20 — "2.18 — Assumes broke. board/review_rules.py:128 admits every non-null fate as an answer. Replaced the fixture's FIXED answer with `[repair of 1.2] The problem remains — NO CHANGE`; with the later complete verdict retained, record_faults returned []. FILED also passed. The promised FIXED-or-CORRECTED answer is still unenforced. The requested ordering, single-consumption, earlier-pass and final-verdict controls held. 2.19 — Assumes broke. Start records both birth=None and tip=None (api/doors.py:342). For that gone-lane state, the checkout fallback reaches runtime/git.py:139, which compares origin/develop...HEAD. On a clean, level checkout after folding, that comparison is empty. Driving the actual door and helper with those Git responses accepted closure without a review. Thus the fallback can still under-ask when birth was also unobserved. Known-birth local, remote and standing controls refused correctly. 2.20 — Assumes broke. api/doors.py:1786 matches paths without matching machines. runtime/service.py:355–380 combines sessions from multiple machines. Reproduced a local Codex author and a remote Claude holder at the same absolute path: the holder priority selected remote Claude and accepted the local author's same-make Codex verdict. Local cross-project paths, stale rows and the ordinary Claude-holder/Codex-reader controls held. All 18 fixture-free review-rule tests passed. API probes used in-memory doubles; the fixture-based API suite was not run. No files were written by this reader." (`how_known: checked`.) Answered by 2.21 to 2.23, fixed in 5187802. Three rounds running have found the same shape — a clause missing from the rules that judge a record — so the next cold read is asked the representation question §13 names, beside the read of these three lines.

## Dispositions

### Pass 1's findings

1. [seam] The close checked a record's plan stem before its date, so a Hello Revenue record written today under that project's old template (a `**Card:**` head, no plan line) would be refused at a close the date rule exempts — FIXED in 6de3898; reaches `api/doors.py::_refuse_a_record_that_skipped_the_read` only (the ratchet never checked a stem, and `board/progress.py` matches a record to its card by stem for the face without refusing anything); assumes a record dated on or before 2026-09-10 owes the door nothing but a dated name and a file inside the project's tree, and every other reader of a record's plan stem tolerates a record that names none.
2. [verification] A Codex row's effort and sandbox were read from the first 64 KB of the rollout, and the first `turn_context` sits at 24 KB to 3.1 MB in forty real rollouts, so every row would have said unknown — FIXED in 6de3898; reaches `runtime/codex.py::_context_of` and every caller of `rollouts()` — `sessions()`, `find()`, `warm()`, `fresh_since()` — through `_rollout_of`, plus the fake floor's rollouts (`tests/floor.py::write_rollout`, `tests/fakes/bin/codex`), which now carry a `turn_context`; assumes a rollout is appended to and never rewritten in place, so the first `turn_context` found stands for the file's life, and a scan that found none may resume from where it stopped.
3. [seam] [repair of 1.2] The scan advanced its offset past a `turn_context` line still being written, so the whole line was never read and the row said unknown for the file's life; and a rollout rewritten in place — the test floor does — answered from what was read before — FIXED in b956be5; reaches `runtime/codex.py::_context_of` and the `_Context` it keeps per path, and through it every row of the one list (`sessions()`, `find()`, `warm()`, `fresh_since()`), the picked line of a bare-name call, and the floor's `write_rollout`, which rewrites at one path and is now read again; assumes Codex writes a rollout one whole record per line, ending each with a newline, so a line without one is a line in flight and a complete line never changes — and that a rewrite that leaves the same bytes at the same place is the same record.
4. [seam] [repair of 1.3] The cache over the scan failed a second way — a scan that found nothing resumed past a context a rewrite had put before its offset, and identical bytes at one place did not prove the record was still the first — so the representation changed: no cache, every read scans from the head to the first complete `turn_context` line — FIXED in 91c87da; reaches `runtime/codex.py::_context_of` alone and, through `_rollout_of`, every caller of `rollouts()` — `sessions()`, `find()`, `warm()`, `fresh_since()`, so the one list the loop reads on its beat and `wait` reads every two seconds, and the picked line of a bare-name call — whose cost is now the scan itself, measured at 15 ms over the 58 rollouts of the last day (118 MB on disk) and 49 ms for the whole Codex list; the writers the scan reads are Codex, the floor's `write_rollout`, and the fake's `lane` (writes a `turn_context`) and `resume` (appends records); assumes the first complete `turn_context` line in the file is the first turn's whatever wrote it, a line without a newline is a line in flight, and a scan capped at 4 MB reaches the record in every rollout written on this machine (the farthest measured at 3.1 MB).
5. [feature] [repair of 1.4] The first complete `turn_context` is not the first turn's on two real rollouts — a worker whose first turn carried no context and whose later turn did, and a fork carrying its parent's context before its own turn — so the row answered another turn's words; a Codex row's effort and sandbox are now its latest turn's, from the last `turn_context` in the file, read backwards in growing blocks from the end — FIXED in af35337; reaches `runtime/codex.py::_context_of` (now with `_turn_context` beside it) and every reader of a Codex row's `effort` and `sandbox` — the one list (`sessions()`, `find()`, `warm()`, `fresh_since()` through `_rollout_of`), the picked line of a bare-name call, `sessions --json` over the wire — and the two docstrings that said "first turn" (`Rollout`, `Session.sandbox`); the writers are Codex, the floor's `write_rollout` and the fake's `lane` and `resume`; the cost is now 97 ms over the 59 rollouts of the last day (128 MB) and 201 ms for the whole Codex list, since a long last turn is read back to its context; assumes a turn's context opens the turn and the turn's records follow it, so the last complete `turn_context` line is the latest turn's whatever wrote it and whatever the first turns carried; a line without a newline at the end is a line in flight; and 4 MB back from the end reaches the latest turn's context.
6. [seam] [repair of 1.5] A trailing line in flight longer than the first block emptied the block and the scan raised through `rollouts()`, taking the whole Codex list down; the growing blocks read up to 8 MB past a 4 MB cap the line claimed; the fake's `resume` started a turn without a context where a real resumed worker writes one; and two docstrings still said "first turn" and "started with" — FIXED in d922a16; reaches `runtime/codex.py::_context_of` (the tail is dropped until a newline is met, every block is bounded by the remaining budget), `tests/fakes/bin/codex::resume` (carries the last turn's settings into the resumed turn's context), the docstrings of `Rollout.effort`, `Rollout.sandbox` and `Session.effort`, and the test module's head; through `rollouts()`, every reader of the one list and the picked line as before; assumes a line in flight ends the file and never sits before a complete line, so dropping the tail up to the first newline met drops the in-flight line and nothing else; a context farther back than the cap reads as none, which the row tells rather than guesses; and Codex writes a `turn_context` for a resumed turn as it does for a first (the two real rollouts of round three).
7. [verification] [repair of 1.6] The fake's carry-forward turned a missing setting in the latest context into an earlier turn's word, so a test reading the row could not see an absent setting; and the picked line said "effort X, sandbox Y" as if it were the next turn's, where the scan establishes only the latest recorded turn's — FIXED in 3e8e4c4; reaches `tests/fakes/bin/codex::resume` (the latest context's words as they are, null included) and `api/runtime_cli.py::picked_line` with its three expectations in `tests/api/test_fresh_call.py` and `tests/api/test_calls.py`; no reader of the row changes; assumes the row's two fields are read as history — what the latest turn ran at — by every caller, which the docstrings of `Rollout` and `Session` now say, and that a caller choosing a session is served by that history and told nothing it cannot check.
8. [record] [repair of 1.7] Two docstrings still claimed present capability from history — `Session.sandbox` said the caller "can see whether it can run the probe the question needs", the scan's own said it answers what the session "runs at now" — CORRECTED in e026f8f, the writer's own re-read: both say the latest turn's, history and never a prediction of the next.

### Pass 2's findings

1. [feature] An earlier verdict under a pass satisfied later, unread repairs of the same round: a verdict breaking 1.2 followed by a FIXED disposition answering it closed with no further read — FIXED in 29735dc; reaches `board/review_rules.py::record_faults` and, through it, the close door for every project and Needle's own ratchet, the README's rule, the two other READMEs, and the fixtures in `tests/board/test_review_rules.py` and `tests/api/test_close_record.py` that now carry a second verdict; assumes the last verdict under a pass is the round's last word, so a round ends when it says complete or when every address it broke is answered by a record-only correction, and a FIXED answer to a break is a new repair the next verdict reads.
2. [feature] A bare `[repair of <address>]` mark with no fate answered a broken claim ("still broken; no repair has been made" closed the round) — FIXED in 29735dc; reaches `record_faults`'s answers, which now take only dispositions with a fate, and the reader, which now reads `CORRECTED` as a fate of its own (`domain.document.Fate.CORRECTED`), so the README's record-only correction is one token and not a convention; assumes a disposition's fate is the last clause-opening token of its line, as ruling 4 set it, and that the writer of a correction writes the word.
3. [feature] A verdict in the form's prefix but saying neither complete nor broke ("unable to read the files") satisfied the round — FIXED in 29735dc; reaches `record_faults`, which now names it off the form, and the README's sentence; assumes a reader that read the round says one of the two words, and one that could not has not read it.
4. [seam] A lane whose worktree exists only on the rented machine read as gone through a local `is_dir`, so the close asked only the project root and never the wire, and the second-machine test hid it on a shared disk — FIXED in 29735dc; reaches `api/doors.py::_lane_root` and both its callers, `_refuse_a_code_lane_without_its_review` (whose `lane_files` now routes by the same placement) and `_refuse_a_record_that_skipped_the_read`, with a new floor case whose lane record names a path this disk does not hold; assumes a lane the board places on another machine is answered for by that machine's runtime over the wire, and answered empty when it is gone, which `Runtime.lane_files` and `lane_docs` already do.
5. [feature] The counters counted repairs, not findings: two full-pass findings marked `[repair of 1.1]` were one escape, and a later verdict breaking 1.1 erased both, so the five-close loop would have judged a different unit from the baseline's 82/37 — FIXED in 29735dc; reaches `board/parse.py::review_of`'s two counts, the `Review.escaped` docstring, the progress line and the card's chased line, and the plan's ruling 8 as this record refines it; assumes caught is every address a cold reader broke, once, and escaped is every marked finding a full pass made whose repair no verdict under that pass or an earlier one had broken — a finding, as the baseline counted.
6. [feature] Every lane, a lane of Codex's included, was told to reach the other make through `needle call codex --fresh`, which the door then refuses as the lane's own make — FIXED in 29735dc; reaches `board/brief.py::completeness_read`, Needle's README and Hello Revenue's (acd073cee) and the machine's (eb94d3e), which now name the channel per make: a fresh Codex thread from a lane of Claude's, a warm call to a Claude slot or session from a lane of Codex's; assumes a Claude colleague reached by `needle call <slot>` is a windowless session of the other make the door accepts, which `verdict_faults` does by the call's slot.
7. [seam] A lane the board held no session for read as Claude's, so a Codex reader was accepted for an author of unknown make — FIXED in 29735dc; reaches the door, which now takes the make from the session the board saw on the lane, else the runtime's own record of who it started on the card (`store.session_slots()` by the lane's name), and `verdict_faults`, which refuses to judge with no make on record; assumes every lane the board started has a session slot row or a snapshot session, so a close with neither is a lane the board never saw start.
8. [seam] A fresh call's answer, schema and log paths were unique to the second, so two fresh calls on one note in one second shared them — FIXED in 29735dc; reaches `api/runtime_cli.py::_call_fresh` alone (the warm call's path is named by the colleague's short id); assumes two calls never share a microsecond stamp on one note, and that the row, not the path, is the call's identity.
9. [seam] Any earlier logged tool error replaced an ending the runtime had established (a signal after a recovered error read as the error) — FIXED in 29735dc; reaches `runtime/calls.py::judge`, which takes the log's error only when the runtime names no reason, and `runtime/service.py::judge_call`, which passes no reason when the runtime's own word is that the cause is not established; assumes the runtime's established causes — a signal, a boot, a stop, a wall — outrank a line in the worker's log, and that an unestablished cause is no cause.
10. [feature] FIXED lines under a heading other than `## Dispositions` read as a zero-finding record and passed every check — FIXED in 29735dc; reaches `record_faults`, which refuses a head that counts findings with no dispositions section, and the README; assumes a record's `**Findings:**` line counts what its dispositions list, which the README has said since card #60.
11. [verification] The fake's null-setting resume had no regression test — FIXED in 29735dc; reaches `tests/api/test_fresh_call.py` (a latest context with effort and sandbox null, resumed, read as unknown on the row); assumes the fake's `resume` is what every call test drives, so a fallback returning would fail there first.
12. [record] Item 3's stance quoted "1 caught, 0 escaped" where the record already read more — CORRECTED in the plan at the fold, once the last pass is on the record; the stance names the count this record reads then.
13. [feature] The repair mark was read anywhere in a disposition's words, so 2.5 above, which quotes `[repair of 1.1]` in its prose, read as marked and this record counted one escape it never had (the author's re-read of the record through the reader, after the cold read) — FIXED in fea48ac; reaches `board/parse.py::_REPAIR_OF` and every reader of a disposition's mark — the counts, the rules' answers, the card's chased line — and the README's form, which puts the mark after the class and nowhere else; assumes a mark sits at the head of the line after the class, as the README sets it, and that a line quoting the form in its words is prose.
14. [feature] [repair of 2.1] An answer under an earlier pass satisfied a later verdict that broke the same address again — FIXED in fea48ac; reaches `board/review_rules.py::record_faults`'s `answers`, used by both the unread-fix check and the unanswered-break check, the fault's words (which now say "under pass N or later"), and the two tests that read those words in `tests/board/test_review_rules.py` and `tests/api/test_close_record.py`; assumes an answer to a break sits under the verdict's pass or a later one, which is where the README puts a round's repairs, so an earlier pass's disposition is another round's word.
15. [seam] [repair of 2.4] A remote lane whose tree was gone answered an empty file list and read as docs-only, so the code lane's close needed no record — FIXED in fea48ac; reaches `api/doors.py::_refuse_a_code_lane_without_its_review` alone (`_lane_root`'s other caller already fell back to the project's checkout), with the floor case that now closes the gone remote lane without a record first and reads the refusal; assumes a lane the board recorded a tip for folded up to that tip, so the project's checkout diffed from the lane's birth to it says what it folded, and a lane with no tip folded nothing.
16. [seam] [repair of 2.7] The fallback for a lane's make matched the lane's name against the runtime's session ledger across projects, so two projects' card 1 answered for each other — FIXED in fea48ac; reaches `api/doors.py::_refuse_a_record_that_skipped_the_read` (the fallback is gone; the make is the snapshot session's or nothing) and `board/review_rules.py::verdict_faults`'s docstring, which said an unknown make read as Claude's; assumes a lane the board started carries its session on the snapshot the close reads, so a close with none is a lane the board never saw start and is refused as such.
17. [record] [repair of 2.11] The line said the fake's `resume` is what every call test drives, where the fresh call drives its `lane` — CORRECTED in the record: the null-setting case covers the warm resume, which is the path the carried defect's evidence ran through; the fresh thread's context is written by `lane` from its own argv.
18. [feature] [repair of 2.14] Answers were matched by pass, not by break, so one correction answered every later verdict breaking its address, and a fix read complete by a later pass's verdict was refused as unread under its own pass — FIXED in 4851aa3; reaches `board/review_rules.py::record_faults` (answers are claimed one per break in the record's order — by pass, then as written — and the unread check reads the record's last verdict only), its fault words, the README's rule, and the tests in `tests/board/test_review_rules.py`; assumes verdicts under one pass are written in the order the reads happened, so the line's order is the round's order, and a later pass's verdict is a later read of the tree that read every earlier fix.
19. [seam] [repair of 2.15] A gone lane with no recorded tip — the fold does not write one, and Start writes none — skipped the checkout and closed without a record — FIXED in 4851aa3; reaches `api/doors.py::_refuse_a_code_lane_without_its_review` alone, which now reads the checkout from the lane's birth to the recorded tip or, with none, to the trunk's head; assumes a missing tip is a missing observation and not "folded nothing", so reading to the head over-asks for a record at worst — the loud failure — and never under-asks.
20. [seam] [repair of 2.16] The lane's make came from the snapshot's session, whose selector (`board/lane.py::_sessions_in`) matches a session's name across projects, so another project's same-named lane answered for this one — FIXED in 4851aa3; reaches `api/doors.py::_refuse_a_record_that_skipped_the_read` alone (the make is now the slot of a session on the lane's own worktree path, from the runtime's one list, a session that holds the worktree outranking one merely working in it), and the floor case, which now registers the rented lane's session on its remote path; the snapshot's selector is untouched and its name match is filed below as a defect outside this change; assumes the closing session runs in the lane's worktree — a close is made from the lane — so a lane no session is on is a lane the board never saw start, and a cold reader of the other make started in the lane's directory never holds the worktree.
21. [feature] [repair of 2.18] A `NO CHANGE` or `filed` disposition carrying the mark answered a break, where the README says an answer is a fix or a correction — FIXED in 5187802; reaches `board/review_rules.py::record_faults`'s claimable answers alone, and `tests/board/test_review_rules.py`; assumes "no change" and "filed" say the claim stands or leaves the lane, which is not an answer to a reader who broke it, so only FIXED and CORRECTED claim a break.
22. [seam] [repair of 2.19] A gone lane the board recorded neither a birth nor a tip for — Start writes neither, the fold writes no tip — diffed a level checkout against itself and closed as docs-only — FIXED in 5187802; reaches `api/doors.py::_refuse_a_code_lane_without_its_review` alone, which now refuses to say what such a lane folded and asks for a record, and the floor case; assumes a lane whose tree is gone and whose birth was never observed has nothing the board can diff, so the loud failure — a record asked for — is the right one, and a lane standing anywhere still diffs its own tree.
23. [seam] [repair of 2.20] The lane's make was read from any session on the lane's path on any machine, and the one list merges every machine's sessions at the same absolute paths — FIXED in 5187802; reaches `api/doors.py::_refuse_a_record_that_skipped_the_read` alone (a session counts only on the machine the board places the lane on; a row with no machine name was read here) and the two floor cases, which now register the rented lane's session on the rented floor; assumes the runtime stamps every merged row with its machine's name, and a lane the board places on a machine has its session there and nowhere else.

## The throwaway round (item 2), verbatim

A scratch project of one module (`meter.py`: `bill` writes the `billed` flag; `sweep` and `nightly` both read it; `invoice` reads `id` and `amount` and never `billed`) and a note holding two FIXED lines, one whose stated premise is false and one whose reach omits a real second reader the reader is not told about:

```
1. [seam] The sweep re-billed a row on a retry — FIXED in 4d1e2f; reaches the sweep alone; assumes only the sweep reads the billed flag
2. [feature] The invoice printed the amount before the bill was written — FIXED in 5e2f3a; reaches the invoice line only; assumes bill() writes the row's amount before invoice() reads it
```

The call, from the scratch directory (`needle call codex --fresh …/round-one.md --objective "Try to break each FIXED line's reach and its premise against meter.py; answer complete, or broke <line numbers> with what you found."`):

```
call 68: a fresh codex thread 01a08aa2 is working on /home/dennis/.claude-accounts/hrclaude/jobs/bff81378/tmp/scratch-round/round-one.md, effort high, sandbox read-only, in /home/dennis/.claude-accounts/hrclaude/jobs/bff81378/tmp/scratch-round
  the answer lands in /home/dennis/.claude-accounts/hrclaude/jobs/bff81378/tmp/scratch-round/from-codex-fresh-092511-re-round-one.md
  wait for it: needle wait 68
```

`needle wait 68`, 34 seconds after the call:

```
landed: /home/dennis/.claude-accounts/hrclaude/jobs/bff81378/tmp/scratch-round/from-codex-fresh-092511-re-round-one.md landed at 2026-09-10T09:25:45+00:00: broke 1, 2
1. Both halves broke: the reach omits nightly(), which also reads billed at line 21, disproving the premise that only sweep() reads it. bill() writes that flag at line 10.
2. The premise broke: bill() writes only billed and returns the row (lines 10–11); it never writes amount. invoice() reads the caller-supplied amount directly at line 26 without calling bill(). The reach search found no other amount reader in meter.py.
```

The answer file, in the shape asked (`how_known: checked`; `sources`: `round-one.md: FIXED lines 1–2`, `meter.py:8–11 bill()`, `meter.py:14–16 sweep()`, `meter.py:19–21 nightly()`, `meter.py:24–26 invoice()`, `rg search of meter.py for billed, amount, and all four function names`). The row, read from the shared store: id 68, session `01a08aa2-ab01-71c0-b8bb-49d8bc91e7d5`, slot `codex`, called at 09:25:11Z, ended at 09:25:54Z with the verdict's words. So the reader drove the false premise (line 2: `bill()` never writes `amount`) and found the omitted reader (line 1: `nightly()`), named by function and line, with the reach search it ran.

## The corpus table (item 1)

The reader run over the three corpora before and after its repair, 150 records: the pass count differed on one record, Hello Revenue's campaign record of 2026-09-08 (157 before, 22 after; the execution role counted 22 margin entries between lines 169 and 411 by hand); 1,958 FIXED lines by the word, 0 carrying both `reaches` and `assumes`. The fate token moved 55 dispositions on the first reading — 30 an all-caps `FILED`, 3 a token after a semicolon, 1 after a closing quote, which the pattern now takes — and 26 after the repair, all in records dated on or before the fold and all of one shape: a fixed finding whose line goes on to a sibling's `NO CHANGE` or `filed`, which the last-token rule reads as the sibling's; the README now says one finding, one line. Three records name no token a clause opens with (`(FIXED with 22)`, "PARTLY FIXED, PARTLY FILED", "first answered NO CHANGE (…), then FIXED") and read as no fate, where the old reader read the first word it found.

## The baseline reading of Hello Revenue #456 (item 3)

The plan's Loop carries the counts — 82 escaped, 37 caught, by the record's own words — and the rule that counted them. The reading itself, every finding with its section, its line, the repair it blames and its verbatim quote, made by a search role over the record's 541 lines on 2026-09-10, follows so the number has a source someone can re-read.

## Table 1 — candidate findings

| # | Section | Item | Line | Tier | Earlier repair blamed (record's words) | Verbatim quote |
|---|---|---|---|---|---|---|
| 1 | Pass 2's findings | 1 | 151 | A | pass 1's reconcile post (`reconcile_lp_pages_post`, pass 1 fix) | "pass 1's reconcile post ran after the acceptance read, against three trunk ratchets and against the critic's own intent" |
| 2 | Pass 2's findings | 9 | 159 | B | pass 1's reconcile post (its docstring) | "the reconcile's docstring and the records said "only what they outgrew" while it also retried every degraded page — FIXED with the post's removal" |
| 3 | Pass 3's findings | 1 | 169 (para 17) | A (para) / B (line) | pass 2's `hold_bound_shape` floor ("The floor that had replaced pass 1's reconcile post") | para: "The floor that had replaced pass 1's reconcile post — `hold_bound_shape` — was held by NOTHING"; line: "the whole `hold_bound_shape` floor held by nothing (four mutations survived the full suite)" |
| 4 | Pass 3's findings | 2 | 170 | B | pass 2's `hold_bound_shape` floor | "the floor let the primitive section's optional action fields through, so a repair landed in the record and never reached the visitor" |
| 5 | Pass 3's findings | 3 | 171 | B | pass 2's widened image swap ("swaps a changed image everywhere the composition named it") | "the image swap corrupted a URL that extends the swapped one" |
| 6 | Pass 3's findings | 4 | 172 | B | pass 2's new Inngest job (`infrastructure/inngest_pen_recompose.py`) | "the pen's job uploaded the live page before its version-guarded commit" |
| 7 | Pass 3's findings | 5 | 173 | B | pass 2's new Inngest job | "the job could retire itself on a row it was too early to read" |
| 8 | Pass 3's findings | 6 | 174 | B | pass 2's `hold_bound_shape` floor | "the floor armed where a recompose was already certain" |
| 9 | Pass 5's findings | 1 | 197 (para 21) | A (para) | pass 4's accessible-name stamp ("two of them defects the FOURTH pass had introduced") | para: "two of them defects the FOURTH pass had introduced, which is why the loop keeps reading"; line: "every logo-wall image was named for the client, not the partner" |
| 10 | Pass 5's findings | 2 | 198 (para 21) | A (para) | pass 4's anchor stamp | para: "The anchor stamp overwrote the composer's own `id` while the ask promised its ids were its own"; line: "the anchor stamp destroyed a composer-written section id" |
| 11 | Pass 5's findings | 3 | 199 (para 21) | A (para) | pass 4's `lp-section` stamp | para: "the `lp-section` class stamped for hrtag's observer is not a neutral marker"; line: "the `lp-section` stamp imported the kit's padding and reveal animation onto the composed design" |
| 12 | Pass 5's findings | 4 | 200 | A | pass 4's split of the Verify step into `engine/lp_composition_inspect.py` | "four `bs4.element` imports and `BaseModel` left by the split" |
| 13 | Pass 7's findings | 1 | 209 | A | pass 6's collage fix | "pass 6's collage fix was not in the tree" |
| 14 | Pass 7's findings | 2 | 210 | A | pass 6's link-label fix | "pass 6's link-label fix was not in the tree, while its sheet half was" |
| 15 | Pass 7's findings | 3 | 211 (para 25) | A (para) | the commit named "the review's sixth pass" | para: "carried a review agent's stray mutation as its only change to that file — silently unnaming every item image on a composed page, the defect pass 5 had just fixed"; line: "a review agent's stray mutation was committed, unnaming every item image" |
| 16 | Pass 7's findings | 4 | 212 | B | pass 6's link-label fix (missing) | "`_LINK_LABEL_NOTE` was unreachable while pinned and vouched — FIXED by 2: the label now exists" |
| 17 | Pass 7's findings | 6 | 214 | A (record) | the sixth commit body's claim | "this record and the sixth commit body both said findings 1 and 2 were fixed when they were not" |
| 18 | Pass 8's findings | 2 | 219 (para 27) | A | pass 1's ratchet walking a hand list; pass 2's hand-widening ("two passes earlier") | line: "the composition ratchet walked a hand-written module list, which is how both that path and the pen's shipped unheld"; para: "which is also how the pen's insert shipped unheld two passes earlier, when the answer taken was to widen the list by hand" |
| 19 | Pass 8's findings | 3 | 220 (para 27) | B | pass 2's swap sweep | para: "the swap sweep ran after the per-binding pass had written the correct src and rewrote it" |
| 20 | Pass 8's findings | 5 | 222 (para 27) | A (para) | pass 7's optionality test | para: "pass 7's optionality test was a membership check that stayed green when the rule widened to make the specialists' headline optional" |
| 21 | Pass 8's findings | 6 | 223 (para 27) | A (para) | pass 4's change to the sticky bar's hero lookup | para: "the sticky bar's hero lookup, changed in pass 4 to one comma-separated list, silently moved every KIT page's observed box" |
| 22 | Pass 9's findings | 1 | 227 (para 29) | A (para) | the eighth pass's swap-sweep fix | para: "the first was the eighth pass's own fix done for one half only: the swap sweep shielded a `src` it had CHANGED, not every src it had bound" |
| 23 | Pass 9's findings | 2 | 228 (para 29) | A (para) | pass 8's reconcile-before-persist order | para: "the reconcile-before-persist order pass 8 introduced was held by nothing" |
| 24 | Pass 10's findings | 1 | 233 | A | pass 5's replacement of hrtag's selector arm | "because pass 5 replaced the arm that served the customer's own site instead of adding to it" |
| 25 | Pass 10's findings | 2 | 234 (para 31) | A (para) | the ninth pass's shield | para: "the ninth pass's shield covered a bound image's `src` and nothing else, so its own `srcset`, a `<source>` beside it, its inline background and its stylesheet rule were all still swept" |
| 26 | Pass 11's findings | 1 | 238 (para 33) | A (para) | the tenth pass's fix (marker-scoped shield) | para: "its first two said the tenth pass's fix was incomplete on its own terms — the bound-image shield was scoped to the MARKER while ownership belongs to the URL" |
| 27 | Pass 11's findings | 2 | 239 (para 33) | A (para) | the tenth pass's in-marker swap losing pass 3's guard | para: "the in-marker swap had lost the whole-token guard pass 3 added" |
| 28 | Codex's findings on the new contract | 3 | 247 (para 37) | A (para) | the contract commit ("every image reference belongs to one binding") reintroducing what pass 3 fixed | para: "the same prefix confusion pass 3 fixed in the rebind, reintroduced in the inspector" |
| 29 | Codex's findings on the new contract | 6 | 250 | C | the contract commit's own tests | "two of my own counterexamples overclaimed (a nesting test with no nested markers asserting an absence; an exchange test comparing URL counts)" |
| 30 | Codex's findings on the new contract | 1, 2, 4, 5 | 245–249 (para 37) | C | gaps in the contract commit, the fix for pass 11 | para: "reproduced five gaps in the implementation while keeping the model" |
| 31 | Pass 12's findings | 1 | 254 (para 39) | B | `displays_image` / the background check from Codex's contract round; the session's own test | para: "my own test for this used a bare URL rather than a `url()`, so it never touched the shape a composer writes"; line: "a marker satisfied its binding from `mask-image`, `cursor` or a custom property and showed no picture" |
| 32 | Pass 12's findings | 2 | 255 | B | the contract commit's setter | "an in-page anchor inside an image marker was overwritten with the image's URL on the first render after any change" |
| 33 | Pass 13's findings | 2 | 262 (para 41) | A (para) | pass 12's CSS-property filter | para: "pass 12's CSS-property filter, correct for satisfaction, quietly made a `mask-image` URL unownable" |
| 34 | Pass 13's findings | 1 | 261 | B | `displays_image` (Codex contract round) | "`displays_image` accepted tag/attribute pairs a browser paints nothing from, so a marker could report a picture it never showed" |
| 35 | Pass 13's findings | 3 | 263 | B | the one-function shape each earlier narrowing patched | "one function answered ownership, satisfaction and render from one list, which is why each narrowing opened the next escape" |
| 36 | Pass 13's findings | 8 | 268 | A | the ninth pass's logo fix | "the ninth pass's logo fix was held by nothing; reverting it left every renderer test green" |
| 37 | Pass 14's findings | 4 | 275 | B | pass 1's AST ratchet (`test_lp_edits_reconcile_the_composition.py`) | "three of the six reconcile sites were held only by an AST ratchet, which a call that never executes satisfies" |
| 38 | Pass 14's findings | 7 | 278 | B (record) | the review's own changes to the composed render | "the plan's item-3 stance cited retained proof renders written eleven commits earlier, before the review changed what the composed render emits" |
| 39 | Pass 14's findings | 8 | 279 | C | pass 14's own commit | "three of the fixes' own new tests were never collected, because they landed as nested defs inside a module-level function" |
| 40 | Pass 15's findings | 4 | 286 (para 45) | A (para, sibling) | pass 14's head-line fix | para: "Three of the four head lines beside the one pass 14 fixed were held by nothing, and one is the brand's typeface" |
| 41 | Pass 15's findings | 6 | 288 | B | pass 2's reconcile-time detonation fix (its test) | "the composer-detonation test cleared a composition its fixture never had" |
| 42 | Codex's findings on the second read | 1 | 292 (para 47) | A (para) | the third pass's separation of upload from commit | para: "The third pass separated the upload from the commit; this was the gap left between the commit and the step result." |
| 43 | Codex's findings on the second read | 2 | 293 (para 47) | A (para) | the thirteenth pass's `clip-path:url(#shape)` allowance | para: "the thirteenth pass let the composer write `clip-path:url(#shape)` — a reference into the page's own inline SVG, which the tag floor permits — but told only the inspector" |
| 44 | Codex's findings on the second read | 3 | 294 (para 47) | C | this round's own fragment filter | para: "a survivor of this lane's own mutation gate — the third finding of this round" |
| 45 | Pass 16's findings | 1, 2, 3 | 298–300 | B | pass 2's pen job and its chat events (1, 2); the lane's revise→recompose wiring (3) | "the chat's pen-notice arm set could be deleted with the whole frontend suite green"; "`_announce_step` — the only thing that tells a client their page was redesigned"; "the Design Critic's revise → recompose seam was held on neither platform" |
| 46 | Pass 17's findings | 4 | 307 (para 51) | A (para) | pass 16's test masking the wiring | para: "the composer half is driven by pass 16's test, which patches that function out" |
| 47 | Pass 18's findings | 1 | 311 | A | pass 17's fix of the revise | "the Design Critic's revise was still dead on Google Search after pass 17's fix, on a second read of the same absent thing eleven lines below" |
| 48 | Pass 18's findings | 5 | 315 | B | pass 17's secondary-action-as-link fix (the inspector's new bar) | "the sheet note placing a secondary action inside an anchor was unheld while the inspector refuses the draft without it" |
| 49 | Pass 18's findings | 2 | 312 | B (weak) | pass 17's newline normalisation in the rebind | "a line break in bound copy serialised as `<br></br>` and reached the visitor as two breaks" |
| 50 | Pass 19's findings | 3 | 322 (para 57) | A (para) | pass 2's floor on the acceptance read's repair only | para: "pass 2 replaced its reconcile post with a floor on the acceptance READ's repair only — the editor runs on the same page after the same critic and got no equivalent" |
| 51 | Pass 20's findings | 1 | 329 (para 59) | A (para) | the nineteenth pass's check-cell binding | para: "its first is the NINETEENTH pass's fix applied to the render and not to the pen" |
| 52 | Pass 20's findings | 2 | 330 (para 59) | A (para) | pass 19's new test | para: "pass 19's new test derived its expectation FROM that mapping, so it agreed with any mapping, inverted included" |
| 53 | Pass 20's findings | 3 | 331 (para 59) | A (para) | pass 19's correction of the copy editor only | para: "pass 19 discovered otherwise and corrected only the copy editor" |
| 54 | Pass 21's findings | 1 | 337 (para 61) | A (para) | the twentieth pass's step-number binding | para: "its first is the TWENTIETH pass's own fix made for the explicit case and not the default one" |
| 55 | Pass 21's findings | 2 | 338 (para 61) | A (para) | pass 19's move of the render vocabulary | para: "the render vocabulary pass 19 moved had left the fingerprint" |
| 56 | Pass 21's findings | 3 | 339 | A | the previous round's (pass 20's) pick-parity fix | "`kit_pick_keys`'s per-section slicing was the sole guard of the previous round's fix and nothing held it" |
| 57 | Pass 21's findings | 4 | 340 | A (record) | the earlier fixes (pass 19's correction 44 lines below) | "three statements the earlier fixes left standing, including the binding module's own contract paragraph contradicting its code 44 lines below" |
| 58 | Codex's findings on the third read | 1 | 344 | B | pass 19's binding of comparison cells; the rebind's prune | "emptying one comparison cell pruned its element, sliding every later answer in the row one column left, under the wrong heading" |
| 59 | Codex's findings on the third read | 2 | 345 | B | the totality sweeps of passes 19–20 | "both totality checks compose the content they check, so neither could see a design re-bound against content that MOVED" |
| 60 | Codex's findings on the third read | 3 | 346 | C | this round's third sweep | "that sweep could not reach the defect that named it, because its generic payload carries no comparison values" |
| 61 | Pass 22's findings | 1 | 350 (para 65) | A | the fingerprint fix of the round before (pass 21) | para: "the fingerprint fix of the round before was made by hand-listing one file"; line: "the fingerprint's reach was closed by hand-listing one file, in a module whose docstring forbids it" |
| 62 | Pass 22's findings | 2 | 351 (para 65) | A (para) | pass 9's logo fix (header only) | para: "pass 9 fixed the header, the footer was already right, the social proof bar was never converted" |
| 63 | Codex's findings on the completeness read | 1 | 355 | A | pass 22's logo fix | "the logo fix inserted the company name raw into the social proof bar's `alt` where its two siblings escape it" |
| 64 | Codex's findings on the completeness read | 2 | 356 (para 67) | A | pass 22's dispatcher set | para: "the same mistake as the fingerprint hand-list pass 22 had just found, made an hour apart, one file over"; line: "the dispatcher's company-aware set was a hand-written list beside four sets derived by signature introspection" |
| 65 | Codex's findings on the completeness read | 3 | 357 (para 67) | A | pass 21's numbering fix | para: "the numbering fix handled null but not absent"; line: "the step-number derivation handled a null key and not an absent one, which the kit numbers by position" |
| 66 | Pass 23's findings | 2 | 362 (para 69) | B | pass 21's one derivation of the step number ("at the sites the last two rounds had just touched") | line: "the step number was bound as required text on `icon_grid`, whose kit paints a symbol" |
| 67 | Pass 23's findings | 3 | 363 (para 69) | A (para) | the previous round's absent-key fix (Codex completeness read #3) | para: "the shape the previous round's absent-key fix had introduced, whose commit body called it latent because "a validated dump always carries the key" while `MechanismCodeStep` never does" |
| 68 | Pass 23's findings | 4 | 364 | A | the absent-key fix's test | "the absent-key branch was held by nothing: its test built the shape through the model, which always carries the key" |
| 69 | Pass 23's findings | 5 | 365 (para 69) | A | Codex's third-read third sweep | line: "the third sweep's "dropped item" transition never fired, because every payload list had one entry"; para: "while the record and the commit claimed three kinds of edit" |
| 70 | Pass 23's findings | 6 | 366 | B | pass 21's step-number derivation | "a stored 0 showing its position was held by nothing" |
| 71 | Codex's findings on the twenty-third round | 1 | 375 (para 71) | A | pass 23's spoken-attribute floor | para: "The spoken-attribute floor was hand-cut short"; line: "the spoken-attribute floor named four attributes where a screen reader speaks seven" |
| 72 | Codex's findings on the twenty-third round | 2 | 376 | A | pass 23's third-stamp freshness reader | "the integrity reader matched the composed stamp's name anywhere in the served page, so a kit page quoting it would read as composed" |
| 73 | Pass 24's findings | 1 | 380 (para 73) | B | pass 20's pen parity; pass 23's two-entry payloads | para: "the pen parity of pass 20 stripping the extra markers left the extra items visible and unpickable"; "pass 23's two-entry payloads made the case reachable and not observed" |
| 74 | Pass 24's findings | 2 | 381 (para 73) | A | pass 23's stored-number fix ("promises of the last two rounds that survived the suite when broken") | "the stored step number was held by nothing on either render, because every test stored the position" |
| 75 | Pass 24's findings | 3 | 382 | A | pass 23's third-axis reader | "the third axis's reader of the served page was held by nothing — every freshness test patched the seam whole" |
| 76 | Pass 24's findings | 4 | 383 (para 73) | A (para) | pass 14's second guard; pass 23's panel advice | para: "the render refused it (the second guard) while no edit path read the stylesheet, so nothing recomposed it and the new panel advice ("an edit through the page recomposes it") was untrue for it" |
| 77 | Pass 24's findings | 5 | 384 | A | pass 23's retired-key registry | ""rows before the fold keep their class" was held by nothing" |
| 78 | Pass 24's findings | 6 | 385 | A | pass 23's / Codex 23rd's spoken-attribute floor | "the spoken-attribute floor was unheld on bound elements" |
| 79 | Pass 24's findings | 7 | 386 | A | pass 23's integrity-panel repair | "the repair's design-only filter was unheld for a variant stale on the design axis and another, and reported clean after re-rendering it" |
| 80 | Pass 24's findings | 8 | 387 | A | pass 23's sweep fix and its guard | "pass 23's tally was vacuous for the item-list shape and the sweep could not see a dropped item's survivor or its stale words" |
| 81 | Codex's findings on the twenty-fourth round | 1 | 392 (para 75) | A (sibling) | pass 24's `items_shown` | para: "A third kit archetype caps its items"; line: "the footer's minimal strip shows the first two valid links and the walk bound them all" |
| 82 | Codex's findings on the twenty-fourth round | 2 | 393 (para 75) | A | pass 24's stylesheet fix | para: "The stylesheet fix held one of Verify's five floors: the rebind read only `</`"; line: "the render held one of Verify's five stylesheet floors" |
| 83 | Codex's findings on the twenty-fourth round | 3 | 394 | A | pass 24's retired-key test | "the retired key's class was tested as present, not as the class it was" |
| 84 | Codex's findings on the sibling fixes | 1 | 398 (para 77) | A | Codex 24th's `shown_item_indexes` (a cap only) | para: "the first two said the representation was still wrong, the same lesson as pass 11's image ownership: a declaration that mirrors ONE kind of kit rule (a cap) leaves every other kind" |
| 85 | Codex's findings on the sibling fixes | 2 | 399 | A | same cap-only declaration | "the kit skips an item without its words or image at five more sites and the walk bound its wrapper" |
| 86 | Codex's findings on the sibling fixes | 3 | 400 (para 77) | A | Codex 24th's footer helper | para: "Codex's third finding was a typed-call regression in the footer's helper (`.get()` on a `FooterLink`)" |
| 87 | Codex's findings on the sibling fixes | 4 | 401 | A | pass 23's/24's two-item payloads | "a two-item payload could not catch a cap of two or more" |
| 88 | Codex's findings on the rewrite | 1 | 405 (para 79) | A | the rewrite's sweep of the kit's loops | para: "which the sweep of the kit's loops had missed because their guards sit below an `isinstance` line" |
| 89 | Codex's findings on the rewrite | 2 | 406 (para 79) | A | the rewrite's four repaired sites (walk strips, kit reads truthiness) | para: "Whitespace split the two renders: the walk strips before it binds while the kit's guards read truthiness"; "at the four repaired sites and at every `if text:` in the kit" |
| 90 | Codex's findings on the rewrite | 3 | 407 | A | the four-item payload of the sibling-fix round | "the payload could not prove the declarations against the kit" |
| 91 | Codex's findings on the rewrite | 4 | 408 | A | Codex 24th's footer helper (regression, again) | "the footer's helper read a typed link with `.get()`" |
| 92 | Codex's findings on the round after the rewrite | 1 | 412 (para 81) | A | the rewrite round's whitespace fix (footer rule missed) | para: "The footer's link rule read truthiness where the kit and the walk read words, so a whitespace-only link counted toward the minimal strip's cap and displaced a real one" |
| 93 | Codex's findings on the round after the rewrite | 2 | 413 | B | the declaration representation (three shapes; a fourth missing) | "a table's headers show only with rows and the walk bound them regardless" |
| 94 | Codex's findings on the round after the rewrite | 3 | 414 (para 81) | A | Codex third-read's "empty entry keeps its element" | para: "a whitespace-only cell, kept for its position, bound as a line break where the kit shows an empty cell" |
| 95 | Pass 25's findings | 1 | 418 | A | the whitespace fix (Codex rewrite round) | "the whitespace fix landed in `_present` and not in the footer's link rule, and the rule itself was held by nothing" |
| 96 | Pass 25's findings | 2 | 419 (para 83) | A | the declaration home built over rounds 24–25 | para: "pass 20's lesson ("the new test derived its expectation FROM that mapping, so it agreed with any mapping") at the rule's new home" |
| 97 | Pass 25's findings | 3 | 420 | A (record) | the rewrite round's record claim | ""the preference flips" was false: no transition rendered a fallback on" |
| 98 | Pass 25's findings | 4 | 421 (para 83) | B | sibling of pass 11's finding (not its fix) | para: "Its fourth is a field-level sibling of pass 11's logo-wall finding" |
| 99 | Pass 25's findings | 5 | 422 | A | Codex 24th's footer rule | "cap-before-filter and the `#` refusal on the footer's rule were held by nothing" |
| 100 | Pass 25's findings | 6 | 423 | B | the repair's refusal (Codex rewrite round) | "the repair's refusal said "added" for a repair that gives words to an item the page hides" |
| 101 | Codex's findings on the twenty-fifth round | 1 | 427 (para 85) | A (sibling) | pass 25's scalar-field rule | para: "The scalar-field rule had one declaration (the footer's name) where the kit has four more fields it shows only as a clause of another's line" |
| 102 | Codex's findings on the twenty-fifth round | 2 | 428 | A | pass 25's literal table | "the literal table pinned `before_outcomes` and not `after_outcomes`" |
| 103 | Codex's findings on the round after the twenty-fifth | 1 | 432 | A | Codex 25th's literal field cases | "the literal field cases held one side per founder class and one archetype per inherited rule" |
| 104 | Pass 26's findings | 1 | 436 (para 89) | A | the last round's four field rules (Codex 25th) | para: "The last round's four field rules were declared for the walk while the kit kept guards of its own at seven sites" |
| 105 | Pass 26's findings | 2 | 437 | A | pass 25's page-level field rule vs the growth floor | "the growth floor built its page from sections alone, so a page-level rule read as shown and a repair was refused once for growth the walk never binds" |
| 106 | Pass 26's findings | 4 | 439 | A | the merged maps / list rule of rounds 25 | "the subclass winning a clash in the merged maps, and the walk handing the page to a list rule, were held by nothing" |
| 107 | Pass 27's findings | 1 | 443 (para 93) | A | pass 26's page threading | para: "every one is the last round's own wiring held at the mechanism and not at its callers"; line: "the page threaded to the growth floor was held at none of its four call sites" |
| 108 | Pass 27's findings | 2 | 444 (para 93) | A | pass 26's walk branches | "the walk's field rule on the image, link, call-to-action and linked-text branches was held by nothing" |
| 109 | Pass 27's findings | 3 | 445 | A (record) | pass 26's record claim | "four of the seven kit rule-reads sit beside a structural guard that hides the same thing, and the record said all seven were load-bearing" |
| 110 | Pass 27's findings | 4 | 446 | A | the base rule (Codex 24th/25th) | "the base rule's filter-then-cap order was held only through the footer's override" |
| 111 | Pass 28's findings | 1 | 450 (para 95) | B | pass 2's job line; pass 19's removal-shifts discovery | line: "the pen's redesign line said "the item you added" on every edit that owed a design, a typo fix on a degraded page and a shifting removal included" |
| 112 | Pass 28's findings | 2 | 451 (para 95) | A | pass 27's test | para: "pass 27's test saw the page arrive at the constructor and nothing saw the floor read it" |
| 113 | Pass 28's findings | 3 | 452 | A | pass 27's "four sites held" | "the repair loop's page was dead code that pass 27 called held" |
| 114 | Pass 28's findings | 6 | 455 | A (record) | pass 27's record arithmetic | ""none was filed" was false and pass 27's arithmetic was off by its controls" |
| 115 | Pass 28's findings | 8 | 457 | B | the need rule (Codex 25th round) | "the need rule's "all" was undecidable on shipped rules" |
| 116 | Codex's findings on the twenty-eighth round | 1 | 461 | A | pass 28's detection-only check | "the detection-only check registered a repair the panel would offer as Apply Fix" |
| 117 | Codex's findings on the twenty-eighth round | 2 | 462 (para 97) | A | pass 28's failure sentence | para: "The failure sentence still implied a prior design ("could not be redesigned around it") for a typo fix on a page the build never composed" |
| 118 | Codex's findings on the twenty-eighth round | 3 | 463 (para 97) | A (sibling) | pass 28's edit-neutral line missing siblings | para: "And the insert-only story survived at six siblings"; line: "the insert-only story survived at six sibling sites, and the check named one writer of a stamp two write" |
| 119 | Pass 29's findings | 1 | 467 (para 99) | A | the previous pass's (Codex 28th's) failure sentence | para: "The pen's failure sentence — the one the previous pass named — was held by nothing" |
| 120 | Pass 29's findings | 2 | 468 (para 99) | A (sibling) | Codex 28th's sibling fix | para: "Five sibling surfaces still told the insert-only story — four of them in Codex's read of the same round, the fifth the comment above the pen's own owed-design call" |
| 121 | Pass 29's findings | 4 | 470 | A | pass 28's new check | "the new check's instant-form guard was held by nothing" |
| 122 | Pass 29's findings | 5 | 471 (para 99) | B (record) | the record's crediting of Codex's coverage fix | para: "Codex's coverage fix was credited to the wrong commit" |
| 123 | Codex's findings on the twenty-ninth round | 1 | 475 | A (sibling) | pass 29's insert-only sweep | "the owed-design field's comment and the reconcile module's header still told the insert-only story" |
| 124 | Codex's findings on the twenty-ninth round | 2 | 476 | A (record) | pass 29's record corrections | "the dispositions' header omitted the filed heading card and pass 28's entry still named a dry-run" |
| 125 | Pass 30's findings | 1 | 480 | B | pass 2's redesign job (no handler where its sibling has one) | "the pen's redesign job had no failure handler, so a client whose run died of a fault around the composer was told nothing" |
| 126 | Pass 30's findings | 2 | 481 | B | pass 23's/24's freshness axis | "the freshness row framed a design-only stale variant as behind the build and promised a re-render" |
| 127 | Pass 30's findings | 3 | 482 (para 103) | B | the lane's third send (pass 2's job) | para: "a class older than the card (two sends already stood before it added a third)" |
| 128 | Pass 30's findings | 4 | 483 (para 103) | A (record) | the review's own stamps, logo name and sticky selector | para: "its bytes moved with the stamps, the logo's name and the sticky bar's selector, and the record says so" |
| 129 | Codex's findings on the thirtieth round | 1 | 488 (para 105) | A | pass 30's new failure handler | para: "the new failure handler always said "a design could not be made", which is false when the run died at the upload after the design was stored" |
| 130 | Pass 31's findings | 1 | 492 (para 107) | A | pass 30's handler | para: "so the new handler would have posted "your change is on the page, but a design could not be made" — both halves false" |
| 131 | Pass 31's findings | 2 | 493 | A | pass 30's handler | "a run that died at the upload after storing the design would say a design could not be made — FIXED with Codex's finding above" |
| 132 | Pass 31's findings | 3 | 494 | A (record) | the handler's docstring (pass 30 / Codex 30th) | "the handler's docstring said the client is told after a load fault, where that is silence by design" |
| 133 | Pass 31's findings | 4 | 495 | B | pass 3's version-carrying event; Codex second read's "nothing owed" | "a version cannot tell a failed save's number from the next save's, so a run could announce a redesign around a change the page does not hold" |
| 134 | Pass 31's findings | 5 | 496 | A | pass 30's reframed freshness row | "the Apply Fix sentence followed the design-only note, pointing "each such variant" at the page a re-render does not help" |
| 135 | Pass 31's findings | 6 | 497 | A | pass 30's freshness row | "the freshness row's missing line named live HTML for a design-only page where the repair names a design" |
| 136 | Pass 31's findings | 7 | 498 | A (record) | pass 30's entry | "pass 30's entry said "three landing-page checks" where the pass drove eight integrity checks through the admin route" |
| 137 | Pass 32's findings | 1 | 502 (para 111) | A | pass 31's copy stamp | line: "latent, made by pass 31's fix"; para: "the first is a regression the round before made" |
| 138 | Pass 32's findings | 2 | 503 (para 111) | A | pass 31's copy stamp | para: "The stamp also missed one field"; line: "the copy stamp's scrub dropped any key containing "signature", so a guarantee's signature was invisible to it" |
| 139 | Pass 32's findings | 3 | 504 (para 111) | A | the handler (Codex 30th / pass 31) | para: "The handler's no-design sentence still said "on the live page" from the deployed URL alone" |
| 140 | Pass 32's findings | 4 | 505 | B | the freshness check (pass 23/24) | "a variant stale on both axes was counted as behind the build only, dropping its design axis from the missing line and the design paragraph" |
| 141 | Pass 32's findings | 5 | 506 (para 111) | A | pass 30's freshness row | para: "a design-only row still carried a repair hint, so the panel offered Apply Fix where the dry-run would re-render no variant" |
| 142 | Codex's findings on the thirty-second round | 1 | 510 (para 113) | A | pass 32's failure line | para: "the failure line still said "the page shows its standard layout for now", which the row cannot know when the apply's own refresh had failed" |
| 143 | Codex's findings on the thirty-second round | 2 | 511 | A | pass 32's hint removal | "the panel's button ignored the outcome's repair hint, so the removed hint changed nothing a person sees" |
| 144 | Pass 33's findings | 1 | 515 (para 115) | A | pass 32's hint removal | para: "so the hint pass 32 removed changed nothing a person sees, and the rule was not applied to its own sibling" |
| 145 | Pass 33's findings | 2 | 516 | A (record) | pass 32's disposition | "pass 32's disposition claimed the button fixed where the hint reached no button" |
| 146 | Pass 33's findings | 3 | 517 | A | pass 32's failure line | "the failure line still asserted what the page shows — FIXED with Codex's finding above" |
| 147 | Pass 33's findings | 4 | 518 (para 115) | B (outside; corrects the premise of pass 31/32's fix) | pass 31's stated premise | para: "That also corrects this job's stated premise: the case the edit record covers is a commit that fails after the send for another reason, not a version refused" |
| 148 | Pass 33's findings | 5 | 519 | A (record) | pass 31's entry and the job's docstring | "the job's docstring, the route's comment and pass 31's entry named a refused version as the case the edit record covers, where that case cannot occur" |
| 149 | Codex's findings on the thirty-third round | 1 | 524 | A | pass 33's replaced sentence | "the failure handler's docstring still described the replaced sentence" |
| 150 | Codex's findings on the thirty-third round | 2 | 525 | A | pass 33's race filing | "the race filing located the guarded write at the chat line's flush where the flag-clearing query autoflushes it first" |
| 151 | Pass 34's findings | 1 | 529 | A | pass 33's hint rule | "the unreachable-only row's missing hint was held by nothing" |
| 152 | Pass 34's findings | 2 | 530 | A | pass 33's hint rule | "a freshness row with no re-render owed shows neither a button nor instructions, and the unreachable-only reason named no action" |
| 153 | Pass 34's findings | 3 | 531 | A (record) | pass 33's button model | "the button's written model in the type's docstring, the page's header and the API type still said a registered repair alone shows the button" |
| 154 | Pass 34's findings | 4 | 532 | A (record) | pass 33's race filing | "the race filing placed the guarded write at the chat line's flush — FIXED with Codex's finding above" |
| 155 | Pass 34's findings | 5 | 533 | A (record) | pass 33's correction of "refused" | "pass 31's entry, one disposition, two test names and two docstrings still said "refused" for a commit that failed" |
| 156 | Pass 34's findings | 6 | 534 | A (record) | pass 32's job docstring | "the job's docstring said a failed commit leaves the row behind for good, where a connection lost after the server committed does not" |
| 157 | Codex's findings on the thirty-fourth round | 1 | 539 | A (record) | pass 34's "refused" correction | "the record called a deleted edit record "a refused save"" |
| 158 | Codex's findings on the thirty-fourth round | 2 | 540 | A (record) | pass 34's "refused" correction | "a route test's comment said "a refused save's" for a failed one" |
| 159 | Codex's findings on the thirty-fourth round | 3 | 541 | A (record) | pass 32/33's state-reader docstring | "the state-reader's docstring and the handler test's said "failed at the commit" without saying whose" |

Unnumbered passages in "The passes" of the same shape (not dispositions):

| Line | Passage | Verbatim quote |
|---|---|---|
| 35 | The loop's own turn | "five consecutive passes had each traded one wrong-picture case for the next in one place" |
| 37 | Codex's read of the contract commit | "The mutation-capture fault of pass 6 recurred here: that commit shipped carrying a live mutation from the running review agent and failed its own tests; the next commit restores the captured line" |
| 65 | Pass 22, closing | "Both of this round's headline fixes were then found UNHELD by the mutation gate and are held now." |
| 125 | reader stopped | "the fault this record names at passes 6 and 7, made this time by the session, which committed twice while a reader was running" |

## Table 2 — the 34 pass paragraphs

| Pass | Line | Paragraph says an earlier pass's fix caused a finding? | Quote (≤150) |
|---|---|---|---|
| 1 | 13 | no | — |
| 2 | 15 | yes | "the first was a design error the first pass had introduced: its reconcile post-processor ran AFTER the acceptance read" |
| 3 | 17 | yes (pass 2's floor unheld) | "The floor that had replaced pass 1's reconcile post — `hold_bound_shape` — was held by NOTHING" |
| 4 | 19 | no (its own walk found its own new sentence contradicted; no earlier pass named) | — |
| 5 | 21 | yes | "two of them defects the FOURTH pass had introduced, which is why the loop keeps reading" |
| 6 | 23 | no (process fault about its own revert only) | — |
| 7 | 25 | yes | "its first was that **pass 6's two fixes were not in the tree**" |
| 8 | 27 | yes | "pass 7's optionality test was a membership check that stayed green"; "changed in pass 4 to one comma-separated list" |
| 9 | 29 | yes | "the first was the eighth pass's own fix done for one half only" |
| 10 | 31 | yes | "both were corrections to earlier passes' own fixes — the pattern the late passes keep surfacing" |
| 11 | 33 | yes | "its first two said the tenth pass's fix was incomplete on its own terms" |
| 12 | 39 | no explicit (gaps in the contract commit's implementation; "my own test for this used a bare URL") | — |
| 13 | 41 | yes | "pass 12's CSS-property filter, correct for satisfaction, quietly made a `mask-image` URL unownable"; "the ninth pass's logo fix held by nothing" |
| 14 | 43 | no (same-round only: "Three of that commit's own new tests") | — |
| 15 | 45 | yes (sibling of pass 14's fix) | "Three of the four head lines beside the one pass 14 fixed were held by nothing" |
| 16 | 49 | no | — |
| 17 | 51 | yes (weak: an earlier test masking) | "the composer half is driven by pass 16's test, which patches that function out" |
| 18 | 55 | yes | "the first was the SEVENTEENTH pass's own fix, done for one platform" |
| 19 | 57 | yes | "pass 2 replaced its reconcile post with a floor on the acceptance READ's repair only — the editor ... got no equivalent" |
| 20 | 59 | yes | "its first is the NINETEENTH pass's fix applied to the render and not to the pen" |
| 21 | 61 | yes | "its first is the TWENTIETH pass's own fix made for the explicit case and not the default one" |
| 22 | 65 | yes | "the fingerprint fix of the round before was made by hand-listing one file"; "pass 9 fixed the header" |
| 23 | 69 | yes | "the shape the previous round's absent-key fix had introduced" |
| 24 | 73 | yes | "promises of the last two rounds that survived the suite when broken"; "pass 23's guard for its own sweep fix was vacuous" |
| 25 | 83 | yes | "the whitespace fix landed in `_present` and not in the footer's link rule" |
| 26 | 89 | yes | "The last round's four field rules were declared for the walk while the kit kept guards of its own at seven sites" |
| 27 | 93 | yes | "every one is the last round's own wiring held at the mechanism and not at its callers" |
| 28 | 95 | yes | "pass 27's test saw the page arrive at the constructor and nothing saw the floor read it" |
| 29 | 99 | yes | "The pen's failure sentence — the one the previous pass named — was held by nothing" |
| 30 | 103 | no explicit (a record claim corrected; no earlier fix named as cause) | — |
| 31 | 107 | yes | "so the new handler would have posted "your change is on the page, but a design could not be made" — both halves false" |
| 32 | 111 | yes | "the first is a regression the round before made" |
| 33 | 115 | yes | "so the hint pass 32 removed changed nothing a person sees, and the rule was not applied to its own sibling" |
| 34 | 119 | yes (record findings on earlier fixes) | "pass 31's entry and a disposition still said "refused", and two test names and docstrings with them" |
