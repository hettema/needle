# Review — work the laptop interrupted comes back by itself, and the board says truly how it ended

**Plan:** docs/plans/done/2026-09-05-work-the-laptop-interrupted-comes-back-by-itself-and-the-board-says-truly-how-it-ended.md
**Reviewer:** the build session (Claude Fable 5.1, card #68's lane) for its own cold re-read and the reader on the real pile; Codex, called warm through `needle call`, for the independent pass
**Diff range:** 095cdd3 (the trunk the lane rebased onto after #99's fold) .. the close commit that carries this record
**Findings:** see the passes below

## The passes

The review runs as a loop (`CLAUDE.md`): each pass one lens, the fixes landed, the next pass re-reads the fixed work.

1. **The author's cold re-read, the three lenses in one pass, and the reader run on the real pile.** Against the "done means": every item's stance names the test that holds it. The seams: `_recover` writes the recovery row before the launch and closes it after, and the partial unique index refuses a second open row per card, so a `needle` command's own loop and the server cannot both launch a replacement (finding 1 was found here: the lanes read at the top of the pass were a pass behind the park the same pass had just written, so the park was not on the face until the next beat — `_recover` now answers whether it changed anything, and the caller re-reads). Reading the diff for who the board would bring back found three it must not: a card the owner put in Not now or that sits in Decision moment (#219 stood down beside another card's close and sits there; the plan reads it as his), his own terminal (`move` refuses an interactive session, so the row would have counted a failed attempt every hour), and a worker of the other make (no transcript to resume, plan 57) — findings 2 and 3. A parked lane mid-move said *it carries on by itself once it lands* while it waited on the reset — finding 4. Then the reader was run against Hello Revenue's real pile, read-only over a copy of the shared store (`tools/standing_pile.py`), before any fold: twenty ended lanes. It named the daemon-space kill of 2026-09-05 for lanes whose transcripts show them active *after* that kill (#341, #417, #435), because a telling line only had to be after the life's start — a kill the process outlived is not its cause; the window is now after the last time the process was known alive as well (finding 5). And it read #435's registry `stopped` as a stop through its account, where the daemon had written *ended while the background service was off* for a session it lost — a death it reports, not a stop it made; `stopped` names a stop only with the word `claude stop` writes (finding 6). The journal's last forty lines could not hold a kill four days back in a daemon space that logs hundreds a day: the reader now asks the journal from the life's window on (finding 7). After the fixes the reader's verdict on the pile matched the Discuss door's transcript survey of 2026-09-08 line for line: #147 and #219 his, #409 and #419 walls, #416, #426 and #429 the daemon kill, #341, #417 and #435 unresolved, the rest closed. The boundaries: `board/` reads the handoff's cause from the domain model, never from `runtime/` (the layers ratchet); every machine read goes through `runtime/machine.py` and the floor ratchet lists the account cache; the face sentences pass the vocabulary ratchet; the store's shape matches the migration; types regenerated.

## Dispositions

1. [seam] The lanes read at the top of `_reconcile_project` were a pass behind a park, a lift or an expiry the same pass wrote, so the park reached the face a beat late — FIXED in this lane: `_recover` answers whether it changed what the lanes read, and the caller re-reads; `test_a_lane_whose_memory_was_taken_waits_on_the_floor_and_comes_back_once_it_holds` reads the park on the face in the pass that wrote it.
2. [feature] A card in Not now or Decision moment with a dead lane read as unfinished work and would have been brought back — FIXED in this lane: `board/lane.py::disposition` reads both columns as the owner's; the real pile's #219 reads his.
3. [feature] The owner's own terminal and a Codex worker were candidates for a resume the runtime refuses, which would have written a failed attempt on the card every hour — FIXED in this lane: `_interruption` answers None for an interactive session and for the other make.
4. [feature] A parked lane mid-move said it carries on by itself once it lands — FIXED in this lane: the moving face's last clause is the park's own words while one stands, and `Lane.park` is set for a moving or blocked lane too.
5. [verification] A kill the process outlived was named as its cause: three lanes on the real pile active after the daemon-space kill of 2026-09-05 read as killed by it — FIXED in this lane: the journal window opens at the later of the life's start and the last time the process was known alive; `test_a_kill_inside_this_life_names_the_space_it_took` holds the outlived case.
6. [verification] The registry's `stopped` was read as a stop through the account whatever its detail, and the daemon writes that state with *ended while the background service was off* for a session it lost (#435) — FIXED in this lane: only the bare `stopped` that `claude stop` writes names a stop; anything else is read on for the cause and kept as evidence.
7. [verification] Forty journal lines from the tail could not reach a kill four days back in a daemon space that logs hundreds of lines a day, so the real pile's daemon kills were unreadable — FIXED in this lane: `machine.journal` takes `since` and `reasons.cause_of` asks from the life's window on; verified on this laptop against `claude-daemon-hrme.scope`'s 17:59Z kill of 2026-09-05.

## What was checked

- **The reader on the real machine:** `tools/standing_pile.py hellorevenue` over a private copy of the shared store (never the served one: a worktree's `needle` migrates whatever store it opens, card #51's incident), reading the real registries, journals, transcripts and boot list; twenty lanes, table in the plan's close-out.
- **The three writers of one handoff file**, by reading `~/.local/bin/claude-acct` on this laptop (lines 1358–1420, 1640–1700, 1795–1830 on 2026-09-09): a wall's `why` is the rule's reason, recover's is `connection back after a transient death`, the switch-back's ends `; back on Fable`; the seven files in `handoff/bg/` on this laptop carry all three.
- **The account's limits snapshot**, by reading `~/.cache/omarchy/claude-acct/<slot>.json`: `limits` by label as a share, `resets` by label as ISO, `fetchedAt` as epoch seconds.
- **The boot list and the journal's shape**: `journalctl --list-boots -o json` (five boots on this laptop, boot -1 ended 2026-09-07 16:51Z), `journalctl --user -u <unit> -o short-iso --since` (`2026-09-05T21:32:51+0200 DH systemd[1288]: <unit>: systemd-oomd killed 127 process(es) in this unit.`).
- **The suites:** `tests/board`, `tests/infrastructure`, `tests/runtime`, `tests/ratchets` and every file under `tests/api` green in the lane, but one ratchet, `test_every_title_is_in_the_owners_words`, red on `origin/develop` itself since 66b7fca over a suggestion of 2026-09-08 whose title uses *fold* — outside this change, filed as `docs/slice-suggestions/2026-09-09-a-defect-filed-from-another-project-lands-in-the-owners-words-or-not-at-all.md`. `tsc --noEmit` clean after `needle types` (`ending.ts` new, `lane.ts` and `slot.ts` regenerated); `vitest run` (68); the fixture regenerated.

## What the build learned the plan got wrong

- The plan's item 1 discriminator — a row that had a pid and now has none — cannot serve the standing pile, which predates any sighting: a row with no sighting older than the verify window is named from what the machine holds, and only a row younger than it is a birth. Stated in the item's stance.
- The plan said a wall park lifts *sooner when the rule finds headroom on another slot*; on the real pile a wall handoff from four days back names a rung that is spent today, so a resume after a death asks the rule now and passes its answer to the move, and the handoff's own rung is used only for the live hop it was written for.

## Not done, stated

- The crash between a launch and its record is not thrown inside `claude --bg` on the floor; the row it leaves behind is what the test builds and reads.
