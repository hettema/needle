# Review — work the laptop interrupted comes back by itself, and the board says truly how it ended

**Plan:** docs/plans/done/2026-09-05-work-the-laptop-interrupted-comes-back-by-itself-and-the-board-says-truly-how-it-ended.md
**Reviewer:** the build session (Claude Fable 5.1, card #68's lane) for its own cold re-read and the reader on the real pile; Codex, called warm through `needle call`, for the independent pass
**Diff range:** 095cdd3 (the trunk the lane rebased onto after #99's fold) .. the close commit that carries this record (the build ddb2085, the first pass 29109a1, the second pass's fixes, the close)
**Findings:** 27 — 8 the author's, 19 Codex's; 26 fixed in this lane, 1 a correction to this record

## The passes

The review runs as a loop (`CLAUDE.md`): each pass one lens, the fixes landed, the next pass re-reads the fixed work.

1. **The author's cold re-read, the three lenses in one pass, and the reader run on the real pile.** Against the "done means": every item's stance names the test that holds it. The seams: `_recover` writes the recovery row before the launch and closes it after, and the partial unique index refuses a second open row per card, so a `needle` command's own loop and the server cannot both launch a replacement (finding 1 was found here: the lanes read at the top of the pass were a pass behind the park the same pass had just written, so the park was not on the face until the next beat — `_recover` now answers whether it changed anything, and the caller re-reads). Reading the diff for who the board would bring back found three it must not: a card the owner put in Not now or that sits in Decision moment (#219 stood down beside another card's close and sits there; the plan reads it as his), his own terminal (`move` refuses an interactive session, so the row would have counted a failed attempt every hour), and a worker of the other make (no transcript to resume, plan 57) — findings 2 and 3. A parked lane mid-move said *it carries on by itself once it lands* while it waited on the reset — finding 4. Then the reader was run against Hello Revenue's real pile, read-only over a copy of the shared store (`tools/standing_pile.py`), before any fold: twenty ended lanes. It named the daemon-space kill of 2026-09-05 for lanes whose transcripts show them active *after* that kill (#341, #417, #435), because a telling line only had to be after the life's start — a kill the process outlived is not its cause; the window is now after the last time the process was known alive as well (finding 5). And it read #435's registry `stopped` as a stop through its account, where the daemon had written *ended while the background service was off* for a session it lost — a death it reports, not a stop it made; `stopped` names a stop only with the word `claude stop` writes (finding 6). The journal's last forty lines could not hold a kill four days back in a daemon space that logs hundreds a day: the reader now asks the journal from the life's window on (finding 7). After the fixes the reader's verdict on the pile was: #147 and #219 his, #409 and #419 walls, #416, #426 and #429 the daemon-space kill, #341, #417 and #435 unresolved, the rest closed — which agrees with the Discuss door's transcript survey of 2026-09-08 on the walls, the three it cannot name and on leaving #147 and #219 alone, and names the daemon kills for three lanes the survey left unnamed or read as an earlier life (disposition 26 says on what evidence). The boundaries: `board/` reads the handoff's cause from the domain model, never from `runtime/` (the layers ratchet); every machine read goes through `runtime/machine.py` and the floor ratchet lists the account cache; the face sentences pass the vocabulary ratchet; the store's shape matches the migration; types regenerated.

2. **Codex's pass — the reader who was not the author.** Called warm through `needle call` (call 60, the finished Needle-repo rollout 01a0802f resumed with the brief) on commit 29109a1, the frozen revision; it opened every changed file and the diff, ran nothing, and answered nineteen findings, all inside the change. Every one was re-read against the code before it was taken: eighteen held and one held in part. The ones that changed the mechanism: a live handoff bypassed the gate entirely and moved to a rung chosen hours before (finding 9); a closed card's ended lane and a lane that had put a decision to him both read *Something is wrong … open the card to resume it or start again* (8); an ASK row from a previous life, or any RULED row, decided the disposition, and `asks_owner` matched "your decision" inside "your decision was applied" (13); a parked lane whose interruption stopped qualifying kept its park, because the interruption was read before the disposition (14); the once-per-cause count was not a claim — a reader that counted before another's attempt landed could launch a second replacement (16); a session resumed fresh because its transcript was too large could never be found on a restart (18); the rescope and the release were recorded as done before the act (19); `abs()` on the clock let an attempt recorded after the clock fall out of the count (20); the count said "in the last hour" over every attempt ever, and "came back" of an attempt that failed (21); the prompt told a resumed session that nothing after its last tool result was on disk (22); `-n 400` still capped the journal under `--since` (23); the reader tool opened the served store, whose constructor migrates it (24); the tool predicted a resume the loop would refuse (25); and the first pass's record claimed a line-for-line match with the survey where it named more than the survey had (26). On the reader itself: a sighting's stale `last_seen` beat later transcript activity, and both spaces were read whatever space the sighting recorded (10); every telling line became a memory kill, so a unit failed on a signal read as the machine taking back memory and was resumable (11); the boot's casualty was decided by proximity alone and the recorded boot id went unused (12). The one taken in part: finding 12's second half, that a machine's last journal entry may mark a suspension before a later power loss — that is #85's own shape, and the machine's last record is the truest time anyone has for it; the words now say "the machine's last record before it went down", and the boot id must match the sighting's.
3. **The fixed work again, the author's re-read.** Every gate applies to a hop as to a resume: a live handoff younger than the horizon keeps the rung the wall detector chose (fresher than the rule's cache about the account that just ran out), anything older asks the rule now, and the floor is read fresh for both; the once-per-cause rule is a claim the store makes in one transaction (`open_recovery(horizon_seconds=)`), so the count and the launch cannot be split across two readers; the disposition is read before anything else and a park with nothing left to bring back lifts; `owner_decision_outstanding` reads the rows of this life; the ended face says *its close landed*, or *Your move: decide what it asked and bring it back* with the amber word *asked you*; the reader takes the latest evidence of life, reads the space the sighting names, names a signal or a crash as an ending that is not the memory's and never resumes it, and requires the boot id to match; the sighting row starts a new life when the process under the id changes and a session seen alive again loses its death row; the acts of rescope and release are recorded after they ran; a fresh-started replacement is found by its lane and birth time; the journal is read whole inside the window; the tool copies the store before it opens it and applies the loop's own exclusions. Re-reading the reworked `_recover` found nothing new. The floor cases were rewritten where the words changed and two were added to `tests/runtime/test_reasons.py` (the stale sighting, the other space, the signal, the mismatched boot). The pile was read again with the corrected reader (the table in the plan's close-out is the after-read).

## Dispositions

1. [seam] The lanes read at the top of `_reconcile_project` were a pass behind a park, a lift or an expiry the same pass wrote, so the park reached the face a beat late — FIXED in this lane: `_recover` answers whether it changed what the lanes read, and the caller re-reads; `test_a_lane_whose_memory_was_taken_waits_on_the_floor_and_comes_back_once_it_holds` reads the park on the face in the pass that wrote it.
2. [feature] A card in Not now or Decision moment with a dead lane read as unfinished work and would have been brought back — FIXED in this lane: `board/lane.py::disposition` reads both columns as the owner's; the real pile's #219 reads his.
3. [feature] The owner's own terminal and a Codex worker were candidates for a resume the runtime refuses, which would have written a failed attempt on the card every hour — FIXED in this lane: `_interruption` answers None for an interactive session and for the other make.
4. [feature] A parked lane mid-move said it carries on by itself once it lands — FIXED in this lane: the moving face's last clause is the park's own words while one stands, and `Lane.park` is set for a moving or blocked lane too.
5. [verification] A kill the process outlived was named as its cause: three lanes on the real pile active after the daemon-space kill of 2026-09-05 read as killed by it — FIXED in this lane: the journal window opens at the later of the life's start and the last time the process was known alive; `test_a_kill_inside_this_life_names_the_space_it_took` holds the outlived case.
6. [verification] The registry's `stopped` was read as a stop through the account whatever its detail, and the daemon writes that state with *ended while the background service was off* for a session it lost (#435) — FIXED in this lane: only the bare `stopped` that `claude stop` writes names a stop; anything else is read on for the cause and kept as evidence.
7. [verification] Forty journal lines from the tail could not reach a kill four days back in a daemon space that logs hundreds of lines a day, so the real pile's daemon kills were unreadable — FIXED in this lane: `machine.journal` takes `since` and `reasons.cause_of` asks from the life's window on; verified on this laptop against `claude-daemon-hrme.scope`'s 17:59Z kill of 2026-09-05.
8. [feature] A closed card's ended lane, and a lane that had put a decision to him, read *Something is wrong … open the card to resume it or start again* — FIXED in this lane (Codex's finding 1): the ENDED sentence reads the close and the last words; `test_a_closed_card_with_no_fold_record_is_finished_and_nothing_resumes` and `test_a_lane_that_put_a_decision_to_the_owner_is_his_even_when_the_machine_killed_it` assert the sentences.
9. [feature] A live handoff bypassed the gate and moved to the rung it named hours before — FIXED in this lane (Codex's finding 2): the floor is read for every recovery; a handoff older than the horizon asks the rule; a younger one keeps the wall detector's rung, which is fresher than the rule's cache about the account that just ran out, stated in `_recover`.
10. [verification] With a sighting, later transcript activity was ignored and both spaces were read — FIXED in this lane (Codex's finding 3): the latest evidence of life wins, and only the space the sighting names is read; `test_a_kill_inside_this_life_names_the_space_it_took` holds both.
11. [verification] A unit failed on a signal or a core dump was named as the machine taking back memory and resumed as such — FIXED in this lane (Codex's finding 4): `Cause.KILLED`, never resumed by the board; only a line saying oom names a memory kill.
12. [verification] The boot's casualty was decided by proximity alone; the recorded boot id went unused — FIXED in part in this lane (Codex's finding 5): the boot id must match the sighting's, and the words say "the machine's last record before it went down"; the proximity rule stands, because the machine's last record is the truest time anyone has for a power loss after a suspend (#85), and is stated as such in `BOOT_SLACK_SECONDS`.
13. [feature] An ASK row from a previous life or any RULED row decided the disposition, and `asks_owner` matched "your decision" inside a report — FIXED in this lane (Codex's finding 6): the rows of this life, by the audit's time, and a stricter reading of the last words.
14. [feature] The interruption was read before the disposition, so a park on a lane that closed or whose cause stopped qualifying stood forever — FIXED in this lane (Codex's finding 7): disposition first; a park with nothing left to bring back lifts.
15. [feature] A repeat wall's park said "or an account has room sooner" while only the clock lifted it — FIXED in this lane (Codex's finding 8): the words follow what lifts the park; the timestamp-first lift is kept because the gate re-reads the floor and the rule before any launch.
16. [seam] The once-per-cause count was not a durable claim: two readers could each launch a replacement for one interruption — FIXED in this lane (Codex's finding 9): `open_recovery` refuses, in its own transaction, while an attempt for the cause started inside the horizon; `test_one_recovery_is_open_per_card_and_a_closed_one_keeps_its_verdict` holds it.
17. [seam] A new process under the same session id kept the first life's sighting, acts and death — FIXED in this lane (Codex's finding 10): a changed pid or boot starts the life over, and a session seen alive again loses its death row (`forget_death`).
18. [seam] A replacement started fresh (transcript above the resume limit) could never be found on a restart and was reported lost — FIXED in this lane (Codex's finding 11): `_settle_recovery` also finds a live background session in the lane born after the attempt.
19. [seam] Rescope and release were recorded as done before the act — FIXED in this lane (Codex's finding 12): recorded after; a repeat is harmless on both.
20. [seam] `abs()` on the clock dropped an attempt recorded more than an hour ahead — FIXED in this lane (Codex's finding 13): only the horizon's start bounds the window, and an in-flight row recorded after the clock is not lost.
21. [record] "Attempt N in the last hour" counted every attempt ever, and "came back once already" was said of a failed attempt — FIXED in this lane (Codex's finding 14): the count is the window's, and the park says whether the earlier attempt came back.
22. [verification] The resume prompt asserted nothing after the last tool result was on disk — FIXED in this lane (Codex's finding 15): the session is told its last tool call may have run without its result reaching it, and to check what landed.
23. [verification] `-n 400` still capped the journal read under `--since` — FIXED in this lane (Codex's finding 16): the interval is read whole; `-n` only bounds a read with no window.
24. [boundary] `tools/standing_pile.py` opened the store at `db_path()`, whose constructor migrates it — FIXED in this lane (Codex's finding 17): the tool copies the store and its WAL to a temporary directory and opens the copy.
25. [verification] The tool predicted "the board brings it back" without the loop's exclusions — FIXED in this lane (Codex's finding 18): the owner's terminal and the other make are excluded, and the verdict says the next pass decides through the gate.
27. [boundary] `frontend/node_modules` was tracked on the trunk as a link (#99's close, 89ff839) and the main checkout's own became a link to itself, so no type check or dist build ran anywhere from 12:13 — FIXED in this lane as far as the fold needs (the link removed from the tree, the two installs replaced; adjacent, since the close needs a buildable trunk), the class filed as `docs/slice-suggestions/2026-09-09-a-sessions-installed-packages-never-reach-the-shared-work.md`.
26. [record] The first pass claimed a line-for-line match with the Discuss survey — CORRECTED in this record (Codex's finding 19): the reader agrees with the survey on the walls, the three it cannot name, and on leaving #147 and #219 alone; it names the daemon-space kills for #416, #426 and #429 where the survey named nothing or an earlier life, on the evidence that each transcript's last record lies before that kill and the life's window holds it — a claim about the latest life the survey did not make, stated as such in the close-out.

## What was checked

- **The reader on the real machine:** `tools/standing_pile.py hellorevenue` over a private copy of the shared store (never the served one: a worktree's `needle` migrates whatever store it opens, card #51's incident), reading the real registries, journals, transcripts and boot list; twenty lanes, table in the plan's close-out.
- **The three writers of one handoff file**, by reading `~/.local/bin/claude-acct` on this laptop (lines 1358–1420, 1640–1700, 1795–1830 on 2026-09-09): a wall's `why` is the rule's reason, recover's is `connection back after a transient death`, the switch-back's ends `; back on Fable`; the seven files in `handoff/bg/` on this laptop carry all three.
- **The account's limits snapshot**, by reading `~/.cache/omarchy/claude-acct/<slot>.json`: `limits` by label as a share, `resets` by label as ISO, `fetchedAt` as epoch seconds.
- **The boot list and the journal's shape**: `journalctl --list-boots -o json` (five boots on this laptop, boot -1 ended 2026-09-07 16:51Z), `journalctl --user -u <unit> -o short-iso --since` (`2026-09-05T21:32:51+0200 DH systemd[1288]: <unit>: systemd-oomd killed 127 process(es) in this unit.`).
- **The served board after the fold**, on this laptop at 11:26Z: the first passes named twelve deaths on Hello Revenue and parked five lanes it could name on the memory floor (3.3 GB available against the 5 GB floor), each card carrying *Waiting to bring it back after …* once, and left the three it cannot name and the two that are the owner's where they fell; no launch was made, because the floor held nothing back. The table is in the plan's close-out.
- **The suites:** `tests/board`, `tests/infrastructure`, `tests/runtime`, `tests/ratchets` and every file under `tests/api` green in the lane after the second pass — run one file at a time, because the harness killed three whole-suite runs at 2 GB available — but one ratchet, `test_every_title_is_in_the_owners_words`, red on `origin/develop` itself since 66b7fca over a suggestion of 2026-09-08 whose title uses *fold* — outside this change, filed as `docs/slice-suggestions/2026-09-09-a-defect-filed-from-another-project-lands-in-the-owners-words-or-not-at-all.md`. `tsc --noEmit` clean and `vitest run` (68) after `needle types` (`ending.ts` new, `lane.ts` and `slot.ts` regenerated) and after the lane's packages were reinstalled — the trunk carried a `frontend/node_modules` link since #99's close that made both die without a word (disposition 27); the fixture regenerated. `tests/runtime/test_codex.py` failed twice under load with a real `codex exec` (the reviewer) alive on this machine, because `runtime.codex.processes` reads the real `/proc`; it passed alone and after, and the floor's blindness to real processes is outside this change.

## What the build learned the plan got wrong

- The plan's item 1 discriminator — a row that had a pid and now has none — cannot serve the standing pile, which predates any sighting: a row with no sighting older than the verify window is named from what the machine holds, and only a row younger than it is a birth. Stated in the item's stance.
- The plan said a wall park lifts *sooner when the rule finds headroom on another slot*; on the real pile a wall handoff from four days back names a rung that is spent today, so a resume after a death asks the rule now and passes its answer to the move, and the handoff's own rung is used only for the live hop it was written for.

## Not done, stated

- The crash between a launch and its record is not thrown inside `claude --bg` on the floor; the row it leaves behind is what the test builds and reads.
