# Review — a decision parked on you leaves your column when the record already answers it

**Plan:** docs/plans/done/2026-09-07-a-decision-parked-on-you-leaves-your-column-when-the-record-already-answers-it.md
**Reviewer:** a Claude session of the lane's own make (Opus, the `hrclaude` slot, session b174f727 resumed from f1d4a6a3, a reading session whose home is the main checkout and not a card's lane — chosen so the call moved no finished card, the defect #123's lane filed on 2026-09-12 — and a thread apart from the challenger's, so the reader had no share of the plan's challenge), Needle call 112, reading the lane's worktree cold on this (rented) machine, asked through `/home/dennis/.cache/needle/notes/from-needle-82-review.md`; its answer is beside the note (`from-f1d4a6a3-re-82-review.md`) and quoted in the dispositions. Before build, the same-make challenge the board's team line asked for was call 110 (session 5a8331ec resumed from f53f2ba3): ten material corrections, landed as the plan's rulings 4–12 before the first line of code.
**Diff range:** 8a4897f..98e3c01 (the revision frozen while read: the feature commit with the plan's stances). Repairs landed after it in the fix commit named under Verification; the implementing lane verified them, and the reader is not represented as having read them.
**Findings:** 7 — five repaired (three demonstrated, one record-class, one bounded fate), one ruled on and repaired in part (a WATCH cannot carry a question; `now` stays, recorded as ruling 13), one measured and left (the histories' cost).
**Verification:** after the repairs: `tests/board/test_parked.py` (16 passed, with new cases for findings 3 and 4), `tests/api/test_parked_cards.py` (2 passed, the walk now refusing `waiting` on #149's ASK and keeping #139's WATCH untouched on the owner's park), `tests/board/test_dial.py`, `tests/board/test_assemble.py`, `tests/board/test_brief.py`, `tests/board/test_evidence.py` (all passed in one run); `tests/api/test_dial.py`, `tests/api/test_title.py`, `tests/api/test_the_triage_seat.py`, `tests/api/test_defects_column.py` — see the evidence section for the counts; `npx tsc --noEmit` clean and `npx vitest run` 83 passed; the types and the frontend fixture regenerated and current; ruff clean on every changed file but the pre-existing long line in `api/board_cli.py`'s usage head. The full suite at 98e3c01, one process: 4 failed, all four finding 1 (three in `tests/api/test_dial.py`, one in `tests/api/test_title.py` expecting argparse's refusal where the door now refuses), everything else passed; the four are repaired and rerun.
**Completion:** Complete: every verified finding inside the change is repaired and its repair holds in a test; finding 4's recommendation was ruled on (ruling 13) and the half that is a demonstrated silent exit is refused at the door; finding 6 was measured on a copy of the served store and left, with the number. What remains unverified: the served page in a browser after the restart, and the first real run's refusals (item 3's done-means says the record reads them by hand) — the run starts on the first beat after the fold behind every unread defect and title, so at the close there are none yet to read; the close's WATCH row names the audit that reads them.

## Dispositions

1. [verification] `tests/api/test_dial.py:865, :1130, :1304` and `tests/api/test_title.py:296` — the suite was red at 98e3c01: three tests counted launches or the dial's running number and expected nothing but their own act, and the beat now opens a reading on each of the fixture's four parked cards once the defects and titles are read; the fourth expected argparse to refuse a mark's result without `--title` (SystemExit), and the verb now takes `--title` optionally so a parked card's reading can land without one, the door refusing a mark's reading without it instead. Demonstrated by the reader (`uv run pytest -q tests/api/test_dial.py`: 3 failed in the lane, 16 passed on the trunk) and by this lane's full-suite run. Violated CLAUDE.md's "nothing lands red". — FIXED in the fix commit: the three counts read `acts(machine_floor)` (every launch but a reading, the helper card #100 introduced for exactly this) and `running == triaging`; the title test expects the door's refusal (`main(...) == 1`, "A title verdict is") rather than argparse's. The four pass; the parked readings are what the tests now expect, not what they skip.
2. [feature] `api/doors.py:1296–1298` against `:1310` — `waiting` on a card the owner parked himself replaced the card's WATCH row before `owner_parked` was consulted, though ruling 7 promised the door "writes the result and its history line and moves nothing"; the close's promise, or one he wrote, would be gone from the rows of a card he chose to hold. Demonstrated by reading. — FIXED in the fix commit: `_decide` reads the placement first and writes the WATCH only where the card moves on it; on his park the result lands with the signal in its words and the card's own WATCH stays. `tests/api/test_parked_cards.py` lands a result on the owner's re-park of #139 and asserts no WATCH row on the card.
3. [feature] `board/assemble.py:711–718` with `board/parked.py:296–308` — after a refused `stale` whose commitment was settled since without an answer (a RULED written, a signal read), `parked_doubt` returned None and the face fell to "…You parked it yourself, so it stays until you move it" on a card the machine parked: false, and nothing re-reads the card. Demonstrated by reading. — FIXED in the fix commit: the words come from one function, `board/parked.py::parked_words`, which takes whether the owner parked the card (from the placement `summarize` already holds) and says, on a settled refusal, that the board refused to close it over what was then unaccounted for, that this is settled since, and that the card waits for his move; "you parked it yourself" only when he did. `tests/board/test_parked.py::test_the_face_never_says_you_parked_it_on_a_card_the_machine_parked` holds every branch.
4. [feature] `api/doors.py:1275` and `board/brief.py`'s COMMITMENT_RULE — the reader's hypothesis, proved on the fixture: a commitment left the column through `now` (#139, an unanswered ASK, to Planned) and through `waiting` (#149, an unanswered ASK, to Executed), while item 3's sentence promises every commitment accounted for or transferred to a WATCH that still watches it — and a WATCH cannot carry a question in his words or a RULING with no RULED; a `waiting` on such a card is a silent exit (the signal's delivery closes the card with the question unanswered). Recommended: refuse `now` and `waiting` on those two kinds, or record why the reader's judgment is the defence. — RULED and FIXED in part, in the fix commit: `Commitment` (domain) says whether a WATCH can carry it; `waiting_refused` refuses while an ASK/Q with no answer or a RULING with no RULED stands, naming it, and the brief's `waiting` line says so; `now` is not refused — a `now` claims the record already answers the decision the question asks, names the source, and sends the card back into execution where he ranks it, and the audit reads that claim against the source, the dangerous exit staying `stale` (ruling 1). Recorded as ruling 13 in the plan. `tests/api/test_parked_cards.py` shows #149's `waiting` refused and `his` landing; `tests/board/test_parked.py` holds the transferable/held split. The `now` re-test not reading commitments stands with the ruling: what it re-tests is the source the reading relied on.
5. [record] `api/dial.py:717` (`_follow_triages`) — the note on a reading that died counted every reading on the card's whole life while the cap (`_readings_that_died(since=parked_at)`) counts one park, so a second park's first death could say "4 readings have died on it; the board stops opening them" and the beat open another. Demonstrated by reading. — FIXED in the fix commit: the note counts from the park when the card is in Decision moment, the same way the beat does.
6. [seam] `infrastructure/live.py::_histories_for_parked` — one history query per parked card and per card a reading moved, on every board read; Hello Revenue's 38 parked cards → ~39 queries per read against #123's bar (a click answered within a second). Hypothesis; the reader did not measure. — NO CHANGE, measured: on the copy of the served store (Hello Revenue, 38 parked cards), the 38 histories take 12.0 ms (11.5 ms best of five) beside 51.6 ms for the cards and placements the same read already loads (`/home/dennis/.claude-accounts/hrclaude/jobs/cb2a1f18/tmp/measure.py`); a fifth of the read's existing cost, under 50 ms a second at four reads a second. The two-query shape (the latest answer and the newest ASK/Q writing per card) is the fallback if the served board ever reads slow, and is noted in the code's docstring.
7. [feature] `api/dial.py:1272` (`_moved_on`) — the fate of an older parked reading was computed from the newest `RECORD_ANSWERED` move at or after its time, so a card read and moved twice showed the later reading's move on the earlier decision's line; `--returned --count` unaffected. Hypothesis, low weight. — FIXED in the fix commit: the move is bounded by the next parked reading on the card.

Verified by the reader on the way, no finding: one reading per park across every card shape it could construct (owner-parked, import-born, an answer older than the reading, a refused `stale`), none re-opening per beat; the per-result re-test doubting a moved source, an unreadable or different WATCH, a returned commitment, and holding otherwise; a refused `stale` leaving the record open and the beat ending it once the turn is done, the process stopped on the next beat; migration 0024's batch add with a server default and `migrate()` to head on open; `board/parked.py` importing only `board.*` and `domain.*` with no cycle; routing never meeting a parked row; the typed edges (`ground`, `routing: Routing | null`, `returned`, `decision`) with `tsc` clean; the brief fixture, the four refusals at the verb, the `his` Answer door and his answer re-reading the card, the WATCH-replaced history line, `needle decisions --returned --count`.

## Verification evidence

- Pre-build challenge: Needle call 110 (session 5a8331ec resumed from f53f2ba3, opus on hrclaude), note `/home/dennis/.cache/needle/notes/from-needle-82-challenge.md`, answer `from-f53f2ba3-re-82-challenge.md`: fifteen entries — ten corrections, one deviation to record, one clarification, three holds. Every correction is a ruling in the plan (rulings 4–12), landed before the first line of code; the `**Challenged:**` line on the plan's head counts them.
- Independent reading: Needle call 112 (session b174f727 resumed from f1d4a6a3) at 98e3c01. The reader read the plan whole, `board/parked.py` whole, the diffs of every changed backend module, the migration, the OpenCard section and the two new test files; ran the change's own suites, `tests/ratchets`, `tests/api/test_dial.py` in the lane and on the trunk, and `tsc`. No `needle` verb was run from the worktree and nothing was edited.
- Repairs: see the fix commit's hash in the plan's Status line and `git log` on the trunk; on it: `uv run pytest -q tests/board/test_parked.py tests/board/test_dial.py tests/board/test_assemble.py tests/board/test_brief.py tests/board/test_evidence.py` all passed; `tests/api/test_parked_cards.py` 2 passed; `tests/api/test_dial.py tests/api/test_title.py tests/api/test_the_triage_seat.py tests/api/test_defects_column.py` — RERUN PENDING; ruff clean on every changed file but `api/board_cli.py:12` (pre-existing).
- Frontend: `cd frontend && npx tsc --noEmit` clean; `npx vitest run` 83 passed; `uv run needle types` and `uv run python tools/board_fixture.py` current.
- Migration on real data: 0024 rehearsed by this lane with `/home/dennis/.claude-accounts/hrclaude/jobs/cb2a1f18/tmp/rehearse.py`, `upgrade.py` and `downgrade.py` on a copy of this (rented) machine's `~/.local/share/needle/needle.db` (0023; 555 cards, 5165 audit rows, 0 readings): the upgrade added `triages.ground` with the default `mark` and changed no row (555 cards, 5165 audit rows after); the downgrade dropped the column and put the version at 0023; the upgrade again landed 0024. Read from the copy through the lane's code: 39 cards parked on the owner across the boards (Hello Revenue 38 — 11 by the owner, 12 by the machine with evidence named, 10 by the 0.1 import, 5 by a session or the machine with none; hr3 1; Needle, the machine's board and dennishettema 0); three parked dates (#147, #180, #410) verified against each card's history to the second. Neither shared store was touched by a lane verb: every lane verb ran with `NEEDLE_DB`/`NEEDLE_DATA_DIR` on the copy, and board verbs through `uv --project /home/dennis/Work/needle`.
- Finding 6's measure: `measure.py` on the same copy — cards and placements 51.6 ms, 38 histories 12.0 ms (best of five 11.5 ms), latest readings 1.7 ms.
- Full suite at 98e3c01, one process (`uv run pytest -q -p no:cacheprovider`, about 65 minutes): 4 failed, all finding 1, everything else passed.
- Not run: the served page in a browser after the restart; the first real run's refusals — none exist at the close, since the run starts behind every unread defect and title; the close's WATCH row names the audit that reads what left the column.
