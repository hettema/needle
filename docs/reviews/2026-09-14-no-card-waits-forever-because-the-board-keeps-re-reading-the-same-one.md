# Review — no card waits forever because the board keeps re-reading the same one (#138)

**Plan:** docs/plans/done/2026-09-13-no-card-waits-forever-because-the-board-keeps-re-reading-the-same-one.md
**Reviewer:** session 6e915b42, a cold Claude session of this lane's own make with no share of its context, called through `needle call 00d3b67c` (call 134) on the note at `/home/dennis/.claude-accounts/armana/jobs/918c6e7f/tmp/review-note.md`; its answer is at `/home/dennis/.claude-accounts/armana/jobs/918c6e7f/tmp/from-00d3b67c-re-review-note.md`. It read the whole diff, ran `tests/board/test_dial.py` (20 passed) and two probe tests of its own on the floor that demonstrate findings 1 and 2 (`/home/dennis/.claude-accounts/armana/jobs/00d3b67c/tmp/test_probe_138.py`). A separate same-make challenge round before the build (call 133, session 893e3e9c) made three material corrections to the plan, recorded on the plan's head.
**Diff range:** cf32333..2d31d23 reviewed (that tree rides the trunk as c31b991 after a rebase over 3fa8ba2, a docs-only commit); repaired in 61d23f5 (3f6cb81 after the rebase), final revision named under Verification.
**Findings:** 5 — 2 demonstrated defects, 1 demonstrated limit of the plan's measure, 2 unverified hypotheses. All inside the change; none filed outside it; none rejected. Plus one pre-existing red test on the trunk met while verifying, recorded under Verification and already filed.
**Verification:** each repair has a test that fails without it (the reviewer's two probes, inverted, are now tests in `tests/api/test_the_triage_seat.py`); the whole suite ran one module per process on 2d31d23 (105 of 106 green; the one red went green alone) and the modules the repairs touch ran again on the final revision. Evidence below.
**Completion:** every verified finding is repaired inside the change with its own test; the two hypotheses are addressed where the repair was one line and recorded where it was not. The plan's three items hold, item by item, with `**Met:**` lines on the plan naming what shows each; item 3's number and first-day watch are written after the fold, at the close.

## Dispositions

1. [feature] `board/assemble.py` (`state_of`'s `stopped` branch and `summarize`), `api/dial.py::waiting` — a card whose third reading on a text *settled* it (two died, the third landed `his`; or `now` on a `now` document; or a title placed on the third reading) read as broken, "the board read this 3 times on this text and nothing settled it", on its face, on the head and in `needle fixes`: `readings_spent` counted the readings and `stopped_words` spoke from the count alone. Violated: item 2's "the words never claim needs triage for a card the board has given up on" and the acceptance "a held card tells him what it waits for" — here broken painted over yours. Demonstrated by the reviewer's probe. FIXED in 61d23f5: `ReadingsSpent` gains `wanted` — whether the card's own reader would open on this text today by its own rule (`seat_opens` for a defect, `wants_title_reading` for a title, `wants_parked_reading` for a parked card, and no hands on the card) — computed in `Live.readings_spent` beside the count, and `stopped_words` speaks only where the fuse is what holds the card; the seat still opens nothing on a spent text either way. Verified by `tests/api/test_the_triage_seat.py::test_a_third_reading_that_settles_the_card_is_settled_not_stopped` (the probe inverted) and `tests/board/test_dial.py::test_the_words_say_how_many_and_what_starts_the_readings_again`.

2. [feature] `infrastructure/live.py::readings_spent` (the parked branch) against `api/doors.py::_decide` — a parked reading writes its TRIAGED row (and a `waiting`'s WATCH) on the card before it ends its session, so the record the seat counted by after a landing was not the record the reading was bound to at its open: for parked cards the fuse counted only readings that died, the very cap the plan replaced. Violated: item 2, "no card is read more than three times on the same text, whatever the readings landed … for a defect, a title and a parked card alike". Demonstrated by the reviewer's probe (bound at open `5067e394…`, counted by `270a052f…`, opened 0 after one landed reading). FIXED in 61d23f5: `board/parked.py::reading_text` is the text a parked reading is counted by — the record less the rows a landing writes (`LANDING_WRITES`) — used by `Live.readings_spent`, which is what `_triage` binds with; `record_fingerprint` stays what the audit reads. The count's window is also now `max(park, the owner's answer)`, so an answer resets the count exactly where `wants_parked_reading` re-opens the reading, and the fuse's parked words ("your answer on it … starts them again") are true. Verified by `tests/api/test_the_triage_seat.py::test_a_parked_readings_landing_does_not_move_the_text_it_was_bound_to` (the probe inverted).

3. [verification] the plan's Loop line and `api/board_cli.py::decisions` — the awk reads the printed line as intended (the reviewer showed it on a synthetic listing), with two limits of the measure: `Decision.text` was the document alone where the fuse keys a mark's reading on document and source, so a source that legitimately moved twice under one document text would print as a false "read more than twice"; and `needle decisions` lists mark and parked readings, never a title's, so a title loop would not show in the Loop's number. FIXED in 61d23f5 for the first: `Decision.text` is `board/dial.py::text_of_mark(document, source)` for a mark's reading, the one composition the seat counts by (`mark_text` calls it), so the Loop and the fuse name the same text; verified by `tests/board/test_dial.py::test_a_marks_text_is_the_document_and_its_source_together`. ACCEPTED for the second and for a parked reading's record: written into the plan under item 3 as the measure's named limits — the fuse bounds both loops regardless, and a nonzero count is a prompt to read the lines it names.

4. [seam] `infrastructure/live.py::readings_spent` from `board()` and `detail()` — *an unverified hypothesis (efficiency), not a demonstrated defect.* The count ran three store queries plus one file read per parked card on every board render and every card detail, and the detail computed every card's count to use one. FIXED in 61d23f5 where the repair was small: `detail()` asks for one card (`number=`), and a parked card's text is read from its file once per fingerprint (`Live._text_of`, bounded by `TEXTS_KEPT`). The queries per board read stay — a handful, never one per card, as `_histories_for_parked` already spends — and are not measured here.

5. [seam] `board/assemble.py::claims_of` — *an unverified remark.* A card with a lane on it whose text was spent would still count under "cards the board stopped reading" on the head while its face said working. NO CHANGE NEEDED after finding 1's repair: `wanted` is false for a card with hands on it (`hands_off`), so the claim is not made; the same `hands_off` is the seat's own rule in `_wants_a_reading` and `_wants_a_title_reading`, so the head and the seat cannot disagree.

## Verification evidence

**On the reviewed revision 2d31d23**, before the repairs: the whole suite, one module per process, three at a time (`/home/dennis/.claude-accounts/armana/jobs/918c6e7f/tmp/suite_parallel.sh`, summary in `…/tmp/suite/summary.txt`): 106 modules, 105 green; `tests/api/test_a_card_that_finishes_or_needs_you_rings.py` red under load (a ring not played) and green alone. `tests/api/test_defects_column.py::test_a_defects_reading_without_a_grade_is_refused_and_a_graded_one_shows_its_word` failed twice in this lane at its line 182 ("mark being read" where "no plan yet" was expected) and passed in the sweep; run again in this lane with timing, the face read "no plan yet" and no reading opened — the dial's sixty-second timer landing between the edit and the face read. On the base revision cf32333 (extracted with `git archive`) the same test fails earlier, at its line 187 (`'your ruling' == 'loses'`), which is the trunk's own red already filed as `docs/slice-suggestions/2026-09-14-the-face-of-a-graded-defect-says-what-the-reading-graded-it.md`. Not this lane's. Frontend: `npx tsc --noEmit` silent, `npx vitest run` 85 passed on the regenerated types and fixture.

**On the final revision 61d23f5 (3f6cb81 after the rebase; the docs commit above it changes no code)**: `tests/board/test_dial.py` (25 tests, the stance table, the count, the words, the mark's text and `hands_off`), `tests/board/test_parked.py`, `tests/board/test_assemble.py` — green; `tests/api/test_the_triage_seat.py` (18 tests, the four fuse and review tests among them) — green; `tests/api/test_parked_cards.py`, `tests/api/test_title.py`, `tests/api/test_dial.py`, `tests/api/test_doors.py`, `tests/api/test_reading.py` and every ratchet under `tests/ratchets/` — green, one module per process; `tests/api/test_defects_column.py` red at the trunk's own assertion (`'your ruling' == 'lies'`, the face of a graded `his` reading with the Answer door open), which is the filed defect named above and fails on cf32333 without this lane. Frontend on the final types: `npx tsc --noEmit` silent, `npx vitest run` 85 passed; the fixture tool reports the fixture current. `ruff check` clean on every file this lane touched (one pre-existing long line in `api/board_cli.py`'s docstring is not this lane's).

**The migration**: `tests/ratchets/test_the_store_holds_its_shape.py` green (the models and migration 0027 agree); the fixture tool and the types regenerate cleanly.
