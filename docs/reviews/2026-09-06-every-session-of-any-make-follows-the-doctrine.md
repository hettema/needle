# Review — every session, of any make, follows the doctrine at least as well as Hello Revenue's did

**Plan:** docs/plans/2026-09-05-every-session-of-any-make-follows-the-doctrine-at-least-as-well-as-hello-revenues-did.md
**Reviewer:** the build session (Claude Fable 5.1), reading its own work through the three lenses in turn; the failure histories by the search role, the payload capture by the execution role, each result read at the file before it was used
**Diff range:** d9a8eca..HEAD
**Findings:** 6 so far — 5 fixed in this lane, 1 fixed at the close; passes 4 and 5 follow the owner's reading

## The passes

The review runs as a loop (`CLAUDE.md`): each pass one lens, the fixes landed, the next pass re-reads the fixed work. Every finding below opens with its class, as `docs/reviews/README.md` asks from this date — this record is the first the new ratchet reads.

1. **The feature against the plan's "done means".** Item 1: 167 rows over the 152 paragraphs of Hello Revenue's file and 7 for the carriers, checked by `tools/doctrine_table.py --stances project-file` (every paragraph named; the count printed is the table's count); the six distinctions the suggestion names are rows 7b, 8, 9a, 10b, A and E, each marked missing portable doctrine with a failure cited by path and line that this lane read; every global-wins row quotes both sentences — except row D, which quoted only Hello Revenue's (finding 1), and row 5, a heading with nothing to quote on either side (finding 2). The three contested rows carry both readings and wait for the owner's batch. Item 2: 3,509 words against 3,523; the four rules are in §8 with their discriminators; §7 stands whole; every paragraph of the old text has a row (`--stances departed`, 71 of 71); the register ratchet is green in the same commit (129b72c); the commit names #60 and cites the table; the owner's reading is not yet on the card. Item 3: the register's §8 line names the classed findings as its trace and their reader, and the class ratchet as the one held clause; §13 names two held clauses; no line stretches a mechanism over a section. Item 4: the captured payloads (below) show a Claude session and a Codex session on a scratch project receiving the two sections on the word and answering the nonce; the output-style decision and its alternative are row F; the Omarchy card is `omarchy-machine/docs/plans/2026-09-05-26-a-backbrief-on-this-laptop-re-reads-the-doctrine-to-codex-too.md`, commit cde5b6f. Item 5: the baseline is in the plan's Loop, dated, with the records it was read from; the trace is `tests/ratchets/test_every_finding_says_its_class.py` and the two READMEs. Findings 1 and 2 came from this pass.
2. **The seams.** Concurrency: the anchor reads the doctrine file at fire time with no lock; a read that lands mid-write finds fewer than two sections and prints nothing, and the next prompt reads the whole file — silent for one turn, never wrong. Failure and restart: a missing file, a renamed section, a subagent's prompt, a prompt without the word each print nothing with exit 0 (`tests/api/test_hook_script.py`); the board's word path is untouched, and `answer()` is still the one stdout writer. The truth of what the board shows: the class ratchet reads records by filename date, so this record is the first it holds; the register's §8 line cites the plan at its live path, which the archive moves (finding 6). The installer: with the event added, the existing test's fixture — a project with its own hook on UserPromptSubmit — broke, and the seam it protects turned out to be real: Hello Revenue runs `backbrief.sh` on the same event, so `needle hook install` there would fire two anchors on one word (finding 4). Findings 4 and 6 came from this pass.
3. **The boundaries.** Layers: the hook is standard library only and `answer()` catches all (ratchet green); `tools/` is outside the never-runs scope and `subprocess` there reads git only. The board never runs: nothing new spawns under `board/`. Typed edges: no domain type changed. One way: one hook, one stdout writer, one table reader for three tables, one class vocabulary in both repositories. Nothing half done: no deferral marker. A doctrine edit names its card: every commit names #60. The public tree carries no real card title: the ratchet found one — Hello Revenue's research note cited by its path in row 43b, whose filename is a card title (finding 5). The table reader's bare run read `~/.claude/CLAUDE.md`, which since card 23 is the one text, so card #54's table faulted on its own check (finding 3). Findings 3 and 5 came from this pass.

## Dispositions

1. [record] Row D of the Hello Revenue table quoted only Hello Revenue's sentence where the done-means asks for both — FIXED: §3's sentence quoted beside it.
2. [record] Row 5 is a heading and quoted nothing — NO CHANGE: a heading has no sentence on either side; the row now says so rather than leaving the reader to wonder.
3. [boundary] `tools/doctrine_table.py`'s bare run read the live global path, which now resolves to the one text, so card #54's table faulted on rows 57–60 — FIXED: the default reads the machine repository's commit 7894dfc (287 lines, 60 paragraphs, the last before the file became a link), through a `--repo` the reader now takes; the plan's terrain named 1cabeb0, which has 56.
4. [seam] `api/board_cli.py::hook_install` gained UserPromptSubmit and the test that seeds a project's own hook on that event broke; the seam — a project's own hook stays beside ours, first — was unnamed, and Hello Revenue is exactly such a project — FIXED: the test holds the order and the count; the Hello Revenue suggestion filed on this card says its card retires the local hook before the installer runs there (commit 979d6bc16 in that repository).
5. [boundary] Row 43b cited a Hello Revenue research note by a path whose filename is a real card title, into this public repository — FIXED: cited by folder, date and line, with the reason.
6. [record] `docs/HOW-WE-HOLD-IT.md` §8 cites the plan at its live path — FIXED AT THE CLOSE: the archive moves the plan and the citation follows it, in the same commit.

## What was checked

- **The two tables, mechanically.** Every paragraph of Hello Revenue's file (152) and of the old one text (71) is named by a row with a legal stance; the reader refuses otherwise, and the card #54 table still passes on its own revision.
- **Every cited failure, at the line.** The search role returned each incident as path and line in Hello Revenue's repository; this lane read each line (the 2026-04-20 plan at 57 and 121, the 2026-07-24 plan at 82, the resonance doc at 167–168 and 139, the 2026-05-02 plan at 40 and 46, the 2026-08-31 plan at 12, the research note at 54, the prompt pattern at 527, the brand-reading ratchet at 14–16, the migration docstring at 1–4, the 2026-08-07 plan at 138) and the commits `5d827ac19`, `cac24c90a`, `17260ba48`, `893e4d69c`, `77695c5fb`, `36d4463ee`, `74f22c66d` before a row cited them.
- **The hook, by hand and by test.** A backbrief prompt through the script prints the two sections (4,469 characters, under Claude Code's 10,000 cap, held by a test); a plain prompt prints nothing. `tests/api/test_hook_script.py` covers the word's four spellings, three near-misses, a subagent, a missing file and a renamed section; `tests/ratchets/test_one_rule_one_hook.py` holds that every section the anchor names is a heading the text has, that no string in the script is a sentence of the text, that each read event names a ceiling, and that no event runs the hook twice.
- **Delivery on both makes, captured.** The execution role ran `tmp/capture/run.sh` once: a scratch project, a copy of the text with the nonce `QUOKKA-4419` inside §3, the hook registered on UserPromptSubmit through a wrapper that recorded its stdin and stdout. Read by this lane before the item claimed anything:

  Claude, `claude -p`, session `dfd569d1-e0de-4cf3-bceb-bde8d14f25d3` — the hook's stdin:
  ```json
  {"session_id":"dfd569d1-e0de-4cf3-bceb-bde8d14f25d3","transcript_path":"…/dfd569d1-e0de-4cf3-bceb-bde8d14f25d3.jsonl","cwd":"…/tmp/capture/project","prompt_id":"e0d37c85-757a-4e19-91d3-dc30f3b0864a","permission_mode":"default","hook_event_name":"UserPromptSubmit","prompt":"backbrief: the context you were given for this turn contains a nonce of the form WORD-DIGITS. Reply with that nonce only, nothing else. If no nonce was given to you, reply NONE."}
  ```
  the hook's stdout (3,887 characters; the head): `{"hookSpecificOutput": {"hookEventName": "UserPromptSubmit", "additionalContext": "BACKBRIEF — two sections of the one text, read back verbatim from …/tmp/capture/doctrine.md:\n\n## 3. A session's economics are inverted\nThe nonce for this capture is QUOKKA-4419.\n\nA session does the whole thing right, …`; the session's result: `QUOKKA-4419`, one turn.

  Codex 0.152.1, `codex exec -s read-only --dangerously-bypass-hook-trust`, session `01a07394-6c24-7a02-91c6-0cb4e1e5e73b`, model `gpt-5.6-sol` — the hook's stdin:
  ```json
  {"session_id":"01a07394-6c24-7a02-91c6-0cb4e1e5e73b","turn_id":"01a07394-6c70-70e1-8c97-e8a28e599251","transcript_path":"…/codex-home/sessions/2026/09/05/rollout-2026-09-05T23-58-21-01a07394-6c24-7a02-91c6-0cb4e1e5e73b.jsonl","cwd":"…/tmp/capture/project","hook_event_name":"UserPromptSubmit","model":"gpt-5.6-sol","permission_mode":"bypassPermissions","prompt":"backbrief: …"}
  ```
  the hook's stdout byte-identical to Claude's (`cmp`); Codex's own stderr: `hook: UserPromptSubmit` / `hook: UserPromptSubmit Completed`; the answer written to `-o`: `QUOKKA-4419`. The nonce appears nowhere in the real text (`grep -c` is 0), so the only way to it was the hook. Codex's trusted path — the hook without the bypass flag — is the Omarchy card's done-means.
- **The register.** `tests/ratchets/test_every_rule_says_what_holds_it.py` green on the rewrite: 15 sections, one line each, the §7 debt still dated.
- **The Hello Revenue suggestion's mark.** `needle kinds hellorevenue` reads it as `defect`, `Fix: when … file … by 2026-09-12`.
- **The suites.** Backend and ratchets green after the fixes above (the run is quoted at the close); no frontend file changed.

## What the build learned the plan got wrong

- **"Shorter than today's by word count" and "on Hello Revenue's shape" pull against each other.** The shape costs about forty words a section and the four rules cost about three hundred and fifty; the first honest draft was 4,411 words and reached 3,509 only by cutting argument the old text carried — every cut is a row in the departed table, and the owner will see where the line was drawn.
- **The plan's terrain named the wrong revision of the former global file** (1cabeb0, 56 paragraphs) for a purpose the lane did not need it for; the table reader needed 7894dfc.
- **The count the plan asked for does not exist in the records.** Item 5 anticipated it ("if the records do not carry the class in a countable form"), and that is the branch taken: the sieved lower bound points the opposite way from the thesis, is read as who looked rather than who slipped, and the trace is what the loop will actually read.

## Not done, stated

- **The owner's reading and the three rulings.** The rewrite is a lane commit until he has read the full text and ruled on rows 43a, 43c and 152; passes 4 and 5 re-read the fixed work after his answer, and the record's tally is final then.
- **Codex's trusted registration** is the machine's (Omarchy card 26), not this lane's; the capture used the bypass flag, as the plan's done-means allows and the Omarchy card's does not.
- **Hello Revenue's own hook and style** stay as they are until that project's card retires them; until then `needle hook install` is not run there.
