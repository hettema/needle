# Review — the map (slice 01)

**Plan:** docs/plans/done/2026-09-03-01-the-map.md
**Reviewer:** the build session (Claude Fable 5.1 at xhigh), reviewing its own diff before the fold. No second session was in the loop; the owner's read of the board is the sign-off this record cannot replace.
**Diff range:** 0dedd6e..7362fc2
**Findings:** 6

## What was checked

- **The parser, against the whole Hello Revenue corpus** (690 documents: 18 live plans, 490 archived, 150 live suggestions, 32 archived): every one parses; 683 yield an essence, 134 a gate, 442 a status line, 689 a date; the seven without an essence open with a table or a code list and are still cards with a title. The corpus's head variants (`## Intent — what this achieves and why`, `## 1. Intent`, `**Found by:**` spanning two lines, a gate in either case) are each a test in `tests/board/test_parse.py`.
- **The 0.1 import, against the real card file** (`docs/board/needle-board.json`, read only): 135 cards → 132 imported in 19 groups, 97 linked to a document by citation, 26 retired numbers recorded, numbering continues at 262; the three machine-born asks (#204, #205, #206) skipped and named on the command line. The registration sweep then birthed 121 cards (119 suggestions into Backlog's unnamed tail, 2 plans into Planned's) and relinked one (the deploy plan names `**Card:** #241`). One card cites a document that is nowhere: #120, whose 0.1 citation holds two file names joined with " · ". The board's attention line says so, in red, as the comp promised.
- **The store**: a move survives a reopen of the file; a refused move changes nothing and says why in one sentence; a no-op move writes no audit row; every write carries its audit row in the same transaction; the migration and the models agree (Alembic's own comparison, as a ratchet). `tests/infrastructure/test_store.py`.
- **The corpus watcher**: a plan and a suggestion written into a temporary corpus are cards within two seconds, marked NEW, counted as arrived, with the version bumped for the stream; an archive and a rename keep the card's number. `tests/infrastructure/test_corpus.py`. Against the real corpus, the deploy plan was archived by another session while this one built, and the smoke store recorded it: "Its document was archived to docs/plans/done/…" on #241.
- **The API doors**: board, detail, move (200 with the new truth; 409 with the store's sentence; 500 with the database's own words, verified by breaking the store), files (traversal and non-docs paths refused), the stream (version events, a move bumps it). `tests/api/test_api.py`.
- **The page**: ten vitest scenarios — the board at rest with rails furled, a rail unfurled and furled, a keyboard lift that shows "#241 lands here — rank 1" and drops there, a walk into a named group of the next column, a cancel that writes nothing, a failed write that leaves the card in place with the reason and a working Retry, the open card with its five sections in order and an empty record that says so, Move to, the lens re-sorting without a write, and a board that cannot be read saying so. `frontend/tests/board.test.tsx`.
- **The page, live**: three screenshots of the built page against the real store at 1440×900, 2560×1100 and with #253 open, compared by eye with the two signed comps — column widths, the rails, the amber decision column, the chips, the five states, the open card at 560px with head fields and the intent rendered. And a real pointer drag driven over Chromium's DevTools protocol (`tools/drag_check.py`): gap "#253 lands here — rank 1", order after release `[253, 241, 174]`, audit row "Ranked 1 in Up next — was 2" by the owner.
- **The ratchets** (15): layers import downward; `board/` and `domain/` spawn nothing; no deferral marker in code; one design system with no class name, style, colour or form element outside it; no `any`, no `Record<string, unknown>`, generated types match the domain, the compiler is strict; the migration matches the models; the store's default path is outside the repository; board state on the page is set only from the store's answers. Two of them caught real defects during the build (findings 2 and 3), which is the evidence they hold something.

## Dispositions

1. dnd-kit's `onDragOver` fires only when the droppable under the pointer changes, so within one group a pointer drag never retargeted and the gap never appeared; found by the driven drag, invisible to the keyboard tests — FIXED in fd7d635 (the target is recomputed on `onDragMove`).
2. The card shell typed its drag props as `Record<string, unknown>` — caught by `tests/ratchets/test_typed_edges.py` — FIXED in fd7d635 (dnd-kit's own attribute type, minus `role`).
3. Two `# type: ignore` in the backend: a `JSONResponse` returned past a `response_model`, and the SQLite pragma hook typed as `object` — FIXED in 7362fc2 (a `StoreFailure` exception with its handler; `sqlite3.Connection`).
4. The audit row for a card born at server start read "while the board was watching" when the board had been down — FIXED in 7362fc2 ("after registration").
5. Comp 1 said the one broken citation on the board was #241 → `docs/board/MAIN-HOLD.md`. The build's read of the file: that path appears in #241's REC row and #244's WATCH row as text, and is not a citation; the one broken citation is #120's. — NO CHANGE: the comp misread a row for a citation; the count it promised (one) is what the board shows, on the right card.
6. The definitions of Executed, Done and Not now were not in the comp (it showed them furled) and were written by the build session from 0.1's README grammar — NO CHANGE: they are data in `domain/column.py`, one hover away on the page, and comp 1's call 2 asked the owner to correct any definition that is not his ruling; these three are the ones to read first.

## What the build learned the comp got wrong

- With the signed widths (288px columns, 14px gaps) the five working columns are 1,536px wide; on a 1,440px laptop Decision moment is half off-screen and the board scrolls sideways. The comp's "the five working columns fit together" holds from 1,536px. Not changed — the widths are signed; the owner may want 272px columns, which is one token.
- The comp's Up next carried five cards from a later read of the board; the file holds two. The Backlog the comp showed as 29 is 148 under the corpus ruling: the owner's 29 triaged cards in their groups, then 119 suggestions nobody carded, below the line, folded after eight behind "+ 140 more".

## Not done, stated

- No automated pointer-drag test: jsdom cannot lay out a page. The check is `tools/drag_check.py` against a running server, run by hand before this record; the next review runs it again.
- The page follows the system theme; there is no in-page Dark toggle (the comp's was comp chrome, not product).
- One project on the page: the project pill is static. The API already lists projects and the page takes `/p/<slug>`; a switcher is a line of UI the day a second project is registered.
- The stream reconnects on its own (EventSource), and while it is down the header says "page not connected" and dates the board it shows; nothing polls.
