# Review — a card that finishes or needs you stays on screen until you dismiss it

**Plan:** docs/plans/done/2026-09-05-a-card-that-finishes-or-needs-you-stays-on-screen-until-you-dismiss-it.md
**Reviewer:** Codex (session 01a085dc, called warm through `needle call` #61) for the independent pass; the build session (Claude Fable 5.1, the lane on card #41) for its own cold pass before and its re-read after
**Diff range:** 32e8bc4 (`origin/develop` after #68's fold) .. the close commit that carries this record (the build b6cc02a, the window fix and the plan's stance 606471d, the review's fixes 2b9a067, the close)
**Findings:** 14 — 3 from the author's cold pass, 10 from Codex's, 1 from the re-read; 12 fixed in this lane, 1 left as it is and said here, 1 a correction to the plan's own words

## The passes

The review runs as a loop (`CLAUDE.md`): each pass one lens, the fixes landed, the next pass re-reads the fixed work.

1. **The author's cold pass, before the reviewer's, the three lenses in one.** Against the done-means: the four items were read again against the code and the tests, and two of item 1's letters fell to the machine itself — the notifier lets a critical notification through do-not-disturb only under the app name `notify-send` (a notice sent as *Needle* was closed in six milliseconds and filed to history unseen, reproduced with `dbus-monitor`), and it draws no buttons, invoking only the action named `default` on a click. The plan's Met lines say both. The seams: the page kept `null` for "no show heard yet" and so never acted on the first press a page ever heard (finding 1); a blocked lane's registry detail was pasted whole into the popup (finding 2); his own interactive terminal, stopped on a card, would have rung at him (finding 3). The boundaries: nothing under `board/` spawns — the loop calls the runtime's `tell`, the runtime spawns; the two new commands are named in `runtime/notice.py` only and the floor ratchet lists them and the new path (`applications_dir`); the typed mirror carries `told` and `Shown`. On the search: the focus existed (`focus_window`) and was split into `focus_address` rather than written twice; the stream already carried the version, and `shown` rides beside it rather than in the board's state. Live on this laptop: the notice stood on screen with its shell and `notify-send` alive after the bus showed no close, and `needle show needle 41` focused a board window — the wrong one, Omarchy's, which is the window fix in 606471d.
2. **Codex's pass — the reader who was not the author.** Called warm on the frozen 606471d; it read the plan, the rules and every changed file, and named ten, all inside the change. Two it found were the author's findings 1 and 4 in other words (the first show a page hears; a server that restarts and counts from one again), already fixed by then. Of the rest, all right: a lane the machine parks to bring back by itself rang as his moment (finding 5); a prompt from the registry inherited an older hook message's stamp and could never ring after that message's ring (finding 6); an untold exit older than a resume rang over a card that was working again (finding 7); his Stop was tied to the exit by sixty seconds of proximity, which silenced a distinct death after Stop then Resume and rang for a Stop whose exit took longer (finding 8); the popup was spawned before its `told` row committed, so a crash between them rang twice, against the plan's own ruling (finding 9); the button's command names the venv the server runs from, which a lane's probe outlives (finding 10, left as it is); `needle show` reported success with the card never opened when the board could not be asked (finding 11); and the plan's own reading in item 4 had nothing to read for how long a popup stood — the detached shell threw that evidence away (finding 12).
3. **The fixed work again.** Findings 5 to 9, 11 and 12 landed as one commit. `_exit_owed` judges the card's *newest* move, whatever its actor: only when it is the machine's out of Executing is an exit owed, so his drag and a resume's hands-on move both supersede an old exit; a standing park rings nothing; his Stop counts when it falls in the lane's current life, between the move into Executing and the exit, and the sixty-second window is gone with its constant. A prompt's start is the registry's own stamp. The `told` row lands before the popup, so a crash between them loses one ring rather than ringing twice — the test for the crash now reads both sides. `show` opens the desktop entry on the card's page whenever the board could not be asked, window or no window, and says so; the popup's shell appends one line to `told.log` beside the store when it is answered or dismissed. The re-read of the reworked code found one thing more (finding 13): `_tell_owner`'s docstring still promised the old crash behaviour; fixed. A test written before #68 folded asked for an oom-killed lane's ring; the trunk now brings that lane back by itself, so the test reads the death the machine cannot name instead (finding 14, a correction to the test's premise, not to the code). Nothing else new.

## Dispositions

1. [seam] The page never acted on the first show it heard — FIXED: `frontend/src/state/board.ts::nextShown`, with `frontend/tests/shown.test.ts`.
2. [feature] A blocked lane's detail pasted whole — FIXED: `first_line`.
3. [feature] His own terminal would ring at him — FIXED: an interactive session's wait is his, never rung.
4. [seam] A server restart counts shows from one and the page ignored the repeat — FIXED with 1 (Codex's finding 4 in its words).
5. [seam] A lane parked to come back by itself rang as his moment — FIXED (Codex 2): `lane.park` standing rings nothing; when the park lifts without a return the exit rings if still inside the hour.
6. [seam] A registry prompt inherited an older hook stamp — FIXED (Codex 3): a blocked lane's wait begins at the registry's own stamp.
7. [seam] An untold exit rang over a resumed card — FIXED (Codex 5): the card's newest move is the judge.
8. [seam] His Stop by proximity — FIXED (Codex 6): his Stop in the lane's current life; `OWN_MOVE_SECONDS` removed.
9. [seam] Popup before the row: a crash rang twice — FIXED (Codex 7): the row first; `test_a_ring_the_record_owes_is_made_at_the_next_reconcile_and_a_failed_one_is_one_row` reads a crash on either side.
10. [seam] The button's command names the server's venv — LEFT AS IT IS (Codex 8): the loop runs in the server's own venv at `/home/dennis/Work/needle/.venv`, which outlives every lane; the one popup raised from this lane's venv was the live probe, and it says so on its face. A popup that outlives a rebuilt venv runs nothing and says nothing; the record's `told` row still stands.
11. [boundary] `show` claimed the effect with the card never opened — FIXED (Codex 9): a board that could not be asked opens the entry on the card's page instead.
12. [feature] The plan's reading had no trace of how long a popup stood — FIXED (Codex 10): `told.log` beside the store, written by the popup's shell; `tests/runtime/test_notice.py` reads a dismissal and a press.
13. [feature] A docstring promising the old crash behaviour — FIXED in the re-read.
14. [verification] The oom-killed ring test assumed a trunk before #68 — TEST CORRECTED: the machine brings that lane back; the death it cannot name is the exit that rings.

## What was checked

- **The notifier's own rules**, by hand on this laptop: `busctl … GetCapabilities` lists `persistence` and `actions`; `notify-send --wait` with no action stood six seconds under `timeout`; with an action and any app name but `notify-send` it returned in 18 ms, exit 0, no output, and `dbus-monitor` showed `NotificationClosed` reason 2 six milliseconds after `Notify`; `~/.local/state/omarchy/notifications.json` holds `"dnd": true`; `NotificationLogic.js::shouldBypassDnd` and `Service.qml::invokePopupDefault` say why. The same alert the account watcher raises is silenced by the same rule: filed on the machine's board (`omarchy-machine` f2b8b7f).
- **The live notice**, raised through the lane's runtime with the real shape: no close on the bus in 3.5 s, the shell and `notify-send` alive after; it stands on the owner's screen as this record is written, its click running this lane's `needle show needle 41`.
- **The button's verb**, live: `needle show needle 41` focused a board window, proved by `hyprctl activewindow`; the served board answered 405 to the POST because it predates this fold, which is the case `show` now answers by opening the entry.
- **The suites:** the backend suite whole on the levelled tree (32e8bc4 plus this lane), result in the close commit's body; `tsc --noEmit` clean after `needle types` wrote `notice.ts` and `audit.ts`; `vitest run` 71, three of them the page's rule for a show.
- **The baseline**, read from a copy of the shared store: Hello Revenue 40 machine exits in fourteen days, median 200 minutes to his next act; Needle 10, median 11.
