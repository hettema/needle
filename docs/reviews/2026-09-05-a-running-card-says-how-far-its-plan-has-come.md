# Review — a running card says how far its plan has come (slice 13)

**Plan:** docs/plans/done/2026-09-05-13-a-running-card-says-how-far-its-plan-has-come.md
**Reviewer:** the build session (Claude Fable 5.1 at medium, the card's gate), on its own work. No second session was in the loop; the served throwaway board and the served board after the fold are the readings this record cannot replace.
**Diff range:** 05f428b..HEAD on the lane `card-38-13-a-running-card-says-how-far-i` — one feature commit (the grammar in the plans README, the two readers, the domain shapes, the loop's read of the lane's copy, the brief's sentences, the four primitives and the two faces, the fixture plan in list shape, the comp's frozen record, twenty-one tests), then the review's fixes folded into it before the commit, then the docs commit that carries this record and the archived plan.
**Findings:** 8 — 6 fixed before this record, 2 no change with the reason.

## The passes

The review ran as a loop (`CLAUDE.md`): each pass one lens, the fixes landed, the next pass re-read the fixed work.

1. **The feature against the plan's "done means".** Item 1: `docs/plans/README.md` carries the `Met:`/`Deviated:` line with its two-line why; `items_of` reads `### N.` sections at the deepest numbered level and the bold `N. **Title.**` run; the inline habit reads as met; the booking-office fixture plan is the heading shape and the storm-warning plan was given four items in the list shape. Hello Revenue's 504 archived plans read without error — the counts are in the plan's close-out — and checking the handout's table by eye against four plans made findings 1 and 2. Item 2: the door test on the real two-lane fixture writes `**Met:**` on item 2 in the worktree and reads 1 of 3 with item 2's title while the trunk's plan is unchanged; a removed worktree reads nothing; the read measured 1.16 ms median per lane. Item 3: the canvas is committed as the frozen record; the collapsed face and the open card follow it; the served throwaway board showed every state on a real Executing lane, which made finding 4. Item 4: the Start brief carries the sentences and the door test reads them off the launched brief; the wording made finding 3. Item 5: the thirteen records read without error; three checked by eye against their `Findings:` line, which made finding 5.
2. **The seams.** Concurrency: the loop reads a plan file the lane may be writing at that instant; a half-written read counts wrong for one beat and the next beat rereads, and nothing is stored (finding 6). Failure and restart: nothing about progress is stored, so a restart forgets nothing and the first beat after it counts again. The truth of what the board shows: every item met with no record naming the plan showed nothing where the plan's terrain note says the open card should say so (finding 7); the strip declares `live` once and paints only met and deviated segments, so a zero count is a grey strip under "0 of N met"; a record whose dispositions say FIXED and NO CHANGE in one entry counts as fixed, and the `Findings:` line is not read for the split, so two of thirteen records count differently from their own summary line (finding 8).
3. **The boundaries.** Layers: `board/progress.py` and `board/parse.py` import domain and each other only; the file reads sit in `api/loops.py` beside the plan-footprint read that was already there, one way. The board never runs: nothing new spawns or opens. Typed edges: `Item`, `Stance`, `ReviewPass`, `Review`, `Progress` are Pydantic; `document.ts`, `lane.ts` and `board.ts` regenerated; `Document.items` is a required field so every hand-built document says `items=[]`; the page fixture regenerated with five progress states from the real derivation; `tsc` clean, no `any`. One design system: `Strip`, `HowFar`, `Items`, `Passes` live in `components/ui/`, painted only through `data-meaning`; the colour ratchet passes and the page test reads back that the strip claims `live` and the open counter `broken` and nothing else in the row claims a colour. Nothing half done: no deferral marker; `ruff check` on the lane's files clean (the repository-wide run has 28 pre-existing long lines, filed by an earlier lane). Nothing found.
4. **The fixed work again.** Re-read the six fixes: the task-named heading rule and the head-first record match in `board/parse.py` and `board/progress.py`; the brief's sentence; the open card's headers and the review-less section. The full backend suite (including the ratchets), `tsc`, 63 page scenarios, the door test on the real worktree, and the throwaway served board once more after the header fix. Nothing new. Clean.

## Dispositions

1. **A bold list before the task list was read as the items.** Hello Revenue's reference-image plan opens with a bold numbered decision log, and the first-run rule read that as the task list and missed the `[DONE …]` item under `## Task breakdown`. FIXED: the bold run under a heading the corpora use for the task list (task, tasks, item, items, slice, slices, work, steps, breakdown) wins, else the first bold run; the archive count rose from 3 marked plans to 9 (16 met), against the plan's "17 in five spellings" — the rest are ticks on bullets and table rows, which are not items and are not counted.
2. **The reviews folder was parsed whole for every record on every beat in the review loop.** Thirteen records cost 7 ms per lane per beat, against the plan's prediction of under 2 ms. FIXED: `plan_stem_of` reads only the head; the one record whose `Plan:` names the lane's plan is parsed whole. Measured 1.19 ms median after.
3. **The brief said "never by editing a file", which now also reads as the plan and the record.** FIXED: the sentence says the rows are written through the command line, never by editing the board's own files, and a new paragraph says the plan and the record are the lane's own documents that the fold carries.
4. **The open card's section headers wrapped at 560 px** under a `from` line that carried the whole count line and the record's path — a width defect jsdom cannot show; the served throwaway board did. FIXED: the items header carries the count and where it was read; the review header carries the counter line and the path sits under the passes.
5. **A record's `Findings:` summary and its dispositions can disagree, and the face reads the dispositions.** Two of thirteen records say "1 filed" or "1 no change" on their summary line while the disposition entry itself says it was fixed or left alone. NO CHANGE: the dispositions are the record's own word on each finding, one entry each, and the summary line is prose; a face that read the summary would count what the writer said rather than what the record holds. The plan's close-out tables both.
6. **A plan file read mid-write counts wrong for one beat.** NO CHANGE: the read is a plain file read of the lane's own copy; the next beat (30 s) rereads and nothing is stored between, so the wrong count lives for at most one beat and leaves no trace.
7. **Every item met and no record naming the plan showed nothing.** The terrain note says the open card should say so. FIXED: the open card carries a review section that says every item is marked and no record under `docs/reviews/` in the lane's worktree names this plan yet.
8. **An unused CSS rule for a strip in the section header.** FIXED: the strip sits in the section body with its own rule.

## What was checked

- **The suite:** the whole backend suite with the ratchets — 9 new in `tests/board/test_progress.py` (heading items with their done-means and stance; the bold list run with the inline habit; a plain numbered list is not a task list; a plan carries items and a suggestion none; a record's plan, passes, findings and dispositions; a clean pass and a no-change entry; a record without the sections; the count from the lane's copy reading the record only once every item is met; a plan with no items and a plan with nothing marked), 1 new in `tests/api/test_doors.py` on the real-worktree fixture (the brief's sentences; 1 of 3 with item 2's title while the trunk's plan is unchanged; the review counter once every item is met and the item count once one is unmarked again; nothing once the worktree is removed). `tsc` clean; 63 page scenarios, 3 new (the strip and the count in five states on an Executing card with nothing on a plan without items; only the strip and the open counter carry a meaning; the open card's items with their stance and the review's passes with the filed finding named).
- **Hello Revenue's archive with the item reader** (504 plans): no errors; counts in the plan's close-out; four plans opened by eye — three the table says are marked (bootcamp sign-up: six ticked config lines; bid competitively: two of three SHIPPED; local Needle board: item 1 DONE) and one it said was not (reference image payload), which was finding 1.
- **The thirteen records with the record reader:** no errors; three checked by eye against their `Findings:` line (a running lane hears the board: 11, 8 fixed, 2 no change, 1 filed — agrees; conversations and lanes: 5 fixed by the dispositions against "4 fixed, 1 no change" on the line; defects fix themselves: 5 fixed, 4 no change against "5 fixed, 3 no change, 1 filed"), which was finding 5.
- **The cost of the read**, over this lane's own worktree (13 records): the git edits read the loop already does, 7 to 10 ms median; the plan read and count, 1.16 ms median, 4.6 ms max; the review read once every item is met, 1.19 ms median after finding 2 (6.9 ms before).
- **The served throwaway board** (Harbourmaster as a git repository on the fixture floor, a real worktree for the storm-warning card with a live process registered, the bundle built from this worktree, headless Chromium over DevTools): the Executing card read "2 of 4 met · last: One click sends the warning" with two filled segments; the open card listed the four items with met, met, not yet, not yet and each item's evidence or done-means; with every item met and a record written into the lane, the line read "review · pass 3 · 9 found, 8 fixed, 1 filed" in the broken colour and the open card listed three passes with the third open and the filed finding named; with a clean fourth pass appended, "review clean · 4 passes · 9 found, 8 fixed, 1 filed" with no colour. Each state arrived within one beat of the file write. Screenshots were taken at each state and read; the collapsed card at 288 px truncates the line after "last: On…", one line as the comp draws it.

## What the build learned the comp got wrong

- The comp's open card shows a found count per pass ("4 found"). The records do not carry a count per pass reliably — a pass names its findings by number in prose — so the open card shows each pass's own text beside its lens and whether it read clean, and the counts on the header line come from the record's dispositions.
- The comp asked whether the head's "Executing · 1 of 3" (the card's position) misreads beside a count of items. The page's open card shows the column chip and no position, so nothing had to be decided.
- The comp places the items section between "What it makes true" and "The record"; on the page it sits between "What it makes true" and "The lane", since the lane sections follow the essence there and the record sits lower.

## Not done, stated

- The plan hands items 1 and 5 to the `execution` role. This session runs under an account config dir (`~/.claude-accounts/armana`), which has no `agents/` directory; the role files live in `~/.claude/agents/` and the dispatch was refused with "Agent type 'execution' not found". Both handouts were run here as scripts and verified as the plan asks; the close will write `execution ×0 (named 2)`, which is true. The fix is the machine repository's (the role files reachable from every account), not Needle's.
- Item 4's second half — a real lane started after the fold with a stance in its plan and a record with more than one commit behind it — is the plan's loop and cannot be shown before the fold. This lane's own plan carries five `Met:` lines written in the feature commit, the first real stances; the first five lanes after the fold are the signal.
- The screenshots are not committed (no record before this one commits images); what they showed is written above and the files went to the owner with the close.

## The served board, after the fold

Read on 2026-09-05 after `needle fold`, the bundle rebuilt from the main checkout and `needle-serve.service` restarted: card #38 (this plan) in Executed with its four rows written; its lane, still with hands on the worktree at the moment of the read, carried "review clean · 4 passes · 8 found, 6 fixed, 2 no change, 0 filed" — this record's own counts, read from the lane's copy — and the open card listed the plan's five items, all met, with the evidence written beside each. The board read its own review record through the reader it had just shipped, and the count was right.
