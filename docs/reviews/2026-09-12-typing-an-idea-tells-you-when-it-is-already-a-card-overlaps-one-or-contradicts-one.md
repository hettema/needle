# Review — typing an idea tells you when it is already a card, overlaps one or contradicts one

**Plan:** docs/plans/done/2026-09-07-typing-an-idea-tells-you-when-it-is-already-a-card-overlaps-one-or-contradicts-one.md
**Reviewer:** a Claude session of the lane's own make (Opus, the `gmail` slot, session fb2b5d30 resumed from fa65d1d3, a reading session whose home is the main checkout and not a card's lane — chosen so the call moved no finished card, the defect #123's lane filed on 2026-09-12), Needle call 114, reading the lane's worktree cold on this (rented) machine, asked through `/home/dennis/.claude-accounts/hrclaude/jobs/4633cee3/tmp/review-69.md`; its answer is beside the note (`from-fa65d1d3-re-review-69.md`) and quoted in the dispositions. Before the build, a challenge round of the same make (session c288acc6 resumed from 158d793a, call 109) read the plan and the lane's design cold and measured them over the real corpora: nine material corrections, in the plan's rulings under "From the challenge before build".
**Diff range:** 8a4897f..b5f4970 (the revision frozen while read: the five commits with the plan's stances for items 2 and 4). Repairs landed after it in the close commit (the one carrying this record and the archived plan), whose sha the card's fold names; the implementing lane verified them, and the reader is not represented as having read them.
**Findings:** 4 — all demonstrated, all inside the change, all repaired; two were the reader's live consequences named without a finding (below the dispositions).
**Verification:** after the repairs: `tests/board` and `tests/ratchets/test_layers.py` (293 passed), `tests/api/test_doors.py` + `tests/api/test_dial.py` + `tests/board` + `tests/ratchets` in one run (426 passed), `npx tsc --noEmit` clean and `npx vitest run` 85 passed, the frontend fixture regenerated and current, ruff clean on every changed file. The full backend suite at b5f4970, every module as its own process six at a time, run by the execution role as item 2's handout: 100 modules, 797 passed, 0 failed; the lane's own earlier run of the same script at a7722bf had one flake, `tests/api/test_defects_column.py::test_the_column_the_beat_and_the_verb_read_one_order_gravest_first`, which passed alone (11 passed with the typed-edges ratchet) and passed in the execution role's run.
**Completion:** Complete: every verified finding inside the change is repaired and its repair holds in a test or in the measured numbers; nothing was filed outside. What remains unverified until after the fold, by the plan's own words: item 1's hand-read of the served board (types, fixture, dist, restart, then the count and the row on a real card) and item 3's first live Idea door conversation, both recorded below once done; and the two Loop rows, which the close's WATCH row carries.

## Dispositions

1. [feature] `board/parse.py:410-413` (`_HELD_LOOSE`, with `board/sequencing.py::unplaced` and `board/lane.py::doors_for`) — the loose reading took any one-to-three-digit run after a hold word as a plan number, so a quantity became a hold the board cannot place: `held_names_of("after 2 ChatGPT accounts exist") == ["2"]`, likewise "after 50 builds have run", "once 10 clients are live", "after 3 weeks of the signal", "waits on 2 of the boards being level", "after the 5 GB floor lands" — a Start closed in the broken voice on a hold the writer never meant, on the one door item 4 exists to make honest; no live line on the four boards tripped it today, and Omarchy's "after a second ChatGPT account exists" passed only because the number was a word — FIXED: a bare number is a name only when what follows it is the end, a separator, filler or another name (`_HELD_LOOSE`'s lookahead); the six quantities and "after 12 (the reader) and 13 (the item read)" are in `tests/board/test_sequencing.py::test_a_line_that_means_a_hold_the_board_cannot_place_says_which_names`.
2. [feature] `board/neighbours.py:40` (`COUNTED_FROM`) — a calendar day, 2026-09-13, compared with the card's UTC birth date: a fold on the 12th would have exempted every document born between the fold and midnight from the count although its writer's brief already carried the neighbours, and a birth in the owner's 00:00–02:00 Stockholm window on the 13th read as the 12th — FIXED: `COUNTED_FROM` is a moment in UTC, set to the fold's time in the fold commit, and `counted` compares `card.born_at` with it; `tests/board/test_neighbours.py::test_only_a_document_born_after_the_reader_counts` and the API test hold it with datetimes.
3. [record] `board/neighbours.py` (`GLUE_SHARE`, `RARE_WORDS` docstrings) — the measured numbers did not all reproduce: Omarchy read 10 of 27 documents finding a candidate where the docstring said 3, Hello Revenue's grounded median was 5 (226 documents) where it said four, and Needle's glue was nine paths in all, not "nine core files and two doctrine documents" — FIXED: both docstrings re-measured at 21:10Z on 2026-09-12 with the corpus sizes named (Needle 65 live documents, Hello Revenue 226, Omarchy 27) and the note that the numbers move with the corpus; the lane's earlier Omarchy figure came from a run under the previous, fixed floor and was carried over unread.
4. [feature] `board/neighbours.py::Corpus.by_words` — the floor `RARE_WORDS × ln(N / 2)` was nothing at two live documents and 0.81 at three, below the weight of a word one document shares, so on a board of two to four documents nearly any shared word made a candidate (hr3 holds one live plan; its second document would have been the first's candidate on a single word) — FIXED: a board smaller than `SMALL_BOARD` (eight) is read as one of eight for the weights and the floor alike, so a candidate always needs rare words in common; a board of three with one shared word makes none, three rare words make one (`test_word_candidates_are_the_nearest_few_in_a_fixed_order`); the real boards, all past eight, read the same as before.

Named by the reader as live consequences, not findings: at the fold, card #42 on Needle ("Every loop a plan names is watched until it closes", in Planned with a gate) closes its Start in the broken voice and the head counts one "hold the board cannot place", because its Sequencing line reads "after 12 (…) and 13 (…)" — the plan-number hold item 4 exists for, and the one unplaceable name on the four boards; and `Live.beside` recomputes on every rescan since `read_at` moves whether or not a document did — a cost, not a defect.

Verified by the reader on the way, no finding: ground by named paths with existence unchecked and one glue rule for paths and words; word candidates never counted; naming in every form (`#N`, a Sequencing name, a path plain or backticked); the board read and the open card reading one cached derivation keyed on the index's read time and every card's number, link and fold; the block composed into the six writing paths and into nothing else — `render()`, `needle card`, the Plan door and the Execute text unchanged; the Idea door's brief with a line matching nothing and with an empty line; every live Sequencing line on Needle, Hello Revenue, Omarchy and hr3 (no false hold, one true one); the frontend's guards on an older API's `beside: null`, both claims in `CLAIM_VALUES` and `CLAIM_MEANING`, the head's filter; the #82 seam (`Ground` and `NeighbourGround` are different concepts; the overlaps are textual — a `Claim` member each, `CLAIM_VALUES`, a `CardSummary` field, `LiveProject` state and one keyword each on `assemble_board` in `Live.board`, so the second to fold rebases and keeps both); and the Met lines for items 2 and 4, every named test existing and passing.

## Verification evidence

- Independent reading: Needle call 114 at b5f4970 (the reader's first line names the full sha). The reader ran the diff whole; the changed board modules, the two changed API modules and the ratchets (135 passed); `tsc` and `vitest` in the worktree (85 passed); scripts over the live corpora of Needle, Hello Revenue, Omarchy and hr3 through `board/parse.py::parse_document`, `board/neighbours.py::beside_all` and `Corpus.by_words`, `held_names_of` and `unplaced`; `idea_brief` called directly on an empty line, a whitespace line and a line matching nothing; #82's worktree diff for the seam; and, over ssh, the laptop's clones' HEADs and live-document counts to test whether the code's numbers came from another checkout (they did not). No `needle` verb was run from the worktree and nothing was edited.
- The challenge before build: Needle call 109 at the plan's revision on 8a4897f, over the plan and the lane's written design; its measurements (199 file pairs on Needle, 50 of 63 documents counted on day one, a median of 21 word neighbours) are in the plan's rulings and were confirmed by the lane's own measuring script (`/home/dennis/.claude-accounts/hrclaude/jobs/4633cee3/tmp/measure.py`) and by the search role's sizing (item 1's handout: every live document on the boards with its paths and words; the same 60 non-docs file pairs on Needle and the same commonest words).
- Repairs: `uv run pytest tests/board tests/ratchets/test_layers.py` 293 passed; `tests/board/test_neighbours.py` 8 passed with the new small-board case; `tests/api/test_doors.py -k "born_beside or sequencing_line or idea_opens or discuss"` 5 passed; `uv run pytest tests/api/test_doors.py tests/api/test_dial.py tests/board tests/ratchets` after the repairs: 426 passed in 9 m 35 s; one earlier run of the same modules hit `test_a_document_born_beside_…` carding a birth twice (an IntegrityError: the test thread's rescan raced the watcher's, which runs in the server's own loop), so the test now rescans through the portal as `reconcile` does — a test-side seam, since production rescans only in the loop thread; ruff clean on every changed file; the measuring script re-run after the repairs reads the real boards unchanged (Needle 15 of 65 candidates, Hello Revenue 120 of 226, Omarchy 10 of 27).
- Frontend, at b5f4970 and unchanged by the repairs: `cd frontend && npx tsc --noEmit` clean; `npx vitest run` 85 passed; `uv run python tools/board_fixture.py` rewrote an identical fixture after `needle types`.
- Full suite at b5f4970, every test module as its own pytest process six at a time (`/home/dennis/.claude-accounts/hrclaude/jobs/4633cee3/tmp/suite_parallel.sh`, run by the execution role): 100 modules, 797 passed, 0 failed, 0 errors; `grep -v '^0 ' …/suite/summary.txt` prints only `DONE`. The lane's own run of the same script at a7722bf: 97 modules green, `tests/ratchets/test_layers.py` (a hand-kept standard-library list without `math`, fixed in b5d50a9 by reading `sys.stdlib_module_names`), `tests/ratchets/test_typed_edges.py` (types regenerated in b5f4970) and the ordering flake named above, which passed alone.
- No migration, no store touched: every lane verb ran with `NEEDLE_DATA_DIR` on a private copy, and board verbs through `uv --project /home/dennis/Work/needle`.
- The served board and the first Idea door conversation: recorded below after the fold.

## After the fold — the served board, read by hand

- The fold: origin/develop 941f529 at 21:27Z on 2026-09-12, over #82's still-open edits in eleven files (named on both cards and the watercooler; #82 rebases). The laptop's checkout was levelled by the fold; the lane rebuilt `frontend/dist` there over ssh (`npm run build`, 169 ms) and restarted `needle-serve` at 21:28Z; the service came back active. No migration, so the store is untouched.
- Needle's head, read through the served API (`/api/projects/needle/board`): Broken lists `no review 3` and `hold unread 1` — the one is card #42, "Every loop a plan names is watched until it closes", whose face reads `hold unread` in the broken colour and whose Start door says "Something is wrong: its Sequencing line means a hold on '12' and '13', which the board cannot place, so it cannot start. A hold names a card as #N, or <project> #N for another board's. Fix the line and Start opens, or waits on the card it names." — `readiness.unplaced == ['12', '13']`, as the reader predicted. No `beside unnamed` count yet: every live document was born before 21:45Z and reads as a quiet fact, which is the count's promise.
- The row on a real card: #29, "Real card titles never reach the public repos", opened through `/api/projects/needle/cards/29`: `beside.sentence` "sits beside #84 on tests/ratchets/test_the_fixture_project_is_synthetic.py and does not name it.", born 2026-09-04, not counted, one file neighbour (#84, not named); the same reading on the board's summary. Forty-one Needle cards carry a beside line; #48 and #128 carry word candidates ("near #98, #97, #102 by its words — candidates, not a verdict"); #112, #113 and #121 name a neighbour (#83) beside them and say so. Hello Revenue: 161 cards with a beside line, no new claim on its head; Omarchy: 9, none.
- Not yet: the first live Idea door conversation after the fold (the owner's to have), which the close's WATCH row reads; and the page itself in a browser — the API is what was read, and the page's tests hold the rendering.
