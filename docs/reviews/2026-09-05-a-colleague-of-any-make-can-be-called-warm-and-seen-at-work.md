# Review — a colleague of any make can be called warm and seen at work (card #57)

**Plan:** docs/plans/done/2026-09-05-a-colleague-of-any-make-can-be-called-warm-and-seen-at-work.md
**Reviewer:** the build session (Claude Fable 5.1 at medium, the card's gate), on its own work, with the make whose format this reads as the second reader: Sol (Codex, rollout 01a07211) was called warm through the verbs the card ships and answered on the reader's two discriminators before the review closed — row 7 of #51's comparison, and pass 2 below.
**Diff range:** 8738603..HEAD on the lane `card-57-a-colleague-of-any-make-can-be-c` — one feature commit (the Codex reader, the call path, the floor's fake and rollout writer, the tests, the board's one `where` phrase), one fix commit from the passes (the launcher hand-off, this record, the plan archived).
**Findings:** 9 — 7 fixed before this record, 2 no change with the reason. Nothing outside the change to file; one flake in a door test is recorded under "What was checked" with what was seen.

## The passes

The review ran as a loop (`CLAUDE.md`): each pass one lens, the fixes landed, the next pass re-read the fixed work.

1. **The feature against the plan's "done means".** Item 1: `codex` resolves to the most recent worker, an id or a prefix to its rollout, two rollouts under one prefix to `Ambiguous` naming both, a terminal and a mid-turn worker to the two refusals a Claude row gets, and the floor's `codex_log` is empty after every refusal; Claude resolution is tested unchanged beside Codex rows. Item 2: the fake `codex exec -o … resume …` receives the brief and the answer path, the answer lands through the existing `Call` record and `calls.judge` says `LANDED`, a silent worker is `ENDED … without its note`, a failed launch is `DEAD` with the stderr's last line and records nothing, no `-s` and no `danger-full-access` in the argv. Item 3: mixed rows in one list, a malformed head skipped, a file that vanishes between the listing and the read skipped, a day-old ended rollout unlisted but reachable by id, `doing` the tool's name with the credential-bearing input nowhere in the row. Found here: (1) the process finder read the id right after `resume` and missed every real worker, since `--skip-git-repo-check` sits between — the floor test showed the called worker `ended` while it worked; (2) a worker that answered inside the observation window landed its answer before the call it answered, because the CLI stamped `called_at` after the launch; (3) the runtime's own child read as alive after it died, because a zombie has a stat line; (4) the test's stand-in process lost the id from its argv, because `sh -c 'sleep 300'` execs the sleep in its own place. All four fixed; the tests re-run green.

2. **The seams.** Concurrency and races: the answer file is Codex's own write at turn end, read by mtime against a `called_at` taken before anything runs; the worker is init's child by a double fork, so neither the CLI's exit nor a reap races it. Failure and restart: a launch that cannot exec writes the error to the log and ends, which the caller reads as a death; a worker's death has its words in the log beside the answer, and a verified worker sits in `needle-<name>.scope` with its journal. The truth of what the board shows: (5) the board said `fable on <slot>` for any row without a model — a guess for a terminal, a lie for Codex — fixed as one helper, `board/lane.py::where_of`, for the two places that phrased it; (6) **the different make's reading of its own format**: Sol corrected the note twice — an unknown `source` must not default to worker (fixed: `WORKER_SOURCES = {"exec"}`, `cli` a terminal, anything else shown by its own word in `recorded` and refused with that word), and the tool-call set is four record types (the code had four; the note said two — no code change, the note's claim corrected here); (7) the launcher's pid hand-off could return nothing and raise the wrong exception — fixed as an `OSError` the caller already reads as a death. No change, with the reason: `needle window` on a live Codex row runs `claude attach` through Codex's directory and fails in the owner's window — loud and immediate, convention is enough (`CLAUDE.md`, the discriminator); `describe_launch` prints the Codex attempt's rung as `codex/default`, where `default` is the top rung's word — cosmetic, the row above it says the truth. Measured: the /proc scan that maps a rollout to its process costs 5 ms and the Codex rows 8 ms in all on this machine (511 pids, 29 rollouts), on top of the Claude list's 16 ms.

3. **The boundaries.** The runtime stands on the floor: `codex_home()` is one more path the ratchet checks under the floor and `codex` one more command it holds to `tests/fakes/bin/`; nothing under `runtime/` but `machine.py` reads the environment, a home path or `/proc`. The board never runs: nothing under `board/` changed but a phrase. Typed edges: no domain model changed, so no type was regenerated and the frontend is untouched — a Codex row is a `Session`. One way: no second protocol — the same `Call`, `judge`, `needle wait` and loop tending; the provider is one field (`slot`) read in three places, and no adapter was born. Nothing half-done: no marker; what is not done is stated below.

4. **The fixed work again.** Re-read `runtime/codex.py`, `runtime/launch.py::call`/`call_codex` and `runtime/machine.py::detach` as committed after passes 1 and 2: the allowlist is the one discriminator for kind and for the refusal; `recorded` carries the source and nothing else reads it for Codex; the hand-off raises `OSError` and `call_codex` catches it. Nothing new. The suite on the levelled tree (rebased over #54's four commits) is green; the Codex suites re-run green after every fix.

## Dispositions

1. **The process finder missed every real worker.** `\bresume\b.*?<uuid>`: the first id after the word, past any flag. FIXED (pass 1).
2. **A fast answer predated its call.** `called_at` taken before `Runtime.call`. FIXED (pass 1).
3. **A zombie read as alive.** `machine.process_start` answers `None` for state `Z`. FIXED (pass 1).
4. **The fixture's shell dropped the id.** `sh -c 'sleep 300; true'`. FIXED (pass 1, test-side).
5. **`fable on codex`.** `where_of`: the model and slot when a model is recorded, the slot alone otherwise. FIXED (pass 2).
6. **An unknown source defaulted to worker.** A positive allowlist; the unknown shown by its word and refused with it. FIXED (pass 2, Sol's finding).
7. **The launcher could hand back no pid.** `OSError`, which the caller reads as a death. FIXED (pass 2).
8. **`needle window` on a Codex row fails in the window.** NO CHANGE: loud and immediate.
9. **`codex/default` in the attempt line.** NO CHANGE: cosmetic, the head line says where it runs.

## What was checked

- **The whole suite on the floor** after every fix: backend tests with the ratchets, `tsc --noEmit`, `vitest run` (66). One flake seen once in the first full run — `tests/api/test_doors.py::test_a_plan_that_lands_citing_suggestions_takes_the_first_card_and_folds_the_rest` read `linked` where it expected `moved` as the first audit entry — green alone and green in the second full run on the levelled tree; outside this change (the reconcile's audit order), not reproduced, so recorded here and not filed.
- **The one list, live** on this machine's real rollouts, read-only: the called worker `working` in `needle-codex-01a07211.scope` with `exec, 0 s ago`; two terminals of the owner's `idle interactive`, mapped by the rollout each holds open; every worker of the day `ended`.
- **The cost, measured** on the real machine, five runs each: `codex.processes()` 4.9 ms, `codex.rollouts()` 1.2 ms, the Codex rows 8.2 ms in all; `Runtime.sessions()` 29 ms for 267 rows against 16 ms for the Claude registries alone. Nothing runs on the hook.
- **The answer channel, live, twice:** `codex exec -o <file> resume <id> "…"` on a small rollout wrote the one-word reply to the file by Codex's own hand outside the sandbox (the session id unchanged, no fork); then the real exchange below with `--skip-git-repo-check` after `resume`, which Sol confirmed is accepted in 0.152.1.
- **The warm exchange, through the verbs:** `needle call 01a07211 <note>` at 16:07:49Z (a private `NEEDLE_DB`, since the served board runs the old code and its tending would have ended a call whose session it could not see), the process verified at 16:07:55, five `exec` tool calls from 16:07:56, the last message at 16:08:57, the file landed at 16:08:58 and one `needle wait` returned it: **69 s, zero polls**, the worker at 39,633 tokens, two questions asked and closed, the two sources re-read both named in the note, two corrections of the note (both in this lane). **Its cold pair:** the same note to a fresh `codex exec` one-shot by the same chair at 16:10:05, the file at 16:11:44: **99 s**, 49,741 tokens, the same two corrections. Rows 7 and 8 of #51's comparison, written by both parties at the time.
- **The two refusal demonstrations** (the plan's close-out): on the floor, a terminal (`source: cli`) refused with "runs in a terminal of its own" and a mid-turn worker with "is working on its turn", both with the fake's log empty; live, `needle call 01a06f2b` would name the owner's Sol terminal and is not run here, since the floor's refusal is the same code path and the owner's chair is not a fixture.
- **Not done, stated:** row 6 of #51's comparison (row 5's cold pair, Codex chairing) is not this lane's and stays empty; the plan's acceptance criterion says "the sixth row", and what this lane could truthfully write is a matched warm/cold pair of its own, rows 7 and 8 — the criterion's intent (a warm Codex exchange in the table, written by both parties, beside its cold pair) is met, its letter is not. A worker whose sandbox refuses its own write is reported as `ENDED … without its note` when its last message is empty and as `LANDED` with whatever it said when it is not; the floor proves the first, and the live exchange (told not to write) exercised neither.

## The loop

We think a callable, visible Codex worker changes collaboration from a chair relaying notes by hand into one reciprocal surface, because the relay was the cost, not the thinking. Fixed before the exchange: no manual relay, never the interactive chair, no transcript content leaked as `doing`, no provider-specific caller command. All four held on the first exchange: the answer came through the record, the chair was not touched, `doing` read `exec`, and the caller typed `needle call` and `needle wait`. Warm landed in 70 % of cold's time on 80 % of its tokens with the same corrections — faster, not half; plan 17's reading of that case is "the cost is thinking", and one pair is one reading. The WATCH row asks for the next warm Codex call by another chair, written by both parties, by 2026-09-12; if it needs a hand between the answer and the caller, item 2 did not land and this card reopens.
