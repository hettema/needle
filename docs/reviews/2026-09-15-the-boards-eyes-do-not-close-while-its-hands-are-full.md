# Review — the board's eyes do not close while its hands are full (card #154)

**Plan:** docs/plans/done/2026-09-15-the-boards-eyes-do-not-close-while-its-hands-are-full.md
**Reviewer:** one cold Claude session of this lane's own make, called warm through Needle's own verb on a note it had no other context for — call 180, `needle call 16fc3cd5`, resumed as session 2334c710. The note is `docs/reviews/notes/2026-09-15-154-cold-read.md` and the answer is kept verbatim beside it at `docs/reviews/notes/from-16fc3cd5-re-2026-09-15-154-cold-read.md`.
**Diff range:** f89c770..acbb5b7 — the revision the reader was given. It did not stay frozen: two repairs from my own re-read landed while the read ran (`e97d816`, `0d04bd0`), which the reader saw, named, and re-established every claim against from its own `git archive acbb5b7` export. Its findings 1–3 are those two repairs, confirmed independently; findings 4–8 were open at both revisions and are repaired in `4972aeb`.
**Findings:** 8 — 3 demonstrated defects (2 already repaired by my own re-read before the answer landed, both confirmed by the reader), 3 more repaired in the lane, and 2 the reader called preferences rather than defects, both taken. The two preferences are classed by where they fall, `[feature]` for the page and `[record]` for the vocabulary, since those are the classes the doctrine's loop counts.
**Verification:** below, under its own head.
**Completion:** every verified in-scope finding is resolved, the plan's five items carry their stance, and the final revision — `12ccfcb` for the suite, `f2966cf` with this record — is green module by module with `tsc` and `vitest` clean. Two limits are named at the end and neither is this change's.

## Dispositions

1. [verification] `tests/api/test_dial.py:766` — the suite was red inside the module this change edits: `assert "4 readings of a mark, …"` failed, because the walk to that card no longer reads exactly four marks once readings stop queueing behind the number. The reader reproduced it alone from its own export, so deterministic and this change's, not card #145's instability. Against CLAUDE.md and §12's "nothing lands red". — FIXED in `0d04bd0`, found by my own re-read before the answer landed: the assertion pins the two decisions the test takes and no longer the total, since the total is now the readings' own bound speaking. Re-run alone: passes.
2. [feature] `board/dial.py:357` — `reading_gaps` read a lane's life from its planning session's start to its end, treating "no `ended_at`" as "still running". A plan held at a closed door has no `ended_at` and is not running by this codebase's own definition (`running` counts it 0; `held_lanes` calls it "no process"), and held is not rare — a switch turned off after a plan lands, a line moved below a planned card, a card waiting on Sequencing. The reader demonstrated it on the exported code: one PLANNED lane planned three days ago gives 24 gap hours while `running` gives 0. That would move the plan's only measure for a cause its thesis does not predict — doctrine §7's "design A to discriminate". — FIXED in `e97d816`, again from my own re-read; the reader then read the repair and checked it by hand against five lane shapes (held, planning alive, planning died, ASKED/FOLDED, STARTED) and called it a sound repair of exactly this finding. The count now reads two spells per lane — the planning session, and the lane from its Start — and never the wait between them.
3. [seam] `api/dial.py:1663` — `needle fixes <slug> --reading-gaps` weighed one board's lanes against every board's readings, so the per-board answer asked a different question from the flag's words. — FIXED in `e97d816`: both halves take the scope the verb was asked about. `all` is unchanged, which is what the Loop reads.
4. [verification] `frontend/src/components/ui/index.tsx:265` — the head's new pair had no test, and item 4's Met said it did, naming a test that asserts on `needle dial`'s captured stdout and never renders the head. The span could have been deleted, or wired to the wrong count, with nothing failing — and a decision-bearing claim in the record pointed at coverage that did not exist. — FIXED in `4972aeb`: `frontend/tests/board.test.tsx` gains two card-154 cases, one reading "2 of 3 readings" beside "1 of 1 live" with the live marking, one checking the pair still says its bound and claims nothing while nothing is read. The plan's Met now says where the page is pinned and where the terminal is, separately.
5. [verification] `tests/board/test_dial.py:317` — the replacement for the test that used to pin the old counting said "two readings open and no lane" in a call that passes no readings, because `running` no longer takes them; the assertion was `0 == 0` and the line above already pinned it. A cold reader would take it as the guard, and it guarded nothing. — FIXED in `4972aeb`: the test now asserts what actually holds at that level — `running` has no parameter left through which readings could be folded back in — and points at the floor test that exercises both directions.
6. [feature] a preference, not a defect: the page said "1 of 3 reading" where the terminal says "3 readings at once". An equally truthful wording, so a preference. — TAKEN in `4972aeb`: the noun pluralises with its bound.
7. [record] a preference, not a defect: `docs/vocabulary.md:62` — item 4's Met said the file needed no line because **triage** already gives "reading" as the owner's word, but that entry names only a defect's mark, while the new bound counts a mark, a title read cold and a card parked on him. — TAKEN in `4972aeb`: the entry says the word covers all three and that the board's bound counts every kind.
8. [verification] the machine's number at 0 — a supported input the reader could only establish by reading: `running(...) >= 0` is always true, so nothing is planned and readings still open to their own bound. Correct, and the sharpest expression of this change, but no beat exercised it. Coverage only, no defect. — FIXED in `4972aeb`: `tests/api/test_dial.py::test_the_number_at_zero_plans_nothing_and_the_board_still_looks` ticks a beat with the number at zero and shows nothing committed and a reading opened.

The reader also noted that `--reading-gaps` silently ignored `--started-off`/`--below-the-line` rather than refusing the combination, and called it not a defect. Taken anyway in `4972aeb`, since a flag quietly ignored is the smallest version of the verb stating a check it never made: the combination is now refused with its reason.

## Verification evidence

What the reader ran, on the frozen revision and its own export: `tests/board/test_dial.py` (23 passed); `tests/api/test_dial.py` (one failure, finding 1, reproduced alone); `tests/api/test_defects_column.py` (one failure, `test_a_defects_reading_without_a_grade_is_refused_and_a_graded_one_shows_its_word`, which is the trunk's own red filed on 2026-09-14 and one of the two modules card #145 names — its edit to that module is elsewhere in the file); `tests/ratchets` (all green); `tsc` clean and `vitest` 86 passing; and a `reading_gaps` probe over four constructed lane shapes, which is finding 2.

What this lane ran, one module per process from the lane's worktree. Before the review's repairs: `tests/board` and `tests/ratchets` green; `tests/api/test_dial.py` green after the restatements; `test_parked_cards.py`, `test_title.py`, `test_triage.py`, `test_reading.py`, `test_a_full_machine_admits_nothing_new.py`, `test_api.py`, `test_cli.py`, `test_serve.py`, `test_doors.py` green; `test_a_card_whose_machine_is_full_never_stops_the_board_reading_the_rest.py` green after its own repair (3 passed); both defects-column tests green alone. After the repairs: `tests/board/test_dial.py` (23 passed), the new and changed `tests/api/test_dial.py` cases, `tsc` clean and `vitest` 88 passing — the two new head cases included. The last whole-module pass, on a quiet machine after every repair: `tests/api/test_the_triage_seat.py` (19 passed), `tests/api/test_the_line.py` (2), `tests/api/test_defects_column.py` (8 — including the test the reader saw red, which is the instability card #145 names), `tests/ratchets` (93) and `tests/board`, then `tests/api/test_dial.py` whole (23) at `12ccfcb`, the last revision the suite read whole. `tsc` clean and `vitest` 88 passing at the same revision.

Three tests were repaired because they read "a beat opened nothing" as "every card has been read". That is no longer the same sentence: a beat also opens nothing while every reading seat is taken, so those walks stopped early and left cards unread — one of them then opened a reading in a test that asserts none opens. Each walk now lands every reading standing open, and the quiet test asks that none stands. The one-launch-per-beat shape is still pinned directly on the raw launch log in two of the new tests.

The verb was probed against a copy of a real store, never the board's own: `needle fixes all --reading-gaps` answers zero for the last day on a store with 42 fix lanes and none in it, and `reading_gaps` over the same rows with the window moved to where lanes ran reports six hours in which a lane ran and nothing was read. So the counter is known to move before it is relied on to sit at zero.

Not verified here, and said plainly: the served page. A lane cannot serve the board, so the head's two pairs are checked in the rendering test and in the API payload, not in the browser; the first turn after the fold on the laptop is where the owner sees them.

Two limits, neither this change's. `tests/api/test_defects_column.py` does not give the same verdict twice at this revision — the reader saw it red where this lane saw it green, on the test the trunk filed on 2026-09-14 — and that is card #145's ground and the defect filed then. And `tests/api/test_the_line.py` failed once in this lane under a machine running seven suites at once, and passed alone twice and in the final pass; same family, same card.

One repair came from the final pass itself, after the review: the floor test asserted "0 open now" inside `needle dial`'s line at a moment when the board may now be reading something, which is the change's whole point. It pins the bound and the two lane numbers now; what stands open at that instant is pinned where it is the subject. Three of this lane's test repairs share one shape — an assertion that read "a beat opened nothing" or "one reading at a time" as a fact about the world, when both were facts about the old coupling.

The lane was rebased onto the trunk at the fold (`68e3d66`, card #143's, which touches `api/loops.py` alone and meets nothing here), and everything was re-run on the rebased tree: `tests/api/test_dial.py` (23), `tests/api/test_a_card_whose_machine_is_full_never_stops_the_board_reading_the_rest.py` (3), `tests/board/test_dial.py` and `tests/ratchets` together (116), `tsc` clean and `vitest` 88. That pass found one more, and it is recorded here rather than left as a flake: `tests/api/test_defects_column.py::test_a_defect_verified_before_the_scale_is_read_again_before_it_is_taken` pinned the exact sentence the board gives for leaving a card alone, and a reading can now already stand open on that card when the assertion runs — so the card truthfully says "a reading is verifying its mark now" instead. Both sentences are the same card a moment apart; the test accepts either and still pins its own subject, that the card is read again before it is taken. Re-run whole: 8 passed.
