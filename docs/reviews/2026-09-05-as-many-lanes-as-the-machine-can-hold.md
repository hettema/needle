# Review — as many lanes as the machine can hold run side by side, and the fold settles what they share

**Plan:** docs/plans/done/2026-09-05-as-many-lanes-as-the-machine-can-hold-run-side-by-side-and-the-fold-settles-what-they-share.md
**Reviewer:** the build session (Claude Fable 5.1 at high, the card's gate), on its own work, in the lane `card-50-a-plan-the-board-cannot-start-ho`. No second session was in the loop; the served board after the fold is the sign-off this record cannot replace, and its reading is at the end.
**Diff range:** bf4fa03..HEAD on the lane — one feature commit (the four items, the fixture suites, the page, with the review's first three passes run on the working tree before it and their fixes inside it), then the docs commit that carries this record, the archived plan and the two carried suggestions.
**Findings:** 6 — 4 fixed before the feature commit, 2 no change with the reason.

## The passes

The review ran as a loop (`CLAUDE.md`): each pass one lens, the fixes landed, the next pass re-read the fixed work.

1. **The feature against the plan's "done means".** Item 1: on the door fixture, #253's plan naming the file #241's live lane is editing opens Start as *Start · fable on alpha — shares 1 file with #241's lane; the second to fold rebases*, the pill reads *shares ground with #241* in the proven colour and never *waits*, the open face carries the sentence with the files and the cards, the brief a lane opens with carries SHARED GROUND and the files, `Start anyway` is gone from `Doors`, the API body, `needle start-card` and the page, and the dial starts a `Fix: now` plan into shared ground on its beat with the ground on the card's history; INTENT lesson 4 is rewritten with the night and the owner's words, and `board/collision.py` names every lane on the ground rather than the first. Item 2: *after #241 (the deploy), and #999 too.* closes Start with *#241 (Executing), #999 (not on the board)* named and the pill *waits on #241, #999*; #241 moved to Done opens the door on the next reconcile; the fixture's *independent of every open card.* holds nothing; the grammar was written against the three corpora's eighteen head lines (`tests/board/test_sequencing.py` carries the real ones). Item 3: a planned card waiting on a Sequencing card is held and not counted, the next defect is planned on the same beat, the head reads *1 held*; at 2.0 GB available the beat opens nothing and the head reads *the machine is full: 2.0 GB available, 3 GB needed*, short swap reads the same way on a machine that has swap, and the number the owner set is unchanged through it. Item 4: a lane whose turn is over with nothing folded is left; folded but not closed is left; folded and closed is stopped on the next reconcile with *Stopped … the lane folded and closed, so its session gives its memory back* on the card, and an ended lane is not stopped twice. Findings 1 to 3 came from this pass.
2. **The seams.** Concurrency: the release runs inside the lane loop's reconcile, under the loops' lock, and the dial's beat runs under the same lock, so a stop and a start never act on one lane at once; `Live.headroom` is written by the dial's thread and read by the page's assembly as one attribute swap. Failure and restart: `Loops._released` is memory, so a restart may say a stop once more for a session whose stop did not end it — once per life of the board, with the runtime's words; a session the stop did end is ended on the next read and never touched again. A machine whose memory cannot be read is full, and the head says so, rather than a dial opening lanes on a number nobody has. The truth of what the board shows: `held` on the head and `held` in the beat come from one function over the loop's last read of every door, so the reason the owner reads is the reason the dial acted on; the terminal's `needle fixes` and `needle dial` run in their own process and would have said nothing about the floor — finding 4. After a release the loop re-reads the sessions, and now the windows too (finding 5), so the snapshot the page gets is the machine after the stop. Findings 4 and 5 came from this pass.
3. **The boundaries.** Layers: `board/sequencing.py` and `board/dial.py::headroom` are pure over domain values; `runtime/machine.py` is the one door to `/proc/meminfo` and the floor's ratchet reads it as one more path under the floor. The board never runs: the release goes through the runtime's `stop`, the one that already ends finished reading and planning sessions. Start is the owner's click: the loops stop, they never start, and the ratchet is unchanged and green. Typed edges: `Wait`, `SequencedCard`, `Meminfo`, `Headroom` and the two new `DialState` fields are Pydantic; `needle types` regenerated `lane.ts`, `dial.ts`, `document.ts`; `tsc` clean; no `any`. One design system: the head's *held* and *full* words live in `DialControl` under `components/ui/`, painted only through `data-meaning`. One way: one Start door where there were two; one reader of a Sequencing line; one reader of the machine's memory; one stop for every session the board ends. Nothing half done: no deferral marker; the ratchet is green. Finding 6 came from this pass.
4. **The fixed work again.** Re-read the fixes in the feature commit: the windows are re-read beside the sessions after a release; `Dial._full` reads the machine when the board has not, so the terminal's line and the served head say the same thing, proved by `needle dial` in the held-plan test reading *1 live now, 1 held* and then the full sentence; the head's dial line carries both words. 389 backend tests including the ratchets, `tsc` clean, 66 page scenarios. Nothing new. Clean.

## Dispositions

1. **`needle sessions` keeps the released lane's row.** Item 4's done means says the lane is *gone from `needle sessions`*; the one list keeps every ended session's record with no process, as it keeps every other ended session's, and hiding one would make the list lie. NO CHANGE to the list: the process is gone and the card says why; the plan's item carries `**Deviated:**` with this pointer.
2. **Look on a released lane is the ended lane's Look.** Item 4 says Look *opens a fresh session in the project's checkout, as it does for a card with no lane*; a card with no lane has no Look, and the one Look the board has for an ended lane opens a fresh session in the worktree from the transcript while the worktree stands. NO CHANGE: one Look, not a second one for released lanes; recorded on the plan's item.
3. **The head's dial line in the terminal said *live* and nothing else.** `needle dial` printed the count against the number with no *held* and no floor, so a night like the first would have read *4 live now* from the terminal as it did from the page. FIXED before the feature commit: the line carries *, N held* when any is held and the floor's sentence when the machine is full.
4. **The terminal never read the machine.** `needle fixes` and `needle dial` build their own board in their own process, whose `headroom` is None until a beat runs, so the reasons they print would never say the machine is full while the served board said so. FIXED before the feature commit: `Dial._full` reads the machine through the runtime when the board has not, and `Dial.state()` is what the terminal prints.
5. **The windows were stale for one read after a release.** The loop re-read the sessions after stopping a lane but kept the windows from before the stop, so a Watch window the stop ended read as open until the next read. FIXED before the feature commit: the windows are re-read beside the sessions.
6. **The search the plan hands out ran on the main thread.** Item 2 hands the corpora's Sequencing table to the `search` role; the role's file exists under `~/.claude/agents/` but the Agent tool under this account's config directory does not list it (the machine fact from slice 13), so the one `grep` across the three corpora ran here. NO CHANGE in this lane: the handout is the plan's word and the role is the machine's; the HANDED OUT row at the close will say the dispatch did not happen, which is the row's purpose.

## What was checked

- **The suite:** 389 backend tests including the ratchets — 4 new on the real-worktree floor (`tests/api/test_doors.py`: shared ground opens Start, shows it on the card and briefs the lane; a Sequencing line naming a card in flight holds Start until it ships; `tests/api/test_dial.py`: a held plan does not count and the memory floor stops the beat, with the terminal's line; a lane that folded and closed gives its memory back and no other ending is touched), 6 new pure (`tests/board/test_sequencing.py` over the real corpora's lines and the board's placing of them; `tests/board/test_dial.py` for the held count and the floor; `tests/board/test_lane.py` for the shared-ground door and the plan's hold; `tests/board/test_language.py` for the two pill words), the collision test rewritten for the new voice, the runtime ratchet reading the meminfo path. `tsc` clean; 66 vitest scenarios, 3 new (Start over shared ground with no second door; the open card saying why a plan waits; the head reading *4 held* and the full sentence) and the colour language's table carrying *shares ground* and *waits* where *collides* was.
- **The three corpora's Sequencing lines**, read 2026-09-05 (item 2's handout, run here — disposition 6): eighteen head lines. Two name cards the way the board reads: Hello Revenue #411 (the plan-grammar plan of 2026-09-05) names omarchy #17 (Up next) and Needle #20 (Up next) — **held**; Hello Revenue's plan of 2026-09-03 on the authors' cascade names #222 (Done) and #235 (Executed) — **not held**, and its later sentences naming #228 and #219 are prose. Sixteen name none: ten of Hello Revenue's (one of them #386, whose *#384* stands mid-sentence and is read as prose); Omarchy's plan 15 (*beside Needle's plan 16*); Needle's 08, 14, 15, 16 and this plan (*beside 15 and 16*). Two lines under sections (search authority's second, restricted vertical's amended) are not head lines and are not read. On Harbourmaster, the fixture, #196's *after #139, the four berth-first rulings.* holds on #139 (Decision moment) — the fixture's own held example.
- **Memory held by lane scopes on this machine**, before the fold, read with `systemctl --user show -p MemoryCurrent`: nine scopes of cards in Executed or Done — Needle #17 117 MB, #38 7 MB; Hello Revenue #350 226 MB, #361 161 MB, #377 89 MB, #379 132 MB, #383 87 MB, #385 127 MB, #391 146 MB — 1,092 MB between them, with 5.3 GB available and 9.1 GB of swap free on a 16 GB machine. The reading after the fold is below.
- **The served board after the fold:** below.

## What the build learned the plan got wrong

- "Two plans naming one file both Start, both cards show the shared ground and each other": the card of the lane already running shows the planned card only once both lanes edit the file, through drift, which is the board's existing word for two lanes editing now; before that, the planned card names the running lane and the running lane's card does not name the planned one. The fixture proves the planned side and drift proves the other.
- "A lane that folds and closes is gone from `needle sessions` within one reconcile": the process is gone; the row stays, as every ended session's does (disposition 1).
- "Look on a finished card opens a fresh session in the project's checkout": the board has one Look for an ended lane, in its worktree from the transcript (disposition 2).
- The Sequencing grammar had to be narrower than "names cards in the board's vocabulary": Hello Revenue's #386 names #384 in the middle of a sentence about shared alembic heads, and reading every `#N` on the line would have held it falsely. The names lead and the prose follows; words before a `#` that are no project's end the reading, so *beside #15* is prose.
- "The dial's `_take_next` and `_start` take an eligible defect whose plan shares ground the way they take any other" needed nothing: both read the door, and the door opens.

## Not done, stated

- The search role was not dispatched (disposition 6); the table above was read on the main thread.
- The loop's twenty-lane reading has no date yet: it is read at twenty fix lanes after the fold through `needle fixes all` and the trunk, and the WATCH row names it.
- Hello Revenue's #411 reads *waits on Omarchy #17, Needle #20* from the fold onward, which is the plan's own word doing what card #411's item 1 asked a lane to remember; that item is now the board's and #411's plan can drop it when its lane runs.
