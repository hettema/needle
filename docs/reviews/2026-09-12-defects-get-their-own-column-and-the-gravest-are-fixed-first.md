# Review — defects get their own column and the gravest are fixed first

**Plan:** docs/plans/done/2026-09-09-defects-get-their-own-column-and-the-gravest-are-fixed-first.md
**Reviewer:** a Claude session of the lane's own make (Opus, the `hrclaude` slot, session f53f2ba3 resumed from f1d4a6a3, a reading session whose home is the main checkout and not a card's lane — chosen so the call moved no finished card, the defect #123's lane filed on 2026-09-12), Needle call 108, reading the lane's worktree cold on this machine, asked through `/home/dennis/.cache/needle/notes/from-needle-100-defects-column-review.md`; its answer is beside the note (`from-f1d4a6a3-re-100-defects-column-review.md`) and quoted in the dispositions.
**Diff range:** ba76382..e03a46f (the revision frozen while read: the feature commit with the plan's stances). Repairs landed after it in 24473ed; the implementing lane verified them, and the reader is not represented as having read them.
**Findings:** 9 — seven repaired, one a record correction made in the plan, one recorded as a preference and repaired anyway since the fix was one word.
**Verification:** after 24473ed: `tests/api/test_defects_column.py` (7 passed, with new cases for findings 1, 5 and 9 and the assertions for 2), `tests/board/test_grade.py` (5 passed, a case for 8), `tests/api/test_dial.py` (16), `tests/api/test_the_triage_seat.py` (13), `tests/api/test_title.py` + `test_cli.py` + `test_api.py` (29), `tests/board` + `tests/infrastructure` + `tests/ratchets` (all passed in one run); `npx tsc --noEmit` clean and `npx vitest run` 83 passed; the frontend fixture regenerated and current; ruff clean on every changed file but the pre-existing long line in `api/board_cli.py`'s usage head. The full suite before the repairs, at e03a46f, one process per module six at a time: 781 passed, 1 failed — `tests/api/test_doors.py::test_a_lane_that_dies_on_a_limit_is_moved_and_the_card_says_where`, which expects "new window opened" in a lane's sentence and passes alone (a window-timing flake under six parallel processes, in a path this change does not touch).
**Completion:** Complete: every verified finding inside the change is repaired and its repair holds in a test; the record correction is made in the plan; the two hypotheses were repaired rather than argued, since each fix was smaller than its argument. What remains unverified: the served page in a browser after the restart — the API shows the column on every project and the page's tests hold it, but nobody has clicked it; the close's WATCH row is the signal that will.

## Dispositions

1. [seam] `api/dial.py:399-409` (`_wants_a_reading`) — the beat never asked whether a reading was already open on the card, so under a number above one it relaunched the same reading every beat (killed at `open_windowless_session`'s refusal) and read nothing else; demonstrated by the reader with a probe on the fixture (switch off, number 2, three beats: `triage-card-270` launched three times, card 271 never opened); violated item 4's "the number bounds readings while off as it bounds everything while on" and acceptance 2 — FIXED in 24473ed: `_take_next` hands `triage_open` in and the gate returns False on it, as `_wants_a_title_reading` did; `tests/api/test_defects_column.py::test_a_defect_verified_before_the_scale_is_read_again_before_it_is_taken` holds two unread defects under a number of two: one reading each, and a third beat launches nothing.
2. [feature] `board/assemble.py:1114-1131` (`defects_column`) — the line over the unread counted cards with no grade while the head line counted from routing and skipped a card whose routing is None (archived or gone document); demonstrated by the reader on a copy of this (rented) machine's store: Needle's head said "55 nobody has read yet" over a group headed "56 nobody has read yet" (#117, an archived suggestion still carded in Defects) — FIXED in 24473ed: both lines count one predicate (no grade standing for the document today); the head splits the graded by routing and names apart the cards with no live document ("N with no live document"); the frontend test and the item-3 floor test assert the two counts agree.
3. [record] `docs/plans/…-defects-get-their-own-column-…md` item 2's Met claimed the frontend fixture carried a reading from before the scale; it carries one graded triage only, and the case lives in `tests/api/test_defects_column.py` — CORRECTED in 24473ed: the sentence points at the test.
4. [verification] `tests/board/test_dial.py:4` — ruff I001, the renamed imports unsorted — FIXED in 24473ed (`ruff --fix`); ruff clean on the file.
5. [record] `frontend/src/board/OpenCard.tsx` — "the next reading grades it" showed for a pre-scale cannot-tell reading that `_wants_a_reading` excluded from re-reading (hypothesis; no such reading on the served store) — FIXED in 24473ed: a cannot-tell without a grade is read again like any ungraded reading, since the grade is from the document alone and not from the mark's evidence; the same floor test records a cannot-tell without a grade and sees its reading open.
6. [seam] `frontend/src/board/GroupBlock.tsx:32` — the board's own group over the unread was a drop target, and a defect dropped there from Not now would be refused naming a group the owner was looking at (hypothesis, refused truthfully) — FIXED in 24473ed: `useDroppable` is disabled for a machine group; `tsc` and `vitest` hold.
7. [record] `domain/document.py:204` — one docstring still said "the defects rail" — FIXED in 24473ed ("lands in Defects").
8. [seam] `domain/triage.py::Grade._whole` — a grade of `nothing` accepted stray `reach_words`/`often_words` (a preference; unreachable from `needle triage`, whose parts are token-and-words together) — FIXED in 24473ed regardless: the validator refuses any of the four on `nothing`; `tests/board/test_grade.py` holds it.
9. [verification] `api/board_cli.py::defects` — `--unplanned-over` counted from the latest reading's time, so a re-reading of a touched document restarted the Loop's seven days and the second Loop line under-read — FIXED in 24473ed: the clock is the first reading that graded the card (`_first_graded`); `tests/api/test_defects_column.py::test_the_loops_clock_is_the_first_grade_not_the_latest_reading` records a grade eight days old and one today and counts 1.

Verified by the reader on the way, no finding: migration 0023 on the store copy (the three strips became the column's unnamed group at position 0, 59/47/14 cards keeping group and position, Backlog's named groups untouched, boards with no strip showing an empty column, six nullable columns on `triages`); the door's grade refusal after every refusal of the mark's result and before anything is recorded; `order_key`'s ladder; the move refusals; a verified defect with no grade read before it is planned.

## Verification evidence

- Independent reading: Needle call 108 at e03a46f. The reader ran the diff whole, ruff, the touched backend modules (57 passed), the ratchets, `tsc` and `vitest` in the worktree; copied this (rented) machine's store to its job's tmp dir, ran migration 0023 on the copy and assembled every board with the lane's code; ran a probe test of the beat from its tmp dir with the tree's conftest loaded. No `needle` verb was run from the worktree and nothing was edited.
- Repairs, on 24473ed: `uv run pytest -q tests/api/test_defects_column.py` 7 passed; `tests/board/test_grade.py` 5 passed; `tests/api/test_dial.py` 16 passed; `tests/api/test_the_triage_seat.py` 13 passed; `tests/api/test_title.py tests/api/test_cli.py tests/api/test_api.py` 29 passed; `tests/board tests/infrastructure tests/ratchets` all passed in one run; ruff clean on every changed file but `api/board_cli.py:12` (pre-existing).
- Frontend, on 24473ed: `cd frontend && npx tsc --noEmit` clean; `npx vitest run` 83 passed; `uv run python tools/board_fixture.py` says the fixture is current.
- Migration on real data: 0023 rehearsed by this lane twice with `/home/dennis/.claude-accounts/hrclaude/jobs/abca6f41/tmp/rehearse.py` — on a copy of this (rented) machine's `~/.local/share/needle/needle.db` (0022; three boards with a strip: hellorevenue 59, needle 47, omarchy 14 cards; 0 triages — the copy the reader also used), and, before the restart, on a copy of the laptop's served store (0022; 66/55/15; 0 triages): after the upgrade the three group rows read `Defects`/NULL/0 with every card's group and position unchanged and six new nullable columns on `triages`; the downgrade put the rows back to `Backlog`/`Defects`/-1 and dropped the six. `needle defects needle` against the migrated rented copy (`NEEDLE_DATA_DIR` set to it) printed Needle's column, every card unread. Neither shared store was touched by a lane verb: every lane verb ran with `NEEDLE_DATA_DIR` on a copy, and board verbs through `uv --project /home/dennis/Work/needle`.
- The served board: after the fold (origin/develop 01faa3d) the laptop's checkout was level; the lane rebuilt `frontend/dist` there and restarted `needle-serve` at 20:50Z (a copy of the store from before, `~/.cache/needle/card-100-rehearsal/needle.db.before-restart-0022`, kept on the laptop); the board came back at 0023 with the Defects column first on every project — Needle 55 · hellorevenue 66 · omarchy 15, every one "nobody has read yet", Backlog holding no defect — and within two minutes, auto-fix off on every board, the first readings were open (`triaging 1`), which is item 4 on the real board.
- Full suite at e03a46f, every test module as its own pytest process six at a time (`/home/dennis/.claude-accounts/hrclaude/jobs/abca6f41/tmp/suite_parallel.sh`): 99 modules, 781 passed, 1 failed (`tests/api/test_doors.py::test_a_lane_that_dies_on_a_limit_is_moved_and_the_card_says_where`, "new window opened" missing from a lane's sentence under load; passes alone; a path the change does not touch).
- Not run: the served page in a browser after the restart — the API shows the column and the page's tests hold it, but nobody has clicked it; the close's WATCH row names the signal that reads the column on the served board.
