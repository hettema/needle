{
  "answer": "Independent review of card #149 at revision 72bac3d (branch card-149-auto-fix-stops-at-the-line-you-d), against the plan docs/plans/2026-09-15-auto-fix-stops-at-the-line-you-drew-and-leaves-the-rest-filed.md and HOW-WE-WORK Sec 13.\n\nThe promise and its supported inputs are as the note states: a line per board, a band of the ladder, set where the switch is set; the beat takes only a verified `now` defect at or above its board's line; a card below it says it is filed; the head counts by the same predicate; a plan written above the line and moved below it holds at Start; `needle fixes --below-the-line --count` counts the class; every board reads the last rung, so nothing changes behaviour by the landing.\n\nThe shape holds. The comparison is written once (`board/triage.py::at_or_above`) and every reader goes through it; `why_not_eligible` carries the line as one more fact, so the beat, the waiting rows and the face say one sentence; both production callers of `held_lanes` pass the `below_line` reader; `line_at` reads the audit in the order of moments as `switch_was_on` does; NULL and the last rung are one fact through `_line_of`, and `_dial` with no row falls to the field default rather than touching `row.line`. I checked the two things most likely to be wrong and both are already held: `record_defects_at_on` is idempotent per board, so a line move that re-posts `on: true` cannot re-record a board's baseline; and `_plan` opens the fix lane with the decision of the same reading whose grade `_take_next` compared, so the band `fixes` compares is the band the beat compared.\n\nFINDINGS\n\n1. [seam] api/dial.py:1377 - `needle fixes` promises the one operative reason the dial leaves a defect; with a board's switch off and a line drawn, `why_not_eligible` (board/dial.py:266) answers with the line's sentence, so the fallback at api/dial.py:1377 that would say \"this board's switch is off\" is never reached for any card below the line. Consequence: in the exact state item 5 creates - Hello Revenue's switch off since 2026-09-05, its line drawn at harm outside - the owner reads \"below this board's line at harm outside\" for the defects under the line and \"this board's switch is off\" only for those at or above it, and the board no longer tells him plainly that nothing runs there at all. Both sentences are true, so this is not a wrong statement; it is the less operative one winning. Fix direction, if you take it: order the switch check before the line check in `waiting`, the way `_start` does (the switch first, the line second). - demonstrated (read board/dial.py::why_not_eligible and api/dial.py::waiting at 72bac3d; the precedence is static, not state-dependent).\n\n2. [feature] board/assemble.py:910 - item 3 promises the face of a defect below the line says what would take it: \"taken the moment you move the line or a fresh reading grades it graver\". The `filed` flag is computed from the routing, the grade and the line only; it does not ask which column the card stands in, while the beat walks only the Defects column (board/dial.py:140, `card.place.column != Column.DEFECTS`). Consequence: a graded verified-`now` defect card that has left Defects - `PROMOTED_FROM` in board/reconcile.py:49 includes Defects, and the Decision-moment branch at board/assemble.py:782 returns before this one, so Backlog, Up next and Not now reach it - would carry a sentence that is false for it: moving the line would not take that card. Fix direction: gate `filed` on the same predicate the beat uses. - hypothesis (verified by reading that the branch is column-blind and that the beat is not; I did not put a graded defect card outside Defects to see the face).\n\n3. [boundary] frontend/src/components/ui/index.tsx:195 and board/triage.py:361 - ruling 4 promises \"the page and the terminal never differ\". Two phrase tables now carry the same six strings, each pinned only by its own test (tests/board/test_the_line.py asserts the Python literals, frontend/tests/board.test.tsx asserts the rendered option labels). Nothing compares the two. `Record<Band, string>` makes tsc refuse a missing rung, so a new band cannot drift, but a reworded phrase can: one side plus its own test is a coherent edit that leaves the other side saying something else, and the failure is silent. Needle's own rule is that a boundary that matters is a ratchet, and Sec 5's discriminator - silent or late gets a mechanism - points the same way. - hypothesis (I read both tables and searched tests/ratchets and every test that reads frontend/src; none compares them).\n\n4. [record] domain/dial.py:45 - at 72bac3d the `changed_at` docstring says \"When the owner last turned this board's switch; None while it has never been touched\", while infrastructure/store.py::turn_dial now also stamps `row.changed_at` on a line move, and the head prints \"changed <then>\" from it. Already corrected on the branch: commit 91f8b68 rewrites exactly this docstring. Noting it so the review record shows it was read, not to ask for anything. - demonstrated, resolved.\n\n5. [record] docs/HOW-WE-WORK.md:241 - the Sec 10 edit left one line at 125 characters in a file otherwise wrapped at about 78 (the next longest is 83). The sentence is right and the four texts agree - Sec 10, docs/vocabulary.md, domain/dial.py's module docstring and the control's tooltip all say a switch per board, a line per board, one number for the machine. Only the wrap. - preference.\n\nVERIFICATION\n\n`tests/api/test_defects_column.py` failed once for me on its first run at this tree: `test_a_defect_verified_before_the_scale_is_read_again_before_it_is_taken` expected \"verified before the board graded defects; a reading grades it first\" and got \"a reading is open on its mark now\". It passed in isolation and passed again on a full re-run of the module, all seven green. I could not attribute it to this change - both sentences pre-date it, the readings path is untouched, and the module launches real reading sessions, so the failure looks like cross-talk between its own tests rather than the line. Raising it because item 3's Met line claims the module green unchanged; that claim is right more often than not, and the module is not reliably green on this machine.\n\nWhat I ran, from the lane's worktree: `uv run pytest tests/api/test_the_line.py` (2 passed), `tests/board/test_the_line.py` with `tests/board/test_dial.py` (26 passed), `tests/api/test_dial.py` (17 passed), `tests/api/test_defects_column.py` (7 passed on the second run, 1 failed on the first, above), `tests/ratchets` (all green but `test_the_fixture_project_is_synthetic.py::test_no_real_card_title_is_in_the_tracked_tree`, which is the trunk red you already named, from 9e90300, on the #150 suggestion's line 13 - not this lane's), and in frontend/: `npx tsc --noEmit` clean, `npx vitest run` 86 passed. Those numbers match the plan's Met lines.\n\nOne note on the frozen revision: the branch has moved to 91f8b68 since the note was written. Both commits after 72bac3d are documents - a suggestion file and the docstring commit that resolves finding 4 - so nothing here is stale except that finding.\n\nFindings: 5 (1 seam, 1 feature, 1 boundary, 2 record; of which 1 demonstrated and resolved already, 1 preference), plus one verification note.\n",
  "how_known": "checked",
  "sources": [
    "docs/reviews/notes/2026-09-15-149-cold-read.md (the question and the frozen revision)",
    "docs/HOW-WE-WORK.md Sec 13 (the review contract) and Sec 10 as edited by this change",
    "docs/plans/2026-09-15-auto-fix-stops-at-the-line-you-drew-and-leaves-the-rest-filed.md (intent, six items with done-means, rulings 1-6, Deliberately not, Loop)",
    "git diff 5cd9539..72bac3d, read whole: board/triage.py, board/dial.py, board/assemble.py, api/dial.py, api/app.py, api/board_cli.py, domain/dial.py, infrastructure/store.py, infrastructure/schema.py, infrastructure/live.py, migration 0028, frontend/src/{api.ts,board/Board.tsx,components/ui/index.tsx,components/ui/primitives.css,types/dial.ts}, frontend/tests/{board.test.tsx,fixture.json}, tests/api/test_the_line.py, tests/board/test_the_line.py, tests/api/test_dial.py, docs/vocabulary.md",
    "Files read at 72bac3d beyond the diff, for the dependencies: infrastructure/store.py::{dial,dials,_dial,_line_of,_declare,record_defects_at_on,triages,triage}, api/dial.py::{turn,tick_now,_take_next,_plan,_start,waiting,fixes}, board/dial.py::{column_defects,switch_was_on,line_at,held_lanes,why_not_eligible}, board/assemble.py::{state_of,summarize,defects_column,is_trigger_card}, board/triage.py::{band_of,BANDS,at_or_above}, domain/triage.py::Band, board/reconcile.py::PROMOTED_FROM",
    "Searches: every caller of held_lanes and why_not_eligible across the tree; every Column.DEFECTS use in board/, api/, domain/; tests/ratchets/ listing and every test that reads frontend/src",
    "Runs from the lane's worktree: uv run pytest on tests/api/test_the_line.py, tests/board/test_the_line.py, tests/board/test_dial.py, tests/api/test_dial.py, tests/api/test_defects_column.py (twice, and the failing test alone), tests/ratchets; npx tsc --noEmit and npx vitest run in frontend/",
    "git log and git diff 72bac3d..91f8b68, to see what the branch did after the frozen revision"
  ]
}
