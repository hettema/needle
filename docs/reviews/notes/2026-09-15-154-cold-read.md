# Independent review — Needle card #154, the board's eyes do not close while its hands are full

You are the independent reader under `docs/HOW-WE-WORK.md` §13. Read the
complete change and the dependencies it touches, and report findings. Do not
edit anything; do not fix anything.

**Where:** /home/dennis/Work/needle/.claude/worktrees/card-154-the-board-s-eyes-do-not-close-wh
**Revision:** acbb5b7, frozen while you read (`git diff f89c770..acbb5b7`)
**Plan:** docs/plans/2026-09-15-the-boards-eyes-do-not-close-while-its-hands-are-full.md

**The promise.** Readings — a defect's mark graded, a plan or idea's title
read cold, a card parked on the owner looked at — were counted against the
same number that bounds fix lanes, so auto-fix at its number stopped the
board looking at anything. After this change: `board/dial.py::running`
counts fix lanes only; readings have their own constant bound
`READINGS_AT_ONCE`; the memory floor still stops both and stops them first;
`needle dial` and the board's head report both pairs; and
`needle fixes <slug|all> --reading-gaps [--count]` counts the whole hours of
the last day in which a fix lane ran and no reading opened.

**Supported inputs.** Any number of boards; the machine's number including
0; readings open across several projects; a machine under its memory floor;
a store with no lanes and no readings; a board whose switch is off.

**Read especially:**
- `api/dial.py::_take_next` — two bounds where there was one. Every path:
  number full and readings free, readings full and number free, both full,
  both free, and the floor before either. Can the beat now do two acts in
  one beat, or open a reading it should not — a card with a lane on it, the
  per-card fuse of card #138, the seat's stance in `seat_opens`, card #148's
  left-out projects, card #82's order between defects, titles and parked
  cards?
- `board/dial.py::running` — does anything still pass or expect `triaging=`?
- `board/dial.py::reading_gaps` — hour arithmetic and timezone, a lane with
  no `ended_at`, a lane that ended before the window, the hour still
  running, and whether "a fix lane ran" reading the planning start is right.
- `infrastructure/live.py::readings_open` — one reader where the beat and
  the head each had their own sum; is every caller correct?
- `api/board_cli.py` — `needle dial`'s last line, `needle fixes
  --reading-gaps`, and the `--count` guard beside the other two flags.
- `domain/dial.py` — `DialState.readings_at_most`, `Fixes.reading_gaps`, and
  the generated `frontend/src/types/dial.ts`, `frontend/tests/fixture.json`,
  `frontend/src/components/ui/index.tsx`.
- The tests changed in `tests/api/test_dial.py`,
  `tests/api/test_defects_column.py` and `tests/board/test_dial.py`: did any
  edit weaken what it pinned rather than restate it?

Report every finding with file, line, the promise it violates and the
consequence, and say whether it is demonstrated or an unverified hypothesis.
Say plainly if you find nothing. Write your answer to the answer file.
