{"answer":"Four material corrections before build. These are demonstrated gaps in the proposed reader reuse; I checked them with executable examples against the current code.\n\n1. **Select the current request’s text.** Plan lines 30–38 rely on `board/lane.py:782`, which selects the first ASK/Q row but accepts any current writing of that kind. `infrastructure/store.py:799` appends ASK/Q rows. With an old ASK followed by a current ASK, the reader returned **“OLD checkpoint.”** Bind the request text to the current writing, including repeated checkpoints. Add an old-plus-new ASK regression; the proposed stale-only test misses this.\n\n2. **Recognize answers before promoting this reader into exit and display.** Plan lines 25 and 50 promise that answered requests do not park work. `owner_decision_outstanding` never reads ANSWERED. `api/doors.py:570` records a successful answer without removing ASK or resetting the lane record. My ASK → successful ANSWERED example still returned an outstanding request; `board/parked.py::_answered_after` already recognizes that answer. Reconcile these semantics through a shared reader and test answer → resumed work → silent ending, plus a new request after that answer. Coordinate the narrow overlap with #136; its broader row-retirement work need not enter this lane. Do not blindly treat every ANSWERED event as successful delivery: `api/doors.py:565` records failed delivery under that kind too.\n\n3. **Compare RULING/RULED order within the lane’s life.** At `board/lane.py:789–792`, any current RULED suppresses every current RULING. My RULED → new RULING example returned **None**. Compare the answer with the request it follows, and test both orderings. To answer your specific cross-life question: an older RULED before `since` does not suppress a new RULING; a RULED earlier in the same life does. Reusing this unchanged violates the checkpoint promise.\n\n4. **Give the face the same fallback coverage as the exit.** The proposed facts population covers only cards with lane records, while `api/loops.py:2233` explicitly supports their absence. I constructed an ended session without a record: it passes the exit guard and `disposition` returns OWNERS, but a record-only `standing` map leaves its sentence broken. Populate current-request facts for the cards whose lanes are evaluated, using the same history fallback, and cover this case alongside the recorded-lane regression.\n\nThe proposed placement **after the existing guard is sound** for preserving its established boundary. At that point, the extra disposition branches you named are unreachable; current close precedence remains intact. Keep tests for the guard and higher-priority exits.\n\nASK is supported by `needle row`. Add ASK to the brief’s example list; teaching Q as another handoff is unnecessary. Qualify the new sentence about lanes without ASK returning to the queue: closes, folds, prose questions and the guard remain exceptions. Include a continuation check through the existing Resume door: ended lanes do not ordinarily offer Answer, so the regression should prove that the same work can continue without a new Start.\n\nNo files changed; these corrections are for the caller to land.","how_known":"checked","sources":["docs/plans/2026-09-16-work-waiting-for-your-feedback-stays-ready-for-your-answer.md:25","board/lane.py:774","board/lane.py:834","board/lane.py:1255","infrastructure/store.py:774","api/doors.py:545","api/doors.py:666","api/loops.py:1366","api/loops.py:2233","board/parked.py::_answered_after","Read-only Python reproductions using tests/board/test_lane.py fixtures: repeated ASK, answered ASK, reordered RULING/RULED, and ended session without a lane record"]}