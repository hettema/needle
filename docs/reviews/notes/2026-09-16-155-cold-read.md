# Cold read — card #155, Work waiting for your feedback stays ready for your answer

Your seat: the independent reviewer HOW-WE-WORK §13 asks for, a colleague of the other make with no share of the writer's context. Read `docs/HOW-WE-WORK.md` §13 first; it is the contract. Read-only. Answer in the shape below.

## The promise

`docs/plans/done/2026-09-16-work-waiting-for-your-feedback-stays-ready-for-your-answer.md` — the plan, with the challenge round's corrections already landed in it (call 240; the note and answer are `docs/reviews/notes/2026-09-16-155-challenge.md` and `from-codex-re-2026-09-16-155-challenge.md`). The intent: work that has reached a planned human-feedback checkpoint stays visibly ready for that feedback when its session exits, with its existing work preserved and a clear path to continue; it must not look like failed work to start again.

## The revision

3e8f0b6 — the commit on this worktree's branch; read it with `git show 3e8f0b6 --stat` and `git diff ccb7b74..3e8f0b6` where BASE is `ccb7b74` (the trunk the lane started from, which already carries the plan). Files: `board/lane.py`, `board/parked.py`, `api/loops.py`, `api/doors.py`, `tests/board/test_lane.py`, `tests/api/test_doors.py`, the plan.

## Supported inputs

A card in Executing whose lane ended (session gone, worktree on disk or not) with: nothing folded; a fold nobody wrote up; a landed close; a stale DELIVERED; an ASK, Q or RULING row written in this life of the lane or before it; an ANSWERED audit row after or before the newest such row; a RULED before or after the newest RULING; a WAITS row alone; last words that ask or do not; a lane record or none. The brief a launched worker opens with. The parked card's commitments reader in `board/parked.py`, which now imports the one `answered_after`.

## What to read for

- `board/lane.py::exit_for` — the new branch sits after the `folded is None and lane.session is None` guard and asks `disposition`; everything above keeps its order. Is any lane that reaches it wrongly the owner's? Is any lane that should be his still sent back?
- `board/lane.py::owner_decision_outstanding` — newest row of each kind, `answered_after`, a RULED only after the RULING it follows. Reproduce the three shapes the challenge round found and any fourth.
- `board/lane.py::life_of` — one reader of a lane's life for the recovery, the exit and the face. Is it the same as the two inlined forms it replaced?
- `api/loops.py::_standing` and `LaneFacts.standing` — computed for cards with a record, in Executing or in Decision moment. A lane the exit judges that the face does not; a cost you would refuse.
- `board/lane.py::lane_for` ended branch — the `standing` sentence sits after `asks_owner(said)` and before `on_disk`. Does the face ever disagree with the exit or with `board/assemble.py::_lane_died`?
- `api/doors.py` — the brief's new paragraph: true to the code on every clause?
- Tests: do the new cases pin the intent, or the method? Anything the plan's done-means promises that no test holds — the record-less lane, the answered ASK, the old-plus-new ASK, Resume offered.
- Concurrency, restart, the truth of what the person sees on the page: the face word "asked you" for a Decision moment card with an ended lane.

## The answer's shape

Every finding names its file, line, the violated promise and the consequence; say whether it is a demonstrated defect (with the reproduction), an unverified hypothesis, or a preference. An equally truthful wording is not a defect. Say which suites or probes you ran and on which revision. If you find nothing, say zero findings and what you checked.
