{
  "answer": "Independent review of card #149 at revision 72bac3d (branch card-149-auto-fix-stops-at-the-line-you-d), against the plan docs/plans/2026-09-15-auto-fix-stops-at-the-line-you-drew-and-leaves-the-rest-filed.md and HOW-WE-WORK §13. Read the complete diff 5cd9539..72bac3d and the files at that revision; the working tree is three docs-only commits ahead of 72bac3d (e497d83, 0451fda, 1144137 — suggestions only; `git diff 72bac3d HEAD --stat` touches no code and no plan).\n\n[feature] api/board_cli.py:1158 — `needle fixes` prints \"its card was at or above its board's line\" for every lane whose `below_the_line` is False, but api/dial.py:1631 sets False both for a lane genuinely at or above the line and for a lane with no band at all (`band is not None and (...)`); domain/dial.py's own field docstring says so: \"False for a lane from before the seat graded defects, whose reading has no band to compare\". So the verb states a check that was never made. — The owner reads `needle fixes` to judge the Loop this card writes; an unchecked lane prints the same clean verdict as a checked one, which is the \"shows something false as true\" kind on the one verb the Loop is read through. On a copy of ~/.local/share/needle/needle.db all 42 fix lanes have `decision` NULL, so every one of them would print it (that store is the rented machine's stale copy — newest lane 2026-09-05, no triages at all — so I have not counted how many such lanes the live board holds). Suggested shape: say \"no band to compare\" for those lanes, or make the fact tri-state. — demonstrated (read api/dial.py:1620-1640 and api/board_cli.py:1145-1160 at 72bac3d; counted the lanes with a sqlite read of a copy of the store)\n\n[seam] frontend/src/components/ui/index.tsx:195 — a second, hand-written LINE_WORDS map duplicating board/triage.py:361, with nothing holding the two together. Ruling 4 promises \"the line is one phrase wherever it is said — the head, its said-sentence, `needle dial`, `needle fixes` — so the page and the terminal never differ\", and CLAUDE.md holds a boundary that matters with a ratchet, not a convention. This project already has the mechanism for exactly this map: REACH_WORDS, OFTEN_WORDS and BREAKS_WORDS are generated into frontend/src/types/triage.ts from domain/triage.py (\"// Generated by `uv run needle types` ... do not edit\"), which is why no one can make the page and the terminal disagree about a grade's words. LINE_WORDS sits in board/triage.py instead, outside what typegen mirrors, and was copied by hand. — Editing either phrase leaves the head's select and its said-sentence saying one thing and `needle dial`/`needle fixes` another, silently: the divergence ruling 4 exists to prevent, with nothing that fails. Both sides are asserted against literals independently (tests/board/test_the_line.py:46-48 on the Python map, frontend/tests/board.test.tsx:1078-1079 on the option labels) and nothing compares them: `grep -rn LINE_WORDS tests/ frontend/tests/` finds only the Python side. — demonstrated (read both maps and frontend/src/types/triage.ts:155-182 at 72bac3d; grepped the tests)\n\n[verification] tests/api/test_the_line.py:239 — the Loop's counter is never shown to fire. `below_the_line` is asserted only False, for three lanes, and no test anywhere produces True; the class-closer stops the beat before a below-line lane can exist, so the whole detector is exercised in one direction. The plan's Loop (line 90) watches `needle fixes all --below-the-line --count` expect 0 daily until 2026-10-15 — a number that is today indistinguishable from a number that cannot move. §7 asks for an observation the thesis predicts and the rival explanations do not; §5 names a trace the actor can write without doing the act. — I satisfied myself by construction that the flag can fire (api/dial.py:783 stores the verified reading's decision on the lane, and `store.triages(project)` keys the same rows, so the join in api/dial.py:1590-1597 resolves for any lane planned since card #100), so this is a coverage gap, not a dead metric — but nothing in the suite would tell the next session that. A test that plans a lane and then asserts the count reads 1 (or that `line_at`+`at_or_above` return True for a below-line planning moment) closes it. — demonstrated (grep for below_the_line across tests/ and frontend/tests/; read api/dial.py:1580-1640 and infrastructure/store.py:1809-1852)\n\n[record] domain/dial.py:45 — `changed_at`'s docstring still says \"When the owner last turned this board's switch; None while it has never been touched\", but infrastructure/store.py:1651-1656 now stamps `changed_at` on a line move with no switch turn. tests/api/test_the_line.py:124-125 demonstrates it: `needle dial proj --line \"Harm Outside\"` on a board whose switch was never turned prints \"proj: auto-fix off, stops at harm outside; changed …\". The same file's DialChange docstring (domain/dial.py:67-70) still offers two shapes — a row naming a board and a setting, or a row naming the number and no setting — while a line-only move now writes a third: names a board, `on` is None. — A cold session reads the timestamp as the switch's last turn and the audit row as a number change; both are wrong for a line move, and a claim in a docstring is what the next session has instead of this one. Nothing in the code depends on it today (only api/board_cli.py:966 prints it), so the cost is the written record, not behaviour. — demonstrated (read domain/dial.py:32-92 and infrastructure/store.py:1620-1670 at 72bac3d against tests/api/test_the_line.py:114-133)\n\n[record] docs/plans/2026-09-15-auto-fix-stops-at-the-line-you-drew-and-leaves-the-rest-filed.md:48 — item 3's Met says \"tests/api/test_defects_column.py green unchanged\". At this revision that module's verdict is not repeatable: my first full-module run failed test_a_defects_reading_without_a_grade_is_refused_and_a_graded_one_shows_its_word (\"assert 'your ruling' == 'loses'\"), a second identical run of the same module at the same revision passed, and the same module run from an export of the merge-base 5cd9539 failed a different test (test_the_column_the_beat_and_the_verb_read_one_order_gravest_first, \"assert [270, 272, 273, 273] == [270, 272, 271, 273]\"). tests/api/test_the_triage_seat.py behaves the same way: 2 failures + 1 error (one a subprocess.TimeoutExpired) inside my sequence, 1 failure with a different cause (StoreRefusal: \"#270 already has a triage session open\") running the module alone, and the failing test passes by itself. None of this is attributable to this lane — it is card #145 (the suite gives the same verdict twice), plus the face collision filed on 2026-09-14 (docs/slice-suggestions/2026-09-14-the-face-of-a-graded-defect-says-what-the-reading-graded-it.md), both outside this change and both correctly left outside it. — The claim is not false so much as unsupportable as written, and a close that reads \"green\" hands the next lane a verdict this revision does not actually give. The record should say what it says for the synthetic-fixture ratchet: which modules answer differently run to run, with the run that was green, and name #145. — demonstrated (ran each module myself at this revision and one at an export of 5cd9539; commands and outputs listed in sources)\n\n[preference] api/dial.py:1378 — `waiting` returns why_not_eligible's below-line sentence before it reaches \"this board's switch is off\", while `_start` (api/dial.py:986-987) puts the switch first. At the close Hello Revenue has its switch off and its line at harm outside, so `needle fixes hellorevenue` will report \"below this board's line at harm outside\" for the cards under the band and \"this board's switch is off\" for the rest, and a lane's Start note will use the other order. Both sentences are true and the head line above them already prints \"auto-fix off, stops at harm outside\", so this is a preference about which fact leads, not a defect. — preference\n\nFindings: 6\n\nWhat I ran, all from the worktree at this revision unless noted:\n- uv run pytest -q tests/board/test_the_line.py — 6 passed.\n- uv run pytest -q tests/api/test_the_line.py — 2 passed.\n- uv run pytest -q tests/api/test_dial.py — 17 passed.\n- uv run pytest -q tests/board/test_dial.py — 22 passed; tests/board/test_grade.py — 6 passed.\n- uv run pytest -q tests/ratchets — one failure, test_the_fixture_project_is_synthetic.py::test_no_real_card_title_is_in_the_tracked_tree, on docs/slice-suggestions/2026-09-15-a-plan-that-takes-over-an-older-card-gives-it-the-plans-title.md:13, which came in on the trunk with 9e90300 and is not this lane's (already known, not reported). Everything else green, including test_typed_edges.py (so frontend/src/types/dial.ts is what the generator writes) and test_the_store_holds_its_shape.py (so the migrated database matches the models).\n- tests/api/test_defects_column.py and tests/api/test_the_triage_seat.py — the unstable results in the fifth finding.\n- cd frontend && npx tsc --noEmit — clean; npx vitest run — 86 passed, 2 files.\n- Migration 0028 rehearsed on a copy of ~/.local/share/needle/needle.db (copied first; the real store untouched): alembic_version 0027 -> 0028, `line` added to dial and dial_changes, and all five boards read `line = nothing` afterwards — item 1's \"no board changes behaviour by this landing\" holds on real data. The downgrade run on the same copy restored 0027's column list exactly on both tables.\n\nWhat I checked and found clean, so the lane does not have to re-verify it:\n- The comparison is written once. board/triage.py:355 `at_or_above` uses BANDS.index, the enum's order, and the last rung is above every band; board/dial.py:266, board/assemble.py:909-914 and api/dial.py:1631 all go through it, and nothing matches on band words.\n- Ruling 6 holds — the readings path really is untouched by the line. A below-line verified defect now reaches the `elif self._wants_a_reading(...)` arm in api/dial.py:456 where before it never did, which is the one place the line could have started spending reading sessions on filed cards; it cannot, because board/dial.py:505 stances Reason.NOW and Reason.NOW_OVER_MARK as False in SEAT_OPENS, and the one exception above it (a reading with no grade) was already taking that arm before this change.\n- turn_dial's no-ops and its audit. A line move alone writes a row with `on` None, which switch_was_on (board/dial.py:388) skips, so the switch's history is undisturbed; a switch turn carries `_line_of(row).value`, never None, so line_at has a row to read from the moment this lands; `_declare` is still reached only when `on` is not None, and cannot_undo/hold with a line-only move is refused by the pre-existing `not on` guard.\n- _dial handles a board with no row (infrastructure/store.py:3284 returns before _line_of), so a board that was never turned reads the last rung rather than raising.\n- The head and the face read the same predicate as the beat, off the same `current_grade`, and defects_column guards `summary.grade is None` before band_of. Live.below_line reads the newest reading's grade instead, which is deliberate and documented (a planned card's document is its plan, so the fingerprint-bound grade is gone) and is the conservative direction at a Start.\n- held_lanes still filters on FixStage.PLANNED and the new reader is the last of four ors, so a below-line plan is held and not counted as running — matching ruling 5.\n- The `line` local in board/assemble.py:1489-1492 shadows the Band kwarg name but is assigned after it is used; correct, if worth a second look by anyone editing there.\n- HOW-WE-HOLD-IT §10 claims three held clauses and the dial is not among them, so the new sentence in HOW-WE-WORK §10 does not create an unheld claim.\n- docs/HOW-WE-WORK.md:240 is left with one line running well past the file's wrap; cosmetic, not counted above.",
  "how_known": "checked",
  "sources": [
    "docs/reviews/notes/2026-09-15-149-cold-read.md (the thread and the question)",
    "docs/HOW-WE-WORK.md §13 (the review contract) and §10 and §5 and §7, read at 72bac3d",
    "docs/plans/2026-09-15-auto-fix-stops-at-the-line-you-drew-and-leaves-the-rest-filed.md at 72bac3d (intent, six items with done-means, rulings 1-6, Deliberately not, the Loop)",
    "git diff 5cd9539..72bac3d, whole, and the changed files read at 72bac3d via git show",
    "git diff 72bac3d HEAD --stat (three docs-only commits ahead; no code, no plan)",
    "board/triage.py:355-395, board/dial.py:227-310 and :379-412 and :495-526, api/dial.py:213-246, :375-470, :760-790, :974-1000, :1320-1400, :1580-1640, board/assemble.py:644-930 and :1277-1345 and :1471-1495, infrastructure/store.py:1579-1670, :1718-1726, :1809-1852, :3281-3300, infrastructure/schema.py DialRow/DialChangeRow, infrastructure/migrations/versions/0028_auto_fix_stops_at_the_line_the_owner_drew.py, domain/dial.py:32-92 and :360-390, api/app.py DialBody, api/board_cli.py:931-1160 and :1609-1655",
    "frontend/src/components/ui/index.tsx:191-262, frontend/src/board/Board.tsx:290-305, frontend/src/api.ts:106-114, frontend/src/types/dial.ts, frontend/src/types/triage.ts:1-8 and :150-182, frontend/tests/board.test.tsx:1045-1092, frontend/tests/fixture.json",
    "tests/api/test_the_line.py and tests/board/test_the_line.py, whole; the edits to tests/api/test_dial.py",
    "docs/slice-suggestions/2026-09-14-the-face-of-a-graded-defect-says-what-the-reading-graded-it.md and docs/slice-suggestions/2026-09-14-the-suite-gives-the-same-verdict-twice.md (card #145)",
    "docs/HOW-WE-HOLD-IT.md §10, docs/vocabulary.md, CLAUDE.md",
    "runs: uv run pytest -q on tests/board/test_the_line.py (6 passed), tests/api/test_the_line.py (2 passed), tests/api/test_dial.py (17 passed), tests/board/test_dial.py (22 passed), tests/board/test_grade.py (6 passed), tests/ratchets (1 failure, the known trunk-level synthetic-fixture one)",
    "runs: tests/api/test_defects_column.py full module twice at 72bac3d (red then green) and once from a `git archive 5cd9539` export (red, a different test); the failing test alone at 72bac3d (passed)",
    "runs: tests/api/test_the_triage_seat.py in sequence (2 failed + 1 error) and alone (1 failed, different cause), and the failing test by itself (passed)",
    "runs: cd frontend && npx tsc --noEmit (clean) and npx vitest run (86 passed)",
    "run: alembic upgrade to 0028 and downgrade -1 on a copy of ~/.local/share/needle/needle.db in the job's tmp; sqlite reads of pragma table_info(dial), table_info(dial_changes), alembic_version, and of fix_lanes.decision against triages (42 lanes, all decision NULL)"
  ]
}
