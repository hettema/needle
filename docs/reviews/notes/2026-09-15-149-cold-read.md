# Cold read of card #149 — auto-fix stops at the line you drew and leaves the rest filed

You are the independent reader HOW-WE-WORK §13 asks for. Read the contract first: `/home/dennis/Work/needle/docs/HOW-WE-WORK.md` §13. You read; you do not edit, and you never `EnterWorktree`.

**Revision frozen for you:** `72bac3d` on branch `card-149-auto-fix-stops-at-the-line-you-d` in the worktree `/home/dennis/Work/needle/.claude/worktrees/card-149-auto-fix-stops-at-the-line-you-d`. Diff range: `5cd9539..72bac3d` (the commit before it, `c4076a5`, is a docs-only note on #29 and not part of the change). Read with `git -C <that worktree> diff 5cd9539..72bac3d` and the files at that revision.

**The plan and its promise:** `docs/plans/2026-09-15-auto-fix-stops-at-the-line-you-drew-and-leaves-the-rest-filed.md` in that worktree — intent, six items with done-means, rulings 1–6, "Deliberately not". The promise: the auto-fix setting gains a line per board, a band of the grade's ladder (`domain/triage.py::Band`), set where the switch is set and shown beside it; the beat takes only a verified `now` defect whose band is at or above its board's line; a card below it shows on its face that it is filed and what would take it; the head's Defects count reads the line as the beat does; a plan written above the line and the line then moved below it holds at Start and starts by itself when the line moves back; `needle fixes <slug|all> --below-the-line --count` counts every fix lane that began below its board's line; every board reads the last rung (every defect) until the owner moves it, so nothing changes behaviour by this landing. Supported inputs: the route `POST /api/dial` with `line` as a `Band` value or absent; the verb `needle dial <slug> --line <rung>` with a band's words or `every defect`; the select on the head's control.

**What to read, and against what:**
- `infrastructure/store.py::turn_dial`, `_line_of`, `dial_changes`, `_dial`; `infrastructure/schema.py` DialRow/DialChangeRow; migration 0028 — against ruling 3 (NULL and the last rung are one fact; no clearing word), the audit shape (`line` on every board row after the turn, None on a number change), and the no-op rule (nothing written on a move to the same rung, or to the last rung on a board with no row).
- `board/triage.py::at_or_above`, `LINE_WORDS`, `line_from_words`, `line_rungs` — the one comparison and the one phrase (ruling 4).
- `board/dial.py::why_not_eligible` (the `grade`/`line` parameters), `below_line_words`, `held_lanes` (the `below_line` reader), `line_at` — against `switch_was_on`'s shape.
- `api/dial.py::_take_next`, `_start`, `waiting`, `fixes`, `turn`; `infrastructure/live.py::below_line`, `dial_state` — the beat never appends a candidate below the line; the readings path is untouched (ruling 6, #100 ruling 5); the Start hold reads the card's newest reading's grade and why (a planned card's document is the plan, so the fingerprint-bound grade is gone); `below_the_line` in `fixes` reads the line at the planning moment and at the Start moment from the audit and the band from the reading the lane's decision came from.
- `board/assemble.py::defects_column` (the `line` parameter and the "filed below the line" part), `state_of` (the `then` clause of a graded verified-`now` face below the line), `summarize`, `assemble_board`, `assemble_detail` — the head and the face read the same predicate; with the line at the last rung nothing reads differently from before.
- `api/app.py::DialBody.line`; `api/board_cli.py::dial` and `fixes` — the refusals, the printed words, `--count` with either selector.
- `frontend/src/components/ui/index.tsx::DialControl` and `LINE_WORDS`, `frontend/src/board/Board.tsx::turnTheDial`, `frontend/src/api.ts::turnDial`, the regenerated `frontend/src/types/dial.ts`, `frontend/tests/board.test.tsx` — the select posts `{project, on, lanes, line}`, the head's word after a turn, the switch turn carrying the line as it stands.
- The tests: `tests/api/test_the_line.py`, `tests/board/test_the_line.py`, the edits to `tests/api/test_dial.py`. Do they hold the done-means, or only the implementation?
- `docs/HOW-WE-WORK.md` §10, `docs/vocabulary.md`, `domain/dial.py`'s docstring, the control's tooltip — do the four agree?

Concurrency, restart, failure: two boards' turns under the loops' lock; a restart reading the migrated store; a board with no row; a card with no graded reading; a line moved while a planning session is open on a card now below it (what happens to that lane, and is it right?); a lane's `decision` naming a reading that was later re-read with a graver grade (which band does `fixes` compare?).

**What I already know and do not need told again:** `tests/ratchets/test_the_fixture_project_is_synthetic.py` is red on the trunk from `9e90300` (not this lane's; evidence appended to #29 in `c4076a5`). The served-page check with two boards is deviated to the first turn after the fold, as #80's was.

**Answer in this form**, written to the answer file `needle call` gives you (beside this note if none): one line per finding — `[feature|seam|boundary|verification|record] <file:line> — <violated promise> — <consequence> — demonstrated | hypothesis | preference`, with what you ran or read to know it; then a line `Findings: N` and, if none, `Findings: 0` with the revision and what you read. Distinguish a demonstrated defect from an unverified hypothesis and from a preference. An equally truthful wording is not a defect.
