# Challenge before build — card #155, Work waiting for your feedback stays ready for your answer

Your seat, as the board assigned it: YOUR TEAM — a different-make challenge — the hand is claude, hrclaude; codex challenges; exploring different-make. You are the challenging colleague of the other make. Read-only: read the plan, the terrain and my intended change, and land material corrections before I build. Number each correction, name file and line, and say whether it is a demonstrated defect in the plan, an unverified hypothesis, or a preference. Say "no material corrections" if the round changes nothing.

## The plan

`docs/plans/2026-09-16-work-waiting-for-your-feedback-stays-ready-for-your-answer.md` — read it whole. The "Where to look" paragraph under item 1 is my intended change, written after reading the terrain.

## The failure it corrects

Hello Revenue #601 reached its planned owner-calibration checkpoint. The session ended after telling Dennis and Maria to mark four ads (imperative prose, no question mark, plus a WAITS row). The board recorded Executing → Up next, "lane ended with nothing folded", so the work read as failed and ready to start again. A diagnosing session then wrote an ASK row by hand and moved the card to Decision moment.

## The terrain, as I read it

- `board/lane.py::exit_for` (≈834–899): where a card in Executing goes when its lane ended. Order: owner moved it out → None; close landed → Executed / Decision moment; lane away → None; folded but unwritten → Decision moment; DELIVERED from a previous life → Decision moment; `folded is None and lane.session is None` → None; else → `came_from(history)` with "the lane ended with nothing folded". It never asks whether the owner's decision stands.
- `board/lane.py::disposition` (≈799–825) and `owner_decision_outstanding` (≈774–797): the recovery path's reader (plan 68). Reads ASK or Q rows, or a RULING with no RULED, written in this life of the lane (`since` = the lane record's first_seen, else when the card entered Executing; None means every row counts), plus an ended lane's last words through `asks_owner`. Used by `api/loops.py::_recover` (≈1659) so a killed lane that put a decision to him is never resumed.
- `board/lane.py::lane_for` ended branch (≈485–556): the lane's face. `elif asks_owner(said)` → "Your move: decide what it asked and bring it back"; else, on disk → "Something is wrong: … with nothing landed … Open the card to resume it or start again". `board/assemble.py::state_of` (≈690–703) shows "asked you" in amber only when the sentence opens as his, otherwise "session died" in red via `_lane_died`. So #601 today, in Decision moment with an ASK row, still shows "session died".
- `api/loops.py::_machine_moves` (≈2212–2294) computes `since` exactly as `_recover` does and calls `exit_for`. `api/loops.py::_facts` (≈1366–1401) builds `LaneFacts` once per pass; it has the lane records and the store.
- `api/doors.py` (≈387): the brief every launched worker opens with ends "To ask the owner something, end your turn with the question; the board shows it on the card and his answer resumes you." Nothing tells a session what to write when it stops at a checkpoint and will not stay.
- `domain/row.py`: ASK and Q are the owner's rows (ASK_ROWS, amber); WAITS is a brief row that asks nothing.
- Neighbours: Needle #88 (wording of Decision moment reasons), #136 (already-answered requests) — both left alone. Plan 68 introduced disposition.

## What I intend to do

1. `exit_for`: at the one point where it would send the card back to `came_from` (after the `folded is None and lane.session is None` guard, so a hand-placed or traceless lane keeps its behaviour), ask `disposition(card, lane, history, since)`; if it answers OWNERS, exit to Decision moment with `reason = "the lane ended; " + why`, evidence LANE_ENDED. UNFINISHED falls through unchanged. Nothing above it moves. One reader for recovery and exit.
2. `LaneFacts` gains `standing: dict[int, str]` — the owner's decision standing on each card with a lane record, by `owner_decision_outstanding` with the same `since` the two loops compute — filled in `_facts`. `lane_for`'s ended branch reads `asks_owner(said) or facts.standing.get(card.number)` and says his move with the standing decision as the why. The face then reads "asked you", not "session died".
3. `api/doors.py`: the brief gains one paragraph after the ask-the-owner sentence: when the work reaches a point that waits on him and the session will not stay, write `needle row <slug> <n> ASK "<what he needs to do>"` before stopping; a lane that ends carrying a current ASK waits for him in Decision moment, one without goes back to the start queue; WAITS names what the work waits on and asks him nothing.
4. Tests: `tests/board/test_lane.py` — exit with a current ASK → Decision moment naming it; an ASK from before this life → back where it came from; WAITS only → back; ended last words that ask → Decision moment; `lane_for` with a standing decision → his sentence. One api-level regression in `tests/api/` — start a lane, write the ASK row through the API, end the session, reconcile → Decision moment and face "asked you", history says why.
5. No new row kind, column, store, state, regex.

## What I want challenged

- Is the placement in `exit_for` right — after the `folded is None and lane.session is None` guard, or before it? What lane ends up there, and does an ASK on it belong to him?
- Does reusing `disposition` there smuggle a branch that is wrong for the exit (owner_moved_out, Not now, Decision moment are unreachable; ASKING is not ENDED — anything else)?
- Stale versus current: `since` for an ended lane with a record is the record's first_seen. For #601 the ASK was written after the lane ended but after first_seen — current. Is there a life where a previous ASK would be read as current and park work that should start? RULING/RULED: a RULING answered in a previous life but re-asked — does the reader hold?
- The face: is item 2 the smallest truthful way to make the card not read as lost work, or is there a reader I have missed that already carries this to `lane_for`? Cost: one history read per card with a lane record per pass.
- The brief: is ASK the right row for a session to write, given `needle row` and the brief's own list of row kinds (DELIVERED, WATCH, REVIEW, WAITS, RULING)? Should the brief name Q too?
- Anything in the plan's terrain that the code contradicts.
