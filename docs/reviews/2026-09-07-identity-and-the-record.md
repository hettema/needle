# Review — identity and the record

**Plan:** docs/plans/done/2026-09-04-08-identity-and-the-record.md
**Reviewer:** the build session (Claude Fable 5.1), through the three lenses in turn, each pass re-reading the fixed work
**Diff range:** 61a50c8..40af595 (the fold), then the docs commits that carry this close
**Findings:** 7 — 7 fixed in this lane, 0 filed

## The passes

The review runs as a loop (`CLAUDE.md`): each pass one lens, the fixes landed, the next pass re-reads the fixed work.

1. **The feature against the plan's "done means".** Item 1: the rename's three matchers — title, git's own record, the body against the previous read — each hold a test in `tests/board/test_identity.py`, and `tests/infrastructure/test_identity.py` replays the #11/#18 incident end to end through `sweep` (stem and title both change, git never saw it, one card, the new document, the old history, the face following). The face follows a carry and an in-place edit, with the old title in the history; an imported card keeps its own (ruling 1). The citation follows the file (`other_citations`): an archived plan is named once, at done/. Running item 1's letter against the live boards turned up two findings: a board with one card whose document is gone for good would ask git on every read (Hello Revenue's #120; finding 1), and a rename that lands straight in done/ — a close that renames as it archives — was never a rename candidate, so the card read GONE for good (finding 2). Item 2: `tests/api/test_doors.py::test_the_close_refuses_executed_while_the_archived_plan_carries_an_unstanced_promise`; the refusal names the items and the file and lands before any row is written. The spelling deviated from the plan's letter to plan 13's (the plan's Deviated line, ruling 4). Applying the rule to the suite itself found an existing close test whose fixture plan had two unstanced items (finding 6). Item 3: `tests/infrastructure/test_record.py` (the reader, and the migration's backfill rounded down to 0012 and back), `tests/api/test_api.py` (the route, `since`, a bad `since` is 422, an unknown project 409) and `tests/api/test_cli.py` (the verb, `--kind`, `--since`); Hello Revenue's two docstrings landed as commit 7ac1a2c55 on its develop with its close-out tests green (47). The retirements of #11 and omarchy's #13 run against the served board after the fold, as the last pass records. Findings 1, 2 and 6 came from this pass.
2. **The seams.** Concurrency: the previous read lives on `LiveProject.index` and is replaced in `rescan` on the loop's thread; a `needle` command's own sweep has no previous read and asks git only, so a rename by hand that a command's sweep sees before the watcher does can still birth a second card — the race a command's sweep always had, now narrower by one matcher, and named here rather than hidden. Failure and restart: at startup there is no previous read, every archived document counts as newly seen once, and git is asked once per project that has a card without a document (Hello Revenue: one call, 0.2 s); a git that cannot run returns no renames and the title and body matchers still stand. The truth of what the board shows: a retired card's history was re-homed under the survivor's number in the first commit, and `placements` reads each number's last move by id — omarchy's #13 was moved to Not now after #15 reached Done, so the board would have said the owner placed #15 in Not now (finding 3, ruling 3); the survivor now quotes the lines and the retired number keeps them. `Retitled` fires only for a card born from the corpus, so the 74 imported Hello Revenue cards keep the owner's words; a folded card follows its own document. The record's `since` compares ISO strings in UTC, the same form the backfill copies from the audit. Finding 3 came from this pass.
3. **The boundaries.** Layers: `board/` stays pure — the git reader is a callable handed in from `api/` through `infrastructure/live.py::sweep`, never imported; `runtime/git.py::corpus_renames` is the one git read. Typed edges: `AuditKind.RETITLED` and `RowRecord` regenerated by `needle types`; `tsc` clean; the fixture current. One way: three matchers in one `rename_of`; `retire` is the one remover of a card, with the proof of search in its commit; `_retitle` is the one writer of a face. Nothing half-done: no deferral marker. Every commit names #20 and says what prompted it. Two findings on this lane's own text: a line over the project's hundred columns (finding 4), and `RENAME_LOG_DEPTH` claimed a rename older than 400 commits was one the board had followed long ago — measured on Hello Revenue's 4268 commits, `-n400` returned all 615 renames in the same 0.2 s as no limit, because git's `-n` counts commits that match the filter, so the constant bounded nothing and would one day have hidden a rename (finding 5, [verification]). Three pre-existing long lines in `api/board_cli.py` and one in `runtime/git.py` are outside the change and untouched. Findings 4 and 5 came from this pass.
4. **The fixed work again.** Re-read the reconcile's candidate list and its git gate, the store's rename branch with the done/ path, the retirement's quoted history, the rename reader without its depth, and the stanced fixture; ran the ratchets on this record and the plan. `tests/ratchets/test_the_fixture_project_is_synthetic.py` refused the plan: ruling 1 quoted a real Hello Revenue card title beside its plan's, in this public repository (finding 7, the leak that ratchet exists to stop — the same class card #27 shipped on 2026-09-04). Finding 7 came from this pass.
5. **The fixed work again, clean.** The ruling re-read with the titles described and not quoted; the backend suite with the ratchets, `tsc --noEmit` and `vitest run` (68) on the tree this record is committed to. Nothing new.
6. **The retirements, live.** After the fold (origin/develop 40af595), the dist rebuilt and needle-serve restarted, the first read migrated the shared store to 0013 with every one of its 1155 rows dated and signed (0 nulls), retitled 46 cards to their documents' titles with the old title in each history line, and served `/api/projects/needle/rows?since=2026-09-07`. `needle retire needle 11 --into 18` and `needle retire omarchy 13 --into 15` ran from the main checkout; #11 answers 409 as a card, #18 carries its rows and quotes its history, and `tools/renames_kept.py --since 2026-09-01` reads 0 where it read 1. Nothing new.

## Dispositions

1. [seam] A card whose document is gone for good asked git for renames on every read of its project (0.27 s on Hello Revenue's history, for #120) — FIXED in 19d4958: the rename candidates are the unlinked live documents plus the archived ones that appeared since the previous read, and git is asked only when that list is not empty; `tests/board/test_identity.py::test_a_rename_straight_into_done_is_followed_and_git_is_not_asked_while_nothing_appeared`.
2. [feature] A rename that lands straight in done/ was never a rename's destination, so the card read GONE — FIXED in 19d4958: an archived document that appeared is a candidate, the store follows it as archived, and the history line names the new path.
3. [seam] Re-homing a retired card's history under the survivor would have made `placements` read a retired card's last move as the survivor's (omarchy #13's move to Not now, after #15's Done) — FIXED in f6375eb: the survivor's RETIRED line quotes the lines, date, kind and detail; the retired number keeps them; `tests/infrastructure/test_retire.py` reads the survivor's placement as its own.
4. [boundary] A line of `tests/infrastructure/test_identity.py` over the project's hundred columns (ruff E501) — FIXED in 77a133a.
5. [verification] `runtime/git.py::RENAME_LOG_DEPTH` claimed to bound the history walked; it bounded the commits shown — FIXED in 9f867d2: the whole history is read, and the docstring carries the measurement.
6. [feature] `tests/api/test_doors.py::test_the_close_writes_what_the_plan_named_against_what_the_lane_dispatched` closed a card whose plan carried two unstanced items, which the close now refuses — FIXED in 58c7372: the fixture stances them, as any lane's plan must.
7. [boundary] Ruling 1 of the plan quoted a real Hello Revenue card title and its plan's title into this public repository — FIXED in the commit that carries this record: the two titles are described, not quoted; the ratchet is green.

## What was checked

- **The rename matchers**, by test at three altitudes: the pure reconcile (`tests/board/test_identity.py`, eight cases), `sweep` over a corpus on disk (`tests/infrastructure/test_identity.py`), and git on a real repository (`tests/runtime/test_corpus_renames.py`: committed, chained, staged, a rewrite that is no rename). Against the real boards: the #11/#18 rename is `R081` in this repository's log, read by the new reader; omarchy's #13/#15 is `01adff8`.
- **The face and the citation**: the store's rename and relink branches and the `Retitled` effect, by test; the count of cards each board would retitle, read from the shared store read-only (Needle 8, omarchy 2, Hello Revenue 35 corpus-born and 74 imported, the imported left alone).
- **The close's refusal**, by test on the fixture project, and the whole suite re-run under the rule (finding 6).
- **The record**: the migration's backfill rounded down and up on a store with imported rows, fresh rows, a rewrite and a ruled verdict; the route and the verb by test; the shared store confirmed still at 0012 before the fold (this lane ran every `needle` command against a private `NEEDLE_DB`).
- **The retirement**, by test: rows moved, a one-per-card kind quoted, history quoted, positions closed up, refusals for a document in the corpus and for a lane keyed to the card.
- **Hello Revenue**: the two docstring edits, its close-out tests (47 passed), committed and pushed on a clean develop.
- **The suites**: backend with ratchets, `tsc --noEmit`, `vitest run` (68), all green on the tree this record lands on.

## What the build learned the plan got wrong

- The plan spelled the stance in Hello Revenue's close-out form; plan 13 had already chosen the item stance the board counts on a running lane. One grammar means following the shipped one, not the letter written a day earlier.
- The plan asked for the two cards to be "merged"; merging their history lines under one number would have made the board lie about placement. The survivor quotes; the retired number keeps.
- The plan named `infrastructure/corpus.py` (the watcher's paired events) as terrain; the previous read on `LiveProject` was the pair, and the watcher did not change.

## Not done, stated

- The full suite on the levelled tree before the fold was 505 passed and one failure that was the trunk's own: a docs commit from the main checkout (379b6ce) quoted five real Hello Revenue card titles; filed as `docs/slice-suggestions/2026-09-07-a-suggestion-that-quotes-real-card-titles-is-refused-before-it-reaches-the-public-repository.md`, and the owner's session redacted the instance in 20f6a33 before this lane folded; the ratchets are green on the fold.
- Hello Revenue's morning note still reads plans only: its run is a GitHub Actions job and the board runs on the owner's laptop (ruling 6). The verb and the route are what it would read.
- Hello Revenue's archive gate still stances acceptance criteria in a block of its own (ruling 4); whether that list goes is the owner's.
