# Review — the work runs where the horsepower is, and the board feels like it is on your laptop (card #83)

**Plan:** docs/plans/2026-09-07-the-work-runs-where-the-horsepower-is-and-the-board-feels-like-it-is-on-your-laptop.md
**Reviewer:** the building session (Claude Fable 5.1, background lane, moved once by the runtime mid-build from hrclaude to a slot with headroom) read the done-means; a reader of the other make (Codex, `codex exec -s read-only`, reasoning effort high, run in the lane's checkout with the commits' patches) read the seams and the boundaries cold. Its answers are kept verbatim in the building session's scratchpad (`codex-pass2.md` onward) and are quoted below where they bear.
**Diff range:** 01cb588 (the trunk at the lane's birth) to the lane's tip on `card-83-the-work-runs-where-the-horsepow` — the feature commit 1a9138e and the fixes that follow it.
**What this record covers:** the half of the plan the fixture can prove — item 4's fixture clauses and item 5's readers — built before the rented machine exists. The live halves of items 1–5 are the resuming session's, and this record continues then; it is not the close's record yet.
**Findings so far:** 42 across eight reading passes — 28 on the first half (below, as they were) and 14 on item 3's half (29–42, the eighth pass) — 24 fixed (of which 15 re-read as resolved by the reader who found them: 7, 9, 10, 14, 18, 20 on the third pass; 12, 16, 19, 21, 22, 24, 25 on the fourth; 28 on the fifth; 26 on the sixth; and 1, 2, 23 and 27 fixed by the writer without a re-read of their own — 27 is this line), 4 no change with the reason written (3, closed by 19; 4; 8; 17), and seven partials that stand as verification gaps (5, 6, 11, 13, 15, 17 and 8's transport), each named in its disposition with what a test would have to show.
**Where the loop stands:** open again since the eighth pass, the first over item 3's half (2026-09-10): it found live and latent behaviour inside the change (29, 31, 33, 39), so a ninth pass — a cold completeness read of the eighth's repairs — is owed, and the stop signal below is the first half's. **The first half's stop, and by which rule:** the sixth pass found nothing new in the work; the last violation of a promise was found on the fourth pass (26) and its fix re-read as resolved on the sixth. Under the rule the three lanes proposed to the owner on 2026-09-09 (a next full pass is owed only after a live or behaviour finding inside the change), the loop would have ended after the fourth pass with the same code. **The fold-time suite:** the whole backend suite (`tests/`, the live-title ratchet deselected as it is red on the trunk itself) on the tip, run once on 2026-09-10 with its floors on disk after two runs on the laptop's memory-backed `/tmp` ran out of space: every test passed, no failure, no error; the page suite 80 passed and the compiler clean.

## The passes

1. **The work against its done-means (the building session, on 1a9138e).** Item 4, fixture half: on the floor a card in the laptop's own record starts on the laptop while the rented floor has more room; every other card starts on the rented floor through its own `needle start` over the stand-in `ssh`, registers there and nowhere else, is listed on the one list stamped `rented`, and its window on the desktop runs `ssh -t rented -- tmux new-session -A -s needle-lane-<card> …` with the multiplexer's word read back; both floors full is refused with both machines' numbers; a rented floor that does not answer falls back to the laptop (`tests/runtime/test_machines.py`, 15 cases). The face says `on rented` in the lane section only when the board knows more than one machine. Item 5: the two loop readers run from the command line; the laptop's numbers were handed out and are not yet read. Findings 1–4 below; 1 and 2 fixed in the next commit.

2. **The seams and the boundaries, read cold (Codex, on 81e3b7c).** Seventeen findings, 5–21 below, confidence 0.99 in the most severe; "fold blocked" in its own words, and rightly: the wire's quoting was broken outside the fake, a machine that stopped answering read as every lane on it ended, and the board still read a lane's worktree from its own disk. Fixed in e0a55df, each with a floor case except where the finding says otherwise; three stand with the reason written (8, 17, and 3 above closed by 19).

3. **The fixed work, read cold again (Codex, on e0a55df).** Six resolved outright (7, 9, 10, 14, 18, 20); seven partial — six for a test that would not catch the regression it names (5, 8, 11, 15, 16, 21) and one on the wire's deadline claim (17); four partial on substance (6: the memo of last rows does not survive a restart; 12: a dropped connection after a launch still read as dead, and a sighting never changed its machine; 13: the routing map did not survive a restart and a failed worktree read was not an unread machine; 19: a size the other machine could not answer fell back to a read here). Four new, 22–25 below. Fixed in the next commit; what stands, and why, is on each.

4. **The third pass's fixes, read cold (Codex, on 692ce02).** 22, 24, 25, 12, 16, 19 and 21 resolved; 8 stands as written; 23, 5, 6, 11, 13, 15 and 17 partial as their dispositions say; two new, 26–27 below, and "fold blocked" on both. Both fixed in the next commit, with 23's remaining half (28).

5. **The fourth pass's fixes, read cold (Codex, on 0c7daaf).** 28 resolved, proved through the serializer, the parser, the room and the typed reply; 26 partial — a windowless session older than a day may still run and the store cannot know — and 27 partial on two counts still wrong. No new finding. Both fixed in the next commit: the removal reads the one list, and the counts are the dispositions' own.

6. **The fifth pass's fix, read cold (Codex, on fa26e8e).** 26 resolved through six branch probes; 27 still off — the head line said 22 fixed and 3 unchanged against 24 and 4 in the dispositions — and no new finding. The head line above is now counted from the dispositions, by the writer, once; the reader's word on it is not asked again (the rule the three lanes put to the owner that evening).

7. **The served board read live after the fold (the building session, on ec5b7bc).** Dist rebuilt, needle-serve restarted, the shared store migrated to 0017; the one-machine board read exactly as before (every project 200, the Start doors as they were). With `laptop` and `rented` registered, the head named both — `laptop (here) 6.0 GB available`, `rented 30.4 GB available` — the loop read the rented machine over real ssh every pass, and Hello Revenue #121's Start door read `Start · hrclaude on rented`. Two things the floor could not show: the other machine's `needle` is its clone's, so until that clone was levelled to the fold the first passes read `needle room` refused with a usage line, loud on the head and in the journal, never a wrong number; and a Start would have put a lane on a machine that has no board to write to yet (item 3), so the rented machine was unregistered again the same minute, the removal's guards passing. No finding against the code; both are written into the plan for item 3.

8. **Item 3's half, read cold on its first commit (Codex, on c98f980, 2026-09-10).** Fourteen findings, 29–42 below, most graded latent in behaviour; the reader also read the live-half procedure outside the checkout (the rented record's `tools/board-move`, `tools/board-back`, `docs/board.md`; the laptop record's tunnel units and sshd drop-in) and found seven of the fourteen there (34–37, 41, 42 and half of 35), which are repaired in those records and not in this lane's commit. Its inventory of every subcommand and which side it belongs on stands as the map of what `board=True` marks: the registry verbs moved to the board's side (30); the runtime verbs stay this machine's, since the board asks them of it. Two lenses it applied that the first seven passes had not: the copied store's topology (a store with both machine rows on both machines, 29) and what a caller's relative path means on the other side (31). Repairs: 29, 30, 31, 32, 33, 38, 39, 40 in the lane; 34, 35, 36, 37, 41, 42 in the records; its reading of the three tests (each proves less than its name) answered by four more cases. Fold blocked by this pass until the ninth reads the repairs complete.

## Dispositions

1. [seam] **A group's owner was matched by pid alone across machines.** `board/dial.py::who_is_home` looked every group's pids up in one map of every session's pid, so a group read on the rented machine could read as home to a laptop session holding the same number and never be swept, or a stranger there be called a session here. FIXED: the map is keyed by `(machine, pid)`, both stamped by the runtime that read them; a floor case holds two groups with the same pid on two machines and reads the laptop's home and the rented one's nobody home.
2. [feature] **The Start preview never named the machine, while the plan's ruling said it does when the board knows more than one.** `board/lane.py::driver` built its words from the rung alone. FIXED: it appends `on <machine>` when the placement carries a name, which `api/loops.py::_placement` blanks on a one-machine board, so every existing face reads as before.
3. [seam] **A fresh session from a remote session's transcript is judged against the wrong transcript.** `runtime/windows.py::look_command` reads the transcript's size here (`machine.transcript_size`) for a session that ran on the other machine, finds nothing, and asks for a resume there rather than a fresh brief; the other machine's own `claude --resume` then holds the size rule. NO CHANGE: a resume that the other machine refuses for size is a window whose first line says so, loud and once, and the fresh-brief branch is a size optimisation, not a claim; noted for the live read of item 4.
4. [record] **The desktop's `needle tell` keeps its own ledger of how a popup was answered, beside the desktop's store, not beside the board's.** The loop writes that ledger's path and never reads it back (`api/loops.py::_tell_owner` writes `told.log`; no reader), so nothing the board shows depends on which machine holds the file. NO CHANGE; item 3's live read says which machine the plan's own reading of card #41 should read the ledger on.
5. [seam] **A stray group on one machine could get a live group on the other stopped.** The sweep's memos were keyed by unit alone, and the machine map by unit; with the same unit on both machines the rented one's destination was overwritten. FIXED: every memo and the eligibility are keyed by `(machine, unit)`, the owner is alive only on the group's own machine, `ScopeState` carries its machine, and the store's `who_is_home` case reads two like-named groups apart. What the third pass asked and this record does not have: a sweep case with two machines holding one unit name, which needs the floor's manager to hold groups per machine; the sweep's own case is item 4's live read, and the keys are read by eye until then.
6. [seam] **A failed session read became evidence that sessions ended.** `Runtime.sessions` dropped every row of a machine that did not answer, so its lanes read as ended, a death could be named, a group swept. FIXED: the last rows read stand, the machine is named in `Runtime.unread` with the transport's words, `scopes` offers no group of an unread machine, and the loop names no death, brings nothing back, releases nothing and stamps no worktree gone for a session or record on one; a floor case turns the rented host off and reads the lane still live and the machine unread, then on again. **What stands (the third pass):** the last rows are the server's memory; a board restarted while the machine is unreachable has none, and until the machine answers its lanes read as ended on the face while the loop, which reads the lane record's machine, still acts on none of them. Written here for item 4's live read: the face's word for an unread lane is the resuming session's to give, since it is a new state the page's language must carry.
7. [seam] **The real `ssh` lost the quoting around the login-shell script.** `bash`, `-lc` and the line were three `ssh` arguments; `ssh` joins them, so the other side's shell ran `needle` alone. FIXED: one quoted argument (`runtime/machine.py::remote_argv`); the stand-in now joins the words and parses them with `shlex` as that shell would, refusing a `bash` line that does not carry its script whole, and runs a plain command line (a terminal's `ssh -t host -- tmux …`) as given.
8. [seam] **Calling a colleague on another machine invoked the local launcher.** NO CHANGE to the call's path; REFUSED by name instead: the note and the answer are files here, and the call cannot travel yet, so `Runtime.call` answers dead with the machine's name rather than act on this machine's processes for a session that is not here; a floor case calls a rented colleague and reads the refusal with this machine's launcher never run (the third pass asked for it). Item 4's live read names the call's travel.
9. [feature] **A card in a machine's own record bypassed the room, and the machine chosen never rechecked its floor at the launch.** FIXED: a full ground refuses with that machine's numbers and "runs nowhere else"; `Runtime.start` and `start_windowless` read this machine's room at the moment of a local launch and refuse in the head's words, which is what the other machine's own `needle start` does when asked; a one-machine board places as before and refuses at the door and the start in the words it had (the floor's memory suite reads the same sentence as before).
10. [seam] **Forgetting a machine with work on it routed that work here.** FIXED: `remove_machine` refuses while a lane record or a session record names the machine; a name the board no longer knows is a machine with no host, so everything routed to it answers unreachable by that name and nothing runs here in its stead.
11. [seam] **Recovery gated on this machine's room and resumed on another.** FIXED: `_recover` and `_park_lifts` read the room of the machine the session runs on (`_room_of`), read fresh; the resume goes to that machine, where the worktree is. Not shown by a case with opposite rooms on the two machines (the third pass): the recovery walk on the floor is the wall-and-park suite's, and its two-machine form is item 4's live read.
12. [seam] **A lost launch reply was a dead launch, and a remote session's kill was counted against this machine.** FIXED: a timeout is `RemoteTimeout` and the launch answers `unconfirmed` with "may have landed", never dead (a floor case lays a slow host); a sighting carries the machine the process was seen on, and `killed_on` reads the sighting first, the slot record second, this machine last. The third pass: a connection dropped after the launch (`ssh` exit 255) is unconfirmed too now, and a sighting re-recorded from another machine keeps the machine it was last seen on.
13. [seam] **The board read a remote lane's worktree on its own filesystem.** The largest finding: `worktrees`, `edits`, `lane_files`, `branch_tip`, `lane_folded`'s tip, the plan's progress and the close's dispatches were all local reads, so a rented lane would have read as gone — "the disk wins" — the moment item 4 went live. FIXED: `Runtime.worktrees` merges every machine's checkouts and remembers which machine each was seen on; `edits`, `lane_files`, `lane_tip`, `lane_docs`, `dispatches` and `transcript_size` go to that machine over the wire (six new verbs, each answering a domain value); the lane record says which machine; a machine that does not answer keeps the paths it was last seen with, never gone. A floor case with a real lane worktree reads each of them over the stand-in `ssh` for a lane placed on the rented machine and with no wire for one placed here. The third pass: the routing map is seeded from the lane records at every read, so a restart keeps a lane on its machine, and a worktree read that fails names the machine unread for the pass, so its paths are never stamped gone. The floor case still places the lane by hand rather than by the wire's discovery, since both floors share one filesystem; discovery on two real clones is item 4's live read.
14. [seam] **A fresh conversation on the other machine could reopen an old one under a new identity.** FIXED: a viewer into a lane reattaches (`-A`) to the one session named for the lane; a fresh conversation's multiplexer session carries the conversation's own tag and never reattaches — a name already held is refused by the multiplexer, loud in the terminal; the stand-in refuses a duplicate as the real one does; a floor case opens two Discuss windows on one card and reads two sessions.
15. [seam] **Concurrent high-water readings could raise the day's mark.** FIXED: one conditional `UPDATE … WHERE least_available > :new`, then an insert on a missing row, retried once on a race; a floor case writes 10, 3, then 7 and reads 3 — which, as the third pass says, the old read-compare-write would also have passed in sequence; the defence is the statement's shape, read by eye, and the loop's two-week mark is where a raised mark would show.
16. [seam] **A missing remote boot list fell back to this machine's boot.** FIXED: a named machine with no boots read is unknown; only an unnamed row (this machine's) reads this machine's boots; a loop case reads the rented boot as none and the unnamed row's as the laptop's (the third pass asked for it).
17. [seam] **A slow remote read holds the beat's lock and every door with it.** NO CHANGE beyond the per-read bound: a read is deadlined at twenty seconds (`runtime/remote.py::READ_SECONDS`), the rule's answer and a stop at sixty, a start at four minutes as a local walk is. A pass makes several: per machine its sessions, its room, its groups and its boots; per project its checkouts; per lane with hands on its edits and its documents — so a machine that stalls on every one holds the lock for the sum, not for a minute (the fourth pass corrected the earlier "minute or two"). Isolating remote sampling from the door lock is a change to the loop's own shape that a local walk under the same lock already invites; this plan's loop is where a stalled machine would show first, and the resuming session reads this before item 4 goes live.
18. [verification] **The window fixture could not prove attachment on the requested machine.** FIXED: the stand-in `tmux` keeps its sessions per machine identity, so the proof asks the rented floor and the laptop floor apart; the lane case asserts the session is under the rented identity and not the laptop's.
19. [seam] **Look read a remote session's transcript size here.** FIXED: `Runtime.transcript_size` reads it on the session's machine (`needle transcript-size` over the wire) and `look_command` takes the size; a size the other machine could not answer stays unknown and the resume there holds its own rule, never a read of this machine's transcript (the third pass); disposition 3 above is closed by this.
20. [record] **`needle machines --json` did not carry the timings the plan said it read back.** FIXED: `MachineRoom.timings` carries the latest measured time per step, from the store; a floor case writes two and reads the latest.
21. [seam] **The dial's displayed refusal disagreed with its rule.** FIXED: the head's room is the board's — full only when no machine has room, the sentence naming each full machine — so the dial's word and the beat's act come from one reading; `api/dial.py::_full` reads that one. A loop case (the third pass asked for it) reads the head not full with the laptop under the floor and the rented machine with room, then full naming both once both are under it.
22. [seam] **A sighting was written from a cached row of an unread machine.** The last rows read stood in for a machine that did not answer, and `_sightings` recorded each as seen now — an observation nobody made, moving `last_seen` and, with no boot list, erasing the boot. FIXED: no sighting is written for a session on an unread machine; a loop case with the machine named unread reads no sighting.
23. [record] **The head's memory sentence lost the card's name for a reading's or a planning's scope.** Every group of ours is read now, and one the old read never listed was named by its unit alone. FIXED: the names the room is given are every unit a card's sessions may run under, and the sentence says `#7's planning session` rather than `#7's lane` for one, by the unit's own prefix; the read itself stays every lane with hands on, by name, so the floor's memory case reads the same calls as before.
24. [seam] **Every review record was read on every pass of every unfinished lane.** The one read that answered over the wire read the reviews eagerly, where the loop had read them only once every item was met (plan 13). FIXED: `lane_docs` answers the plan alone unless asked for the reviews, and the loop asks for them from inside `progress_of`'s callable, as before; the worktree case reads no review on the first read and the record on the second.
25. [seam] **A machine with only history on it could never be forgotten.** FIXED: only a lane still on the machine refuses its removal; session records are history and stay under the name.
26. [seam] **A machine with a session started on it, whose lane the loop had not read yet, could be forgotten.** The third pass's guard kept only lane records as work; a start writes its session record at once and the worktree is read a pass later, and a reading has no worktree at all — Codex's probe removed a machine right after an alive launch and the session's destination had no host. FIXED twice: a session record younger than a day refuses the removal by name in the store; and, since a reading older than a day may still run (the fifth pass), `needle machine rm` reads the one list first and refuses while any row with a process is on the machine, or while the machine did not answer the read. The floor case starts a windowless reading there and reads both refusals, live and unread.
27. [record] **The pass summary miscounted.** "Five resolved" listed six, "two partial" four, the head line said seven partials and 21 re-read. FIXED in the words: the counts above are the dispositions' own, and a fix awaiting its reader's re-read is said to await it.
28. [seam] **The other machine's room named its groups by unit alone** (23's remaining half). FIXED: the loop hands the card names it knows to the other machine's `needle room --owner unit=slug:number`, so its reading names a group by its card as this machine's does.
29. [seam] **The moved store's topology made the runtime reads call each other.** Both machines hold both machine rows after the move; the board asks the laptop `sessions`, the laptop's runtime fanned out over its rows and asked the rented machine, which asked back. FIXED: `Runtime.machines` answers this machine alone when `board.json` names another machine (`runtime/service.py`), so a runtime that hands its verbs away never fans out; proved with both rows on both floors and the rented floor's board file naming a host never laid — no call to it (`test_a_machine_that_is_not_the_boards_answers_for_itself_alone`).
30. [feature] **The registry and its measurements were left on the abandoned store.** `machines`, `machine add/rm/timing/host` and `where --high-water` ran on the ledger here after the move. FIXED: those registrations carry `board=True` (`where` only for its high-water read, a callable mark), so they run on the board's machine like every verb that opens its store; a timing written from the other side lands in the board's store and not the ledger (same test). `needle board NAME` still reads the ledger's rows by design — it names the destination from what this machine last knew of the board — and its docstring says so.
31. [seam] **A caller's relative path crossed as text.** `add .` and `fold --worktree .` resolved in the other side's login directory. FIXED: `api/cli.py::_absolute` resolves the fold's worktree and a project's path here before they cross (`test_a_path_the_caller_gave_relative_to_its_directory_crosses_absolute`); a review's path under `docs/reviews/` stays a corpus path, read by the board in its own levelled checkout, as a close has always read it (the main checkout's corpus, slice 10).
32. [seam] **A lost line was reported as a verb that never ran.** `ssh`'s 255 can follow a committed row. FIXED in the words: `forward` says the line was lost and whether the verb ran is not known here, read the board before repeating a write; `Runtime.fold` says the machine did not answer the push and the board's next read of the lane's tip against the trunk settles it.
33. [seam] **Levelling the other machines' clones held the loop's lock for up to four minutes per project.** FIXED: `Remote.level` is bounded like a verb (60 s), a machine this pass already found unread is not asked (`level_elsewhere`), and the loop levels the other machines' clones outside its lock (`Loops.level_trunks` → `level_clones_now` after the lock is released); the board's own checkout is still levelled under it.
34. [seam] **The move did not stop every writer before copying a live database.** FIXED in the rented record: both services are stopped, the store crosses through SQLite's own backup (consistent beside a stray writer), and the copy's tables and row counts are proved equal before it is sent.
35. [feature] **The move did not level the rented checkout before relying on verbs an older one lacks.** FIXED in the rented record: the checkout is pulled level with the trunk, synced, and asked for `push`, `level`, `board` and `machine host` by name before the laptop's board stops.
36. [seam] **The revert refused every interrupted state.** FIXED in the rented record: `tools/board-back` works from any state — each step tolerates a machine that never got that far, the store comes back only when the rented machine's is newer, and a rented machine that does not answer leaves the laptop restored with the two lines for its side printed.
37. [feature] **Moving back left the rented machine writing a store no board reads.** FIXED in the rented record: the revert points the rented machine's `needle` at the laptop's board (`needle board laptop` there) and takes the machine off the board, since a lane there would post to a board that is not there (the first evening's finding); `docs/revert.md` says why and how it comes back.
38. [seam] **A remote clone's shortfall was shown as the board's own checkout's.** The trunk note carried it, and the face said the main checkout was not level beside a green trunk. FIXED: the trunk's state is the board's checkout's alone again; a stale clone is `MachineRoom.clones` on that machine's own line of the head (`describe_room`, `Board.tsx::machineWords`), proved on the fixture with the rented floor down (`test_the_head_says_which_clones_are_not_level_on_each_machines_own_line`).
39. [feature] **`machine host` could report a rewrite it never persisted.** The runtime's implicit self, with no row, was found by name and the store's False ignored. FIXED: registered rows only, and the store's answer is read (the implicit machine refuses by name in the test). The identity probe proves the machine, not the login's user or store, and the docstring says so.
40. [seam] **A signal that ended `ssh` here surfaced as an exit of 241.** FIXED: a negative return is answered as a shell answers it, 128 plus the signal; the verb on the other side runs to its end and a write there is one row, whole or absent, which `forward`'s docstring says. No cancellation crosses the wire: none is promised.
41. [verification] **The move announced success after a failed probe.** FIXED in the rented record: a failed read fails the move, and the proof through the tunnel is the same project count as before the move; what the script cannot prove — a card started, its window here, its session on the board — is named as the hand's proof after it.
42. [record] **The sshd claim said "reachable only from the tailnet".** FIXED in the words of both records: sshd listens everywhere as it does by default; the drop-in admits one user from one address by key, and the firewall line opens the port on the tailnet interface alone.
