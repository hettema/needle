# Review — you name what matters now, a colleague finds what holds it back, and the board sorts every card by whether it moves that

**Plan:** docs/plans/done/2026-09-07-you-name-what-matters-now-a-colleague-finds-what-holds-it-back-and-the-board-sorts-every-card-by-whether-it-moves-that.md
**Reviewer:** the build session (Claude Fable 5.1), through the three lenses in turn, each pass re-reading the fixed work; the suites run in this lane by the session itself, since a delegated result would have needed re-running the one failing test anyway
**Diff range:** 7873301..HEAD (the lane, rebased onto the trunk after #68's and #41's folds; the first commit is a321027)
**Findings:** 13 — 11 fixed in this lane, 2 filed

## The passes

The review runs as a loop (`CLAUDE.md`): each pass one lens, the fixes landed, the next pass re-reads the fixed work.

1. **The feature against the plan's "done means".** Item 1: the fixture's `docs/FOCUS.md` reads whole into `FocusDocument`; a missing measure and an unreadable recheck name their line in `doubts`; a ruling at A over a document at B is proposed; the verb (`needle strip`) and `GET /focus` return one object; the ratchet refuses a chosen strip over a mismatched fingerprint at construction. Reading the done-means cold found that a document whose recheck could not be read showed as "What holds it back is not settled" though the diagnosis was there — the state was keyed on the outcome line alone (finding 1). Item 2: the door opens with its own brief, refuses a second while one lives, the rail lists `Focus`, a landing moves the strip to proposed, and an ended conversation says so. Item 3: one check per proposal fingerprint in a fresh thread, read again after an edit, the ruling offered with the runtime's reason after two calls end without an answer. Reading the landing path found that a verdict read on one text would have been bound to whatever text was on disk when it landed (finding 2). Item 4: every open card read once, each in a thread of its own; only an edited card again; a renamed focus stales all; the verb refuses other words and moves nothing. Running the walk found a shipped card with no reading read on the first pass, since "no reading" and "re-read after the recheck" were one condition (finding 3), and an archived document off the shipped columns counted in the coverage though the loop never reads it (finding 4). Item 5: the arrangement has a test per rule; the acceptance moved only the ticked cards with his name and one batch; reading the position rule found an unticked card dropped from the order its neighbours were placed against (finding 5), the put-back restoring in reverse order landed a card one place short (finding 6), and a retired acceptance still stood on the strip (finding 7). Item 6: the click reads the baseline; each recheck word shows its sentence and pauses the order; a missed date expires by itself. Findings 1–7 came from this pass, each fixed and the walk run again.
2. **The seams.** Concurrency: the focus loop beats under the loops' lock like the dial, and opens one reading per beat; a landed call is ended by whichever loop sees it first (`Loops._tend_calls` or `Focus._tend`), and both are idempotent on an ended row — read, not assumed: `end_focus_call` and `end_call` write only while `ended_at` is None. Failure and restart: an open reading survives a restart as a `focus_calls` row; on the first beat the answer file is read before the process is judged, so a thread that answered while the board was down still lands, and one that died lands the runtime's words and counts against the cap. The one seam that was wrong: the board's server is a threaded process and the runtime's `detach` double-forked, which this platform's spawn could not replace with `setsid` (finding 8, fixed by spawning by pid in a new process group with a `cd` shim); and a dropped `Popen` handle warned at collection and failed the suite (finding 9, the same fix). The truth of what the board shows: the strip is built only from the store's facts and the document on disk, and `FocusStrip` refuses to say chosen otherwise; the `Live.detail` summary carried no class, so a Start under the lens wrote the lens and not the class (finding 10). Findings 8–10 came from this pass.
3. **The boundaries.** Layers: `board/focus.py` and `board/leverage.py` import `domain` and `board` only; `infrastructure/live.py` composes them; `api/focus.py` is the one module that asks a colleague. One way: the project's focus prints under `needle strip`, since `needle focus <ref>` was already the runtime's verb for a window (finding 11 — a name, fixed before the first commit); `detach` is the one spawner for both the warm call and the fresh ask. Typed edges: `domain/focus.py` is mirrored, `tsc` passes, the fixture is regenerated and carries a chosen focus. Nothing half-done: the marker ratchet caught a `placeholder` attribute on the Focus door's input and an exception's name in a docstring (finding 12, fixed). The ratchet of this plan's class line holds: nothing under the dial imports the leverage result. The vocabulary: every strip and face sentence is built through `say()` and the two that said "the other make" now say "the other kind"; the suggestion this lane files was retitled once, since its first title used "lane". Outside this change: the trunk arrived red on `test_every_title_is_in_the_owners_words.py` over a suggestion whose title uses "fold", filed from the main checkout where no suite runs (finding 13, filed). Findings 11–13 came from this pass.
4. **The fixed work again.** Re-read `board/focus.py` (`strip_of`, `paused_why`, `wants_card_reading`), the three landing doors and their fingerprint refusals, `Doors.accept_order` and `put_back`, `Live.focus_of`, `runtime/machine.py::detach` and the fake Codex's fresh branch; ran `tests/board`, `tests/ratchets`, `tsc`, `vitest` (73) and the full backend suite (`tests/api`, `tests/infrastructure`, `tests/runtime`) on the tree rebased over #68's fold, and the full backend suite again on the tree rebased over #41's. Nothing new.
5. **The served board, live.** After the fold (origin/develop da56da4, the main checkout level), `npm run build` in the main checkout and `systemctl --user restart needle-serve.service`; the store migrated to 0016 at the restart and the board answered on the port within thirty seconds. Read through its API: Needle's board (80 cards, version 33) carries `focus.state` "no focus" with the sentence "No focus chosen", "Talk it through" offered and the two other doors closed with their reasons, and `leverage.available` false with "no focus is chosen"; omarchy and hr3 read the same; `needle strip needle` from the main checkout prints the same four lines and "Leverage order unavailable: no focus is chosen", and `needle strip --unbound --count` prints 0. Hello Revenue's board answers 500 — the journal names `CardState` refusing a "session died" face with a quiet detail, the defect #41 filed before this fold (`docs/slice-suggestions/2026-09-09-a-finished-card-that-never-shipped-does-not-take-the-whole-board-down.md`) and not the strip, which is built from `say()` on every branch and never reaches that validator. Nothing new of this lane's.

## Dispositions

1. [feature] A document with a diagnosis but an unreadable recheck read "What holds it back is not settled" — FIXED in a321027: `strip_of` keys the outcome-only state on the diagnosis line being absent, and any other doubt is a proposal that cannot be chosen yet, naming the line.
2. [feature] A verdict, a class or a recheck read on one text could land on the fingerprint of the text on disk at landing — FIXED in a321027: every landing door takes `read=`, the fingerprint(s) the reading was opened on, and refuses when the document moved underneath; the loop passes them.
3. [feature] A shipped card with no reading was read on the first pass — FIXED in a321027: `wants_card_reading` reads a shipped card only when a recheck said the work did not link and the card was read as helping.
4. [feature] A card behind an archived document off the shipped columns counted as unread in the coverage — FIXED in a321027: `Live.focus_of` skips it as the loop does.
5. [feature] An unticked card was dropped from the order a ticked neighbour's position was computed against — FIXED in a321027: only the ticked cards leave their group.
6. [feature] Putting back in reverse order landed a card one place short of where it was — FIXED in a321027: places are restored from the lowest recorded position up.
7. [feature] A retired acceptance stayed on the strip with its put-back closed — FIXED in a321027: a hand move drops it from the strip.
8. [seam] `runtime/machine.py::detach` double-forked, which a threaded server may not do, and this platform's spawn has no `setsid` — FIXED in a321027: `os.posix_spawn` with `setpgroup=0` and a `cd`-and-`exec` shim; finished children are reaped on the next spawn.
9. [seam] A `subprocess.Popen` handle dropped while the worker ran warned at collection and failed `tests/api/test_calls.py` — FIXED in a321027 by the same change: no handle, a pid.
10. [seam] The open card's summary carried no class, so a Start under the lens recorded the lens alone — FIXED in a321027: `assemble_detail` takes the card's `CardLeverage` and the Start door reads it.
11. [boundary] The plan's verb name `needle focus` was already the runtime's verb for bringing a window forward — FIXED in a321027: `needle strip`; the plan's `Loop:` line follows.
12. [boundary] A `placeholder` attribute and an exception's name in a docstring were deferral markers to `test_nothing_ships_half_done.py` — FIXED in a321027: the door carries its question as a label, and the docstring says what the platform refused.
13. [boundary] The trunk arrived red: a suggestion filed from the main checkout with "fold" in its title fails the title ratchet for every lane — FILED as `docs/slice-suggestions/2026-09-09-a-suggestion-filed-from-the-main-checkout-cannot-turn-everyones-checks-red.md` (Fix: now); outside this change, and the one check was deselected in this lane's runs.

## What was checked

- **The document and the derivation**, by test: `tests/board/test_focus.py` (the fixture reads whole; doubts name their line; a ruling binds to one fingerprint; every strip state has its sentence and doors; expiry, a failed recheck and a changed outcome pause the order; a reading goes stale when either text changes; a shipped card is read again only after a failed link; the coverage counts).
- **The arrangement**, by test: `tests/board/test_leverage.py`, one test per rule the plan names — helps-remove into Up next by likelihood then cost, does-not-address out of it with a readable wake, a Backlog defect never into Up next, a parked plan forward and a parked defect to the rail, Executed and Decision moment ordered but never moved, protects after helps-remove and the rest by his rank, an unticked move not re-proposed, a paused order ordering nothing.
- **The walks on the served board over the floor**: `tests/api/test_focus.py`, sixteen walks across the six items, with the fake Codex playing the fresh thread (its `-o` branch, new on this card).
- **The boundaries**: `tests/ratchets/test_the_lens_never_chooses_work.py` (the dial imports no leverage result; a chosen strip cannot be built over a mismatched fingerprint), and every existing ratchet on the levelled tree.
- **The page**: `tsc --noEmit`; `vitest run` (73), with five new cases — the strip on every lens and the arranged columns storing nothing, the ticked acceptance and the put-back, the unavailable state and the ruling on a proposal, the Focus door, the lens on a Start click.
- **The suites**: `tests/board`, `tests/ratchets` (one trunk-born check deselected, finding 13), `tests/api`, `tests/infrastructure`, `tests/runtime` green on the tree rebased over #68's fold; the served board is read in the last pass after the fold.

## What the build learned the comp got wrong

- The comp counted the fixture's open cards at 21; the derivation counts 20, since a card behind an archived document off the shipped columns is not read (finding 4), and 15 unread beside the five read, not 16. The comp was corrected before the build followed it, which the design rules allow only because it is unsigned.
- The comp shows the Focus door's question as a placeholder inside the input; the build carries it as a label beside the input, since a placeholder is a deferral marker to the ratchet.

## Not done, stated

- **Needle's own focus is not chosen at the close.** The plan's acceptance asks for a focus reached through the door, the conversation, the other kind's reading and his click; the click is his and this lane ran unattended, and a proposal written cold would have invented the outcome he types. The strip on Needle's board says "No focus chosen" with the door; the card's WATCH row waits on his first use of it, and the review of the first real focus's rows is what that signal delivers.
- **The comp is not signed** (`docs/design/README.md` asks for a signature before the build); its two owner's calls — every move ticked by default, and a parked card coming forward — are at its foot and stand as this lane's decisions until he rules.
- **The first ask of a colleague in a fresh thread on the served board has not run.** Every ask is played by the fake Codex on the floor; the real `codex exec -s read-only -C <checkout> -o … --output-schema …` argv combines flags each verified on 2026-09-07 and 2026-09-08 but not together, so the first proposal that lands on any board is the live check of it. The loop records a thread that dies with the runtime's words, and after two the strip offers the ruling with the reason.
- The measures of the first real focus are read by the loop on the recheck's cadence; the fixture's measures point at files that do not exist, so every baseline in the tests reads "not delivered", which is the honest reading and not a proof of the command reader.
