# Review — every card title says what it is for at a glance, in plain words

**Plan:** docs/plans/done/2026-09-07-every-card-title-says-what-it-is-for-at-a-glance-in-plain-words.md
**Reviewer:** the build session (Claude Fable 5.1), through the three lenses in turn, each pass re-reading the fixed work
**Diff range:** e8c0357..4226a86 (the fold), then the docs commit that carries this record's last pass
**Findings:** 6 — 4 fixed in this lane, 2 no change with the reason verified, 0 filed

## The passes

The review runs as a loop (`CLAUDE.md`): each pass one lens, the fixes landed, the next pass re-reads the fixed work.

1. **The feature against the plan's "done means".** Item 1: `docs/vocabulary.md` carries twenty-five words with the fourteen the intent counted; `board/title.py` is the one reader; the planning brief's rule 1 and the filing rule name it by path and the birth reading quotes it whole; `tests/ratchets/test_every_title_is_in_the_owners_words.py` fails "A lane the machine ended…" in its own test and was red on the live corpus (14 titles) until the sweep landed, then green; the README's title paragraph points at the file. Item 2: the search role listed 193, 35, 17 and 1 live documents, each count checked against `ls`; the sweep script renamed 149 on Hello Revenue, 27 here, 6 on the machine, skipping the six documents whose cards had hands on them, keeping the old title on a `**Formerly:**` line and following every citation of a renamed path; read back from the served board afterwards, no card born, none archived, `tools/renames_kept.py --since 2026-09-07` 0; the rehearsal is `tests/api/test_title.py`'s rename test (git mv, retitle, rescan before the commit; the card keeps number and history and gains one renamed row carrying the new title). Reading the ratchet's own output against the letter found the plural rule reads "rings" the verb as the noun (finding 4). Item 3: every clause of the done means is a test in `tests/api/test_title.py` — the idea born with "lane", the plan carded with "lane", the closed door and its reason, the hold through a rewrite, the door opening on the passing read, the same on a bare second project, the history row and the record naming the session and the words, a defect's routing unchanged by its title; `routing_of` has no diff. Item 4: the parser prefers "The intent it breaks" and skips any sentence with a path, function name or backtick; two fixture defects carry the two cases and the served board's faces carry no backtick, `::` or `()` (the same test); the live check of the served page is the last pass's. Reading the letter against the plan found the sentence rule now applies to plans too, not only to defects (finding 5). Item 5: the numbered plans here and on the machine are renamed; both READMEs and the plan-write skill say the rule and why; the ratchet's stem test fails `2026-09-07-17-x`; the planning brief cites by card. Findings 4 and 5 came from this pass.
2. **The seams.** Concurrency: a second reading on one card is refused by the store (one open record per card and kind), and a title reading rides on a defect's mark reading rather than opening beside it, so no card holds two seats; the dial's own timer beats under the test client too, and the helper that walks the fixture read a reading the timer had opened as "the beat opened nothing" and failed one run in three (finding 3). Failure and restart: a reading that dies leaves no record and the beat opens another up to the cap, counting title readings as landed results (`_landed`) so a plan's reading is not stopped as "without a result"; a title rewritten under a failing verdict stays held until a reading of the new title passes, and the door says the title changed. The truth of what the board shows: the hold is derived from the last verdict by one function (`board/title.py::title_hold`) and the face, the claim, the door and the pill all read it; a shipped card's failing verdict shows nowhere; with the dial off no title is read on any board, which is where the mark's reading already stood, and the card's face says nothing rather than something stale. The brief of a title-only reading carried the whole document, and a plan on Hello Revenue runs past forty thousand characters of status log (finding 1). Findings 1 and 3 came from this pass.
3. **The boundaries.** Layers: `board/title.py` imports `domain` and `board.triage` only; the file read is in `board/` because the ratchet and the brief both read it, and the brief carries the words in, never a path the reading resolves itself. Typed edges: `TitleReading`, `TitleVerdict`, `Claim.TITLE_FAILS`, `StartState.TITLE_FAILS`, `AuditKind.TITLE` and `CardSummary.title_reading` regenerated by `needle types`; `tsc` clean; the fixture current. One way: one reader of the vocabulary, one fingerprint of a title, one hold; the owner's test sentence is carried verbatim in `TITLE_TEST` beside `THE_RULE` for the same reason (a paraphrase of a test is a different test), and the README stays the source. Nothing half-done: the ratchet passes. Every commit names #74 and says what prompted it. Lint on the lane's own files found three unsorted import blocks and seven lines over the project's hundred columns, all this lane's (finding 2); the three long lines in `api/board_cli.py` predate the lane and are untouched. Finding 2 came from this pass.
4. **The fixed work again.** Re-read the capped title brief, the sorted imports and the wrapped lines, and the helper that now continues when any reading is open in the store; ran the brief, parser and ratchet tests. Nothing new.
5. **The rebased work, and the served board.** The trunk moved twice under the lane (#53's code and close, #69's plan, card 87's plan). The rebase met two documents the sweep had renamed that the trunk had archived meanwhile — the renamed live copies dropped, the trunk's archived versions and their pointers kept — and a types file both sides regenerated, regenerated again; the fixture was current; `tsc`, `vitest` (68) and the whole backend suite with the ratchets green on the rebased tree. Two live titles that arrived on the trunk after the sweep's listing used "door" and "lane"; the ratchet refused them, which is what it is for, and both were retitled in the lane. Folded at origin/develop 4226a86, the main checkout level, the dist rebuilt and needle-serve restarted: the first read migrated the shared store to 0014. Read live from the served API afterwards: every face on Needle's board (62) and Hello Revenue's (316) carries no backtick, `::` or `()` — item 4's live check; `tools/titles_unplaced.py --since 2026-09-08` answers `none` and `tools/renames_kept.py --since 2026-09-07` answers 0 with Needle's 29 renames now on the board too. Read honestly: a defect without the new section shows its first path-free sentence, and for some older defects that is a sentence from the middle of the evidence ("The second clause is false whenever the first is true."); the filing brief now asks every new defect for the section, and the older ones read that way until a writer adds it. Nothing new.

## Dispositions
1. [seam] A title-only reading's brief carried the whole document, however long — FIXED: `DOCUMENT_EXCERPT` caps it at 8000 characters with the path to the rest (the commit after 298baf6)
2. [boundary] Three unsorted import blocks and seven lines over 100 columns in the lane's own files — FIXED in the same commit
3. [seam] The rail-walking test helper failed one run in three because the dial's 60 s timer opened a reading between its steps — FIXED: it continues when any reading is open in the store and fails only when nothing is open and nothing opened (`tests/api/test_dial.py::read_the_rail_until`)
4. [feature] The vocabulary ratchet's plural rule reads "rings" the verb as "ring" the noun — NO CHANGE: the list is the floor, not the test; a false positive costs one rewrite and the reader applies the owner's test, while an exception per verb form is the special-case drift the ratchet exists to refuse; the one live instance was rewritten by the sweep
5. [feature] The path-free sentence rule applies to a plan's intent as to a defect's body — NO CHANGE: the plan's letter is "no path, function name or backticked term reaches the face of a closed card", and a plan whose intent opens with a path is the same wrong face
6. [record] The plan's table of live documents (23/166, 9/20, 6/8, 1/0) was three days stale by the sweep (26/167, 11/24, 6/11, 1/0) — NO CHANGE: the plan carries the counts it was written from and the Met line carries the counts the sweep read; the sweep verified its own counts against `ls`, as the item said

## What was checked
- The backend suite with the ratchets, `tsc --noEmit` and `vitest run` (68) on the tree this record is committed to
- The served boards after the sweep, read-only from the shared store: cards per project, born and archived rows since the sweep, renamed rows, `tools/renames_kept.py`
- Hello Revenue's two docs-reading ratchets from its own checkout after its sweep (5 passed)
- The fixture through the real seat: readings opened by the dial's beat, landed through the CLI verb, faces read through the API

## What the build learned the plan got wrong
- The plan wrote "that sentence moves into the document's intent or observation" for what a rewrite drops; over 182 documents the uniform, lossless act is a `**Formerly:**` line under the head, and the Met line says so.
- The plan's sequencing said item 2 waits on #20; it did, and #20 had folded before this lane started.
- The reading is the dial's seat and runs only while the dial is on; the plan says "read cold at its birth" and does not say by whom. The Met line names the dial.

## Not done, stated
- Item 4's live check of the served page runs after the fold and the restart; the last pass records it.
- The owner's read of the new titles on each board is the loop, one week out, not this record.
