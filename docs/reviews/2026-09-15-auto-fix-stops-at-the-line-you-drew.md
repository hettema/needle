# Review — auto-fix stops at the line you drew and leaves the rest filed (card #149)

**Plan:** docs/plans/done/2026-09-15-auto-fix-stops-at-the-line-you-drew-and-leaves-the-rest-filed.md
**Reviewer:** two cold Claude sessions of this lane's own make, each called warm through Needle's own verb on the same note and neither with any context of this lane's work beyond it. The first to answer was call 150 (`needle call 6c033d34`, resumed as session 16fc3cd5); the second was call 144 (`needle call 2a1a6790`, resumed as 481177f2), which hit its account's usage limit mid-read and landed its answer after the first. Both answers are kept verbatim beside the note that asked, at `docs/reviews/notes/from-6c033d34-re-2026-09-15-149-cold-read.md` and `docs/reviews/notes/from-2a1a6790-re-2026-09-15-149-cold-read.md`; the note is `docs/reviews/notes/2026-09-15-149-cold-read.md`. The second read was not commissioned to answer the first — both were called before either replied, because three earlier calls died in a way the lane could not then explain (now filed, see below).
**Diff range:** 5cd9539..72bac3d — the revision was frozen while both read; the commits standing ahead of it at those moments were documents only, and both readers checked and said so.
**Findings:** 8 across the two reads, 3 of them the same finding twice — 5 repaired inside the change, one record repair already made before either answer landed, one record repair to the plan, one preference left alone with its reason.
**Verification:** below, under its own head. The final revision is named there.
**Completion:** every verified in-scope finding is resolved, the plan's six items carry their stance, and the suite, `tsc` and `vitest` are green on the final revision but for one ratchet that was red on the trunk before this lane began.

## Dispositions

1. [feature] `api/board_cli.py:1158` — `needle fixes` printed "its card was at or above its board's line" for every lane whose `below_the_line` was False, and the field was False both for a lane genuinely at or above the line and for a lane with no band at all, so the verb stated a check that was never made. The reader counted 42 fix lanes on a copy of the store, every one with a NULL decision and so every one printing it. That is the "shows something false as true" kind on the one verb this card's Loop is read through. — FIXED. `FixReport.below_the_line` is three values now, not two: True is a lane read as below its line and is what the Loop counts, False is a lane checked and found at or above it, None is a lane with no band to compare and says so ("no band to compare against its board's line"). `--below-the-line` selects `is True`, so a never-checked lane is not counted. The frontend type was regenerated (`boolean | null`). Held by the new last third of `tests/api/test_the_line.py::test_the_beat_takes_only_a_verified_defect_at_or_above_its_boards_line`, which opens a lane with no decision and asserts both the sentence and that the count does not move.
2. [seam] `frontend/src/components/ui/index.tsx:195` — a second, hand-written copy of the line's words, duplicating `board/triage.py`, with nothing holding the two together, against this plan's own ruling 4 ("the line is one phrase wherever it is said … so the page and the terminal never differ") and CLAUDE.md's rule that a boundary that matters is a ratchet and not a convention. The reader named the mechanism the project already has: `REACH_WORDS`, `OFTEN_WORDS` and `BREAKS_WORDS` are generated into `frontend/src/types/triage.ts` from `domain/triage.py`, which is why nobody can make the page and the terminal disagree about a grade's words; the new map sat in `board/triage.py`, outside what the generator mirrors. — FIXED, in the lane: the seam is this slice's own ruling, so fixing it serves this intent rather than widening it. `LINE_WORDS` and `EVERY_DEFECT` now live in `domain/triage.py` beside `Band`; `uv run needle types` emits `LINE_WORDS` into `frontend/src/types/triage.ts`; the page imports it and the hand-written copy is gone. `board/triage.py` keeps `line_words`, `line_from_words` and `line_rungs` over the one map. Now editing a phrase in one place changes both surfaces, and `tests/ratchets/test_typed_edges.py` fails if the generated file is not what the generator writes.
3. [verification] `tests/api/test_the_line.py:239` — the Loop's counter was never shown to fire: `below_the_line` was asserted False only, and the class-closer stops the beat before a below-line lane can exist, so the detector was exercised in one direction and the Loop's daily `expect 0` was indistinguishable from a number that cannot move. The reader satisfied itself by construction that the flag can fire, and named the gap as the thing the next session would not be told. — FIXED. The same test now draws the line at harm outside and opens a fix lane straight in the store on a card below it — the second path to Start the Loop exists to catch, which the beat itself will not do — and asserts `needle fixes all --below-the-line --count` prints 1 and the lane's line reads "its card was BELOW its board's line when planning began". So the 0 the Loop watches is now a number that has been seen to move.
4. [record] `domain/dial.py:45` — `changed_at` still read "when the owner last turned this board's switch" after a line move began stamping it, and `DialChange`'s docstring still offered two row shapes where a line-only move now writes a third. — FIXED in `91f8b68`, before this answer landed: my own re-read of the frozen revision found the same thing, and the reader's independent finding confirms it. Wording only; `tests/board` green after.
5. [record] `docs/plans/…-auto-fix-stops-at-the-line-you-drew-….md:48` — item 3's Met said "`tests/api/test_defects_column.py` green unchanged", which this revision cannot support: the reader had that module red once and green once on the same commit, and red on a different test from an export of the base. — FIXED in the plan: the Met now says the module is unchanged by this lane and green in this lane's run, that it does not give the same verdict twice at this revision, and names card #145 and the face collision filed on 2026-09-14 as where that belongs. A record correction, checked by its writer, which does not restart the review.
6. [seam] `api/dial.py:1378` — `waiting` put the below-line sentence before "this board's switch is off" while `_start` puts the switch first. Both readers landed on this spot and graded it differently: the first called it a preference about which true fact leads; the second demonstrated the consequence and called it a seam. The second is right, and it is the state item 5 creates — Hello Revenue's switch off since 2026-09-05 and its line drawn at harm outside. There the owner would read "below this board's line at harm outside" for the cards under the band, which implies that moving the line would start them; nothing on that board runs at all while the switch is off, so the implication is false even though the sentence is true. — FIXED. `waiting` now asks the line only of a board whose switch is on, so a board that is off says so for every card, which is the order `_start` already read them in. Held by the new middle of `tests/api/test_the_line.py`'s beat test: with the line at harm outside and the switch turned off, the waiting rows for a card below the line and a card above it both read "this board's switch is off".
7. [feature] `board/assemble.py:910` (second read, finding 2) — the face's filed sentence was computed from the routing, the grade and the line alone, never from the column, while the beat walks the Defects column alone (`board/dial.py::column_defects`). A graded verified-`now` defect card moved out of Defects — Backlog, Up next, Not now all reach that branch, since the Decision-moment branch returns earlier — would say "taken the moment you move the line", which is false for it: moving the line would not take that card. The reader marked it a hypothesis, having read the branch column-blind and the beat not, without putting a card outside Defects to see the face. — CONFIRMED and FIXED. I reproduced it at the face after writing the repair, by taking the new condition back out: with the gate removed, a graded verified-`now` defect card below the line standing in Backlog renders "Filed below this board's line at harm outside; taken the moment you move the line", which is the promise the reader said it could not keep. `filed` now also asks that the card stands in Defects, and `tests/board/test_the_line.py::test_the_filed_sentence_is_the_defects_columns_and_no_other` holds it for Defects, Backlog, Up next and Not now — the test that failed with the gate out is the one that passes with it in.

   How reachable the old face was is a separate question, and the answer narrows it: the board refuses a hand move of a defect card out of its column (`#100`, item 1 — demonstrated in the api test, which now asserts that refusal), and a plan appearing promotes the card to Planned with the plan as its document, which takes a different branch. So the repair ties the face to the column the beat actually walks rather than fixing a face the owner was likely to meet.
8. [record] `docs/HOW-WE-WORK.md:241` (second read, finding 5) and the shadowed local — the §10 edit left one line at 125 characters in a file otherwise wrapped near 78, and the `line` local in `board/assemble.py` shadowed the `Band` keyword one line from its use. — FIXED, both: the sentence is rewrapped and the local is `head_line`, with a comment saying which of the two "lines" is which. The second reader also confirmed what the first checked separately, that the four texts agree on the dial being three things.

The second read repeated three findings the first had already made, in its own words and from its own runs: the duplicated phrase table (its finding 3, this record's 2), the `changed_at` docstring (its 4, this record's 4, already repaired), and `tests/api/test_defects_column.py` not giving the same verdict twice (its verification note, this record's 5). Two readers reaching the same three from different directions is why they are recorded as one finding each rather than as six.

## Verification evidence

The final revision is the tip of `card-149-auto-fix-stops-at-the-line-you-d` named in the card's close row; every run below is from that worktree.

Backend, before the repairs, one module per process across all 109 modules under `tests/`: 108 exit 0. The one non-zero is `tests/ratchets/test_the_fixture_project_is_synthetic.py::test_no_real_card_title_is_in_the_tracked_tree`, red on `origin/develop` from commit `9e90300` before this lane began — a Needle suggestion quoting a Hello Revenue card's title as its evidence. Outside this change; the third sighting of that class is appended to card #29's own document in `c4076a5`, as CLAUDE.md's rings rule asks.

After the repairs: `tests/board/test_the_line.py`, `tests/board/test_dial.py` and `tests/board/test_grade.py` green together (31); `tests/api/test_the_line.py` and `tests/api/test_dial.py` re-run whole; `tests/ratchets/test_typed_edges.py`, `test_the_store_holds_its_shape.py` and `test_one_design_system.py` green (9), so the generated types are what the generator writes, the migrated database matches the models, and the page holds the design system. `cd frontend && npx tsc --noEmit` clean; `npx vitest run` 86 passing in 2 files. `ruff check` and `ruff format --check` clean on every file this lane touched (`api/board_cli.py:12` carries a pre-existing long line in its module docstring, untouched and outside this change).

Migration 0028 was rehearsed twice on copies of the real store, never on the store itself: once by this lane (five boards, every one reading the last rung after the upgrade, `dial_changes` unchanged) and once by the reader, which also ran the downgrade and confirmed it restores 0027's column list exactly on both tables. That is item 1's "no board changes behaviour by this landing", checked on real data.

What the reader checked and found clean, so it is recorded rather than re-run: the comparison is written once and everything goes through `board/triage.py::at_or_above`, with nothing matching on band words; ruling 6 holds, and in particular a below-line verified defect now reaches the seat's arm in `api/dial.py` where it did not before, and cannot open a reading there because `board/dial.py::SEAT_OPENS` stances that reason False; a line move alone writes an audit row with no setting, which `switch_was_on` skips, so the switch's history is undisturbed, while every switch turn carries a line, so `line_at` has a row from the moment this lands; a board with no row reads the last rung rather than raising; the head, the face and the beat read one predicate off one grade; `held_lanes` still filters on the planned stage, so a below-line plan is held and not counted as running, which is ruling 5; and `docs/HOW-WE-HOLD-IT.md` §10 claims no clause about the dial, so the new sentence in §10 creates no unheld claim.

Two limits, stated rather than papered over. `tests/api/test_defects_column.py` and `tests/api/test_the_triage_seat.py` do not give the same verdict twice at this revision, and the reader demonstrated it on this revision and on an export of the base: that is card #145 and the defect filed on 2026-09-14, both outside this change, and "green" for those two modules names a run and not the module. And item 4's served-page check with two boards is deviated, as card #80's was and for the same reason — a lane cannot serve a second board — to the first turn after the fold on the laptop.
