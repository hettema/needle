# Review — a test run never takes the memory the work needs

**Plan:** docs/plans/done/2026-09-10-a-test-run-never-takes-the-memory-the-work-needs.md
**Reviewer:** the build session (Claude Fable 5.1, card #109's lane) for its own passes; Codex (a fresh `codex exec -s read-only` thread 01a08a3a at high effort, then a fork of it) for the cold reads of another make
**Diff range:** 95d2377 (the plan on `origin/develop`) .. the close commit that carries this record (the build 3cbbc3a, the first pass's fix b14c0cb, the second round's fixes ac1823d, the third round's a7aa1f6, the fourth's 68477d9 (the representation), the fifth's loop line 9a489c7, the sixth's ledger, the close)
**Findings:** 16 — 1 the author's, 15 Codex's over five reads; 14 fixed in this lane (the mount reader and then the loop each replaced by a representation after three reads on one shape), 1 filed as a card (the layered filesystem), 1 a correction to this record
**Stop signal:** STOP_SIGNAL

## The passes

The review runs as a loop (`CLAUDE.md`): each pass one lens, the fixes landed, the next pass re-reads the fixed work, and every round's repairs are read for completeness by a reader of another make.

1. **The author's cold re-read, the three lenses in one pass.** Against the "done means": item 1 was shown on the ratchets folder before the suite ran — a run with no options created nothing new under `/tmp/pytest-of-dennis` (the three entries there before and after were other suites', none with Needle's `machine*` floors) and laid `pytest-1` to `pytest-3` under `~/.cache/needle/floors/pytest-of-dennis`; four runs left the last three; two runs started at once from this lane took `pytest-4` and `pytest-5` under pytest's own lock, each with its own `machine<n>` floors, neither wiping the other (they ran from one worktree, not two — the seam the plan names is one root shared by two runs, and that is what was exercised). Item 2 was proven by flipping the root: the check file with `--basetemp=/tmp/needle-109-probe` fails on `test_the_floors_stand_on_disk` naming `/tmp` and `tmpfs`, with `--basetemp` under `~/.cache` it passes. Item 3 is Omarchy #55 on the machine's board. The seams: two lanes' suites share one root and pytest numbers under a lock (shown); a run that dies mid-way leaves a numbered floor the next runs prune; the board's own truth is the plan's loop, whose command was run under `bash -lc` as the reader runs it and answered `floors-in-memory=0`. The boundaries: no new primitive — the root is pytest's own variable and the check joins the file that already holds the floor; the check pins the intent (a memory-backed filesystem, read from the mount table) and not a path. One finding: the refusal's advice told a refused run to drop `--basetemp` when the root can equally come from the variable the laptop-wide setting will set (finding 1, cosmetic).
2. **Codex's cold read — the reader who was not the author.** A fresh thread at high effort on commit b14c0cb, asked first whether the round's repair was complete and then to read the change for what the author could not see; it read pytest's `tmpdir.py`, Python's `tempfile`, `/proc/mounts` and the callers, and probed the reader function against a written table. Six findings, every one re-read against the code before it was taken, and five held in full: the reworded advice still read as if either setting could be changed where `--basetemp` wins when given (finding 2, record); the fixture tool laid the test board's copy and store through `tempfile.TemporaryDirectory()`, which ignores pytest's root, so two ratchets still put a copy in `/tmp` — item 1's own words promise the board's copy and the store (finding 3, feature, adjacent and fixed here because it is the intent); an empty `PYTEST_DEBUG_TEMPROOT` is absent to pytest but was present to the conftest, which would have re-opened the memory default (finding 4, feature); `unicode_escape` on the mount field mangled a non-ASCII mount name so the mount went unmatched and its parent's type answered (finding 5, boundary); two mounts on one point kept the first where the kernel shows the last, and `/dev`'s `devtmpfs` was not in the memory-backed set (finding 6, boundary). The sixth, that the plan's loop counts only Needle's `machine*` floors and would miss a `--basetemp` into another memory path or the fixture tool's copies, held in part: the fixture tool's copies are finding 3 and now land under the root; the rest is the loop's scope by design (finding 7, verification, no change with the reason under Dispositions).
3. **The fixed work again, and round two's cold read.** The repairs landed as one commit (ac1823d): the tool's `snapshot(into)` and `render(into)` take the directory and the two ratchets pass their floor, the hand run lays its own short-lived one; the conftest reads an empty variable as absent; the mount reader undoes the kernel's octal escapes and re-reads as UTF-8, keeps the latest mount on a point, and `devtmpfs` joins the set; a test holds the reader against a written table with a spaced non-ASCII name and a disk mount hidden under a tmpfs. Re-run: the three touched test files green, the empty-variable run lands under the cache, the refusal still names `/tmp` and `tmpfs`, and the hand run of the tool reports the fixture current. Codex's fork of its thread then read the repairs for completeness on ac1823d — every caller of the tool, the sibling of each fixed branch, whether the new test fails on the old reader, and finding 7's disposition. It found the sibling the equal-point repair had missed: a disk mount at a child point made before a tmpfs is mounted over the parent is hidden by it, and the longest-prefix reader still chose the hidden child and passed it (finding 8, boundary, latent in behaviour — a supported input, a root under such a stack, gets a wrong answer). And it held finding 7 open with an input the disposition had not met: a `--basetemp` given by hand into another path under `/tmp` lays a `machine0` the check refuses but does not remove, outside the loop's `pytest-of-dennis` search, so the loop reads zero over a Needle floor in memory (finding 7, re-opened).
4. **The fixed work again, and round three's cold read.** The reader now returns the last table entry whose point prefixes the path — the table is in mount order, and a later mount either sits inside the visible one or covers it, so the last prefix is the one that shows and the longest is not; the written-table test adds the hidden child and a later inner mount that shows (a7aa1f6). The loop counts `machine[0-9]*` directories anywhere under `/tmp` to depth four, with the reason in the plan's parenthetical; run under `bash -lc` it answers zero today. Codex's fork then read a7aa1f6 for completeness — whether "last prefix" is the mount that shows for every shape one namespace's table can take, whether the widened count over- or under-counts, and whether anything in it is outside the promise. Three findings, the same shape a third time: a mount moved into a later bind keeps its earlier table entry, so "last prefix" answers the bind's disk type over a tmpfs (finding 9, boundary, latent — a supported input gets a wrong answer); a cache pointed under `/tmp` lays the floor at depth six, past the count's depth four (finding 10, verification); a disk bind-mounted under `/tmp` is counted as memory by a search that reads names and places, not filesystems (finding 11, verification).
5. **The representation, and round four's cold read.** Three passes running had found the same shape — the mount table's order or spelling misleading a reader — and HOW-WE-WORK §13 says the next move is then a question about the representation, not another pass. The representation changed (68477d9): the check asks the kernel what filesystem the resolved root stands on (`stat -f %T`, the `statfs` answer for that path), which has no order to misread, and the table reader, its escapes and its written-table test are gone; the loop asks every Needle floor under `/tmp`, at any depth, its filesystem and counts the memory-backed ones, ending in `awk` so a zero exits clean. Item 2's Met line says how the method departed from the item's words while holding ruling 4. Re-run: the check file refused into `/tmp` naming `tmpfs` and passed under the cache; the loop's command under `bash -lc` answers zero. The question was then put to Codex's fork on 68477d9: does asking the kernel for the path close the shape, on every supported input, and is the loop's count right. Two answers. An overlay filesystem mounted under `/tmp` with its writable upper layer on `/dev/shm` stores new floors in memory and answers `overlayfs`: the kernel names the filesystem, not its backing store (finding 13) — outside the promise's boundary, which is a machine whose temp folder is memory or a root given by hand into one, not a stacked mount nobody on the board has; left with the reason, not repaired. And the loop's scan could still say zero when it could not look: `find`'s errors were discarded, so an unreadable directory of the owner's own under `/tmp` hid the floors under it (finding 12, verification — the silent zero card #53 met in another form).
6. **The loop line, and the last read.** The command prunes other users' directories below the root, reads `find`'s and `stat`'s errors into `awk` beside the filesystem answers, and prints `floors-in-memory=unknown scan-errors=N` when any error was met — a line the expectation does not match, so a scan that could not look never reads as delivered. Probed under `bash -lc` as the reader runs it: an unreadable owner's directory holding a floor reads `unknown scan-errors=1`, two readable floors read `2`, a clean `/tmp` reads `0` with exit 0 (the first draft pruned `/tmp` itself, being root's, and read zero for everything — caught by the probe before it was written). The plan was read again by the board's own parser: three items, two stanced, the Loop line a command row with the target verbatim. Codex's fifth read, on 9a489c7, found the scan's third hiding place — pruning by owner also hides the owner's floors under a shared directory owned by someone else, `/tmp/.X11-unix`'s shape (finding 14, verification, latent in behaviour of the loop) — and two record findings: finding 12 was behaviour, not record, so the stop signal as written did not apply (finding 15), and the overlay's exclusion stood on the fleet's absence, not on the promise's words, and §13 files an outside finding rather than leaving it (finding 16).
7. **The loop's representation, and the last read.** Three reads had found the scan of `/tmp` hiding a floor three ways, so the loop's representation changed too: the suite leaves the trace itself. `tests/conftest.py::floors_recorded`, a session fixture, appends one line per run to `~/.cache/needle/floor-runs.log` — the time, the kernel's word for the filesystem the root stands on, and the root — whatever named the root; the plan's Loop line is an `awk` over that file since the fold's hour, and a missing file is a non-zero exit. `filesystem_of` moved to `tests/floor.py` so the check and the ledger read one answer. Probed: a run of the check file into `/tmp/needle-109-probe` wrote a `tmpfs` line and the loop counted it under a `since` of midnight; under the fold's `since` it reads zero; the refusal still fired on that run. The layered-filesystem case is filed as `docs/slice-suggestions/2026-09-10-a-test-run-on-a-layered-filesystem-that-keeps-its-writes-in-memory-is-refused-too.md`, marked `his`, with the decision it implies. ROUND_SIX_RESULT

## Dispositions

1. [record] The refusal's advice told a refused run to drop `--basetemp` when the root can also come from `PYTEST_DEBUG_TEMPROOT` — FIXED in b14c0cb.
2. [record] The reworded advice still read as if changing either setting sufficed, where `--basetemp` wins when given — FIXED in ac1823d (Codex's finding 1): the message states the order and says to point the one in force at disk or drop it.
3. [feature] `tools/board_fixture.py::snapshot` laid the test board's copy and store through `tempfile.TemporaryDirectory()`, outside pytest's root, for `test_every_face_sentence_says_whose_move.py` and `test_the_fixture_project_is_synthetic.py` — FIXED in ac1823d (Codex's finding 2): the directory comes from the caller; the two ratchets pass `tmp_path`; `write()` lays its own for the hand run.
4. [feature] An empty `PYTEST_DEBUG_TEMPROOT` counted as set in the conftest and as unset in pytest, re-opening the memory default — FIXED in ac1823d (Codex's finding 3): `not os.environ.get(...)`; run with the variable empty lands under the cache.
5. [boundary] The mount field's octal escapes were decoded with `unicode_escape` alone, which mangles a non-ASCII name so the mount is never matched — FIXED in ac1823d (Codex's finding 4): `unescape_mount`; `test_the_mount_reader_undoes_escapes_and_takes_the_mount_that_shows`.
6. [boundary] Two mounts on one point kept the first where the kernel shows the last, and `devtmpfs` was absent from the memory-backed set — FIXED in ac1823d (Codex's finding 5): the latest same-length mount wins; `devtmpfs` joins `IN_MEMORY`; the same test holds it.
7. [verification] The plan's loop counted only Needle's `machine*` floors under `/tmp/pytest-of-dennis`, so a `--basetemp` into another path under `/tmp` — a floor the check refuses but does not remove — was outside the count, and the loop would read zero over a Needle floor in memory — first answered NO CHANGE (the loop's thesis is the default, and the fixture tool's copies were finding 3), then FIXED in a7aa1f6 when round two's read showed the input: the count is of `machine[0-9]*` directories anywhere under `/tmp` to depth four, the reason in the plan's parenthetical.
8. [boundary] A disk mount at a child point, hidden by a later tmpfs over its parent, was chosen by the longest-prefix reader and passed — FIXED in a7aa1f6 (Codex's round-two finding): the reader returns the last prefix entry in mount order; the written-table test holds the hidden child and a later inner mount.
9. [boundary] A mount moved into a later bind keeps its earlier table entry, so the last-prefix reader answered the bind's disk type over a tmpfs — FIXED in 68477d9 (Codex's round-three finding) by the representation: the check asks the kernel for the path's filesystem and reads no table.
10. [verification] A cache pointed under `/tmp` lays the floor at depth six, past the loop's depth four — FIXED in 68477d9 (Codex's round-three finding): the loop searches `/tmp` at any depth.
11. [verification] A disk bind-mounted under `/tmp` was counted as memory by a search that read names and places — FIXED in 68477d9 (Codex's round-three finding): the loop asks each floor its filesystem and counts `tmpfs` and `ramfs`.
12. [verification] The loop discarded `find`'s errors, so an unreadable directory of the owner's own under `/tmp` hid the Needle floors beneath it behind a zero — FIXED in the plan's Loop line (Codex's round-four finding): errors are read beside the answers and the line reads `unknown` when any was met; probed under `bash -lc`.
13. [boundary] An overlay filesystem whose upper layer is in memory answers `overlayfs` to the kernel's filesystem question, so a root on such a stack would pass the check and escape the loop — FILED (Codex's round-four finding), outside the change: `docs/slice-suggestions/2026-09-10-a-test-run-on-a-layered-filesystem-that-keeps-its-writes-in-memory-is-refused-too.md`, `his`, because whether the bar is the kernel's name or where the writes land is a ruling. First held in the record as "nobody has it", which round five's read rightly refused (finding 16).
14. [verification] Pruning other users' directories hid the owner's own floors under a shared directory owned by someone else, so the loop read zero over a floor in memory — FIXED by the representation (Codex's round-five finding): the loop reads the suite's own ledger and scans nothing.
15. [record] The stop signal called finding 12 record-class where the loop is a written promise the board meets, so a wrong count is behaviour — CORRECTED in this record (Codex's round-five finding): the stop signal now names pass 6's read as the last behaviour finding and the ledger round as its repair.
16. [record] The overlay finding was held in the record on the fleet's absence instead of filed — CORRECTED (Codex's round-five finding): filed, disposition 13.

## What was checked

- **pytest's own root logic**, read in `.venv/lib/python3.13/site-packages/_pytest/tmpdir.py::getbasetemp` (9.1.1): a given `--basetemp` is wiped and used bare; otherwise `PYTEST_DEBUG_TEMPROOT` or the system temp dir, then `pytest-of-<user>/pytest-<n>` numbered under a lock, the last three kept. Read lazily at first use, so the conftest's `pytest_configure` setting the variable is in time whatever the hook order.
- **The kernel's answer** on this laptop, `stat -f -c %T`: `/tmp` tmpfs, `/dev` tmpfs (a devtmpfs answers so), `/` and `/home/dennis/.cache` btrfs; the mount table agrees (`/tmp` tmpfs of 8026016k, `/` and `/home` btrfs on `/dev/mapper/root`).
- **The runs**, all from this lane on 2026-09-10 (07:24Z to 07:44Z): the check file refused into `/tmp/needle-109-probe` and passed under `~/.cache`; four runs of the check file left `pytest-1..3` under the cache root and nothing new under `/tmp`; two runs of the ratchets folder at once took `pytest-4` and `pytest-5`; the loop's command under `bash -lc`; the three repaired test files; the run with the variable empty; the hand run of `tools/board_fixture.py`. The full suite: FULL_SUITE_RESULT
- **The machine card**: `needle fixes omarchy` shows #55 with its kind and mark; the reach of `environment.d` was shown by `SSH_AUTH_SOCK` from `10-ssh-agent.conf` being present in this session, a transient service.

## What the build learned the plan got wrong

- The plan's terrain said the refusal "joins `test_every_path_the_runtime_reads_is_under_the_floor`"; it joined the file as its own test, so a refused root fails one named check with one message rather than half-way through the path list. Same intent, said in the plan as "where to look".
- Item 2's words and the terrain named the mount table (`/proc/mounts`, the longest prefix) as the way to read what the root stands on. Three cold reads found three ways that representation misleads; the kernel's own answer for the path holds ruling 4 with nothing to misread, and the Met line says so.
- Item 1 named the test board's copy and the store as floors and the terrain said every floor is under `tmp_path`; the fixture tool's two ratchets were the exception the plan did not see (finding 3).

## Not done, stated

- The two concurrent suites ran from one worktree, not two; the seam — one root, two runs, pytest's lock — is the same, and a second worktree's conftest would have been the trunk's until the fold.
- The stale Needle floors under `/tmp/pytest-of-dennis` from before this card: at the fold the numbered directories there were not this card's — other suites' — and STALE_FLOORS_RESULT
- Other lanes whose worktrees predate this fold keep laying floors under `/tmp` until they rebase; the plan's loop would read such a run as a floor in memory, and its B says the reading names the run. The watercooler carries the note.
- The laptop-wide setting is Omarchy #55's to land; until it does, Hello Revenue's suites and hand-run suites lay their floors in memory. Not this card's, by the plan's ruling 1.
