# Review — the team learns which mix of colleagues earns its place

**Plan:** docs/plans/done/2026-09-05-the-team-learns-which-mix-of-colleagues-earns-its-place.md
**Reviewer:** a fresh Codex thread (rollout 01a09129-750e-7060-8ac9-65790337f0bd), Needle call 106, read-only in the lane's worktree on the rented machine, asked through `/home/dennis/.cache/needle/notes/from-needle-58-the-team-learns-which-mix-of-colleagues-review.md`; its answer is beside the note (`from-codex-fresh-155007114507-re-58-…md`) and quoted in the dispositions.
**Diff range:** a6eb528..f50dec1 (the revision frozen while read: the feature commit and the plan's stances). Repairs landed after it in 82307ba and the commits that follow; the implementing lane verified them, and the reader is not represented as having read them.
**Findings:** 12
**Verification:** `tests/board/test_team.py` (28 cases) and `tests/api/test_team.py` (5 cases) pass on the repaired revision, with new cases for each repaired finding; `tests/board/test_dial.py` (13) with two new cases for the defect-lineage reading; `npx tsc --noEmit` clean and `vitest` 82 passed; ruff clean on every changed file but one pre-existing long line in `api/board_cli.py`'s usage head; the full backend suite on the rebased tree is recorded under "Verification evidence". `needle team needle` run on a copy of the store on the rented machine read #54 and #59 from the facts before and after the repairs, and the plan's close-out quotes the second reading.
**Completion:** Complete: every verified finding is repaired inside the change or, for the one seam outside it, the consequence is repaired in the brief and the seam filed as `docs/slice-suggestions/2026-09-11-work-on-a-card-can-call-a-fresh-colleague-of-either-kind-not-only-codex.md`. One clause of finding 7 is disproved with evidence and left unchanged.

## Dispositions

1. [feature] `api/doors.py::Doors.start` — the executed team could differ from its declaration: the route was computed from the cached placement and the launch asked the rule afresh, so a Claude→Codex change between them persisted "different-make" with both hand and challenger Codex while the brief named Claude; a restart kept the old hand — FIXED in 82307ba: the door asks the rule live, placed for the repository (`runtime.where(cached=False, repo=…)`), the same question the launch asks a moment later; when the launch still lands on another make than routed for, the door re-routes for the hand that landed and replaces the row before any work, with a history line saying the brief named the earlier team and the card is the word; a restart on another make keeps the first Start's team (immutable) and the history says the mix, which the reading names as a confound. `tests/api/test_team.py` holds the first-Start, restart and dead-launch paths; the different-make landing is exercised by inspection only, since the floor's rule answers one make.
2. [feature] `api/doors.py::Doors.start` — the assignment was not durable before the work: a `record_lane` failure or a board death between the launch and the row left a running lane with no team — FIXED in 82307ba: the row is written before the launch (`Team.assign`), taken back only when the launch dies (`Team.unassign`, the one caller) and replaced only when another make lands (`Team.reassign`); a board that dies after the launch holds the team the brief carried. `test_a_launch_that_dies_assigns_no_team`.
3. [seam] `board/team.py::executable`, `board/brief.py::team_brief` — executability was enum membership; a same-make challenge for a Claude hand or a different-make one for a Codex hand needs a Claude colleague already running, since `needle call --fresh` starts Codex only, and the prescribed call refuses when none is — FIXED in the brief in 82307ba: the lane is told what to do when the call refuses (`**Challenged:** no round — <why>`, and go on alone), so the reading counts an unread round and never a zero; the router is unchanged, because availability at Start proves nothing about a call made hours later (`executable`'s docstring says so); the seam itself — a fresh colleague of either make — is outside this change and filed as `docs/slice-suggestions/2026-09-11-work-on-a-card-can-call-a-fresh-colleague-of-either-kind-not-only-codex.md` (`Fix: now`).
4. [feature] `board/team.py::read_shape`, `_earned` — closed trials qualified before their fourteen-day escape window matured, so nine trials closed an hour ago read "earned … escaped no defect within 14 days" — FIXED in 82307ba: `read_shape` takes the clock; a trial inside its window is `maturing` on its tally and named as a confound, and a shape whose sample is full but not matured reads `exploring` with the reason. `test_a_trial_inside_its_escape_window_is_not_yet_judged`.
5. [feature] `board/team.py::_quality`, `read_shape` — unread corrections became zero for ranking, so a challenge with every round unrecorded could lead on time "quality being equal" — FIXED in 82307ba: a challenge with any trial's corrections unread is not judged; the shape reads `exploring` naming the unread count. `test_an_unrecorded_round_is_a_confound_not_a_zero_and_blocks_the_judgment`.
6. [feature] `board/team.py::read_shape` — staleness never cleared: one retained old-model trial blocked a leader forever — FIXED in 82307ba: stale trials are set aside, named in a confound and kept in `observations`; the fresh cohort is judged on its own, `stale` only while it is short. `test_a_changed_model_sets_the_old_trials_aside_and_a_fresh_cohort_judges_again`.
7. [feature] `domain/team.py::Route`, `board/team.py::observation_of` — the challenger's model was neither stored nor compared, so a challenger change could not make evidence stale — FIXED in 82307ba: `Route.challenger_model` carries what a fresh Codex call would run (`runtime/codex.py::configured_model`, the top-level `model` of `config.toml`); an observation's `challenger_model` is read from the lane's call rows and the one list's knowledge of that session (`api/team.py::Team._challenger_model`); `_stale` compares both. The clause "explicitly named models in historical declarations are discarded" — NO CHANGE: #59's line names "Claude Fable 5.1" and "Codex gpt-5.6" in prose with no grammar; reading model names out of prose is a guess about which word is a model and which colleague it belongs to, and the plan's item 2 forbids a count the facts do not support. Cards started from here carry both models on the row; the two founding observations carry none, which their `declared_in` says.
8. [feature] `board/dial.py::filed_against`, `board/team.py::escapes_of` — an idea naming the card counted as an escape, an archived defect dropped out of the count, a same-day filing before the close counted, and a defect the card's own lane filed from its review named the card as if against it — FIXED in 82307ba and the following commit: `filed_against` reads defects only (`needle fixes` gains the same precision; `FixReport.defect_filed_against`'s docstring says so) and the reader asks for archived ones too; a defect's birth is the board's own birth of its card, to the second, and the day of its stem only when it has no card (`Defect.precise`); `filed_by_the_card` keeps out what the card's lane, review or close filed. On the store copy #54 went from 0 to 2 to 1 escape and #59 from 2 to 1 as each clause landed; the plan's close-out names the one. `test_escapes_are_defects_born_after_the_close_and_within_fourteen_days`, `tests/board/test_dial.py::test_a_defect_the_cards_own_lane_filed_is_by_the_card_not_against_it`, `…test_filed_against_counts_defects_only_and_the_archived_on_request`.
9. [feature] `board/team.py::_quality`, `read_shape` — quality compared counts of correcting trials, rewarding sample volume, and the earned sentence printed the whole count as "of its first 3" — FIXED in 82307ba: shares of trials on both axes; the sentence counts the first three by close (`_correcting_first`). `test_quality_compares_shares_not_counts`.
10. [feature] `runtime/transcripts.py::tokens` — files that all failed to read returned 0 tokens, a measured cost that could win a tie — FIXED in 82307ba: no usage read is `None`, never zero.
11. [feature] `board/team.py::route_for` — a `Composition:` line with no reason pinned the team — FIXED in 82307ba: a reasonless pin is refused at Start with the words to write one or remove the line. `test_a_pinned_team_overrides_with_its_reason_and_an_unexecutable_pin_is_refused`.
12. [record] plan item 2 and its close-out claimed send-backs and rework as "none" with no reading of send-backs or fixing commits — FIXED in 82307ba and the plan's revision: `send_backs_in` reads the owner's moves out of Executed or Done after the close; `runtime/git.py::fixes_after` counts the trunk's commits naming the card within a week of the lane's tip; both are on the observation, the tally, `needle team` and the close-out, which now quotes the second reading (three fixing commits on #54, none on #59).

## Verification evidence

- Independent reading: Codex call 106 on f50dec1; the reader ran the 23 pure team tests directly, `tsc`, `git diff --check` and an AST inspection of `board/` for `subprocess`, and reported pytest itself unavailable in its read-only sandbox. Its direct invocations are not treated as a suite run.
- Repairs: `uv run pytest -q tests/board/test_team.py tests/api/test_team.py tests/board/test_dial.py` — 46 passed on the repaired revision; `cd frontend && npx tsc --noEmit && npx vitest run` — clean, 82 passed; `uv run needle types` and `tools/board_fixture.py` regenerated and current.
- The reading on real data: `NEEDLE_DATA_DIR=<copy> uv run needle team needle` on a copy of the rented machine's store (`~/.local/share/needle/needle.db` as of 2026-09-11 15:25Z) against the main checkout's corpus, before the repairs (#54 corrections unread, 0 escapes; #59 12 corrections, 2 escapes) and after (#54 1 escape, 3 fixing commits; #59 1 escape, 0 fixing commits); the plan's close-out quotes the second. The live store was not opened by this lane's code: its migration would have taken the `0020` slot #123 holds.
- Full suite: see the line below, written when the run on the rebased tree ended.
