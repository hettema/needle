"""Ratchets: every boundary named in CLAUDE.md, held by a test.

A ratchet holds an intent, never a method: it should survive a better way of
doing the same thing, and fail only when the boundary itself is crossed.
"""
